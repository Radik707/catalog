// Этап 2: навигация по каталогу (категории + товары + пагинация)
// Будет реализовано в Этапе 2
import { Bot } from "grammy";

export function registerCatalogHandler(bot: Bot) {
  // placeholder — реализуется в Этапе 2
}
