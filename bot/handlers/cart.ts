// Этап 3: управление корзиной через inline-кнопки
// Будет реализовано в Этапе 3
import { Bot } from "grammy";

export function registerCartHandler(bot: Bot) {
  // placeholder — реализуется в Этапе 3
}
