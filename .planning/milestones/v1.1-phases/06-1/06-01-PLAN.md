---
phase: 06-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/sheets.ts
autonomous: true
requirements: [NAV-01, NAV-04]
must_haves:
  truths:
    - "Витрина получает из Google Sheet поля «Подгруппа» и «Раздел» для каждого товара"
    - "Порядок товаров, пришедший из Sheet (уже отсортированный upload.py), сохраняется в массиве Product[]"
  artifacts:
    - path: "lib/types.ts"
      provides: "Тип Product с опциональными полями subgroup и section"
      contains: "subgroup"
    - path: "lib/sheets.ts"
      provides: "Чтение диапазона A2:K и маппинг колонок J/K в Product"
      contains: "A2:K"
  key_links:
    - from: "lib/sheets.ts"
      to: "lib/types.ts"
      via: "import { Product }"
      pattern: "subgroup:\\s*row\\[9\\]"
    - from: "lib/sheets.ts"
      to: "Google Sheets API"
      via: "range A2:K"
      pattern: "A2:K"
---

<objective>
Расширить слой чтения данных витрины: тип `Product` получает поля `subgroup` (колонка J «Подгруппа») и `section` (колонка K «Раздел»), а `getProducts()` читает диапазон `A2:K` вместо `A2:I` и маппит две новые колонки.

Purpose: Колонки J/K уже заполнены в Google Sheet (Этап 5). Без этого расширения витрина их «не видит» — это фундамент, на котором план 02 строит навигацию раздел→подгруппа. Поля опциональные, поэтому изменение НЕ ломает текущую витрину между планами.
Output: Обновлённые `lib/types.ts` и `lib/sheets.ts`; проект собирается (`npm run build` exits 0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-1/06-CONTEXT.md
@.planning/phases/06-1/06-PATTERNS.md

<interfaces>
<!-- Текущие контракты — исполнитель использует их напрямую, без исследования кодовой базы. -->

Из lib/types.ts (текущий):
```typescript
export interface Product {
  id: string;
  name: string;
  price: number;
  stock: number;
  category: string;
  group: string;
  supplier: string;
  badge?: string;
  imageUrl?: string;
  description?: string;
}
```

Из lib/sheets.ts (текущий маппинг строк, индексы колонок):
```typescript
const range = encodeURIComponent("Товары!A2:I");
// ...
return rows.map((row, index) => ({
  id: String(index + 1),
  name: row[0] || "",
  price: parseFloat(row[1]) || 0,
  stock: parseInt(row[2]) || 0,
  category: row[3] || "",
  group: row[4] || "",
  supplier: row[5] || "",
  badge: row[6] || undefined,
  imageUrl: row[7] || undefined,
  description: row[8] || undefined,
}));
```

Подтверждённый порядок колонок Sheet (D-07):
A Наименование · B Цена · C Остаток · D Категория · E Группа · F Поставщик · G Badge · H ImageUrl · I Description · **J Подгруппа (row[9]) · K Раздел (row[10])**
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Добавить поля subgroup и section в тип Product</name>
  <read_first>
    - lib/types.ts (файл, который меняем — весь, 12 строк)
    - .planning/phases/06-1/06-PATTERNS.md §«lib/types.ts (model)» — точные имена и комментарии полей
  </read_first>
  <files>lib/types.ts</files>
  <action>
    Добавить в интерфейс `Product` два опциональных строковых поля сразу после `description`: `subgroup?: string` (колонка J «Подгруппа») и `section?: string` (колонка K «Раздел»). Поля строго опциональные (`?`) — как все поля после `supplier`. Комментарии на русском по проектному стандарту: рядом с каждым полем указать соответствующую колонку Sheet (J / K) и человекочитаемое название. Порядок полей в интерфейсе соответствует порядку колонок Sheet (A→K). НЕ менять существующие поля.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: интерфейс `Product` содержит строку с `subgroup?: string`
    - Source: интерфейс `Product` содержит строку с `section?: string`
    - Source: оба новых поля расположены после `description`
    - Build: `npx tsc --noEmit` завершается без ошибок
  </acceptance_criteria>
  <done>Тип Product содержит опциональные поля subgroup и section с русскими комментариями; типизация проходит без ошибок.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Расширить диапазон чтения до A2:K и замаппить колонки J/K</name>
  <read_first>
    - lib/sheets.ts (файл, который меняем — весь, 41 строка)
    - lib/types.ts (обновлённый в Task 1 — чтобы маппинг совпал с типом)
    - .planning/phases/06-1/06-PATTERNS.md §«lib/sheets.ts (service)» — точные индексы row[9]/row[10] и паттерн `|| undefined`
  </read_first>
  <files>lib/sheets.ts</files>
  <action>
    Изменить строку диапазона: `encodeURIComponent("Товары!A2:I")` → `encodeURIComponent("Товары!A2:K")`. В объекте, который возвращает `rows.map(...)`, добавить два поля после `description`: `subgroup: row[9] || undefined` (колонка J «Подгруппа») и `section: row[10] || undefined` (колонка K «Раздел») — точно по паттерну существующих опциональных полей (`badge`, `imageUrl`, `description` используют `|| undefined`). Обновить JSDoc-комментарий над `getProducts()` (строки 6-9): дописать в перечень колонок «| Подгруппа | Раздел». Комментарии на русском. НЕ трогать кеширование (`revalidate: 300`), обработку ошибок и проверку env-переменных.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: файл содержит строку диапазона с `A2:K` и НЕ содержит `A2:I`
    - Source: объект маппинга содержит `subgroup: row[9]` и `section: row[10]`
    - Source: оба новых поля используют паттерн `|| undefined`
    - Source: `revalidate: 300` сохранён без изменений
    - Build: `npx tsc --noEmit` завершается без ошибок
  </acceptance_criteria>
  <done>getProducts() читает A2:K и возвращает Product[] с заполненными subgroup/section; типизация проходит.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Проверочная сборка слоя данных</name>
  <read_first>
    - lib/sheets.ts (обновлённый)
    - lib/types.ts (обновлённый)
  </read_first>
  <files>(нет — только проверка)</files>
  <action>
    Запустить полную сборку проекта, чтобы убедиться, что расширение типа и диапазона не сломало текущую витрину (CatalogView ещё использует `group`, новые поля опциональны и не должны мешать). Команда сборки в PowerShell: `npm run build`. Если сборка падает — устранить причину в рамках изменённых файлов слоя данных (типы/маппинг), НЕ затрагивая компоненты — они меняются в плане 02.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Build: `npm run build` завершается с кодом 0
    - Behavior: текущая витрина не сломана — компоненты не затронуты, новые поля опциональны
  </acceptance_criteria>
  <done>npm run build проходит; слой данных готов к использованию планом 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js сервер → Google Sheets API | чтение данных (read-only), ключ API на сервере |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | lib/sheets.ts (GOOGLE_API_KEY) | accept | Ключ остаётся серверным (process.env), новых каналов утечки не добавляется; диапазон расширен с A2:I до A2:K — те же read-only данные того же листа |
| T-06-02 | Tampering | новые поля subgroup/section | accept | Данные read-only из доверенного Sheet; новых пользовательских входов нет; поля только отображаются (план 02), не используются как код/запрос |

Новой поверхности атаки нет: эндпоинты не добавляются, пользовательских входов не появляется, секрет CATALOG_SECRET и серверный ключ API не меняются.
</threat_model>

<verification>
- `npx tsc --noEmit` без ошибок
- `npm run build` exits 0
- `lib/types.ts` содержит `subgroup?` и `section?`
- `lib/sheets.ts` содержит `A2:K`, `row[9]`, `row[10]`
</verification>

<success_criteria>
Тип Product расширен опциональными полями subgroup/section; getProducts() читает A2:K и маппит колонки J/K; проект собирается; текущая витрина не сломана (поля опциональны).
</success_criteria>

<output>
Create `.planning/phases/06-1/06-01-SUMMARY.md` when done
</output>
