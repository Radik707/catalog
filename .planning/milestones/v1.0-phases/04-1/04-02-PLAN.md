---
phase: 04-1-admin-panel
plan: 02
type: execute
wave: 2
depends_on: [04-1-admin-panel-01]
files_modified:
  - scripts/upload.py
  - uploader/admin.py
autonomous: true
requirements: [ADM-03]
must_haves:
  decisions: "D-05, D-07"
  truths:
    - "Владелец вводит «Отображаемое название» в форме правки и сохраняет — строка name пишется во вкладку «Правки»"
    - "При следующей пересборке каталога товар выводится под новым названием, а ключ сопоставления (нормализованное имя из прайса) не меняется"
    - "Пустое поле названия = используется название из прайса (правка name отсутствует)"
  artifacts:
    - path: "scripts/upload.py"
      provides: "Поддержка типа правки name: разбор в load_edit_memory, применение в apply_edit_memory, вывод в products_to_rows"
      contains: "display_name"
    - path: "uploader/admin.py"
      provides: "Поле «Отображаемое название» в форме правки + /save принимает type=name"
      contains: "name"
  key_links:
    - from: "uploader/admin.py (/save)"
      to: "scripts/upload.py (apply_edit_memory)"
      via: "строка type=name во вкладке «Правки» → display_name при пересборке"
      pattern: "display_name"
---

<objective>
Второй ломтик: владелец **исправляет отображаемое название** товара (если 1С прислала кривое имя). Панель пишет правку типа `name` в память, а `upload.py` учится применять её — товар выводится под исправленным именем, не теряя ключ сопоставления.

Purpose: ADM-03 — точечное исправление неверно распознанного поля (названия). Цена сознательно вне границ (D-07); название безопасно, т.к. 1С его надёжно не присылает.
Output: расширение `scripts/upload.py` (тип `name`), расширение формы и /save в `uploader/admin.py`.
</objective>

## Phase Goal

**As a** владелец каталога, **I want to** заменить кривое название товара из прайса на читаемое прямо в панели, **so that** покупатели в каталоге видят понятное имя, а сопоставление с будущими прайсами не ломается.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-1/04-CONTEXT.md
@.planning/phases/04-1/04-PATTERNS.md
@.planning/phases/04-1/04-UI-SPEC.md
@.planning/phases/04-1/04-01-SUMMARY.md

<interfaces>
<!-- Точные места изменения в scripts/upload.py (этап 3). -->
- ALLOWED_TYPES (строка 371): сейчас {"group","photo","description"} → добавить "name"
- apply_edit_memory (строки 429-458): для каждого товара по normalize_name(p["name"]); добавить: if "name" in edit: p["display_name"] = edit["name"]
- products_to_rows (строки 561-599): первая колонка строки = p["name"]; заменить на displayed = p.get("display_name") or p["name"]
- normalize_name (строка 213) — КЛЮЧ сопоставления остаётся по p["name"] из прайса (правка name НЕ меняет ключ — D-05)
- Заголовок листа «Товары»: ["Наименование","Цена","Остаток","Категория","Группа","Поставщик","Badge","ImageUrl","Description"]

<!-- UI: экран 2 правки товара (04-UI-SPEC.md) -->
- Поле «Отображаемое название» (form-control), hint «Оставьте пустым, чтобы использовать название из прайса»
- Кнопка «Сохранить правку» (btn-primary) — disabled пока нет изменений
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: upload.py — поддержка типа правки name (разбор → применение → вывод)</name>
  <read_first>
    - scripts/upload.py (строки 302-401 load_edit_memory с ALLOWED_TYPES; 429-458 apply_edit_memory; 561-599 products_to_rows; 213-215 normalize_name; 795-801 порядок вызовов в main())
    - .planning/phases/04-1/04-PATTERNS.md (раздел «scripts/upload.py — расширение: тип name» — 3 места изменения с номерами строк)
    - .planning/phases/03-1/03-CONTEXT.md (схема «Правки», расширяемость типов, precedence D-06)
  </read_first>
  <behavior>
    - load_edit_memory: строка «товар» | «name» | «Конфеты Ромашка» теперь ПРИНИМАЕТСЯ (раньше отвергалась как неизвестный тип); попадает в mapping[key]["name"]
    - apply_edit_memory: товар с правкой name получает p["display_name"]=значение; ключ сопоставления (normalize_name по p["name"] из прайса) НЕ меняется
    - products_to_rows: первая колонка строки = display_name если задан, иначе name из прайса
    - товар без правки name: display_name отсутствует → выводится p["name"] (поведение этапа 3 не сломано)
  </behavior>
  <action>
    Расширь scripts/upload.py поддержкой нового типа правки `name` (D-05, схема памяти расширяема — этап 3 D-02). Три точечных изменения по карте паттернов: (1) ALLOWED_TYPES в load_edit_memory — добавь "name" к {"group","photo","description"}. (2) apply_edit_memory — после блоков group/photo/description добавь `if "name" in edit: p["display_name"] = edit["name"]` с русским комментарием, что ключ сопоставления остаётся по имени из прайса (правка меняет только отображение). (3) products_to_rows — в формировании строки замени `p["name"]` в ПЕРВОЙ колонке («Наименование») на `p.get("display_name") or p["name"]`; ОСТАЛЬНЫЕ обращения к p["name"] (бейдж, фото, описание — get_badge/get_photo_url/get_photo_description) оставь без изменений, т.к. они работают по ключу прайса. Не меняй порядок вызовов в main(). Русские комментарии.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/upload.py',encoding='utf-8').read()); print('ok')"; python -X utf8 -c "import sys; sys.argv=['x','--path','.','--dry-run']; src=open('scripts/upload.py',encoding='utf-8').read(); assert 'display_name' in src and '\"name\"' in src; print('name-type wired')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: ALLOWED_TYPES в scripts/upload.py содержит "name"
    - Source: apply_edit_memory содержит `p["display_name"] = edit["name"]`
    - Source: products_to_rows формирует первую колонку как `p.get("display_name") or p["name"]`
    - Source: get_badge / get_photo_url / get_photo_description по-прежнему вызываются с p["name"] (ключ прайса не подменён)
    - Behavior: ast.parse проходит; в файле присутствуют display_name и тип "name"
  </acceptance_criteria>
  <done>upload.py применяет правку name: отображаемое имя меняется, ключ сопоставления сохраняется, остальные правки и старое поведение не затронуты.</done>
</task>

<task type="auto">
  <name>Task 2: admin.py — поле «Отображаемое название» в форме + /save type=name</name>
  <read_first>
    - uploader/admin.py (созданный в плане 01 — экран правки, /save, PAGE/JS apiCall)
    - .planning/phases/04-1/04-UI-SPEC.md (экран 2: поле «Отображаемое название», hint, состояние кнопки «Сохранить правку»; копирайтинг-контракт)
    - scripts/sheet_helper.py (CLI append_edit --type name)
  </read_first>
  <action>
    Расширь экран правки товара в uploader/admin.py. Добавь над полем группы поле «Отображаемое название»: form-label «Отображаемое название», input.form-control (предзаполнить текущей правкой name если есть, иначе пусто), hint text-muted «Оставьте пустым, чтобы использовать название из прайса». Над ним показывай неизменяемую подпись «Название (из прайса): «<имя из прайса>»». В /save разреши type="name" (расширь белый список плана 01: теперь {group, name}); пустое значение name трактуй как «правка не нужна» — но в первой версии достаточно записывать строку как есть (память применит её). Кнопка «Сохранить правку» (btn-primary) активируется при любом изменении поля (disabled пока нет изменений — по UI-SPEC). При сохранении вызывай существующий apiCall → POST /<token>/save с {key: normalize_name(имя из прайса), type:"name", value: введённое}; ключ получай через sheet_helper normalize (или нормализуй на сервере в /save — надёжнее). show() с текстом успеха из UI-SPEC. Русские комментарии.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('uploader/admin.py',encoding='utf-8').read()); print('ok')"; python -X utf8 -c "src=open('uploader/admin.py',encoding='utf-8').read(); assert 'Отображаемое название' in src and 'name' in src; print('name field present')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: uploader/admin.py PAGE содержит текст «Отображаемое название» и hint «Оставьте пустым, чтобы использовать название из прайса»
    - Source: /save белый список типов теперь включает "name"
    - Source: ключ правки = нормализованное имя из прайса (normalize на сервере или через sheet_helper)
    - Behavior: ast.parse проходит; поле названия присутствует в шаблоне
  </acceptance_criteria>
  <done>Форма правки умеет менять отображаемое название; /save пишет тип name в память; ключ сопоставления — нормализованное имя из прайса.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер владельца → admin.py /save (type=name) | пользовательский текст названия → память правок |
| upload.py ← вкладка «Правки» (тип name) | применение правки к выводу каталога |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | подмена ключа сопоставления через правку name | mitigate | apply_edit_memory меняет ТОЛЬКО display_name; normalize_name(p["name"]) остаётся ключом — правка name не может «увести» товар на другой ключ (D-05) |
| T-04-07 | Tampering | мусорный type в «Правки» | mitigate | ALLOWED_TYPES в load_edit_memory + белый список в /save = {group,name}; неизвестные типы логируются и пропускаются (поведение этапа 3) |
| T-04-08 | Injection | значение названия попадает в лист «Товары» | accept | значение пишется как данные (append_row/USER_ENTERED только в upload.py при перезаписи «Товары»); риск формулы-инъекции в Google Sheet низкий — лист читается сайтом как текст, не исполняется |
| T-04-SC | Tampering | pip installs | accept | новых пакетов не ставится |
</threat_model>

<decisions_covered>
## Решения CONTEXT.md, реализуемые этим планом
- D-05: редактирование отображаемого названия через новый тип правки name; ключ сопоставления (normalize_name) не меняется (Task 1/2)
- D-07: цена сознательно НЕ редактируется в панели — /save принимает только {group, name}, поля цены в форме нет (Task 2)
</decisions_covered>

<verification>
- `python -c "import ast; ast.parse(open('scripts/upload.py',encoding='utf-8').read())"` → ok; присутствуют display_name и тип "name"
- `python -c "import ast; ast.parse(open('uploader/admin.py',encoding='utf-8').read())"` → ok; присутствует поле «Отображаемое название»
- Ручная (на сервере, план 04): правка name → пересборка → товар выводится под новым именем, фото/группа того же товара сохраняются
</verification>

<success_criteria>
ADM-03 закрыт в части названия: владелец исправляет отображаемое имя, правка переживает обновления и применяется автоматически, ключ сопоставления с прайсом не ломается. Цена остаётся вне границ (D-07).
</success_criteria>

<output>
Создай `.planning/phases/04-1/04-02-SUMMARY.md` по завершении.
</output>
