---
phase: 03-1
plan: 02
type: execute
wave: 2
depends_on: ["03-1-01"]
files_modified:
  - scripts/upload.py
  - scripts/migrate_overrides.py
autonomous: false
requirements: [MEM-01]
must_haves:
  truths:
    - "Зашитые в код переопределения групп (PRODUCT_OVERRIDES) перенесены в память правок (вкладка «Правки») (D-07: вынос захардкоженных правил в память; D-01: хранилище-вкладка)"
    - "После переноса добавление нового переопределения группы не требует правки кода — только строки во вкладке"
    - "Существующие JSON-файлы правок (photo_overrides.json, description_overrides.json, category_map.json) продолжают работать без изменений"
  artifacts:
    - path: "scripts/migrate_overrides.py"
      provides: "Разовая CLI-утилита: PRODUCT_OVERRIDES/PRODUCT_CONTAINS_OVERRIDES → строки во вкладке «Правки» (с --dry-run); чистая функция раскрытия префиксов для локального теста"
      contains: "def expand_overrides"
    - path: "scripts/upload.py"
      provides: "Удалённые/обезвреженные захардкоженные словари переопределений групп после миграции"
  key_links:
    - from: "migrate_overrides.py"
      to: "вкладка «Правки» в Google Sheet"
      via: "batch-запись строк правок типа group через gspread"
      pattern: "value_input_option"
---

<objective>
Закрыть зафиксированный анти-паттерн «маппинг группы захардкожен в коде» (D-07): перенести
словари `PRODUCT_OVERRIDES` и `PRODUCT_CONTAINS_OVERRIDES` из `scripts/upload.py` в память
правок (вкладка «Правки», тип правки `group`). После этого правила групп добавляются без
участия программиста — прямо во вкладке.

Ключевая тонкость (отмечена pattern-mapper'ом): старые словари сопоставляют по ПРЕФИКСУ/
ПОДСТРОКЕ названия, а память — по ТОЧНОМУ нормализованному имени (D-04). План разрешает этот
семантический разрыв явно: миграция раскрывает каждое префиксное/подстрочное правило в
конкретные нормализованные имена товаров текущего каталога, попадающих под правило.

Это срез-уточнение поверх рабочего ядра памяти (План 01): ядро уже умеет читать и применять
правки типа `group`; здесь мы наполняем память реальными правилами и убираем дубль из кода.

Назначение: убрать необходимость деплоить код ради нового переопределения группы.
Результат: новая утилита `scripts/migrate_overrides.py` (с `--dry-run`) и очищенный
`scripts/upload.py` без захардкоженных словарей групповых переопределений.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-1/03-CONTEXT.md
@.planning/phases/03-1/03-PATTERNS.md
@.planning/phases/03-1/03-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Извлечено из кода. Контракты для миграции и очистки. -->

Из scripts/upload.py — что переносим (D-07), строки 304-344:
```python
PRODUCT_OVERRIDES = { "Набор конфет ЛЮСИ": "Коробочные конфеты", ... }   # поиск по началу строки
PRODUCT_CONTAINS_OVERRIDES = [ ("паста шок", "Батончики и шоколад"), ... ] # поиск по подстроке
def apply_product_override(name): ...   # читает оба словаря; вызывается из apply_group_mapping
def apply_group_mapping(products, category_map): ...  # вызывает apply_product_override
```

Из scripts/sheet_tool.py — образец CLI-утилиты записи в лист (копировать паттерн):
```python
def open_spreadsheet():  # авторизация Service Account, GOOGLE_SHEETS_ID, scopes spreadsheets+drive
    ...
    return gspread.authorize(creds).open_by_key(sheets_id)
def copy_sheet(ss, src_name, dst_name):  # создание/очистка листа: add_worksheet / clear / resize / update(value_input_option="USER_ENTERED")
    ...
```

Из scripts/upload.py — текущий каталог для раскрытия префиксов в точные имена:
load_current_groups() возвращает {normalize_name(name): группа} по листу «Товары» — источник
реальных названий товаров, к которым применять старые префиксные/подстрочные правила.

Формат/колонки вкладки «Правки» — взять ИЗ 03-01-SUMMARY.md (зафиксированы в Плане 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Утилита migrate_overrides.py — раскрыть префиксы в точные имена и записать во вкладку «Правки»</name>
  <files>scripts/migrate_overrides.py</files>
  <read_first>
    - scripts/upload.py: PRODUCT_OVERRIDES, PRODUCT_CONTAINS_OVERRIDES, apply_product_override (стр. 304-344) — источник правил
    - scripts/upload.py: load_current_groups (стр. 254-299) и normalize_name (стр. 213-215) — список реальных товаров и нормализация
    - scripts/sheet_tool.py: open_spreadsheet (стр. 43-65), copy_sheet (стр. 68-90), диспетчер команд main (стр. 93-144) — структура CLI и запись в лист
    - .planning/phases/03-1/03-01-SUMMARY.md: точные имена колонок вкладки «Правки» и формат значения
    - .planning/phases/03-1/03-CONTEXT.md: D-04 (точное совпадение), D-07 (что и зачем переносим), Claude's Discretion (способ миграции)
    - .planning/codebase/CONVENTIONS.md: Python (русские docstrings, --dry-run обязателен, raise SystemExit(1), логирование)
  </read_first>
  <action>
    Создать новый скрипт `scripts/migrate_overrides.py` — разовую CLI-утилиту в стиле
    `sheet_tool.py`. Модульный docstring (имя, назначение, usage с `--dry-run`, требуемые
    env/файлы). Скопировать паттерн авторизации из `sheet_tool.open_spreadsheet`
    (Service Account, `GOOGLE_SHEETS_ID`, scopes spreadsheets+drive) — новых env не вводить.

    Импортировать из `upload.py` правила и хелперы: `PRODUCT_OVERRIDES`,
    `PRODUCT_CONTAINS_OVERRIDES`, `normalize_name`, `load_current_groups` (через
    `from upload import ...` либо `importlib`, как удобнее; путь scripts/ в sys.path).

    КЛЮЧЕВОЕ: вынести раскрытие правил в ОТДЕЛЬНУЮ ЧИСТУЮ ФУНКЦИЮ (для локального теста без
    Google, см. Task verify):
    `expand_overrides(product_names: list[str]) -> dict[str, str]` — принимает список РЕАЛЬНЫХ
    названий товаров, возвращает `{normalize_name(имя): группа}` для тех имён, что попали под
    префиксное (startswith по PRODUCT_OVERRIDES, регистронезависимо) или подстрочное
    (substring по PRODUCT_CONTAINS_OVERRIDES) правило. Логика сопоставления — копия
    `apply_product_override` (startswith → substring; первое сработавшее правило побеждает).
    Так каждое широкое правило превращается в набор точечных правок по ТОЧНОМУ нормализованному
    имени (разрешение семантического разрыва D-04). Функция не делает сетевых вызовов — это
    позволяет проверить раскрытие локально на синтетическом списке имён (см. verify).

    Боевой путь: получить реальные названия товаров из листа «Товары» (через тот же
    `get_all_values` / load_current_groups — способ на усмотрение исполнителя, но ключ результата —
    `normalize_name`), передать их в `expand_overrides`, получить точечные правки group.

    Сбор строк для вкладки «Правки»: использовать формат колонок из 03-01-SUMMARY.md
    (`Товар | Тип | Значение` или зафиксированный там). Тип = `group`. Дедуплицировать по
    нормализованному имени. Если товар уже имеет правку group во вкладке — НЕ перезаписывать
    (миграция разовая, не затирает ручные правки): дочитать существующую вкладку и пропускать
    уже присутствующие ключи (log.info про пропуски).

    Запись: дописать новые строки во вкладку «Правки» (append, НЕ clear — там могут быть
    ручные правки владельца из Плана 01). Использовать `worksheet.append_rows(rows,
    value_input_option="USER_ENTERED")` либо чтение+resize+update; вкладку создать при
    отсутствии (паттерн `copy_sheet`/`upload_to_google_sheet`). Значения — простые строки-данные.

    Обязательный `--dry-run`: при флаге печатать, сколько и каких строк было бы записано
    (по каждому исходному правилу — сколько товаров раскрылось), без записи в Google.
    Фатальные ошибки — `raise SystemExit(1)`. Русские комментарии и логи. Реализует MEM-01
    (наполнение персистентной памяти переносимыми правилами групп).

    Примечание исполнителю: команды verify используют bash-конструкции (python -c)
    — запускать через Bash-tool, не через PowerShell.
  </action>
  <verify>
    <!-- Запускать через Bash-tool (не PowerShell): локальная проверка раскрытия БЕЗ Google. -->
    <!-- Проверяет ИМЕННО ценность миграции — раскрытие префикса/подстроки в точное имя (D-04↔D-07), -->
    <!-- не делает сетевых вызовов, не требует credentials. -->
    <automated>python -c "import sys; sys.path.insert(0,'scripts'); import importlib.util as u; spec=u.spec_from_file_location('mo','scripts/migrate_overrides.py'); m=u.module_from_spec(spec); spec.loader.exec_module(m); import upload; pref=next(iter(upload.PRODUCT_OVERRIDES.items())); sub=upload.PRODUCT_CONTAINS_OVERRIDES[0]; pname=pref[0]+' 200г'; sname='шоколадная '+sub[0]+' молочная'; res=m.expand_overrides([pname, sname, 'Товар без правила']); kp=upload.normalize_name(pname); ks=upload.normalize_name(sname); assert kp in res and res[kp]==pref[1], 'префикс не раскрылся в точное имя: '+repr((kp,res.get(kp))); assert ks in res and res[ks]==sub[1], 'подстрока не раскрылась в точное имя: '+repr((ks,res.get(ks))); assert upload.normalize_name('Товар без правила') not in res, 'товар без правила не должен попасть'; assert all('startswith' not in k and not k.endswith('*') for k in res), 'ключи должны быть точными именами, не префиксами'; print('PASS: префикс и подстрока раскрыты в точные нормализованные имена (D-04), %d правок' % len(res))"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `scripts/migrate_overrides.py` существует и парсится Python без ошибок
    - Есть чистая функция `expand_overrides(product_names) -> {норм_имя: группа}` без сетевых вызовов
    - Локальная проверка (verify выше) ДОКАЗЫВАЕТ раскрытие префикса И подстроки в точные нормализованные имена (ключ результата = точное имя, не префикс) — без Google
    - Поддерживает `--dry-run` (боевой запуск печатает план без записи; при отсутствии credentials корректно сообщает и выходит)
    - Импортирует PRODUCT_OVERRIDES/PRODUCT_CONTAINS_OVERRIDES/normalize_name из upload.py
    - Дозапись (append), не затирает существующие правки владельца во вкладке
    - Русские docstring/комментарии; фатальные ошибки через raise SystemExit(1)
  </acceptance_criteria>
  <done>migrate_overrides.py создан; функция expand_overrides раскрывает префиксы/подстроки в точные имена и это доказано локальным тестом без Google; --dry-run готов; утилита готова к боевому запуску на сервере.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Чекпоинт — боевой запуск миграции во вкладку «Правки» (через sysadmin)</name>
  <action>
    Запустить `scripts/migrate_overrides.py` на сервере daniella ТОЛЬКО через субагента sysadmin
    (правило проекта: серверные операции и доступ к боевой таблице — только через sysadmin).
    Сначала `--dry-run`, затем боевой запуск. Дождаться подтверждения, что строки type=group
    появились во вкладке «Правки» и upload.py их читает. Без подтверждения «применено» НЕ
    переходить к Task 3 (удалению словарей из кода).
  </action>
  <what-built>
    Утилита `scripts/migrate_overrides.py` готова, локально доказано раскрытие префиксов/подстрок
    в точные имена (Task 1). Боевой запуск (без --dry-run) пишет строки правок типа `group`
    во вкладку «Правки» рабочей Google-таблицы. Это разовое наполнение памяти — выполняется на
    сервере daniella через субагента sysadmin, потому что локально может не быть боевых
    credentials/доступа.
  </what-built>
  <how-to-verify>
    1. Через субагента sysadmin запустить на сервере (venv /opt/apps/catalog/.venv) сначала
       `python scripts/migrate_overrides.py --dry-run` — убедиться, что число раскрытых правок
       выглядит разумно (примерно по числу товаров, попадающих под старые правила).
    2. Затем боевой запуск `python scripts/migrate_overrides.py` — строки записаны во вкладку «Правки».
    3. Открыть Google-таблицу → вкладка «Правки»: появились строки с типом `group` и точными
       названиями товаров. Ручные правки владельца (если были добавлены при тесте Плана 01) — на месте.
    4. Прогнать `python scripts/upload.py --dry-run` — в логе видно «Загружено правок из памяти: N»
       (N > 0) и переопределения групп применяются.
  </how-to-verify>
  <verify>
    <human-check>Строки type=group появились во вкладке «Правки»; upload.py --dry-run видит их в логе «Загружено правок из памяти: N»</human-check>
  </verify>
  <done>Память наполнена перенесёнными правилами групп через боевой запуск миграции; подтверждено владельцем/sysadmin.</done>
  <resume-signal>Напишите «применено» когда строки появились во вкладке «Правки» и upload.py их видит, или опишите проблему.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Удалить захардкоженные словари переопределений из upload.py</name>
  <files>scripts/upload.py</files>
  <read_first>
    - scripts/upload.py: PRODUCT_OVERRIDES, PRODUCT_CONTAINS_OVERRIDES, apply_product_override, apply_group_mapping (стр. 304-373)
    - scripts/upload.py: блок наложения памяти из Плана 01 (apply_edit_memory) — теперь единственный источник group-переопределений
    - .planning/phases/03-1/03-CONTEXT.md: D-07 (перенос завершён → код чистим), D-06 (порядок: авто → память)
  </read_first>
  <action>
    ТОЛЬКО после подтверждения «применено» в предыдущем чекпоинте (правки group уже в памяти).
    ВНИМАНИЕ: migrate_overrides.py импортирует PRODUCT_OVERRIDES/PRODUCT_CONTAINS_OVERRIDES из
    upload.py — после их удаления повторный запуск миграции станет невозможен (это ожидаемо:
    миграция разовая и уже выполнена). Зафиксировать это в SUMMARY.

    Удалить из `scripts/upload.py` словари `PRODUCT_OVERRIDES` (стр. 304-322) и
    `PRODUCT_CONTAINS_OVERRIDES` (стр. 325-328) и функцию `apply_product_override`
    (стр. 331-344). Из `apply_group_mapping` убрать вызов `apply_product_override` и
    связанный блок `override`/счётчик `overridden` (стр. 358-361, 370-371) — группа теперь
    переопределяется ИСКЛЮЧИТЕЛЬНО памятью правок (apply_edit_memory из Плана 01),
    которая накладывается позже в main(). `apply_group_mapping` остаётся отвечать только за
    category_map → display_group (+ «Другое» для немаппленных) и предупреждение о немаппленных.

    Не трогать существующие JSON-механизмы (photo_overrides.json, description_overrides.json,
    category_map.json) — они продолжают работать. Сохранить логирование о немаппленных категориях.
    Реализует завершение D-07 (анти-паттерн устранён: переопределения групп больше не в коде).

    Примечание исполнителю: команда verify использует bash-конструкции (python -c)
    — запускать через Bash-tool, не через PowerShell.
  </action>
  <verify>
    <!-- Запускать через Bash-tool (не PowerShell): команда самодостаточна, без head/pipe/2>&1. -->
    <automated>python -c "import ast; src=open('scripts/upload.py',encoding='utf-8').read(); t=ast.parse(src); names=[n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)]; assert 'apply_product_override' not in names, 'apply_product_override не удалена'; assert 'PRODUCT_OVERRIDES' not in src, 'PRODUCT_OVERRIDES остался'; assert 'PRODUCT_CONTAINS_OVERRIDES' not in src, 'PRODUCT_CONTAINS_OVERRIDES остался'; assert 'def apply_group_mapping' in src, 'apply_group_mapping пропала'; print('OK: захардкоженные переопределения удалены, apply_group_mapping на месте')"</automated>
  </verify>
  <acceptance_criteria>
    - В `scripts/upload.py` НЕТ `PRODUCT_OVERRIDES`, `PRODUCT_CONTAINS_OVERRIDES`, `apply_product_override` (команда verify проходит)
    - `apply_group_mapping` сохранена и отвечает только за category_map → display_group + «Другое» + лог немаппленных
    - Файл парсится без ошибок; existing JSON-механизмы не затронуты
    - Переопределение группы теперь происходит только через память (apply_edit_memory)
  </acceptance_criteria>
  <done>Захардкоженные словари групповых переопределений удалены из upload.py; источник переопределений групп — память правок.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrate_overrides.py → вкладка «Правки» | Утилита ПИШЕТ в боевую таблицу — операция изменения данных |
| текущий каталог (лист «Товары») → раскрытие правил | Имена товаров используются как недоверенный ввод для генерации правок |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | боевой запуск migrate_overrides.py затирает ручные правки владельца | mitigate | append, НЕ clear; пропуск уже существующих ключей (товар уже имеет group-правку); обязательный `--dry-run` перед боевым; запуск под контролем через sysadmin-чекпоинт |
| T-03-07 | Repudiation/Error | удаление словарей из upload.py до фактической миграции → потеря правил | mitigate | блокирующий чекпоинт «применено» ОБЯЗАТЕЛЕН перед Task 3 (удалением); порядок задач это гарантирует; раскрытие правил доказано локальным тестом до боевого запуска |
| T-03-08 | Information Disclosure | credentials.json при серверном запуске | accept (existing control) | секреты в env/.gitignore; серверный доступ только через sysadmin (правило проекта); новых каналов нет |
| T-03-SC | Tampering | установка пакетов (npm/pip/cargo) | n/a | новых пакетов не ставится — gspread/google-auth уже в окружении; install-задач нет |
</threat_model>

<verification>
- Локальная проверка Task 1 (без Google) доказывает раскрытие префикса И подстроки в точные нормализованные имена (D-04↔D-07)
- `migrate_overrides.py --dry-run` на сервере показывает корректное число раскрытых правок (чекпоинт)
- После боевого запуска (чекпоинт) вкладка «Правки» содержит строки type=group; upload.py их читает
- AST-проверка Task 3 подтверждает удаление захардкоженных словарей
- `upload.py` парсится без ошибок; existing JSON-механизмы работают
- Все команды verify запускаются через Bash-tool (не PowerShell)
</verification>

<success_criteria>
- MEM-01 (расширение): правила переопределения групп вынесены из кода в персистентную память (вкладка «Правки»)
- Анти-паттерн «PRODUCT_OVERRIDES захардкожен в коде» устранён (D-07)
- Добавление нового переопределения группы больше не требует правки кода/деплоя
- Раскрытие префиксных/подстрочных правил в точные имена доказано локально (без Google)
- Существующие JSON-механизмы правок не затронуты
</success_criteria>

<output>
Создать `.planning/phases/03-1/03-02-SUMMARY.md` по завершении. Зафиксировать: сколько правил
раскрылось в сколько точечных правок group; подтверждение боевой записи во вкладку (через
sysadmin); подтверждение, что захардкоженные словари удалены и каталог корректно собирается;
заметку, что после удаления словарей повторный запуск миграции невозможен (миграция разовая).
</output>
