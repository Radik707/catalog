---
phase: 03-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/upload.py
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03]
must_haves:
  truths:
    - "Правка владельца во вкладке «Правки» (группа/фото/описание) переживает перезапись листа «Товары» (D-01: хранилище-вкладка; D-02: схема строки-правки; D-03: объём группа+фото+описание)"
    - "При следующем прогоне upload.py сохранённая правка применяется автоматически — поверх авто-маппинга (D-06: правка владельца побеждает авто-данные)"
    - "В логе прогона выводится число товаров, отсутствующих в памяти правок (новые для разметки) (D-05: новый товар = нет в памяти)"
  artifacts:
    - path: "scripts/upload.py"
      provides: "Функция load_edit_memory() — чтение вкладки «Правки» через gspread + Service Account"
      contains: "def load_edit_memory"
    - path: "scripts/upload.py"
      provides: "Чистая функция apply_edit_memory() — наложение памяти поверх авто-данных, вызывается из main()"
      contains: "def apply_edit_memory"
  key_links:
    - from: "main() в scripts/upload.py"
      to: "load_edit_memory()"
      via: "вызов загрузки памяти рядом с load_photo_data()"
      pattern: "load_edit_memory\\(\\)"
    - from: "main() в scripts/upload.py"
      to: "apply_edit_memory()"
      via: "вызов наложения памяти после apply_group_mapping, перед products_to_rows"
      pattern: "apply_edit_memory\\("
    - from: "наложение памяти (apply_edit_memory)"
      to: "normalize_name(p['name'])"
      via: "ключ сопоставления правки и товара (точное совпадение, D-04)"
      pattern: "normalize_name"
---

<objective>
Построить ядро памяти правок: читать новую вкладку «Правки» той же Google-таблицы и
автоматически накладывать сохранённые правки (группа, фото, описание) поверх данных
прайса при каждом прогоне `upload.py`. Это сквозной рабочий срез этапа: владелец один раз
добавляет строку правки во вкладку (руками, без панели Этапа 4) → правка переживает
полную перезапись листа «Товары» → при следующем обновлении применяется сама.

Дополнительно: вычислять и логировать число товаров, которых ещё нет в памяти правок
(основа критерия «новый товар требует ручной разметки», MEM-03 / D-05).

Цель (история пользователя): **Как** владелец каталога, **я хочу** один раз исправить
группу/фото/описание товара во вкладке «Правки», **чтобы** это исправление автоматически
применялось при каждом обновлении и я не повторял ручную работу.

Назначение: устранить корневую боль — `upload.py` полностью перезаписывает лист «Товары»,
затирая любые ручные исправления. Память правок выносит исправления вне зоны перезаписи.
Результат: изменённый `scripts/upload.py` с новой функцией чтения памяти, точкой
интеграции в `main()` и логом новых товаров.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-1/03-CONTEXT.md
@.planning/phases/03-1/03-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Готовые функции upload.py, которые исполнитель ПЕРЕИСПОЛЬЗУЕТ. Извлечено из кода. -->
<!-- Не исследовать кодовую базу заново — контракты ниже. -->

Из scripts/upload.py (ключ сопоставления правки, D-04 — переиспользовать как есть):
```python
def normalize_name(name: str) -> str:
    """Нормализовать название для сопоставления (без хвостовой единицы, нижний регистр)."""
    return re.sub(r"\s+", " ", clean_new_name(name)).strip().lower()
```

Из scripts/upload.py (образец чтения отдельного листа через gspread + Service Account,
graceful-fallback → {}; копировать скелет авторизации и обработки ошибок):
```python
def load_current_groups() -> dict:
    try:
        import gspread
        from google.oauth2.service_account import Credentials
    except ImportError:
        return {}
    # ... перебор GOOGLE_CREDENTIALS_PATH / credentials.json в SCRIPT_DIR,PROJECT_ROOT ...
    # ... GOOGLE_SHEETS_ID, scopes spreadsheets+drive ...
    try:
        creds = Credentials.from_service_account_file(creds_path, scopes=scopes)
        ss = gspread.authorize(creds).open_by_key(sheets_id)
        values = ss.worksheet("Товары").get_all_values()
    except Exception as e:  # noqa: BLE001
        log.warning("Не удалось прочитать текущие группы: %s", e)
        return {}
    # ... сборка {normalize_name(name): группа} ...
```

ВНИМАНИЕ (D-04): фото/описание сейчас подставляются ПОДСТРОЧНЫМ поиском —
`_find_photo_entry` (upload.py:453-461) сопоставляет по вхождению подстроки в обе стороны.
Для ПРАВОК этот путь использовать НЕЛЬЗЯ: короткое имя товара может приклеить правку к
другому товару, чьё имя его содержит. Правки фото/описания применяются строго по точному
`normalize_name` (см. Task 2, вариант (а) с override-полями).

Из scripts/upload.py — текущая точка интеграции в main() (строки 658-701):
photo_data = load_photo_data() (стр. 659) — загрузка фото; apply_group_mapping (стр. 677);
блок нового формата с new_names (стр. 681-698); products_to_rows(...) (стр. 701) — формирует
строки и ВНУТРИ через get_photo_url/get_photo_description (стр. 504-505) подставляет фото/описание.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Функция load_edit_memory() — чтение вкладки «Правки»</name>
  <files>scripts/upload.py</files>
  <read_first>
    - scripts/upload.py: load_current_groups (стр. 254-299) — образец авторизации и graceful-fallback для копирования
    - scripts/upload.py: normalize_name (стр. 213-215) — ключ словаря памяти
    - scripts/upload.py: load_badges / load_category_map / load_photo_data — паттерн «нет источника → пустая структура, не падать»
    - .planning/phases/03-1/03-PATTERNS.md: раздел «load_edit_memory()» — точный скелет и рекомендуемая форма возврата
    - .planning/phases/03-1/03-CONTEXT.md: D-01, D-02, D-04 и Claude's Discretion (имя вкладки, колонки, формат)
    - .planning/codebase/CONVENTIONS.md: раздел Python (русские docstrings, log.info/warning, type-аннотации, ленивый импорт gspread)
  </read_first>
  <behavior>
    - Нет gspread / нет credentials.json / нет GOOGLE_SHEETS_ID → возвращает {} без исключения
    - Вкладки «Правки» нет (WorksheetNotFound) → возвращает {} и пишет log.info (это норма, не ошибка)
    - Ошибка доступа к таблице → возвращает {} и пишет log.warning
    - Корректная вкладка → возвращает {normalize_name(имя): {тип_правки: значение, ...}}, типы из множества {group, photo, description}
    - Строка с пустым ключом товара или нераспознанным типом правки пропускается (skip + log.warning), весь прогон не падает (fail-safe)
    - Значение ячейки берётся как обычная строка-данные (без выполнения как формула/команда — защита от инъекции)
  </behavior>
  <action>
    Добавить новую функцию `load_edit_memory() -> dict[str, dict[str, str]]` в `scripts/upload.py`
    рядом с `load_current_groups` (после неё, до блока PRODUCT_OVERRIDES). Скопировать скелет
    авторизации и graceful-fallback из `load_current_groups` целиком: ленивый импорт `gspread` +
    `Credentials` с `except ImportError: return {}`; поиск `creds_path` через
    `GOOGLE_CREDENTIALS_PATH` → перебор `credentials.json` в `SCRIPT_DIR`/`PROJECT_ROOT`;
    `GOOGLE_SHEETS_ID`; те же `scopes` (spreadsheets + drive). Новых env-переменных НЕ вводить.

    Имя вкладки — `"Правки"` (на усмотрение исполнителя по Claude's Discretion, но именно это
    зафиксировано в D-01). Читать через `ss.worksheet("Правки").get_all_values()`. Различать
    случаи: `except gspread.exceptions.WorksheetNotFound: log.info("Вкладка «Правки» не найдена — память пуста"); return {}`
    ПЕРЕД широким `except Exception as e: log.warning(...); return {}`. Образец точечного
    `WorksheetNotFound` — `sheet_tool.py:73,81` и `upload.py:597`.

    Схема вкладки (D-02, расширяемая): по строке на правку, минимум колонки —
    ключ товара (нормализованное имя), тип правки, значение. Заголовки задать по-русски,
    например `Товар | Тип | Значение` (точные имена — на усмотрение исполнителя, зафиксировать
    выбор в docstring и в SUMMARY). Найти индексы колонок через `header.index(...)` с
    `except ValueError: return {}` (как в `load_current_groups`).

    Собрать вложенный словарь: ключ = `normalize_name(<ячейка-товар>)`, значение = словарь
    `{<тип>: <значение>}`, накапливая несколько типов на один товар. Допустимые типы правок в
    Этапе 3 — только `group`, `photo`, `description` (D-03); строку с пустым товаром, пустым
    типом или типом вне этого множества — пропустить с `log.warning` и продолжить (fail-safe,
    одна плохая строка не ломает прогон). Значение ячейки трактовать как простую строку-данные
    (никакого `eval`/исполнения; `value_render_option` не требуется для чтения значений).
    В конце — `log.info("Загружено правок из памяти: %d", len(mapping))`. Вернуть словарь.

    Стиль: русские docstring и комментарии, type-аннотация `-> dict[str, dict[str, str]]`,
    логирование через `log`. Реализует MEM-01 (персистентное хранилище памяти).
  </action>
  <verify>
    <!-- Запускать через Bash-tool (не PowerShell): python -c самодостаточен, без head/pipe/2>&1. -->
    <automated>python -c "import ast,sys; t=ast.parse(open('scripts/upload.py',encoding='utf-8').read()); assert any(isinstance(n,ast.FunctionDef) and n.name=='load_edit_memory' for n in ast.walk(t)), 'load_edit_memory не найдена'; print('OK: load_edit_memory определена')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/upload.py` содержит `def load_edit_memory(` с type-аннотацией `-> dict[str, dict[str, str]]`
    - Файл парсится Python без синтаксических ошибок (команда verify выше проходит)
    - Функция использует `normalize_name(...)` для ключа (grep `normalize_name` внутри тела функции)
    - Есть отдельная обработка `WorksheetNotFound` (log.info) и широкого `Exception` (log.warning), оба возвращают `{}`
    - Есть `log.info` со строкой о числе загруженных правок (русская формулировка)
    - Нет новых env-переменных (grep по новым `os.environ` ограничен `GOOGLE_CREDENTIALS_PATH`/`GOOGLE_SHEETS_ID`)
  </acceptance_criteria>
  <done>Функция load_edit_memory() добавлена, парсится без ошибок, возвращает память правок или {} при любой проблеме доступа.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Функция apply_edit_memory() + интеграция в main() + лог новых товаров</name>
  <files>scripts/upload.py</files>
  <read_first>
    - scripts/upload.py: main() блок интеграции (стр. 658-701) — точка вставки памяти (после apply_group_mapping и блока нового формата, перед products_to_rows)
    - scripts/upload.py: products_to_rows (стр. 476-507) — где формируются ImageUrl/Description; get_photo_url/get_photo_description (стр. 464-473) и _find_photo_entry (стр. 453-461) — ПОДСТРОЧНЫЙ поиск (его НЕЛЬЗЯ использовать для правок, см. ниже)
    - scripts/upload.py: load_photo_data overrides-блок (стр. 437-447) — образец «правка владельца побеждает авто-данные» (D-06), но он кладёт ключ name.lower() и тоже читается через _find_photo_entry (подстрока!) — для D-04 этот путь НЕ подходит
    - .planning/phases/03-1/03-PATTERNS.md: разделы «Точка интеграции в main()» и «Определение новый товар»
    - .planning/phases/03-1/03-CONTEXT.md: D-03 (объём: группа+фото+описание), D-04 (ТОЧНОЕ совпадение нормализованного имени), D-05 (новый товар = нет в памяти), D-06 (порядок: авто → память)
  </read_first>
  <behavior>
    - Правка типа group для товара X → его display_group становится значением из памяти (перебивает авто-маппинг)
    - Правка типа photo для товара X → его ImageUrl в итоговой строке становится значением из памяти
    - Правка типа description для товара X → его Description в итоговой строке становится значением из памяти
    - Товар без записи в памяти → данные остаются от авто-маппинга (память ничего не ломает)
    - Правка photo/description привязывается СТРОГО по точному normalize_name (D-04): короткое имя товара НЕ должно приклеиться к другому товару, чьё имя его содержит (в отличие от _find_photo_entry)
    - Память загружается и применяется при любом формате прайса (не только new_format)
    - В логе выводится число товаров, чьего normalize_name НЕТ в памяти правок (новые для разметки, MEM-03)
  </behavior>
  <action>
    Реализовать наложение памяти СРАЗУ как чистую функцию (не инлайн в main с последующим
    рефакторингом). Это та же функция, что покрывается локальным тестом в Task 3.

    1) Добавить функцию
       `apply_edit_memory(products: list[dict], edit_memory: dict[str, dict[str, str]]) -> int`
       рядом с apply_group_mapping. Она проходит по `products`; для каждого `p` берёт
       `edit = edit_memory.get(normalize_name(p["name"]))`; если запись есть:
         - `group` → `p["display_group"] = edit["group"]`;
         - `photo` → `p["photo_override"] = edit["photo"]`;
         - `description` → `p["desc_override"] = edit["description"]`.
       Возвращает число товаров, у которых `normalize_name(p["name"]) not in edit_memory`
       (счётчик «новых для памяти», MEM-03 / D-05). Любой тип правки и их комбинация
       накладываются независимо. Русский docstring, type-аннотации.

    2) ТОЧНОЕ совпадение для фото/описания (D-04 — обязательно). НЕ использовать путь
       `_find_photo_entry` / `get_photo_url` / `get_photo_description` для правок: он ищет по
       ПОДСТРОКЕ и для короткого имени может выдать данные другого товара (нарушение D-04).
       Поэтому выбран вариант (а): правка пишется в отдельные поля `p["photo_override"]` /
       `p["desc_override"]`, а `products_to_rows` отдаёт им приоритет. Изменить две строки
       формирования ImageUrl/Description в products_to_rows так, чтобы сначала проверялось
       override-поле товара, и только при его отсутствии вызывался существующий getter:
         - ImageUrl   = `p.get("photo_override") or get_photo_url(p["name"], photo_data)`
         - Description = `p.get("desc_override") or get_photo_description(p["name"], photo_data)`
       Так правка владельца сопоставлена строго по точному имени (через apply_edit_memory),
       а подстрочный авто-поиск остаётся только для авто-фото у товаров без правки.
       Группа сопоставляется напрямую по точному имени и проблемы подстроки не имеет.

    3) В `main()` загрузить память рядом с фото: сразу после `photo_data = load_photo_data()`
       (стр. 659) добавить `edit_memory = load_edit_memory()`. Вызвать наложение ПОСЛЕ
       `apply_group_mapping` и блока нового формата (после строки ~698), ПЕРЕД
       `rows = products_to_rows(...)` (стр. 701):
       `new_for_memory = apply_edit_memory(all_products, edit_memory)`. Порядок строго D-06:
       авто-маппинг → поверх память.

    4) Лог новых товаров (MEM-03 / D-05): вывести
       `log.info("Товаров без правок (новые для памяти): %d", new_for_memory)`. Логировать
       достаточно — панели в Этапе 3 нет.

    Реализует MEM-02 (авто-применение) и MEM-03 (вычислимость новых товаров).
    Примечание исполнителю: команды verify используют bash-конструкции (python -c)
    — запускать через Bash-tool, не через PowerShell.
  </action>
  <verify>
    <!-- Запускать через Bash-tool (не PowerShell): команда самодостаточна, без head/pipe/2>&1. -->
    <automated>python -c "import ast; src=open('scripts/upload.py',encoding='utf-8').read(); t=ast.parse(src); names=[n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)]; assert 'apply_edit_memory' in names, 'apply_edit_memory не определена'; m=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='main'][0]; body=ast.get_source_segment(src,m); assert 'load_edit_memory()' in body, 'память не загружается в main'; assert 'apply_edit_memory(' in body, 'apply_edit_memory не вызывается из main'; assert 'photo_override' in src and 'desc_override' in src, 'нет override-полей для точного совпадения D-04'; assert 'без правок' in src or 'новые для памяти' in src, 'нет лога новых товаров'; print('OK: apply_edit_memory определена, вызвана из main, override-поля на месте')"</automated>
  </verify>
  <acceptance_criteria>
    - В `scripts/upload.py` есть чистая функция `apply_edit_memory(products, edit_memory) -> int`, вызываемая из `main()` (НЕ инлайн-логика)
    - `main()` содержит вызов `load_edit_memory()` (рядом с `load_photo_data()`) и `apply_edit_memory(...)`
    - Фото/описание правок применяются ТОЛЬКО по точному `normalize_name` через поля `p["photo_override"]` / `p["desc_override"]`; путь `_find_photo_entry` (подстрока) для правок НЕ используется (D-04)
    - `products_to_rows` отдаёт приоритет override-полям товара перед getter'ами фото/описания
    - Наложение группы переопределяет `p["display_group"]`
    - Есть `log.info` с числом товаров без правок (русская формулировка про новые/без правок)
    - Файл парсится без ошибок; команда verify проходит
    - Точка наложения памяти — ПОСЛЕ apply_group_mapping/блока нового формата и ПЕРЕД products_to_rows
  </acceptance_criteria>
  <done>Наложение памяти реализовано как чистая функция apply_edit_memory, вызвана из main(); фото/описание сопоставлены строго по точному имени (D-04) через override-поля; в логе число товаров без правок.</done>
</task>

<task type="auto">
  <name>Task 3: Локальная проверка наложения памяти на синтетических данных</name>
  <files>scripts/upload.py</files>
  <read_first>
    - scripts/upload.py: load_edit_memory (новая, Task 1) и apply_edit_memory (новая, Task 2)
    - scripts/upload.py: products_to_rows (стр. 476-507) — как фото/описание попадают в строку (с учётом override-полей из Task 2)
    - .planning/codebase/TESTING.md (если есть) — текущее состояние тестов проекта
    - .planning/phases/03-1/03-CONTEXT.md: раздел Specifics — Этап 3 проверяем БЕЗ панели
  </read_first>
  <action>
    Серверный прогон недоступен напрямую (только через субагента sysadmin) и в Этапе 3 не
    требуется — достаточно локальной логической проверки наложения памяти БЕЗ обращения к
    Google. Функция `apply_edit_memory` уже выделена в Task 2 — здесь только пишется локальный
    тест поверх неё (никакого рефакторинга, новая логика не добавляется).

    Команда verify (запускать через Bash-tool) импортирует `scripts/upload.py` как модуль и:
      - формирует синтетический список товаров `all_products` (2-3 словаря с `name`,
        `display_group`, `source_category`, `price`, `stock`, `supplier_file`);
      - формирует синтетическую память `edit_memory` (правка group для товара №1, photo+description
        для товара №2) в той же форме, что возвращает `load_edit_memory`;
      - вызывает `apply_edit_memory(all_products, edit_memory)` и проверяет:
        у №1 `display_group` стал значением из памяти; у №2 итоговая строка (через
        `products_to_rows`) содержит URL/описание из памяти; товар без правки не изменился;
      - проверяет точное совпадение D-04: правка для короткого имени НЕ должна приклеиться к
        более длинному товару, имя которого её содержит (синтетическая пара «Сок» vs «Сок ананасовый»:
        правка по «Сок» применяется только к товару «Сок», у «Сок ананасовый» override-полей нет).
    Никаких сетевых вызовов, никакой записи в Google — чистая локальная логика (fail-safe).
    Команда печатает PASS/FAIL по каждой проверке.
  </action>
  <verify>
    <!-- Запускать через Bash-tool (не PowerShell): python -c многострочный, без head/pipe. -->
    <automated>python -c "import sys; sys.argv=['x']; import importlib.util as u; spec=u.spec_from_file_location('up','scripts/upload.py'); m=u.module_from_spec(spec); spec.loader.exec_module(m); prods=[{'name':'Конфеты Ромашка, шт','display_group':'Другое','source_category':'x','price':1,'stock':5,'supplier_file':'f'},{'name':'Печенье Юбилейное','display_group':'Печенье','source_category':'y','price':2,'stock':5,'supplier_file':'f'},{'name':'Сок ананасовый','display_group':'Соки','source_category':'z','price':3,'stock':5,'supplier_file':'f'},{'name':'Сок','display_group':'Соки','source_category':'z','price':1,'stock':5,'supplier_file':'f'}]; mem={m.normalize_name('Конфеты Ромашка, шт'):{'group':'Коробочные конфеты'},m.normalize_name('Печенье Юбилейное'):{'photo':'http://x/p.jpg','description':'вкусно'},m.normalize_name('Сок'):{'photo':'http://x/sok.jpg'}}; n=m.apply_edit_memory(prods,mem); assert prods[0]['display_group']=='Коробочные конфеты','group override FAIL'; rows=m.products_to_rows(prods,None,{},set()); assert 'http://x/p.jpg' in rows[2],'photo override FAIL'; assert 'вкусно' in rows[2],'description override FAIL'; assert 'http://x/sok.jpg' in rows[4],'exact-name photo FAIL (Сок)'; assert 'http://x/sok.jpg' not in rows[3],'D-04 НАРУШЕН: правка Сок приклеилась к Сок ананасовый'; assert isinstance(n,int) and n>=2,'счётчик новых товаров FAIL'; print('PASS: group+photo+description overrides, точное совпадение D-04, счётчик новых работают')"</automated>
  </verify>
  <acceptance_criteria>
    - Команда verify печатает `PASS: ...` (все проверки прошли)
    - Проверка не делает сетевых вызовов и не пишет в Google (чисто локальная логика)
    - Правка group переопределяет display_group; photo → ImageUrl в строке; description → Description в строке
    - ДОКАЗАНО точное совпадение D-04: правка по короткому имени «Сок» НЕ применяется к «Сок ананасовый»
    - `apply_edit_memory` возвращает корректный счётчик товаров без правок (int)
  </acceptance_criteria>
  <done>Наложение памяти (apply_edit_memory) доказано локальной проверкой для всех трёх типов правок И для точного совпадения D-04 (без приклеивания к товару-надстроке).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Sheet «Правки» → upload.py | Содержимое вкладки — недоверенный ввод (владелец/будущая панель пишут произвольный текст в ячейки); upload.py читает его как данные |
| credentials.json → gspread | Сервисный ключ доступа к таблице; секрет, не в git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | строка-правка во вкладке «Правки» (битый/неполный/неизвестный тип) | mitigate | fail-safe в `load_edit_memory`: пустой ключ/неизвестный тип → пропустить строку с `log.warning` и продолжить; одна плохая строка не валит весь прогон |
| T-03-02 | Injection | значение ячейки правки попадает в каталог (ImageUrl/Description/Группа) | mitigate | значение трактуется как простая строка-данные; никакого `eval`/исполнения; запись в лист через существующий `value_input_option` без построения формул из ввода |
| T-03-03 | Information Disclosure | credentials.json / GOOGLE_SHEETS_ID | accept (existing control) | секреты уже в .gitignore и в env; новых секретов и новых каналов доступа не вводится — переиспользуется существующая авторизация |
| T-03-04 | Denial of Service | недоступность Google / отсутствие вкладки «Правки» | mitigate | graceful-fallback → `{}` + log; отсутствие вкладки = норма (память пуста), прогон каталога продолжается без правок |
| T-03-05 | Tampering | случайная перезапись/повреждение самой вкладки «Правки» | accept | upload.py вкладку «Правки» только ЧИТАЕТ, не пишет (D-01/Discretion) → риск повреждения штатным прогоном отсутствует; отдельный бэкап вкладки в Этапе 3 избыточен (зафиксировать в SUMMARY) |
| T-03-09 | Tampering | правка фото/описания приклеивается не к тому товару (подстрочный матч _find_photo_entry) | mitigate | правки фото/описания применяются строго по точному `normalize_name` через override-поля `photo_override`/`desc_override` (D-04); подстрочный путь для правок не используется; доказано локальным тестом (Task 3) |
</threat_model>

<verification>
- `python -c` AST-проверки из задач проходят (функции определены, интеграция на месте)
- Локальная проверка Task 3 печатает PASS для группы, фото, описания и точного совпадения D-04
- `scripts/upload.py` парсится Python без ошибок целиком
- Память загружается рядом с `load_photo_data()` и накладывается через apply_edit_memory перед `products_to_rows`
- В логе присутствует строка о числе товаров без правок (MEM-03)
- Все команды verify запускаются через Bash-tool (не PowerShell)
</verification>

<success_criteria>
- MEM-01: правка во вкладке «Правки» хранится вне кода и переживает перезапись листа «Товары» (хранилище реализовано через load_edit_memory)
- MEM-02: при прогоне upload.py правки группы/фото/описания применяются автоматически поверх авто-маппинга (D-06), фото/описание — строго по точному имени (D-04)
- MEM-03: число товаров без правок вычисляется и логируется (основа «новый товар требует разметки»)
- Все три типа правок и точное совпадение D-04 доказаны локальной проверкой без обращения к Google
</success_criteria>

<output>
Создать `.planning/phases/03-1/03-01-SUMMARY.md` по завершении. Обязательно зафиксировать в нём:
точные имена колонок и формат вкладки «Правки» (для Task миграции и будущей панели Этапа 4);
подтверждение, что фото/описание применяются через override-поля (вариант а) по точному имени (D-04);
подтверждение, что отдельный бэкап вкладки «Правки» в Этапе 3 не делается (upload.py её только читает).
</output>
