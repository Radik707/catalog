---
phase: 20-client-clean-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/HeaderPrimaryAction.tsx
  - components/CartIcon.tsx
  - components/CatalogNav.tsx
autonomous: false
requirements: [HOME-01]
must_haves:
  truths:
    - "У роли client в шапке справа остаётся только ⚙ Настройки (♥ и 🛒 убраны — они в нижних табах)"
    - "У роли sales шапка не меняется (↻ SyncButton + ⚙ + 🛒 на месте)"
    - "До готовности роли (ready=false) раскладка шапки не дёргается — резерв места держится"
    - "Освободившееся место в шапке отдано ряду разделов CatalogNav (разделы видны и удобны)"
  artifacts:
    - path: "components/HeaderPrimaryAction.tsx"
      provides: "Гейт роли: sales → SyncButton, client → резерв места/ничего"
      contains: "role === \"sales\""
    - path: "components/CartIcon.tsx"
      provides: "Верхняя корзина с гейтом роли: скрыта у client, видна у sales"
      contains: "useRole"
    - path: "components/CatalogNav.tsx"
      provides: "Ряд разделов на первом плане после освобождения места"
  key_links:
    - from: "components/CartIcon.tsx"
      to: "lib/useRole"
      via: "useRole() гейт"
      pattern: "useRole"
    - from: "components/HeaderPrimaryAction.tsx"
      to: "lib/useRole"
      via: "role/ready гейт"
      pattern: "role === \"sales\""
---

<objective>
Разгрузить шапку роли «Клиент» от дублей нижних табов: убрать у `client` верхние ♥ Избранное
(`HeaderPrimaryAction` → `FavoritesIcon`) и 🛒 Корзину (`CartIcon`), оставить только ⚙ Настройки.
Освободившееся место отдать ряду разделов (`CatalogNav` — разделы на первый план). Роль `sales`
не трогаем. Это закрывает отложенный в этапе 18 долг «рационализация шапки клиента» (HOME-01, D-03/D-04).

Purpose: убрать дублирование навигации (шапка ↔ нижние табы), дать клиенту чистый вход —
сверху только то, чего нет внизу (разделы, поиск, настройки). Принцип владельца: «без обвесов и
нагромождений».
Output: правки трёх клиентских компонентов с SSR-safe гейтом роли. Новых файлов нет.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-client-clean-entry/20-CONTEXT.md
@.planning/phases/20-client-clean-entry/20-UI-SPEC.md
@.planning/phases/20-client-clean-entry/20-PATTERNS.md

<interfaces>
<!-- Контракты, которые исполнитель использует напрямую — без разведки кодовой базы. -->

Из lib/useRole.tsx:
export type UserRole = "sales" | "client";
export function useRole(): { role: UserRole; setRole: (r: UserRole) => void; ready: boolean };
// role по умолчанию "client"; ready=false до монтирования, true после useEffect.

Эталон SSR-safe гейта — components/HeaderPrimaryAction.tsx (текущий):
  const { role, ready } = useRole();
  if (!ready) return <div className="h-9 w-9" />;          // резерв места, НЕ null
  return role === "sales" ? <SyncButton /> : <FavoritesIcon secret={secret} />;

Эталон гейта роли «только client» — components/BottomTabBar.tsx:
  if (!ready || role !== "client") return null;

Из components/CartProvider.tsx:
  export function useCartContext(): { items: ...[]; addToCartWithQuantity: (p, q) => void; ... };

Точка монтирования (НЕ менять — серверный layout): app/catalog/[secret]/layout.tsx строки 60-68 —
правая группа шапки <div className="flex items-center gap-1 shrink-0"> с HeaderPrimaryAction /
SettingsButton / CartIcon. Гейт делается ВНУТРИ клиентских компонентов, не в layout (он async-серверный).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: HeaderPrimaryAction — у client убрать ♥ Избранное</name>
  <files>components/HeaderPrimaryAction.tsx</files>
  <read_first>
    - components/HeaderPrimaryAction.tsx (изменяемый файл — текущий гейт)
    - components/BottomTabBar.tsx (эталон гейта роли, обоснование: ♥ уже в табах)
    - lib/useRole.tsx (контракт role/ready)
  </read_first>
  <action>
    В `HeaderPrimaryAction` сохранить ветку `sales` без изменений (возвращает `SyncButton`), а для
    `client` больше НЕ рендерить `FavoritesIcon` (он продублирован нижними табами — D-03). Целевая
    форма: `if (!ready) return <div className="h-9 w-9" />;` (SSR-safe резерв места, не `null`,
    чтобы раскладка не дёргалась при гидратации — UI-SPEC §Interaction), затем
    `return role === "sales" ? <SyncButton /> : null;`. Импорт `FavoritesIcon` удалить, если он
    больше нигде в файле не используется. Комментарий на русском обновить под новое поведение.
    Роль `sales` — без изменений.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('components/HeaderPrimaryAction.tsx','utf8'); if(/FavoritesIcon/.test(s)){process.exit(1)} if(!/role === \"sales\" \? <SyncButton \/> : null/.test(s.replace(/\s+/g,' '))){process.exit(2)} if(!/h-9 w-9/.test(s)){process.exit(3)}"</automated>
  </verify>
  <acceptance_criteria>
    - В файле нет ссылок на `FavoritesIcon` (импорт и использование удалены).
    - Ветка `sales` возвращает `<SyncButton />`; ветка `client` возвращает `null`.
    - До `ready` возвращается резерв `<div className="h-9 w-9" />` (не `null`).
    - `npm run build` проходит без ошибок типов/линта по этому файлу.
  </acceptance_criteria>
  <done>У роли client сверху больше нет ♥; у sales — ↻ SyncButton на месте; раскладка стабильна до ready.</done>
</task>

<task type="auto">
  <name>Задача 2: CartIcon — скрыть верхнюю корзину у client, оставить у sales</name>
  <files>components/CartIcon.tsx</files>
  <read_first>
    - components/CartIcon.tsx (изменяемый файл — сейчас рендерит корзину всегда, роль не знает)
    - components/BottomTabBar.tsx (эталон гейта `if (!ready || role !== "client") return null`)
    - lib/useRole.tsx (контракт role/ready)
  </read_first>
  <action>
    Добавить в `CartIcon` импорт `useRole` из `@/lib/useRole` и гейт роли по образцу `BottomTabBar`.
    Хук `useCartContext` вызывать БЕЗУСЛОВНО (правило хуков — выше любого раннего return). Логика:
    `const { role, ready } = useRole();` затем `if (!ready) return <span className="w-9 h-9" />;`
    (резерв места до готовности — чтобы шапка sales не дёргалась) и `if (role === "client") return null;`
    (у client корзина в нижних табах — D-03). Для `sales` оставить существующий рендер ссылки-корзины
    с бейджем `count` без изменений (`items.length`, бейдж `bg-red-500`). Комментарий на русском
    добавить, почему скрываем у client.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('components/CartIcon.tsx','utf8').replace(/\s+/g,' '); if(!/useRole/.test(s)){process.exit(1)} if(!/role === \"client\".{0,30}return null/.test(s)){process.exit(2)} if(!/items\.length/.test(s)){process.exit(3)}"</automated>
  </verify>
  <acceptance_criteria>
    - `useRole` импортирован и используется; `useCartContext` вызывается до любого return.
    - При `role === "client"` компонент возвращает `null` (корзина скрыта).
    - При `role === "sales"` рендерится ссылка `/catalog/${secret}/cart` с бейджем `count` (`bg-red-500`).
    - До `ready` возвращается резерв места (`w-9 h-9`), не голый `null`, чтобы шапка sales не прыгала.
    - `npm run build` проходит без ошибок по этому файлу.
  </acceptance_criteria>
  <done>У client сверху нет 🛒 (есть в табах); у sales 🛒 с бейджем на месте; гидратация без сдвига.</done>
</task>

<task type="auto">
  <name>Задача 3: CatalogNav — разделы на первый план в освободившемся месте</name>
  <files>components/CatalogNav.tsx</files>
  <read_first>
    - components/CatalogNav.tsx (изменяемый файл — компоновка переключателя режима и ряда разделов)
    - app/catalog/[secret]/layout.tsx (контейнер шапки строки 60-68: gap, max-w, h-12 — не менять высоту)
    - .planning/phases/20-client-clean-entry/20-UI-SPEC.md (§Spacing: h-12 не менять, minWidth:46 тап-зон сохранять)
  </read_first>
  <action>
    После удаления дублей справа (задачи 1-2) у client освобождается место — отдать его ряду
    разделов, чтобы иконки «дышали» и не теснились со свёрнутой кнопкой режима «Каталог». Сохранить:
    высоту шапки `h-12` (НЕ менять), инверсию активного чипа `bg-white text-blue-600`, тап-зону
    иконки раздела `style={{ minWidth: 46 }}`, микро-подписи `text-base`/`text-[9px]`, градиенты-
    подсказки прокрутки слева/справа (`bg-gradient-to-r/l from-blue-600`), логику `expanded`/режимов
    `DROPDOWN_MODES`. Цель D-04 — разделы видны и удобны без тесноты с дублирующей кнопкой «Каталог».
    Точная компоновка переключателя режима — на усмотрение (например, оставить свёрнутую кнопку
    компактной, дать `flex-1` ряду разделов как сейчас). Не вводить новый визуальный язык, не
    добавлять новых элементов — только дать разделам пространство.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('components/CatalogNav.tsx','utf8'); if(!/minWidth: 46/.test(s)){process.exit(1)} if(!/bg-white text-blue-600/.test(s)){process.exit(2)} if(!/from-blue-600/.test(s)){process.exit(3)}"</automated>
  </verify>
  <acceptance_criteria>
    - Тап-зона иконки раздела сохраняет `minWidth: 46`; активный чип — инверсия `bg-white text-blue-600`.
    - Градиенты-подсказки прокрутки ряда разделов сохранены (`from-blue-600`).
    - Высота шапки `h-12` в layout не изменена (контейнер не трогался).
    - Новых элементов/виджетов в шапку не добавлено; ряд разделов виден в режиме «Каталог».
    - `npm run build` проходит без ошибок.
  </acceptance_criteria>
  <done>Разделы на первом плане в шапке client, без тесноты; переключатель режима не давит на ряд.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 4: Приёмка владельца — чистая шапка клиента на проде</name>
  <files>—</files>
  <action>Чекпойнт ручной приёмки после деплоя — Claude автоматизировать не может (визуальная проверка на проде).</action>
  <what-built>
    Разгружена шапка роли «Клиент»: убраны верхние ♥ Избранное и 🛒 Корзина (они дублировали нижние
    табы), осталась только ⚙ Настройки; освободившееся место отдано ряду разделов. Роль «Торговый»
    (планшет) не тронута. Деплой: `git push` → Vercel.
  </what-built>
  <how-to-verify>
    После деплоя на прод (Vercel):
    1. Откройте каталог на телефоне (роль «Клиент» по умолчанию). Сверху справа должна быть ТОЛЬКО
       шестерёнка ⚙ — без сердечка ♥ и без корзины 🛒. Снизу — табы Каталог · Избранное · Корзина.
    2. Ряд разделов (категорий) в шапке должен быть виден и удобно листаться, иконки не теснятся.
    3. Переключите роль на «Торговый» (⚙ → роль). Шапка должна вернуться к прежнему виду:
       ↻ Обновить + ⚙ + 🛒 (корзина сверху на месте), нижних табов нет.
    4. Перезагрузите страницу пару раз в роли «Клиент» — шапка не должна «прыгать»/мелькать чужими
       иконками при загрузке.
  </how-to-verify>
  <verify>
    <human-check>Владелец подтверждает на проде: у client только ⚙, разделы видны, sales не изменён, нет мелькания</human-check>
  </verify>
  <done>Владелец принял чистую шапку клиента на проде.</done>
  <resume-signal>Напишите «принято» или опишите, что не так (лишние иконки, сдвиг, теснота разделов)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage("userRole") → рендер шапки | Значение роли из localStorage влияет на то, что показать в шапке |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | localStorage("userRole") | mitigate | `useRole` уже валидирует: принимает только "sales"/"client", иначе дефолт "client" (lib/useRole.tsx, STRIDE T-15-01). Битое значение → нейтральная клиентская шапка, без падения. |
| T-20-02 | Denial of Service | гидратация шапки | mitigate | SSR-safe гейт: до `ready` — резерв места (`h-9 w-9`/`w-9 h-9`), не условный рендер, исключает сдвиг/мелькание layout. |
| T-20-03 | Information Disclosure | — | accept | Этап не добавляет сетевых поверхностей, токенов, PII; правки чисто презентационные. |
</threat_model>

<verification>
- `npm run build` проходит без ошибок типов/линта.
- В роли client шапка: только ⚙; в роли sales: ↻ + ⚙ + 🛒.
- Высота шапки `h-12` не изменена; нижние табы и safe-area не затронуты.
- `force-dynamic`/`cache:"no-store"` витрины и `/api/products` НЕ тронуты (этот план их не касается).
</verification>

<success_criteria>
- HOME-01 закрыт: верхняя панель client не дублирует нижние табы (нет ♥/🛒, есть ⚙), разделы на первом плане; sales не изменён.
- Все три компонента используют SSR-safe гейт роли (резерв места до ready).
- Чекпойнт-приёмка владельца пройдена на проде.
</success_criteria>

<output>
Создать `.planning/phases/20-client-clean-entry/20-01-SUMMARY.md` по завершении.
</output>
