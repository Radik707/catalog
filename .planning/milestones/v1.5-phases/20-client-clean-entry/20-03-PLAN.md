---
phase: 20-client-clean-entry
plan: 03
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - lib/useSearchHistory.ts
  - components/SearchBar.tsx
  - components/CatalogView.tsx
autonomous: false
requirements: [HOME-02, HOME-03, SRCH-01]
must_haves:
  truths:
    - "Над каталогом у client (при непустой истории заказов) есть одна компактная строка «↻ Повторить последний заказ»"
    - "Тап по строке повтора кладёт доступные позиции в корзину и показывает честную сводку (общая ReorderSummaryModal)"
    - "Если добавить нечего — честное сообщение, корзина не открывается"
    - "При фокусе на поле поиска под ним выпадает список недавних запросов; тап повторяет запрос"
    - "История поиска хранится в localStorage, офлайн-безопасна, мягко деградирует на битых данных"
    - "Строка повтора не показывается у sales и до ready (SSR-safe гейт)"
  artifacts:
    - path: "lib/useSearchHistory.ts"
      provides: "localStorage-хук истории поиска (FIFO, дедуп, мягкая деградация)"
      exports: ["useSearchHistory"]
    - path: "components/SearchBar.tsx"
      provides: "Выпадашка недавних запросов при фокусе"
      contains: "history"
    - path: "components/CatalogView.tsx"
      provides: "Строка повтора над списком + запись/чтение истории поиска"
      contains: "useSearchHistory"
  key_links:
    - from: "components/CatalogView.tsx"
      to: "lib/useSearchHistory"
      via: "useSearchHistory()"
      pattern: "useSearchHistory"
    - from: "components/CatalogView.tsx"
      to: "components/ReorderSummaryModal"
      via: "import default (фундамент плана 20-02)"
      pattern: "from '@/components/ReorderSummaryModal'"
    - from: "components/CatalogView.tsx"
      to: "lib/reorder"
      via: "classifyReorder + addToCartWithQuantity"
      pattern: "classifyReorder"
---

<objective>
Закрыть главный B2B-сценарий повтора и историю поиска одним компактным элементом каждый (принцип
этапа: без блоков-витрин). 1) Над списком каталога — одна строка «↻ Повторить последний заказ»
(только client, после ready, только если история заказов непуста): тап → `classifyReorder` →
`addToCartWithQuantity` → общая `ReorderSummaryModal` с честной сводкой (D-06). 2) История поиска:
новый localStorage-хук `useSearchHistory` + выпадашка недавних запросов при фокусе на `SearchBar`,
тап повторяет запрос (D-07/D-08, SRCH-01). Всё офлайн-безопасно (D-09).

Purpose: убрать повтор из «спрятан на 3 тапа» в один тап над каталогом; дать привычный поиск с
историей (как Ozon/WB) — без новых сетевых поверхностей и без нового визуального языка.
Output: 1 новый хук + правки SearchBar и CatalogView. Использует модалку/склонения из плана 20-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-client-clean-entry/20-CONTEXT.md
@.planning/phases/20-client-clean-entry/20-UI-SPEC.md
@.planning/phases/20-client-clean-entry/20-PATTERNS.md
@.planning/phases/20-client-clean-entry/20-02-SUMMARY.md

<interfaces>
<!-- Контракты для прямого использования — без разведки кодовой базы. -->

Эталон localStorage-хука — lib/useOrderHistory.ts (копировать структуру 1:1):
  HISTORY_KEY const; loadHistory()/saveHistory() с typeof window==='undefined'→[], try/catch;
  useState + useEffect(load-on-mount, setIsLoaded(true)) + useEffect(save-on-change при isLoaded);
  возвращает { entries, isLoaded, ... }.

Из lib/reorder.ts:
  export function classifyReorder(historyItems: OrderHistoryItem[], catalog: Product[], priceForm: PriceForm): ReorderResult;
  export interface ReorderResult { lines: ReorderLineResult[]; addedCount: number; }
  // ReorderLineResult: { outcome: 'added'|'price_changed'|'out_of_stock'|'unavailable'; product?; addedQty?; historyItem; ... }

Из components/ReorderSummaryModal.tsx (план 20-02):
  export default function ReorderSummaryModal({ result: ReorderResult; secret: string; onClose: () => void }): JSX.Element;

Провайдеры, доступные в CatalogView (все смонтированы в layout):
  useRole() → { role: "sales"|"client"; ready: boolean }                          (@/lib/useRole)
  useOrderHistoryContext() → { entries: OrderHistoryEntry[]; isLoaded; ... }       (@/components/OrderHistoryProvider)
  useCatalogSyncContext() → { products: Product[]; status; ... }                   (@/components/CatalogSyncProvider) — НЕ дублировать вызов: sync уже читается в CatalogView (строка 37)
  useCartContext() → { items; addToCartWithQuantity: (p: Product, q: number) => void }  (@/components/CartProvider)
  useCatalogSettings() → { priceForm; ... }                                        (@/components/CatalogSettings) — уже деструктурируется в CatalogView (строка 25)
  // entries отсортированы как в истории; ПОСЛЕДНИЙ заказ = entries[entries.length - 1] (D-06)

Образец обработчика повтора (orders/page.tsx handleRepeat, строки 36-55):
  const result = classifyReorder(entry.items, products, priceForm);
  for (const line of result.lines) {
    if ((line.outcome === 'added' || line.outcome === 'price_changed') && line.product) {
      addToCartWithQuantity(line.product, line.addedQty ?? line.historyItem.quantity);
    }
  }
  setReorderSummary({ ...result, secret });

Текущий SearchBar (controlled): props { value: string; onChange: (v: string) => void; count: number }.
Контейнер внутри уже `relative` (строка 12) — якорь для absolute-выпадашки.
В CatalogView: const [search, setSearch] = useState(""); <SearchBar value={search} onChange={setSearch} count={visibleCount} /> (строки 31, 208).
ВАЖНО: на ветке status==="loading" SearchBar рендерится с заглушками (строка 125) — новые пропсы истории сделать опциональными, чтобы этот вызов не ломался.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: lib/useSearchHistory.ts — localStorage-хук истории поиска</name>
  <files>lib/useSearchHistory.ts</files>
  <read_first>
    - lib/useOrderHistory.ts (ЭТАЛОН — структура хука 1:1: load/save, isLoaded, SSR-гейт, try/catch)
    - .planning/phases/20-client-clean-entry/20-PATTERNS.md (раздел useSearchHistory — образец дедупа/потолка)
    - .planning/phases/20-client-clean-entry/20-CONTEXT.md (D-08 — ключ catalog-*, потолок ~8-10)
  </read_first>
  <behavior>
    - addQuery('Молоко'); addQuery('сок') → entries = ['сок', 'Молоко'] (свежие сверху)
    - addQuery('молоко') после ['сок','Молоко'] → ['молоко','сок'] (дедуп регистронезависимо, свежий вариант сверху)
    - addQuery('  ') (пустой/пробелы) → не добавляется
    - 11-й уникальный запрос → длина остаётся MAX_HISTORY_ENTRIES (10), вытесняется самый старый
    - removeQuery('сок') → 'сок' исчезает из entries
    - clearHistory() → entries = []
    - битый JSON в localStorage → loadHistory() возвращает [], не бросает
  </behavior>
  <action>
    Создать `lib/useSearchHistory.ts` по эталону `useOrderHistory.ts`, заменив тип записи на `string`
    и ключ на `catalog-search-history` (в духе `catalog-*`, D-08). Константа `MAX_HISTORY_ENTRIES = 10`.
    `loadHistory()`: SSR-гейт `typeof window === 'undefined' → []`, try/catch вокруг `JSON.parse`,
    мягкая деградация — `Array.isArray(arr) ? arr.filter(q => typeof q === 'string' && q.trim()) : []`.
    `saveHistory()`: try/catch (приватный режим iOS). Хук `useSearchHistory()`: `entries: string[]`,
    `isLoaded`, load-on-mount + save-on-change. Методы: `addQuery(q)` — trim, пропуск пустого, дедуп
    регистронезависимо (`x.toLowerCase() !== query.toLowerCase()`), свежий СВЕРХУ (`[query, ...without]`),
    срез `.slice(0, MAX_HISTORY_ENTRIES)`; `removeQuery(q)` — фильтр по точному совпадению; `clearHistory()`.
    Все методы — `useCallback`. Комментарии на русском. Никаких сетевых вызовов (D-09).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/useSearchHistory.ts','utf8'); if(!/export function useSearchHistory/.test(s)){process.exit(1)} if(!/catalog-search-history/.test(s)){process.exit(2)} if(!/typeof window === 'undefined'/.test(s)){process.exit(3)} for(const m of ['addQuery','removeQuery','clearHistory']){ if(!new RegExp(m).test(s)){console.error('нет '+m);process.exit(4)} }"</automated>
  </verify>
  <acceptance_criteria>
    - Экспорт `useSearchHistory()` возвращает `{ entries: string[]; isLoaded: boolean; addQuery; removeQuery; clearHistory }`.
    - Ключ `catalog-search-history`; потолок 10; дедуп регистронезависимо; свежие сверху.
    - SSR-гейт и try/catch на чтении/записи; битый JSON → `[]`, без исключения.
    - Поведение из `<behavior>` выполняется (можно проверить временным узлом или ручной прокруткой логики).
  </acceptance_criteria>
  <done>Готов офлайн-безопасный хук истории поиска по эталону useOrderHistory; SRCH-01 фундамент.</done>
</task>

<task type="auto">
  <name>Задача 2: SearchBar — выпадашка недавних запросов при фокусе</name>
  <files>components/SearchBar.tsx</files>
  <read_first>
    - components/SearchBar.tsx (изменяемый файл — controlled-инпут, контейнер уже relative)
    - .planning/phases/20-client-clean-entry/20-UI-SPEC.md (§Copywriting История поиска; §Color нейтральная палитра; §z-index)
    - app/catalog/[secret]/orders/page.tsx (кнопка «Очистить» text-red-500 font-medium — образец)
  </read_first>
  <action>
    Расширить `SearchBarProps` ОПЦИОНАЛЬНЫМИ пропсами (чтобы вызов на ветке loading не ломался):
    `history?: string[]; onPickHistory?: (q: string) => void; onRemoveHistory?: (q: string) => void;
    onClearHistory?: () => void;`. Добавить локальный `const [focused, setFocused] = useState(false)`
    на инпуте (`onFocus`/`onBlur` с небольшой задержкой blur, чтобы успел сработать тап по пункту —
    например `onMouseDown`/`onPointerDown` на пункте или `setTimeout` на blur). Под инпутом, когда
    `focused && (history?.length ?? 0) > 0 && !value.trim()` — `absolute` выпадашка (`top-full left-0
    right-0 z-40`, ниже шапки z-50, поверх витрины), `bg-white rounded-xl shadow-lg border border-gray-100`.
    Заголовок (опционально) «Недавние запросы» `text-xs text-gray-400` + справа «Очистить»
    `text-xs text-red-500 font-medium` (вызывает `onClearHistory`). Пункт списка: иконка-часы слева
    `text-gray-400`, текст запроса `text-sm text-gray-800` (тап → `onPickHistory(q)`), крестик `✕`
    справа `text-gray-400` (тап → `onRemoveHistory(q)`, `e.stopPropagation()` чтобы не сработал pick).
    Hover/active — `active:bg-gray-100`. История нейтральная — НЕ соперничает яркостью с синей кнопкой
    повтора (антисвалка). Существующий placeholder `"Поиск товара..."`, счётчик и крестик очистки
    инпута (строки 39-50) — НЕ менять. `aria-label` на иконочных кнопках (доступность).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('components/SearchBar.tsx','utf8'); if(!/history\?:/.test(s)){console.error('нет опц. history');process.exit(1)} if(!/onPickHistory/.test(s)){process.exit(2)} if(!/Поиск товара\.\.\./.test(s)){console.error('placeholder изменён');process.exit(3)} if(!/text-red-500/.test(s)){console.error('нет Очистить');process.exit(4)}"</automated>
  </verify>
  <acceptance_criteria>
    - Новые пропсы истории ОПЦИОНАЛЬНЫ; вызов `<SearchBar value="" onChange={()=>{}} count={0} />` на ветке loading компилируется.
    - Выпадашка появляется только при `focused && history.length>0 && поле пустое`; позиционирована `absolute` под инпутом, `z` ниже шапки.
    - Пункт: тап по тексту → `onPickHistory(q)`; крестик `✕` → `onRemoveHistory(q)` без срабатывания pick; «Очистить» → `onClearHistory`.
    - Палитра нейтральная (`text-gray-*` на `bg-white`); placeholder и крестик очистки инпута не изменены.
    - Иконочные кнопки имеют `aria-label`.
  </acceptance_criteria>
  <done>Поиск показывает недавние запросы при фокусе; тап повторяет, крестик удаляет, «Очистить» чистит.</done>
</task>

<task type="auto">
  <name>Задача 3: CatalogView — строка повтора + проводка истории поиска</name>
  <files>components/CatalogView.tsx</files>
  <read_first>
    - components/CatalogView.tsx (изменяемый файл — состояние search строки 31/208, sync строка 37, priceForm строка 25)
    - app/catalog/[secret]/orders/page.tsx (handleRepeat строки 36-55 — образец обработчика повтора)
    - components/ReorderSummaryModal.tsx (план 20-02 — импорт модалки сводки)
    - lib/useSearchHistory.ts (задача 1 — импорт хука)
    - .planning/phases/20-client-clean-entry/20-UI-SPEC.md (§Interaction место/высота строки; §Copywriting CTA; гейт)
    - .planning/phases/20-client-clean-entry/20-CONTEXT.md (D-06 условия показа; D-09 офлайн)
  </read_first>
  <action>
    В `CatalogView` импортировать: `useRole` (@/lib/useRole), `useOrderHistoryContext`
    (@/components/OrderHistoryProvider), `useCartContext` (@/components/CartProvider),
    `classifyReorder`/`ReorderResult` (@/lib/reorder), `ReorderSummaryModal` default
    (@/components/ReorderSummaryModal), `useSearchHistory` (@/lib/useSearchHistory). Использовать уже
    имеющиеся `priceForm` (строка 25) и `sync.products` (строка 37) — НЕ дублировать вызовы хуков.

    (а) Строка повтора (D-06): получить `{ role, ready } = useRole()`, `{ entries } = useOrderHistoryContext()`,
    `{ addToCartWithQuantity } = useCartContext()`. Состояние `const [reorderSummary, setReorderSummary]
    = useState<(ReorderResult & { secret: string }) | null>(null)`. Обработчик `handleRepeatLast`:
    `entry = entries[entries.length - 1]`; повторить логику handleRepeat из orders/page (classifyReorder
    → цикл added/price_changed → addToCartWithQuantity(addedQty ?? historyItem.quantity)); открыть
    сводку `setReorderSummary({ ...result, secret })`. Secret взять из products/URL — пробросить проп
    `secret` в CatalogView из page.tsx ИЛИ через `usePathname` (на усмотрение; если проще — добавить
    проп). Рендерить строку МЕЖДУ `<SearchBar/>` (строка 208) и контентом, ТОЛЬКО при
    `ready && role === 'client' && entries.length > 0` (гейт; до ready/у sales/без истории — ничего).
    Вёрстка: одна компактная строка ~40-48px, `bg-white border-b border-gray-100`, кнопка-CTA
    «↻ Повторить последний заказ» в стиле `bg-blue-600 text-white text-sm font-semibold rounded-xl
    active:bg-blue-700` (образец кнопки повтора). Это ОДИН элемент, не блок-витрина. Модалку
    `{reorderSummary && <ReorderSummaryModal result={reorderSummary} secret={reorderSummary.secret}
    onClose={() => setReorderSummary(null)} />}` рендерить в конце.

    (б) История поиска (SRCH-01): `const { entries: searchEntries, addQuery, removeQuery, clearHistory }
    = useSearchHistory()`. Записывать запрос при выполнении поиска — по debounce/blur непустого `search`
    (например, `useEffect` с `setTimeout` на `search.trim()`, либо запись при потере фокуса). Пробросить
    в `<SearchBar>` пропсы `history={searchEntries}`, `onPickHistory={(q) => setSearch(q)}`,
    `onRemoveHistory={removeQuery}`, `onClearHistory={clearHistory}`. Источник товаров остаётся
    `useCatalogSyncContext` (офлайн, IndexedDB) — никаких новых сетевых вызовов (D-09).

    НЕ трогать: `force-dynamic`/`cache:"no-store"`, ветки `status==="loading"/"empty-offline"`
    (SearchBar там вызывается без пропсов истории — они опциональны), порядок хуков (все вызовы хуков
    выше ранних return по статусу — правило хуков).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('components/CatalogView.tsx','utf8'); for(const p of ['useSearchHistory','useOrderHistoryContext','classifyReorder','ReorderSummaryModal','Повторить последний заказ']){ if(!new RegExp(p).test(s)){console.error('нет '+p);process.exit(1)} } if(!/role === ['\"]client['\"]/.test(s) || !/entries\.length/.test(s)){console.error('нет гейта строки повтора');process.exit(2)}"</automated>
  </verify>
  <acceptance_criteria>
    - Строка «↻ Повторить последний заказ» рендерится только при `ready && role==='client' && entries.length>0`, между SearchBar и контентом, как одна компактная строка (не блок).
    - Тап → `classifyReorder(entries[last].items, products, priceForm)` → `addToCartWithQuantity` для added/price_changed → открывается `ReorderSummaryModal`; при addedCount===0 модалка показывает «нечего добавить», кнопки перехода нет.
    - SearchBar получает `history/onPickHistory/onRemoveHistory/onClearHistory`; тап по запросу подставляет его в `search`.
    - Запрос пишется в историю при выполнении поиска (debounce/blur непустого значения).
    - Все вызовы хуков выше ранних return; ветки loading/empty-offline не сломаны; `npm run build` зелёный.
  </acceptance_criteria>
  <done>Повтор в один тап над каталогом + история поиска при фокусе; офлайн-безопасно, гейт роли соблюдён.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 4: Приёмка владельца — повтор и история поиска на проде</name>
  <files>—</files>
  <action>Чекпойнт ручной приёмки после деплоя — Claude автоматизировать не может (визуальная/функциональная проверка на проде, включая офлайн).</action>
  <what-built>
    Над каталогом у клиента (если есть отправленные заказы) появилась строка «↻ Повторить последний
    заказ» — тап кладёт позиции в корзину и показывает честную сводку (добавлено N, что пропущено).
    В поиске при фокусе выпадает список недавних запросов — тап повторяет, крестик удаляет, «Очистить»
    чистит весь список. Всё хранится локально, работает офлайн. Деплой: `git push` → Vercel.
  </what-built>
  <how-to-verify>
    После деплоя на прод (роль «Клиент»):
    1. Если есть история заказов — над каталогом видна ОДНА компактная строка «↻ Повторить последний
       заказ». Если истории нет — строки нет вовсе.
    2. Тап по строке → корзина пополняется, появляется сводка: «Добавлено N товаров в корзину» + список
       пропущенных/изменённых с причиной; кнопка «Перейти в корзину →». Нигде нет слов «статус/принято».
    3. Если ни одной позиции добавить нельзя — честное сообщение, кнопки перехода в корзину НЕТ.
    4. Тапните в поле поиска, наберите 2-3 запроса (например «молоко», «сок»). Снова тапните в пустое
       поле — под ним должен выпасть список этих запросов. Тап по запросу — он подставляется и ищет.
       Крестик ✕ у запроса удаляет его; «Очистить» убирает весь список.
    5. Включите авиарежим/офлайн (после первой загрузки) — строка повтора и история поиска работают.
    6. В роли «Торговый» строки повтора над каталогом быть НЕ должно.
  </how-to-verify>
  <verify>
    <human-check>Владелец подтверждает на проде: строка повтора (гейт+сводка), история поиска (выпадашка/удаление/очистка), офлайн, нет слов «статус», у sales строки нет</human-check>
  </verify>
  <done>Владелец принял повтор в один тап и историю поиска на проде.</done>
  <resume-signal>Напишите «принято» или опишите проблему (строка не там/не так, сводка, история поиска, офлайн)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage("catalog-search-history") → выпадашка/поиск | Сохранённые запросы из localStorage отображаются и подставляются в поиск |
| localStorage("catalog-order-history") → строка повтора | Снимок прошлого заказа влияет на содержимое корзины и сводку |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Tampering | catalog-search-history | mitigate | `loadHistory()` фильтрует только непустые строки, try/catch вокруг JSON.parse → битые/инъектированные данные дают `[]`; запросы рендерятся как текст (React экранирует), без `dangerouslySetInnerHTML`/`eval`. |
| T-20-07 | Tampering | catalog-order-history → повтор | mitigate | Повтор идёт через `classifyReorder` против актуального каталога: несуществующие/битые позиции → unavailable/out_of_stock (не добавляются); кол-во капается остатком. Хрупкий ключ имени — мягкая деградация (известный риск v1.5). |
| T-20-08 | Information Disclosure | localStorage | mitigate | В localStorage только неконфиденциальные запросы/история заказов (имена/цены/кол-во) — никаких токенов/секретов/PII. |
| T-20-09 | Denial of Service | новые сетевые поверхности | accept | Этап их не вводит: всё читает localStorage/IndexedDB (`useCatalogSyncContext`); `/api/products` и `force-dynamic` не затронуты (D-09). |
| T-20-10 | Denial of Service | гидратация строки повтора | mitigate | Гейт `ready && role==='client' && entries.length>0` — до ready строка не рисуется; без сдвига layout (строка появляется над контентом по готовности данных истории). |
</threat_model>

<verification>
- `npm run build` зелёный; правило хуков не нарушено (все хуки выше ранних return).
- Строка повтора: гейт роли + непустая история; повтор через classifyReorder + addToCartWithQuantity; сводка через общую ReorderSummaryModal (план 20-02).
- История поиска: localStorage `catalog-search-history`, офлайн-безопасна, мягкая деградация; выпадашка при фокусе.
- Никаких новых сетевых вызовов; `force-dynamic`/`cache:"no-store"` витрины и `/api/products` НЕ тронуты.
- Нигде нет слов «статус/принято/подтверждено» (D-11).
</verification>

<success_criteria>
- HOME-02 закрыт: одна компактная строка «↻ Повторить последний заказ» с честной сводкой (механика этапа 19).
- HOME-03 закрыт: всё новое работает офлайн (localStorage/IndexedDB), PWA/standalone и force-dynamic не сломаны.
- SRCH-01 закрыт: недавние запросы хранятся локально, выпадают при фокусе, повторяются тапом.
- Чекпойнт-приёмка владельца пройдена на проде.
</success_criteria>

<output>
Создать `.planning/phases/20-client-clean-entry/20-03-SUMMARY.md` по завершении.
</output>
