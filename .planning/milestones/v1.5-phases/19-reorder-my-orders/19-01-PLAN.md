---
phase: 19-reorder-my-orders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/reorder.ts
  - lib/useCart.ts
  - components/CartProvider.tsx
autonomous: true
requirements: [REORD-03]
must_haves:
  truths:
    - "Хелпер классифицирует каждую позицию прошлого заказа на один из четырёх исходов: added / price_changed / out_of_stock / unavailable"
    - "Цена при повторе берётся актуальная через effectivePrice (учитывая +5% Ефимовой формы '1'), а не замороженная priceAtOrder"
    - "Товар без совпадения по id и по нормализованному имени помечается unavailable, без молчаливой подмены"
    - "Корзина умеет принять товар в заданном количестве, капнутом по текущему остатку"
  artifacts:
    - path: "lib/reorder.ts"
      provides: "Чистая функция классификации позиций повтора (4 исхода) + актуальная цена"
      contains: "classifyReorder"
      min_lines: 40
    - path: "lib/useCart.ts"
      provides: "Метод добавления товара с количеством (капнутым по остатку)"
      contains: "addToCartWithQuantity"
    - path: "components/CartProvider.tsx"
      provides: "Проброс метода добавления с количеством через контекст"
      contains: "addToCartWithQuantity"
  key_links:
    - from: "lib/reorder.ts"
      to: "lib/pricing.ts"
      via: "import effectivePrice"
      pattern: "effectivePrice"
    - from: "components/CartProvider.tsx"
      to: "lib/useCart.ts"
      via: "проброс метода из useCart в контекст"
      pattern: "addToCartWithQuantity"
---

<objective>
Создать «ядро повтора заказа»: чистый хелпер классификации позиций прошлого заказа против актуального каталога и метод корзины для добавления товара нужным количеством. Это фундамент, который план 19-02 (UI) использует для кнопки «Повторить» и сводки результата.

Purpose: Логика повтора должна быть чистой и переиспользуемой — её же применит «Главная» клиента (этап 20, блок «Повторить последний заказ»). Без побочных эффектов → проверяема и не дублируется.
Output: `lib/reorder.ts` (чистая классификация D-01..D-04), новый метод в `lib/useCart.ts` + проброс через `components/CartProvider.tsx` (D-05). Покрывает REORD-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-reorder-my-orders/19-CONTEXT.md

<interfaces>
<!-- Контракты, которые исполнитель использует напрямую — без исследования кодовой базы. -->

Из lib/types.ts:
```typescript
export interface Product {
  id: string;
  name: string;
  price: number;
  stock: number;
  category: string;
  group: string;
  supplier: string;
  badge?: string;
  imageUrl?: string;
  description?: string;
  subgroup?: string;
  section?: string;
  hidden?: boolean;
}

export interface OrderHistoryItem {
  id: string;            // id товара (Product.id) — основной ключ матчинга
  name: string;          // название на момент заказа — фолбэк-ключ матчинга
  quantity: number;
  priceAtOrder: number;  // замороженная цена из истории (для сравнения «было → стало»)
  unit: string;
  imageUrl?: string;
}
```

Из lib/pricing.ts:
```typescript
export type PriceForm = "1" | "2";
export function isEfimova(supplier?: string): boolean;
export function effectivePrice(product: Product, form: PriceForm): number;
```

Из lib/useCart.ts (CartItem и существующий addToCart):
```typescript
export interface CartItem { product: Product; quantity: number; }
// addToCart(product) — добавляет +1, капает по product.stock; НЕ ломать.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Чистый хелпер классификации повтора (lib/reorder.ts)</name>
  <read_first>
    - lib/types.ts (Product, OrderHistoryItem — поля снимка)
    - lib/pricing.ts (effectivePrice, isEfimova, PriceForm)
    - CLAUDE.md (порог скрытия товара «остаток ≤ 1» — тот же порог для «нет в наличии»)
    - .planning/phases/19-reorder-my-orders/19-CONTEXT.md (D-01..D-04)
  </read_first>
  <action>
    Создать новый модуль `lib/reorder.ts` (комментарии на русском). Экспортировать тип исхода `ReorderOutcome = 'added' | 'price_changed' | 'out_of_stock' | 'unavailable'` и тип результата по позиции `ReorderLineResult`, содержащий: исход `outcome`, исходную позицию истории `historyItem: OrderHistoryItem`, найденный текущий товар `product?: Product`, актуальную эффективную цену `currentPrice?: number` и старую цену `oldPrice: number` (= `historyItem.priceAtOrder`). Также экспортировать агрегатный тип результата `ReorderResult` с полями: `lines: ReorderLineResult[]`, `addedCount: number` (число позиций с исходом `added` или `price_changed`).

    Экспортировать чистую функцию `classifyReorder(historyItems: OrderHistoryItem[], catalog: Product[], priceForm: PriceForm): ReorderResult`. Логика по D-02/D-03/D-04:
    - Матчинг (D-02): сначала по `historyItem.id === product.id`; если не найдено — фолбэк по нормализованному имени. Нормализация: `trim().toLowerCase()` + схлопывание любых последовательностей пробельных символов в один пробел (regex `\s+` → ` `). Вынести нормализацию в локальную функцию `normalizeName`.
    - Не найдено ни по id, ни по имени → `unavailable` (D-02), `product` не задан.
    - Найден товар, но `product.stock <= 1` → `out_of_stock` (D-04, порог тот же, что скрытие на витрине). НЕ добавляется. `currentPrice` всё равно вычислить через `effectivePrice` для возможного показа.
    - Найден, `stock > 1` → вычислить `currentPrice = effectivePrice(product, priceForm)` (D-03, актуальная цена с учётом формы Ефимовой). Сравнить с `oldPrice`: если равны (с точностью до копейки, сравнивать округлённые до 2 знаков) → `added`; если отличаются → `price_changed`.
    - `addedCount` = число строк с `added` или `price_changed`.
    Без побочных эффектов, без обращения к localStorage/DOM — только аргументы → результат. Не использовать `priceAtOrder` для добавления, только для сравнения (D-03).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - lib/reorder.ts экспортирует `classifyReorder` и тип `ReorderOutcome` со значениями added/price_changed/out_of_stock/unavailable
    - Матчинг идёт по id, при промахе — по нормализованному имени (trim + lower + схлопывание пробелов)
    - Для найденного товара цена считается через `effectivePrice(product, priceForm)`, а не из `priceAtOrder`
    - Товар с остатком ≤ 1 классифицируется как out_of_stock и НЕ попадает в addedCount
    - Функция чистая: не читает localStorage, не трогает DOM, не имеет аргументов вне сигнатуры
    - `npx tsc --noEmit` завершается с кодом 0
  </acceptance_criteria>
  <done>lib/reorder.ts содержит чистую функцию classifyReorder с четырьмя честными исходами, актуальной ценой и порогом наличия stock ≤ 1; типы экспортированы; типизация проходит.</done>
</task>

<task type="auto">
  <name>Task 2: Метод добавления с количеством в корзине (useCart + CartProvider)</name>
  <read_first>
    - lib/useCart.ts (существующий addToCart, updateQuantity, ключ catalog-cart — паттерны)
    - components/CartProvider.tsx (интерфейс CartContextValue, проброс методов)
    - .planning/phases/19-reorder-my-orders/19-CONTEXT.md (D-05)
  </read_first>
  <action>
    В `lib/useCart.ts` добавить метод `addToCartWithQuantity(product: Product, quantity: number)` (D-05), не ломая существующий `addToCart` (+1). Поведение: количество капается по текущему остатку через `Math.min(quantity, product.stock)`; если результат ≤ 0 или `product.stock <= 0` — ничего не делать. Если товар уже в корзине — увеличить его количество на `quantity` с тем же капом по остатку (`Math.min(existing.quantity + quantity, product.stock)`), не сбрасывая текущее количество. Если товара нет — добавить новую позицию `{ product, quantity: cappedQty }`. Использовать тот же паттерн `setItems((prev) => ...)`, что и в `addToCart`/`updateQuantity`. Метод обернуть в `useCallback([])` и вернуть из хука вместе с остальными.

    В `components/CartProvider.tsx` расширить интерфейс `CartContextValue` полем `addToCartWithQuantity: (product: Product, quantity: number) => void` и пробросить его через значение контекста (хук `useCart()` уже разворачивается в провайдер целиком — убедиться, что новый метод входит в возвращаемый объект и в тип). Комментарии на русском.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - useCart экспортирует метод addToCartWithQuantity(product, quantity), капающий количество через Math.min(quantity, product.stock)
    - Если товар уже в корзине — количество увеличивается на quantity (с капом по остатку), а не перезаписывается
    - Существующий addToCart (+1) не изменён по поведению
    - CartProvider пробрасывает addToCartWithQuantity, тип CartContextValue содержит этот метод
    - `npx tsc --noEmit` завершается с кодом 0
  </acceptance_criteria>
  <done>useCart и CartProvider предоставляют addToCartWithQuantity с капом по остатку и накоплением для существующих позиций; существующий поток корзины не сломан; типизация проходит.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → клиентский код | История заказов и корзина читаются из localStorage текущего устройства; доверенный источник того же происхождения |
| IndexedDB → клиентский код | Актуальный каталог (передаётся в classifyReorder из 19-02) приходит из IDB через useCatalogSync; доверенный источник |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | lib/reorder.ts (данные истории) | accept | Битая/изменённая запись истории уже отсеивается isValidEntry в useOrderHistory (этап 16); хелпер чистый, не доверяет priceAtOrder для добавления — берёт актуальную цену из каталога |
| T-19-02 | Information Disclosure | содержимое заказа | mitigate | Хелпер не логирует содержимое заказа во внешние сервисы и не делает сетевых вызовов; работает только в памяти |

Новой серверной поверхности атаки этап не вводит: чистый клиент (localStorage + IndexedDB). `force-dynamic` и серверная проверка `params.secret` не затрагиваются (D-09).
</threat_model>

<verification>
- `npx tsc --noEmit` — типизация всего проекта проходит (код 0)
- lib/reorder.ts — чистый модуль без импортов React/DOM; импортирует только типы и effectivePrice
- Метод addToCartWithQuantity присутствует в useCart и в типе CartContextValue
</verification>

<success_criteria>
- classifyReorder возвращает для каждой позиции один из четырёх исходов с актуальной ценой и порогом наличия stock ≤ 1
- addToCartWithQuantity кладёт товар нужным количеством, капая по остатку и накапливая для существующих позиций
- Существующий addToCart (+1) и поток localStorage корзины не сломаны
- REORD-03 (актуальная цена/наличие + честная классификация) покрыт на уровне логики; UI-показ — в плане 19-02
</success_criteria>

<output>
Create `.planning/phases/19-reorder-my-orders/19-01-SUMMARY.md` when done
</output>
