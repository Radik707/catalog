---
phase: 08-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sheet_helper.py
  - scripts/upload.py
autonomous: true
requirements: [HIDE-04]
must_haves:
  truths:
    - "Тип правки `скрыт` принимается памятью правок и не отбрасывается как мусор"
    - "После перепрогона upload.py скрытый товар имеет значение 1 в колонке «Скрыт» (L), товар остаётся в листе «Товары»"
    - "Снятие скрытия (пустое значение) возвращает товар в видимое состояние при следующем прогоне"
  artifacts:
    - path: "scripts/sheet_helper.py"
      provides: "Тип `скрыт` в ALLOWED_TYPES; чтение колонки «Скрыт» в load_products с наложением ожидающей правки"
      contains: "скрыт"
    - path: "scripts/upload.py"
      provides: "Тип `скрыт` в ALLOWED_TYPES; разворот правки в p['hidden']; колонка «Скрыт» (L) в конец строки Sheet"
      contains: "Скрыт"
  key_links:
    - from: "scripts/upload.py apply_edit_memory"
      to: "products_to_rows колонка L"
      via: "p['hidden'] → 12-й элемент строки"
      pattern: "hidden"
---

<objective>
Добавить новый тип правки `скрыт` в Python-пайплайн памяти правок: оба белых списка (`sheet_helper.py`, `upload.py`), разворот правки в поле товара `hidden`, новую колонку «Скрыт» (L) в конец строки листа «Товары» по паттерну expand-contract. Товар при скрытии ОСТАЁТСЯ в листе с флагом `1` (не вырезается).

Purpose: Реализует HIDE-04 — скрытие хранится в памяти правок и переживает перепрогон прайсов. Это фундамент: пока тип `скрыт` не принят пайплайном, кнопка-глазик в админ-панели (план 03) сохраняла бы правку «в никуда».
Output: `scripts/sheet_helper.py`, `scripts/upload.py` с поддержкой типа `скрыт`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-1/08-CONTEXT.md
@.planning/phases/08-1/08-PATTERNS.md

<interfaces>
<!-- Точные точки интеграции, подтверждены grep. Исполнитель использует напрямую. -->

scripts/sheet_helper.py:41 — ALLOWED_TYPES = {"group", "photo", "description", "name", "badge", "подгруппа"}
scripts/sheet_helper.py:226-228 — опциональные индексы колонок subgroup_i/section_i по header.index(...)
scripts/sheet_helper.py:250-252 — чтение subgroup/section из row по индексу (с защитой len(row) > i)
scripts/sheet_helper.py:260-268 — наложение ожидающей правки badge/подгруппа из edit_values поверх значения из листа

scripts/upload.py:503 — ALLOWED_TYPES = {"group", "photo", "description", "name", "badge", "подгруппа"}
scripts/upload.py:596-604 — разворот правки в override-поля (badge → p["badge_override"], подгруппа → p["subgroup_override"])
scripts/upload.py:807-846 — header (заголовок листа) + rows.append (строка): «Подгруппа» (J), «Раздел» (K) добавлены в конец
scripts/upload.py:939-943 — add_worksheet, ширина cols=len(rows[0]) if rows else 11; комментарий упоминает «11 колонок»
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Тип `скрыт` в памяти правок — sheet_helper.py</name>
  <read_first>
    - scripts/sheet_helper.py (целиком: ALLOWED_TYPES ~стр.41, load_products ~стр.217-280, append_edit ~стр.291-300)
    - .planning/phases/08-1/08-PATTERNS.md (раздел «scripts/sheet_helper.py»: SP-1, образец чтения колонки subgroup_i и наложения ожидающей правки)
  </read_first>
  <action>
    Добавить строковый литерал `"скрыт"` в множество ALLOWED_TYPES (~стр.41). Это автоматически расширяет CLI-choices `--type` (~стр.334), правку этой строки не требует.
    В load_products: завести опциональный индекс колонки по образцу subgroup_i — искать заголовок `"Скрыт"` через header.index, с тем же fallback None если колонки нет. Прочитать значение из row с той же защитой `len(row) > i`; пустая строка если колонки/значения нет.
    Наложить ожидающую правку: по образцу веток badge/подгруппа (~стр.260-268) — если для ключа товара в edit_values есть тип `скрыт`, его значение перебивает значение из листа «Товары».
    Положить результат в словарь товара ключом `hidden` (как `subgroup`/`section` кладутся сейчас). Значение — строка `"1"` (скрыт) либо пустая строка (показан); не преобразовывать в bool здесь — фронтенд админ-панели сравнивает с `"1"`.
    Русские комментарии у новых строк (правило проекта).
  </action>
  <verify>
    <automated>python -c "import ast,sys; t=ast.parse(open('scripts/sheet_helper.py',encoding='utf-8').read()); print('скрыт' in open('scripts/sheet_helper.py',encoding='utf-8').read())" </automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import scripts.sheet_helper"` (или прямой импорт модуля) проходит без SyntaxError.
    - В `scripts/sheet_helper.py` множество ALLOWED_TYPES содержит `"скрыт"` (grep -n "скрыт" находит его в строке ALLOWED_TYPES).
    - В load_products присутствует опциональный индекс колонки «Скрыт» (header.index("Скрыт") с fallback None) и наложение ожидающей правки `скрыт` из edit_values.
    - Словарь товара содержит ключ `"hidden"`.
  </acceptance_criteria>
  <done>Тип `скрыт` в белом списке sheet_helper; load_products читает колонку «Скрыт» и накладывает ожидающую правку, кладёт `hidden` в словарь товара.</done>
</task>

<task type="auto">
  <name>Task 2: Разворот правки `скрыт` и колонка «Скрыт» (L) — upload.py</name>
  <read_first>
    - scripts/upload.py (ALLOWED_TYPES ~стр.503; apply_edit_memory разворот override-полей ~стр.596-604; products_to_rows header+rows ~стр.807-846; add_worksheet ~стр.939-943)
    - .planning/phases/08-1/08-PATTERNS.md (раздел «scripts/upload.py»: SP-3 expand-contract, образцы badge и колонок «Подгруппа»/«Раздел»)
  </read_first>
  <action>
    Добавить `"скрыт"` в множество ALLOWED_TYPES (~стр.503).
    В разворот правки (~стр.596-604, рядом с ветками badge/подгруппа): если в правке есть ключ `скрыт`, записать его значение в `p["hidden"]` (значение `"1"` либо `""`). По образцу badge — пустая строка это явное снятие скрытия, её тоже сохраняем.
    В products_to_rows: дописать заголовок `"Скрыт"` 12-м (последним) элементом массива header — В КОНЕЦ, после «Подгруппа»/«Раздел», не сдвигая существующие колонки (expand-contract). В rows.append дописать `p.get("hidden", "")` 12-м (последним) элементом строки, в том же порядке.
    Товар НЕ вырезать из списка строк при `hidden=="1"` — он остаётся в листе «Товары» с флагом (иначе админ-панель его не покажет и не даст вернуть — HIDE-02).
    Обновить add_worksheet (~стр.939-943): в комментарии число колонок 11 → 12; fallback в `cols=len(rows[0]) if rows else 11` → `12`.
    Русские комментарии у новых строк.
  </action>
  <verify>
    <automated>python -m py_compile scripts/upload.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile scripts/upload.py` завершается без ошибок.
    - В `scripts/upload.py` ALLOWED_TYPES содержит `"скрыт"`.
    - Заголовок header в products_to_rows содержит `"Скрыт"` как ПОСЛЕДНИЙ (12-й) элемент; в rows.append `p.get("hidden", "")` стоит последним элементом строки.
    - apply_edit_memory записывает значение правки `скрыт` в `p["hidden"]`.
    - В коде нет вырезания товара из строк по флагу скрытия (товар остаётся в листе).
  </acceptance_criteria>
  <done>upload.py разворачивает правку `скрыт` в `p["hidden"]`; колонка «Скрыт» (L) добавлена в конец заголовка и строки; ширина листа учитывает 12 колонок; скрытый товар остаётся в листе.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| лист «Правки» → upload.py | значение правки `скрыт` приходит из памяти правок (записывается через /save админ-панели под ADMIN_SECRET) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | upload.py разворот значения `скрыт` | mitigate | Витрина трактует скрытым только row[11] === "1" (план 02); произвольное значение в колонке L не скрывает товар и не ломает запись — лишний элемент строки безопасен (expand-contract) |
| T-08-02 | Tampering | белый список ALLOWED_TYPES | accept | Записи проходят только через /save под ADMIN_SECRET (план 03) и валидируются в sheet_helper.append_edit; неизвестные типы уже отбрасываются существующим механизмом |
| T-08-SC | Tampering | npm/pip установки | accept | Этап не устанавливает новых пакетов (только правка существующих .py); порог high не достигается |

Ни одна угроза не достигает порога high.
</threat_model>

<verification>
- `python -m py_compile scripts/upload.py` и `scripts/sheet_helper.py` — без ошибок.
- grep подтверждает `"скрыт"` в обоих ALLOWED_TYPES и `"Скрыт"` в заголовке/строке products_to_rows.
- Боевой перепрогон НЕ выполняется в этом плане (только через сисадмина — план 04).
</verification>

<success_criteria>
Тип правки `скрыт` принят обоими белыми списками; upload.py разворачивает его в `p["hidden"]` и пишет колонку «Скрыт» (L) в конец строки; sheet_helper.load_products читает колонку и накладывает ожидающую правку; товар остаётся в листе при скрытии.
</success_criteria>

<output>
Create `.planning/phases/08-1/08-01-SUMMARY.md` when done
</output>
