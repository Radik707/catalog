---
phase: 08-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/sheets.ts
autonomous: true
requirements: [HIDE-05]
must_haves:
  truths:
    - "Витрина читает колонку «Скрыт» (L) из листа «Товары» в поле Product.hidden"
    - "Запрос /api/products не возвращает скрытые товары — фильтрация на сервере, скрытые в браузер не попадают"
    - "Текущая навигация и fallback восстановления раздела по structure_map не ломаются"
  artifacts:
    - path: "lib/types.ts"
      provides: "Поле hidden?: boolean в интерфейсе Product"
      contains: "hidden"
    - path: "lib/sheets.ts"
      provides: "Диапазон A2:L, маппинг row[11] → hidden, фильтр скрытых на сервере в getProducts"
      contains: "hidden"
  key_links:
    - from: "lib/sheets.ts getProducts"
      to: "ответ /api/products"
      via: "products.filter((p) => !p.hidden)"
      pattern: "filter.*hidden"
---

<objective>
Расширить чтение витрины до колонки «Скрыт» (L) и отфильтровать скрытые товары на сервере, чтобы они не попадали в ответ `/api/products` и в браузер клиента вообще. Добавить опциональное поле `hidden?: boolean` в интерфейс `Product`.

Purpose: Реализует HIDE-05 — скрытые товары отсутствуют в ответе `/api/products`, фильтрация строго на сервере (безопаснее, чем прятать на клиенте). План независим от плана 01: если колонки «Скрыт» в листе ещё нет, `row[11]` будет undefined → `hidden=false` → текущее поведение не меняется (expand-contract совместимость).
Output: `lib/types.ts`, `lib/sheets.ts` с поддержкой и фильтрацией поля `hidden`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-1/08-CONTEXT.md
@.planning/phases/08-1/08-PATTERNS.md

<interfaces>
<!-- Точные точки интеграции, подтверждены grep. -->

lib/types.ts:12-13 — subgroup?: string (J); section?: string (K) — образец опциональных полей
lib/sheets.ts:18 — const range = encodeURIComponent("Товары!A2:K");
lib/sheets.ts:42-43 — subgroup: row[9] || undefined (J); section: row[10] || undefined (K)
lib/sheets.ts:50-79 — fallback-блок восстановления section по structure_map (работает с тем же массивом products)
lib/sheets.ts:~81 — return products; (точка возврата getProducts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Поле hidden в Product + чтение колонки L</name>
  <read_first>
    - lib/types.ts (интерфейс Product, поля subgroup/section ~стр.12-13)
    - lib/sheets.ts (диапазон ~стр.18; маппинг row→Product ~стр.31-44; fallback-блок ~стр.50-79; return ~стр.81)
    - .planning/phases/08-1/08-PATTERNS.md (разделы «lib/types.ts» и «lib/sheets.ts»)
  </read_first>
  <action>
    В lib/types.ts добавить опциональное поле `hidden?: boolean` в интерфейс Product, рядом с subgroup/section, с русским комментарием «колонка L — «Скрыт» (1 = скрыт с витрины)».
    В lib/sheets.ts расширить диапазон чтения: строку `Товары!A2:K` → `Товары!A2:L`. Обновить русский комментарий о диапазоне.
    В маппинге строки в Product добавить `hidden: row[11] === "1"` (колонка L). Значение `"1"` → true, любое другое/undefined → false (совместимость, пока колонки нет).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` проходит без ошибок типов.
    - В `lib/types.ts` интерфейс Product содержит `hidden?: boolean`.
    - В `lib/sheets.ts` диапазон чтения равен `Товары!A2:L` (grep "A2:L" находит, "A2:K" отсутствует).
    - Маппинг присваивает `hidden: row[11] === "1"`.
  </acceptance_criteria>
  <done>Product имеет поле hidden; sheets.ts читает диапазон A2:L и маппит колонку L в Product.hidden.</done>
</task>

<task type="auto">
  <name>Task 2: Фильтрация скрытых на сервере в getProducts</name>
  <read_first>
    - lib/sheets.ts (fallback-блок восстановления section ~стр.50-79; точка возврата return products ~стр.81)
    - .planning/phases/08-1/08-PATTERNS.md (раздел «lib/sheets.ts», «Грабля: учесть fallback-блок»)
  </read_first>
  <action>
    В getProducts отфильтровать скрытые товары перед возвратом: вместо `return products;` вернуть `products.filter((p) => !p.hidden)`.
    Критично: фильтр применять к ИТОГОВОМУ массиву ПОСЛЕ fallback-блока восстановления section по structure_map (~стр.50-79) и любой сортировки — чтобы не сломать восстановление раздела и порядок. Фильтр — последняя операция перед return.
    Не добавлять фильтрацию на клиенте/в компонентах — только здесь, на сервере (HIDE-05): скрытые товары не должны попадать в ответ /api/products.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` проходит без ошибок.
    - getProducts завершается возвратом отфильтрованного массива (grep находит `filter` + `hidden` рядом с return).
    - Фильтр стоит после fallback-блока structure_map, не внутри него.
    - Фильтрация присутствует только в lib/sheets.ts (нет дублирующей фильтрации по hidden в компонентах app/).
  </acceptance_criteria>
  <done>getProducts возвращает только видимые товары; скрытые не попадают в ответ /api/products; fallback восстановления раздела не сломан.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getProducts (сервер) → /api/products → браузер клиента | граница, за которую скрытые товары не должны утечь |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | /api/products | mitigate | Фильтрация `products.filter((p) => !p.hidden)` выполняется на сервере в getProducts ДО формирования ответа; скрытые товары не сериализуются в JSON, в браузер не отдаются |
| T-08-04 | Tampering | значение колонки L | mitigate | Скрытым считается строго `row[11] === "1"`; любое иное значение трактуется как видимый — произвольный ввод не вызывает ложного скрытия и не ломает маппинг |

Ни одна угроза не достигает порога high.
</threat_model>

<verification>
- `npx tsc --noEmit` — без ошибок типов.
- grep: `A2:L` присутствует, `A2:K` отсутствует; `hidden` в types.ts и в маппинге; `filter` + `hidden` у return getProducts.
- Деплой витрины (git push → Vercel) и боевая проверка `/api/products` — в плане 04.
</verification>

<success_criteria>
Витрина читает колонку «Скрыт» (L) в Product.hidden; getProducts фильтрует скрытые на сервере; скрытые товары отсутствуют в ответе /api/products; текущая навигация и fallback не сломаны.
</success_criteria>

<output>
Create `.planning/phases/08-1/08-02-SUMMARY.md` when done
</output>
