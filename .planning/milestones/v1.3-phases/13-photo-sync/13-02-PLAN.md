---
phase: 13-photo-sync
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - components/CatalogSyncProvider.tsx
  - components/SyncButton.tsx
  - components/CatalogView.tsx
  - app/catalog/[secret]/layout.tsx
autonomous: false
requirements: [IMG-01, IMG-02, IMG-03, SYNC-01]
must_haves:
  truths:
    - "Кнопка ↻ видна в синей шапке рядом с шестерёнкой/корзиной всегда (в т.ч. при онлайн+свежо, когда OfflineBar скрыт)"
    - "Нажатие ↻ онлайн запускает refetch: fetch /api/products + prefetch только новых фото"
    - "Во время обновления иконка крутится; по завершении показывается короткая галочка готово"
    - "Офлайн кнопка ↻ заблокирована (disabled) с понятным title"
    - "Кнопка и CatalogView делят один экземпляр useCatalogSync (один refetch, не два sync)"
    - "После ручного refetch метка свежести в OfflineBar обновляется"
  artifacts:
    - path: "components/CatalogSyncProvider.tsx"
      provides: "React-контекст с единым экземпляром useCatalogSync для шапки и витрины"
      exports: ["CatalogSyncProvider", "useCatalogSyncContext"]
    - path: "components/SyncButton.tsx"
      provides: "Клиент-остров кнопки ↻ в шапке: idle → spin → done, disabled офлайн"
      exports: ["default"]
    - path: "app/catalog/[secret]/layout.tsx"
      provides: "SyncButton в шапке + обёртка CatalogSyncProvider вокруг шапки и main"
      contains: "SyncButton"
  key_links:
    - from: "components/SyncButton.tsx"
      to: "components/CatalogSyncProvider.tsx"
      via: "useCatalogSyncContext().refetch + isOnline"
      pattern: "useCatalogSyncContext"
    - from: "components/CatalogView.tsx"
      to: "components/CatalogSyncProvider.tsx"
      via: "useCatalogSyncContext() вместо локального useCatalogSync()"
      pattern: "useCatalogSyncContext"
    - from: "app/catalog/[secret]/layout.tsx"
      to: "components/SyncButton.tsx"
      via: "JSX в правом блоке шапки рядом с SettingsButton"
      pattern: "<SyncButton"
---

<objective>
Дать агенту видимую кнопку «Обновить» (↻) в шапке каталога, которая по требованию подтягивает свежий каталог И новые фото за одно нажатие (SYNC-01), с честным минимальным фидбэком (крутится → галочка, D-02). Поднять единый экземпляр useCatalogSync в контекст, чтобы кнопка (в layout) и витрина (CatalogView) делили один sync/refetch. Завершить этап обязательной (отложенной) приёмкой офлайн-фото на реальном iPhone.

Purpose: закрыть SYNC-01 (ручное обновление) и подтвердить IMG-01/IMG-02/IMG-03 на устройстве (критерии успеха №1, №2, №5 ROADMAP).
Output: новые components/CatalogSyncProvider.tsx и components/SyncButton.tsx, правки CatalogView.tsx и layout.tsx, отчёт iPhone-приёмки.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-photo-sync/13-CONTEXT.md
@.planning/phases/13-photo-sync/13-PATTERNS.md
@.planning/phases/13-photo-sync/13-01-SUMMARY.md

<interfaces>
<!-- Контракты после плана 01. Использовать напрямую. -->

lib/useCatalogSync.ts (после плана 01):
  export interface UseCatalogSyncResult {
    products: Product[];
    isOnline: boolean;
    syncedAt: number | null;
    status: CatalogStatus;
    refetch: () => Promise<void>;   // НОВОЕ из плана 01 — ручная синхронизация
  }
  export function useCatalogSync(): UseCatalogSyncResult;
  // Защита от гонки (syncGenRef/isCurrent) встроена: ручной refetch и авто-sync по событию online не перезатрут друг друга.

lib/useOnlineStatus.ts (этап 12):
  export function useOnlineStatus(): boolean;   // SSR-safe, навешивает online/offline события

components/SettingsButton.tsx (образец стиля кнопки шапки):
  <button className="flex h-9 w-9 items-center justify-center rounded-full transition-colors {active? bg-white/30 : hover:bg-white/15}">
    <svg className="h-6 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.8}>...</svg>
  </button>

components/CatalogView.tsx (текущее):
  Сейчас вызывает useCatalogSync() напрямую (строка 32): const sync = useCatalogSync();
  products = productsProp ?? sync.products;  (строка 36)
  status = productsProp !== undefined ? "ready" : sync.status;  (строка 40)
  → переключить на useCatalogSyncContext() (тот же контракт UseCatalogSyncResult).

app/catalog/[secret]/layout.tsx (СЕРВЕРНЫЙ async-компонент):
  Уже оборачивает дерево в <CatalogSettingsProvider><NavProvider>...</NavProvider></CatalogSettingsProvider> (строки 26-53).
  Правый блок шапки (строки 33-36): <div className="flex items-center gap-1 shrink-0"><SettingsButton /><CartIcon secret={params.secret} /></div>
  OfflineBar встроен под шапкой (строка 41). main — строка 50.

components/OfflineBar.tsx (этап 12):
  Читает syncTimestamp из IDB напрямую (getMeta), раз в минуту перечитывает (setInterval 60s).
  → после ручного refetch syncTimestamp в IDB обновится; OfflineBar подхватит при следующем тике (≤60с) ИЛИ сразу, если OfflineBar тоже перевести на контекст. См. action — минимальный путь: оставить OfflineBar как есть (он сам перечитает), задержка до минуты приемлема; полировка опциональна.
</interfaces>

<provider_decision>
ПОЧЕМУ контекст-провайдер (Claude's Discretion из CONTEXT.md закрыт здесь):
useCatalogSync сейчас живёт ВНУТРИ CatalogView (ниже шапки по дереву). Кнопка ↻ — в layout (выше). Прямой проп невозможен. Решение (по рекомендации PATTERNS.md, паттерн уже используется — CatalogSettingsProvider/NavProvider): поднять ОДИН экземпляр useCatalogSync в CatalogSyncProvider, обернуть им шапку + main в layout. Тогда кнопка и CatalogView читают один и тот же sync/refetch — без второго экземпляра хука (иначе два независимых sync, гонки, двойной fetch).
</provider_decision>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CatalogSyncProvider — единый экземпляр useCatalogSync в контексте + перевод CatalogView</name>
  <read_first>
    - components/CatalogView.tsx (строки 32, 36, 40 — текущий вызов useCatalogSync и использование sync.products/sync.status)
    - components/CatalogSettings.tsx (образец createContext/Provider/useContext-хука — тот же паттерн именования)
    - components/NavProvider.tsx (ещё один образец провайдера в этом layout)
    - lib/useCatalogSync.ts (контракт UseCatalogSyncResult после плана 01 — содержит refetch)
  </read_first>
  <action>
    Создать components/CatalogSyncProvider.tsx ("use client", русские комментарии):
    - createContext&lt;UseCatalogSyncResult | null&gt;(null).
    - Компонент CatalogSyncProvider({ children }) вызывает useCatalogSync() ОДИН раз и кладёт результат в Provider value.
    - Хук useCatalogSyncContext(): UseCatalogSyncResult — читает контекст, бросает понятную ошибку если использован вне провайдера (паттерн как в useCatalogSettings/useNav).
    Затем перевести components/CatalogView.tsx с локального useCatalogSync() на useCatalogSyncContext():
    - Заменить const sync = useCatalogSync() (строка 32) на const sync = useCatalogSyncContext().
    - products = productsProp ?? sync.products и status = productsProp !== undefined ? "ready" : sync.status — оставить как есть (контракт совместим, обратная совместимость с productsProp сохраняется).
    - Обновить импорт: убрать import { useCatalogSync } from "@/lib/useCatalogSync"; добавить import { useCatalogSyncContext } from "@/components/CatalogSyncProvider".
    Не менять остальную логику CatalogView (фильтры, grouped, lightbox).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - components/CatalogSyncProvider.tsx существует, "use client", экспортирует CatalogSyncProvider и useCatalogSyncContext.
    - Provider вызывает useCatalogSync() ровно один раз (grep: useCatalogSync встречается 1 раз в файле провайдера).
    - useCatalogSyncContext бросает ошибку при использовании вне провайдера (grep: throw).
    - CatalogView.tsx больше НЕ импортирует useCatalogSync напрямую; использует useCatalogSyncContext (grep: useCatalogSyncContext в CatalogView; useCatalogSync отсутствует в импортах CatalogView).
    - npx tsc --noEmit без ошибок.
  </acceptance_criteria>
  <done>Единый экземпляр sync в контексте; CatalogView читает из контекста; типы зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: SyncButton (↻ в шапке: idle → spin → done, disabled офлайн) + встройка в layout</name>
  <read_first>
    - components/SettingsButton.tsx (полный — стиль кнопки шапки h-9 w-9, svg h-6 w-6 text-white, hover bg-white/15, active bg-white/30)
    - components/OfflineBar.tsx (строки 49-53 — паттерн кратковременного state-тика; useOnlineStatus)
    - app/catalog/[secret]/layout.tsx (строки 26-53: провайдеры-обёртки, правый блок шапки 33-36, main 50)
    - components/CatalogSyncProvider.tsx (из Task 1 — useCatalogSyncContext, refetch, isOnline)
    - .planning/phases/13-photo-sync/13-PATTERNS.md (раздел SyncButton — форма кнопки, состояния, размещение)
  </read_first>
  <action>
    Создать components/SyncButton.tsx ("use client", PascalCase, русские комментарии, только Tailwind):
    - Читать { refetch, isOnline } из useCatalogSyncContext() (НЕ создавать второй useCatalogSync; isOnline берём из контекста, он уже там есть — не дублировать useOnlineStatus).
    - Локальные состояния: busy (идёт обновление) и done (кратковременная галочка ≈1.5с после успеха).
    - handleRefresh: если !isOnline или busy — выход; setBusy(true); try { await refetch() } finally { setBusy(false); setDone(true); setTimeout(() => setDone(false), 1500) }.
    - Разметка по образцу SettingsButton: button h-9 w-9 rounded-full, aria-label="Обновить каталог", disabled={!isOnline || busy}, title при !isOnline = "Нужен интернет для обновления" (паттерн disabled кнопки отправки заказа этапа 12); className busy/done → bg-white/30, иначе hover:bg-white/15; при disabled — приглушённый вид (opacity-50 cursor-not-allowed).
    - Иконка: SVG arrow-path (heroicons) белая h-6 w-6 text-white; при busy добавить animate-spin; при done показать SVG-галочку (check) вместо стрелки. Рисовать тем же приёмом svg, что в SettingsButton.
    Затем в app/catalog/[secret]/layout.tsx:
    - Импортировать SyncButton и CatalogSyncProvider.
    - Обернуть содержимое (шапку + OfflineBar + SubgroupFlyout + SettingsPanel + main + TelegramButton) в &lt;CatalogSyncProvider&gt; — разместить ВНУТРИ NavProvider/CatalogSettingsProvider (порядок провайдеров не критичен, но и шапка, и main должны быть внутри CatalogSyncProvider, чтобы SyncButton и CatalogView читали один контекст).
    - В правый блок шапки (строки 33-36) добавить &lt;SyncButton /&gt; ПЕРЕД &lt;SettingsButton /&gt; (слева от шестерёнки, D-01).
    layout остаётся серверным async-компонентом — SyncButton и CatalogSyncProvider встраиваются как готовые клиентские узлы (паттерн client-острова, как OfflineBar/SettingsButton).
  </action>
  <verify>
    <automated>npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - components/SyncButton.tsx существует, "use client", default export, читает refetch и isOnline из useCatalogSyncContext (grep: useCatalogSyncContext).
    - Кнопка disabled при !isOnline || busy (grep: disabled); title-пояснение при офлайн (grep: title).
    - Иконка крутится при busy (grep: animate-spin); галочка при done (grep: done).
    - app/catalog/[secret]/layout.tsx содержит <SyncButton /> в правом блоке шапки и обёртку <CatalogSyncProvider> вокруг шапки и main (grep: SyncButton и CatalogSyncProvider).
    - npm run build — Compiled successfully.
    - Поведение (проверяется визуально на сборке npm start / устройстве): онлайн нажатие ↻ → иконка крутится → галочка; офлайн кнопка серая и не нажимается.
  </acceptance_criteria>
  <done>Кнопка ↻ в шапке работает: крутится при обновлении, галочка по завершении, заблокирована офлайн; единый sync с витриной.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Отложенная iPhone-приёмка офлайн-фото (HUMAN-UAT)</name>
  <what-built>
    Полный слой офлайн-фото: карточки рендерят raw Cloudinary URL (unoptimized) → SW кэширует их CacheFirst (только 200, maxEntries:450/7д); Lightbox показывает hi-res офлайн; кнопка ↻ в шапке делает refetch каталога + prefetch только новых фото (diff); незакэшированное фото офлайн → иконка-заглушка.
  </what-built>
  <how-to-verify>
    Деплой: git push → дождаться автодеплоя Vercel. Затем на РЕАЛЬНОМ iPhone (Safari, не симулятор — критерий успеха №5 ROADMAP; на этапах 11-12 iPhone-приёмка откладывалась на конец вехи, эта задача её НЕ блокирует — авто-задачи 1-2 уже выполнены):
    1. Открыть каталог по секретной ссылке онлайн, дождаться загрузки.
    2. Прокрутить и открыть 5-10 карточек с фото; открыть 2-3 в полноэкранном просмотрщике (Lightbox) с зумом.
    3. Включить авиарежим. Открыть те же карточки → фото видны. Открыть те же фото в Lightbox с зумом → видны (критерий №1).
    4. Открыть товар, который НЕ просматривали онлайн → должна быть иконка-заглушка «нет фото», не битая картинка (D-06).
    5. DevTools (Safari Web Inspector с Mac, опционально) → Cache Storage cloudinary-images ≤ 450 записей, суммарно ≤ ~40-50 МБ, без QuotaExceededError (критерии №3, №4).
    6. Снова онлайн → нажать ↻ → иконка крутится → галочка; метка свежести в OfflineBar обновилась; в Cache Storage НЕ перекачались старые URL заново, добавились только новые (если были) (критерий №2).
    Если реального iPhone нет под рукой — оформить как ОТЛОЖЕННУЮ человеческую приёмку (по образцу этапов 11-12): создать чеклист в SUMMARY, пометить human_needed, НЕ блокировать завершение этапа авто-частью.
  </how-to-verify>
  <resume-signal>Напишите «принято» (или опишите расхождения), либо «отложить приёмку iPhone» — тогда фиксируем отложенный HUMAN-UAT-чеклист и завершаем этап по авто-критериям.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| агент (UI) → refetch | Нажатие кнопки запускает fetch /api/products + prefetch фото |
| client island (layout) ↔ React Context | SyncButton и CatalogView делят один экземпляр sync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Denial of service | Кнопка ↻ (повторные нажатия) | mitigate | disabled={!isOnline || busy} — повторный refetch во время обновления заблокирован; защита от гонки syncGenRef в useCatalogSync (этап 11) страхует параллельные вызовы |
| T-13-05 | Spoofing/Info | refetch → /api/products | accept | Тот же эндпоинт, что и авто-sync; серверная проверка UUID на /catalog/[secret] не затрагивается; новых серверных эндпоинтов нет |
| T-13-06 | Info disclosure | UUID-секрет в SW precache | accept | План не меняет precache; precache только app-shell (этап 10), не содержит UUID-данных каталога; /api/products — NetworkFirst (этап 10), не precache |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | План НЕ устанавливает пакетов: react/next/serwist уже в зависимостях. Новых install-задач нет → package legitimacy gate неприменим |
</threat_model>

<verification>
- npx tsc --noEmit — без ошибок.
- npm run build — Compiled successfully.
- grep: useCatalogSyncContext (provider, SyncButton, CatalogView), SyncButton + CatalogSyncProvider (layout), animate-spin + disabled (SyncButton).
- HUMAN-UAT: iPhone-приёмка офлайн-фото (критерии успеха №1-5 ROADMAP) — выполняется или оформляется как отложенная (по образцу этапов 11-12).
</verification>

<success_criteria>
- Кнопка ↻ всегда видна в шапке; онлайн запускает refetch (каталог + prefetch новых фото); офлайн заблокирована с пояснением.
- Фидбэк: крутится → галочка готово.
- Кнопка и витрина делят один useCatalogSync через CatalogSyncProvider.
- iPhone-приёмка офлайн-фото пройдена ИЛИ зафиксирована как отложенный HUMAN-UAT-чеклист.
</success_criteria>

<output>
Create `.planning/phases/13-photo-sync/13-02-SUMMARY.md` when done.
</output>
