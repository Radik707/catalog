---
phase: 14-install-prompt-android-ios
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - app/layout.tsx
autonomous: false
requirements: [PWA-02]

must_haves:
  truths:
    - "В корневом viewport присутствует viewport-fit=cover — иначе env(safe-area-inset-*) не работает (D-07)"
    - "В standalone-режиме каталог занимает весь экран без лишних отступов и без адресной строки (критерий успеха #4)"
    - "Ручная приёмка на реальном iPhone оформлена как явный чекпойнт; может быть отложена по образцу этапа 13 (критерий успеха #5)"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Viewport с viewport-fit=cover для корректной safe-area в standalone"
      contains: "viewport-fit"
  key_links:
    - from: "app/layout.tsx"
      to: "env(safe-area-inset-*) в InstallPrompt и островках"
      via: "viewport-fit=cover активирует safe-area insets"
      pattern: "viewport-fit"
---

<objective>
Обеспечить корректное отображение каталога в standalone-режиме после установки (критерий успеха #4) и провести ручную приёмку на реальном iPhone (критерий успеха #5).

Цель (Purpose): `env(safe-area-inset-*)`, использованный в плане 02 для нижней шторки/баннера, работает ТОЛЬКО если в viewport задан `viewport-fit=cover` (D-07). Без него установленное приложение покажет лишние отступы или шторку под чёлкой. После правки — финальная проверка всего этапа глазами на устройстве.

Выход (Output): правка `app/layout.tsx` (viewport), чекпойнт ручной приёмки.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-install-prompt-android-ios/14-CONTEXT.md
@.planning/phases/14-install-prompt-android-ios/14-02-SUMMARY.md

<interfaces>
<!-- Текущий viewport — изменить точечно. -->

Текущий app/layout.tsx экспортирует:
```typescript
export const viewport: Viewport = {
  themeColor: "#2563eb",
};
```
Next.js 14 Viewport-тип поддерживает поле `viewportFit: "cover"` (рендерится как `viewport-fit=cover` в meta viewport). НЕ создавать второй export viewport — расширить существующий.

Safe-area уже используется в components/InstallPrompt.tsx (план 02) через env(safe-area-inset-bottom) — без viewport-fit=cover он молча не сработает.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: viewport-fit=cover в корневом viewport</name>
  <read_first>
    - app/layout.tsx (изменяется — существующий export const viewport)
    - .planning/phases/14-install-prompt-android-ios/14-CONTEXT.md (D-07 — safe-area и viewport-fit)
  </read_first>
  <action>
    В `app/layout.tsx` расширить существующий `export const viewport: Viewport` полем `viewportFit: "cover"` (рядом с `themeColor`). НЕ создавать второй export, НЕ дублировать. Добавить русский комментарий: viewport-fit=cover активирует env(safe-area-inset-*) для нижней шторки/баннера в standalone на iPhone с чёлкой (D-07).

    Опционально, если требуется явный контроль масштаба в standalone — можно добавить нейтральные поля Viewport (width="device-width", initialScale=1), НО только если их ещё нет и они не ломают текущий рендер. Минимально-инвазивно: главное — viewportFit: "cover".
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/layout.tsx','utf8'); const m=s.match(/export const viewport/g)||[]; if(m.length!==1){console.error('Должен быть ровно один export viewport, найдено:',m.length);process.exit(1)} if(!/viewportFit:\s*['\"]cover['\"]/.test(s)){console.error('Нет viewportFit: cover');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - В `app/layout.tsx` ровно один `export const viewport`.
    - Объект viewport содержит `viewportFit: "cover"`.
    - Поле `themeColor: "#2563eb"` сохранено.
    - `npm run build` проходит без ошибок.
    - В собранном HTML meta viewport содержит `viewport-fit=cover` (проверяется при ручной приёмке через DevTools).
  </acceptance_criteria>
  <done>viewport-fit=cover активен; safe-area insets из плана 02 начинают работать в standalone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ручная приёмка установки на реальном iPhone</name>
  <what-built>
    Полный UX установки: хук-детектор (план 01), Android-баннер + iOS bottom-sheet + пункт «Установить приложение» в настройках (план 02), viewport-fit=cover для корректного standalone (план 03, Task 1). Деплой — git push → Vercel.
  </what-built>
  <how-to-verify>
    Сначала задеплоить: `git push origin main` → дождаться автодеплоя Vercel. Затем на РЕАЛЬНОМ iPhone (Safari):

    1. Открыть секретную ссылку каталога в Safari.
    2. Немного полистать каталог ИЛИ подождать ~25 секунд → должна появиться нижняя шторка с инструкцией «Нажмите „Поделиться" → „На экран „Домой""». Текст понятен без техзнаний (критерий #2).
    3. Закрыть шторку («Позже»/крестик). Перезагрузить страницу → шторка НЕ появляется снова автоматически (критерий #3 — однократность).
    4. Открыть настройки (шестерёнка) → пункт «Установить приложение» присутствует → нажать → шторка-инструкция открывается снова (D-05).
    5. Следуя инструкции, установить каталог на домашний экран.
    6. Запустить с домашнего экрана → каталог открывается БЕЗ адресной строки Safari, занимает весь экран, нижняя шторка/баннер (если бы были) не уходят под чёлку/системную зону (критерии #4, safe-area D-07).
    7. В standalone-режиме: открыть настройки → пункт «Установить приложение» СКРЫТ (устанавливать нечего, D-05/D-04).
    8. (Android, если доступен) Аналогично: после просмотра — баннер «Установить» снизу → клик → системный диалог → установка → запуск с домашнего экрана без браузерной строки (критерий #1).

    ⏸ Если реального iPhone сейчас нет под рукой — приёмку МОЖНО отложить на конец вехи v1.3 (по образцу этапов 11/12/13). В этом случае: отметить этап завершённым по авто-критериям (#1 косвенно, #3, #4 кодом), а пункты #2/#5 (iPhone-глаз) внести в .planning/STATE.md → «Отложенные элементы» как iPhone-приёмку PWA-02.
  </how-to-verify>
  <resume-signal>Напишите «принято» (всё работает), «отложено» (iPhone позже — оформить отложенную приёмку), или опишите проблемы.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| устройство → системная зона экрана | safe-area insets определяют, где интерфейс не перекрывается чёлкой/жестовой полосой |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Denial of Service | интерфейс под чёлкой в standalone | mitigate | viewport-fit=cover + env(safe-area-inset-*) (план 02) — контент не уходит под системные зоны (D-07, критерий #4) |
</threat_model>

<verification>
- `npm run build` проходит без ошибок.
- meta viewport содержит viewport-fit=cover.
- Ручная приёмка пройдена ИЛИ явно отложена с записью в STATE.md.
</verification>

<success_criteria>
- viewport-fit=cover активен — safe-area работает в standalone (критерий #4).
- Ручная iPhone-приёмка проведена или корректно отложена (критерий #5, по образцу этапа 13).
- Все 5 критериев успеха этапа закрыты (кодом + приёмка/отложенная приёмка).
</success_criteria>

<output>
Create `.planning/phases/14-install-prompt-android-ios/14-03-SUMMARY.md` when done
</output>
