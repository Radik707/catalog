---
phase: 14-install-prompt-android-ios
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - components/InstallPrompt.tsx
  - components/SettingsPanel.tsx
  - app/catalog/[secret]/layout.tsx
autonomous: true
requirements: [PWA-02]

must_haves:
  truths:
    - "На Android по сигналу вовлечённости появляется фирменный баннер снизу с кнопкой «Установить»; клик вызывает системный диалог установки"
    - "На iOS Safari по сигналу вовлечённости появляется bottom-sheet с пошаговой инструкцией «Поделиться → На экран Домой», понятной без техзнаний"
    - "Баннер/шторку можно закрыть (крестик или «Позже»); после закрытия флаг сохраняется и при следующих визитах подсказка не появляется автоматически"
    - "В standalone-режиме баннер/шторка не показываются и пункт «Установить приложение» в настройках скрыт"
    - "В панели настроек есть пункт «Установить приложение», открывающий установку (Android) или ту же шторку (iOS)"
    - "InstallPromptProvider обёртывает дерево каталога, баннер смонтирован рядом с OfflineBar"
  artifacts:
    - path: "components/InstallPrompt.tsx"
      provides: "Баннер Android + bottom-sheet iOS, монтируется в layout"
      min_lines: 60
    - path: "app/catalog/[secret]/layout.tsx"
      provides: "InstallPromptProvider-обёртка + <InstallPrompt /> рядом с OfflineBar"
      contains: "InstallPrompt"
    - path: "components/SettingsPanel.tsx"
      provides: "Пункт «Установить приложение» (скрыт в standalone)"
      contains: "Установить приложение"
  key_links:
    - from: "components/InstallPrompt.tsx"
      to: "components/InstallPromptProvider.tsx"
      via: "useInstallPromptContext()"
      pattern: "useInstallPromptContext"
    - from: "app/catalog/[secret]/layout.tsx"
      to: "components/InstallPrompt.tsx"
      via: "монтирование <InstallPrompt /> внутри InstallPromptProvider"
      pattern: "<InstallPrompt"
    - from: "components/SettingsPanel.tsx"
      to: "components/InstallPromptProvider.tsx"
      via: "useInstallPromptContext() для кнопки установки и проверки standalone"
      pattern: "useInstallPromptContext"
---

<objective>
Построить пользовательский UX установки поверх хука-фундамента (план 01): фирменный баннер для Android, bottom-sheet с инструкцией для iOS, постоянный пункт «Установить приложение» в настройках, и смонтировать всё в layout под общим провайдером.

Цель (Purpose): закрыть требование PWA-02 — Android-агент получает подсказку об установке, iPhone-агент получает понятную инструкцию «Поделиться → На экран Домой». Постоянная кнопка в настройках (D-05) — страховка: баннер показан раз, но возможность установить всегда под рукой.

Выход (Output): `components/InstallPrompt.tsx` (новый остров), правки `SettingsPanel.tsx` и `layout.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-install-prompt-android-ios/14-CONTEXT.md
@.planning/phases/14-install-prompt-android-ios/14-01-SUMMARY.md

<interfaces>
<!-- Контракты из плана 01 — использовать напрямую, без исследования кодовой базы. -->

Из components/InstallPromptProvider.tsx (план 01):
```typescript
export function useInstallPromptContext(): UseInstallPromptResult;
export default function InstallPromptProvider({ children }): JSX.Element;
```

Из lib/useInstallPrompt.ts (план 01) — UseInstallPromptResult содержит:
```typescript
platform: "android" | "ios" | "installed" | "unsupported";
canPromptAndroid: boolean;
isStandalone: boolean;
dismissed: boolean;
engaged: boolean;
promptInstall(): Promise<void>;   // Android: системный диалог
dismiss(): void;                   // ставит флаг закрытия + localStorage
```

Образец визуального острова — components/OfflineBar.tsx:
- "use client"; fixed/sticky-позиционирование; max-w-screen-2xl mx-auto контейнер; фирменные классы Tailwind; условный return null когда показывать нечего.

Образец стиля кнопок и структуры панели — components/SettingsPanel.tsx:
- Кнопки rounded-lg, bg-blue-500/text-white для активного, переходы transition-colors; панель fixed top-12.

Точка монтирования — app/catalog/[secret]/layout.tsx:
- Провайдеры обёрнуты так: CatalogSettingsProvider > NavProvider > CatalogSyncProvider > (header, OfflineBar, SubgroupFlyout, SettingsPanel, main, TelegramButton).
- <OfflineBar /> стоит сразу после </header> — <InstallPrompt /> монтировать рядом.

Брендинг (D-08): фирменный синий #2563eb (bg-blue-600), белый фон шторки/баннера, скруглённые углы, мягкая тень, fixed снизу (bottom-0).
Safe-area (D-07): для нижних островков использовать env(safe-area-inset-bottom) — Tailwind-классы pb-[env(safe-area-inset-bottom)] или style. viewport-fit=cover проверяется/добавляется в плане 03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: InstallPrompt — баннер Android + bottom-sheet iOS</name>
  <read_first>
    - components/InstallPrompt.tsx (создаётся)
    - components/OfflineBar.tsx (ЭТАЛОН визуального острова: "use client", позиционирование, условный return null, фирменные цвета)
    - components/InstallPromptProvider.tsx (план 01 — useInstallPromptContext, поля результата)
    - .planning/phases/14-install-prompt-android-ios/14-CONTEXT.md (D-02 баннер, D-03 iOS-шторка, D-07 safe-area, D-08 брендинг)
  </read_first>
  <action>
    Создать `components/InstallPrompt.tsx` с "use client". Компонент читает состояние через `useInstallPromptContext()`.

    Условие показа (общая логика): НЕ показывать, если `isStandalone` ИЛИ `dismissed` ИЛИ НЕ `engaged` (сигнал вовлечённости ещё не наступил) → `return null`. Это обеспечивает однократность (D-01) и «уже установлено» (D-04).

    Ветка Android (`platform === "android"` и `canPromptAndroid`):
    - Фирменный баннер `fixed bottom-0`, белый фон, скруглённые верхние углы, мягкая тень, синий акцент #2563eb (bg-blue-600).
    - Короткий дружелюбный текст-приглашение + кнопка «Установить» (вызывает `promptInstall()`) + крестик/«Позже» (вызывает `dismiss()`).
    - После `promptInstall()` (accepted ИЛИ dismissed) — скрыть баннер (полагаться на обновление состояния хука).

    Ветка iOS (`platform === "ios"`):
    - bottom-sheet `fixed bottom-0`, белый фон, скруглённые верхние углы, тень, slide-up желателен (не обязателен).
    - Пошаговая инструкция: «Нажмите „Поделиться" ⬆️ → выберите „На экран „Домой""» — с иконкой системной кнопки «Поделиться» (символ ⬆️ или SVG квадрат-со-стрелкой). Текст понятен без техзнаний (D-03).
    - Кнопка/крестик «Понятно»/«Позже» → `dismiss()`.

    Safe-area (D-07): нижний отступ контейнера учитывает `env(safe-area-inset-bottom)` (класс `pb-[env(safe-area-inset-bottom)]` или inline-style), чтобы шторку не перекрывала системная зона iPhone.

    z-index: выше контента, не ниже шапки z-50 конфликтно — поставить баннер на z-50 (он fixed снизу, с шапкой не пересекается). Комментарии на русском. НЕ инлайнить большие блоки кода — структуру брать из OfflineBar.tsx.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('components/InstallPrompt.tsx','utf8'); const need=['use client','useInstallPromptContext','promptInstall','dismiss','isStandalone','engaged','fixed','bottom-0','safe-area-inset']; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('Отсутствует:',miss);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `components/InstallPrompt.tsx` существует, начинается с "use client".
    - Читает состояние через `useInstallPromptContext()`.
    - Возвращает null, если `isStandalone || dismissed || !engaged`.
    - Android-ветка: кнопка «Установить» вызывает `promptInstall()`; есть закрытие через `dismiss()`.
    - iOS-ветка: текст-инструкция содержит «Поделиться» и «На экран» (или «Домой»); есть закрытие через `dismiss()`.
    - Позиционирование `fixed bottom-0`; учтён `env(safe-area-inset-bottom)`.
    - Фирменный синий/белый стиль по образцу OfflineBar.
    - `npm run build` проходит без ошибок.
  </acceptance_criteria>
  <done>Баннер Android и bottom-sheet iOS показываются по сигналу вовлечённости, закрываются с сохранением флага, не появляются в standalone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Монтирование в layout + пункт «Установить приложение» в настройках</name>
  <read_first>
    - app/catalog/[secret]/layout.tsx (изменяется — добавить провайдер и InstallPrompt)
    - components/SettingsPanel.tsx (изменяется — добавить пункт установки)
    - components/InstallPromptProvider.tsx (план 01 — что импортировать)
    - .planning/phases/14-install-prompt-android-ios/14-CONTEXT.md (D-05 кнопка в настройках, D-06 точка монтирования)
  </read_first>
  <action>
    Часть А — `app/catalog/[secret]/layout.tsx`:
    - Импортировать `InstallPromptProvider` из `@/components/InstallPromptProvider` и `InstallPrompt` из `@/components/InstallPrompt`.
    - Обернуть существующее дерево каталога в `<InstallPromptProvider>` — разместить ВНУТРИ `CatalogSyncProvider` (или вокруг него), но так, чтобы и `SettingsPanel`, и `InstallPrompt` оказались потомками провайдера. Минимально-инвазивно: добавить `<InstallPromptProvider>` как обёртку вокруг содержимого, где сейчас живут header/OfflineBar/SettingsPanel/main.
    - Смонтировать `<InstallPrompt />` рядом с `<OfflineBar />` (сразу после неё, по образцу D-06).

    Часть Б — `components/SettingsPanel.tsx`:
    - Импортировать `useInstallPromptContext` из `@/components/InstallPromptProvider`.
    - Прочитать `{ platform, isStandalone, promptInstall }` (и при необходимости функцию открытия iOS-шторки — см. ниже).
    - Добавить в панель пункт-кнопку «Установить приложение» (стиль по образцу существующих кнопок панели: rounded-lg, синий акцент).
    - Поведение клика:
      - Android (`platform === "android"`): вызвать `promptInstall()`.
      - iOS (`platform === "ios"`): открыть ту же bottom-sheet инструкцию (D-05). Механика открытия из настроек — на усмотрение исполнителя: проще всего ввести в хуке/контексте флаг «forceOpen» (метод `openFromSettings()`), который игнорирует `engaged`/`dismissed` и заставляет InstallPrompt показаться. Если такой метод добавляется — обновить `useInstallPrompt.ts` и `UseInstallPromptResult` (план 01), и учесть его в условии показа InstallPrompt (Task 1): `forceOpen` показывает шторку даже если `dismissed`.
    - Скрыть пункт целиком, если `isStandalone` (D-05 — устанавливать нечего).
    - Комментарии на русском.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const l=fs.readFileSync('app/catalog/[secret]/layout.tsx','utf8'); const p=fs.readFileSync('components/SettingsPanel.tsx','utf8'); const ok=l.includes('InstallPromptProvider')&&l.includes('<InstallPrompt')&&p.includes('useInstallPromptContext')&&p.includes('Установить приложение')&&p.includes('isStandalone'); if(!ok){console.error('layout/SettingsPanel не содержат нужных вставок');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` импортирует и монтирует `<InstallPromptProvider>` так, что SettingsPanel и InstallPrompt — его потомки.
    - `<InstallPrompt />` смонтирован рядом с `<OfflineBar />`.
    - `SettingsPanel.tsx` содержит пункт «Установить приложение» со стилем панели.
    - Пункт скрывается при `isStandalone`.
    - Клик на Android вызывает `promptInstall()`, на iOS открывает шторку-инструкцию.
    - Если введён `openFromSettings`/`forceOpen` — он отражён в useInstallPrompt и в условии показа InstallPrompt.
    - `npm run build` проходит без ошибок.
  </acceptance_criteria>
  <done>В шапке/настройках есть постоянный путь к установке; баннер смонтирован; standalone скрывает пункт.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| пользователь → системный диалог установки | promptInstall() инициирует браузерный prompt; результат userChoice контролирует браузер |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Repudiation | повторный показ баннера после закрытия | mitigate | dismiss() пишет флаг в localStorage; условие показа InstallPrompt проверяет dismissed → баннер не возвращается (критерий успеха #3) |
| T-14-04 | Denial of Service | InstallPrompt перекрывает контент/системные зоны | mitigate | env(safe-area-inset-bottom) + возможность закрыть (крестик/«Позже»); z-index не выше шапки конфликтно |
</threat_model>

<verification>
- `npm run build` проходит без ошибок типов.
- grep-проверки задач проходят.
- Дерево каталога обёрнуто в InstallPromptProvider; SettingsPanel и InstallPrompt — потомки.
- Существующие провайдеры (CatalogSettings/Nav/CatalogSync) не сломаны (build это подтверждает).
</verification>

<success_criteria>
- Android-баннер и iOS-инструкция показываются по сигналу вовлечённости и однократно (D-01).
- Пункт «Установить приложение» в настройках работает на обеих платформах и скрыт в standalone (D-05).
- Всё смонтировано под единым провайдером — баннер и кнопка делят одно событие.
</success_criteria>

<output>
Create `.planning/phases/14-install-prompt-android-ios/14-02-SUMMARY.md` when done
</output>
