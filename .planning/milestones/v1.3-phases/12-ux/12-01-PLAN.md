---
phase: 12-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/useOnlineStatus.ts
  - lib/formatSyncTime.ts
  - components/OfflineBar.tsx
  - app/catalog/[secret]/layout.tsx
autonomous: true
requirements: [SYNC-03]
user_setup: []

must_haves:
  truths:
    - "Онлайн + данные свежее 24 ч → полоска скрыта (чистая шапка)"
    - "Офлайн → серая нейтральная полоска «Офлайн • данные за 08:30»"
    - "Данные старше 24 ч (онлайн или офлайн) → жёлтая полоска-предупреждение"
    - "Полоска показывает время последней синхронизации в человекочитаемом формате (сегодня/вчера/дата)"
  artifacts:
    - path: "lib/useOnlineStatus.ts"
      provides: "Лёгкий хук статуса сети (navigator.onLine + события online/offline), SSR-safe"
      exports: ["useOnlineStatus"]
      min_lines: 20
    - path: "lib/formatSyncTime.ts"
      provides: "Чистая утилита форматирования метки свежести данных (D-04)"
      exports: ["formatSyncTime"]
      min_lines: 20
    - path: "components/OfflineBar.tsx"
      provides: "Клиентская полоска статуса под шапкой с тремя состояниями (D-01)"
      contains: "use client"
      min_lines: 30
    - path: "app/catalog/[secret]/layout.tsx"
      provides: "Вставка <OfflineBar /> под </header>"
      contains: "OfflineBar"
  key_links:
    - from: "components/OfflineBar.tsx"
      to: "lib/useOnlineStatus.ts"
      via: "import useOnlineStatus"
      pattern: "useOnlineStatus"
    - from: "components/OfflineBar.tsx"
      to: "lib/catalogDb.ts"
      via: "getMeta('syncTimestamp')"
      pattern: "getMeta"
    - from: "components/OfflineBar.tsx"
      to: "lib/formatSyncTime.ts"
      via: "import formatSyncTime"
      pattern: "formatSyncTime"
    - from: "app/catalog/[secret]/layout.tsx"
      to: "components/OfflineBar.tsx"
      via: "JSX вставка под </header>"
      pattern: "<OfflineBar"
---

<objective>
Сделать офлайн-режим и свежесть данных видимыми: тонкая полоска статуса под синей шапкой каталога с тремя состояниями (скрыта / серая офлайн / жёлтая «данные устарели»).

Purpose: Агент в полях должен с первого взгляда понимать, в каком режиме он работает и насколько свеж каталог (требование SYNC-03, критерии успеха №1 и №2 из ROADMAP). Закрывает D-01..D-04 из CONTEXT.md.

Output: Хук `useOnlineStatus`, утилита `formatSyncTime`, компонент `OfflineBar`, вставка полоски в layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ux/12-CONTEXT.md
@.planning/phases/12-ux/12-PATTERNS.md

<interfaces>
<!-- Контракты, которые исполнитель использует напрямую — без исследования кодовой базы. -->

Из lib/catalogDb.ts (источник времени синхронизации — НЕ запускать полный useCatalogSync):
```typescript
export async function getMeta<T = unknown>(key: string): Promise<T | undefined>;
// Ключ "syncTimestamp" хранит unix-ms последней синхронизации (или undefined до первой)
```

Из lib/useCatalogSync.ts (паттерн SSR-safe online/offline — копировать структуру, НЕ импортировать сам хук ради статуса):
```typescript
// Начальное значение SSR-safe:
const [isOnline, setIsOnline] = useState<boolean>(true);
// В useEffect: navigator только внутри callback, события online/offline + cleanup
```

Из lib/nav.ts (паттерн чистой утилиты без внешних зависимостей):
```typescript
// Именованные экспортируемые функции, без "use client", без dayjs/moment, комментарии на русском
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Хук статуса сети useOnlineStatus + утилита formatSyncTime</name>
  <files>lib/useOnlineStatus.ts, lib/formatSyncTime.ts</files>
  <read_first>
    - lib/useCatalogSync.ts (паттерн SSR-guard для navigator: строки 47-52 — useState(true); строки 183-214 — useEffect с навигатором, слушателями online/offline и cleanup)
    - lib/nav.ts (паттерн чистой утилиты-модуля: именованные экспорты, нет "use client", нет внешних зависимостей, комментарии на русском)
    - lib/types.ts (формат Product — нужен ли импорт; для formatSyncTime скорее всего НЕ нужен)
  </read_first>
  <action>
    Создать `lib/useOnlineStatus.ts` — клиентский хук («use client", импорт `useState`, `useEffect` из "react"). Сигнатура: `export function useOnlineStatus(): boolean`. Скопировать SSR-safe паттерн из useCatalogSync.ts: инициализация `useState<boolean>(true)` (сервер не имеет navigator); в `useEffect([])` исправить значение через `if (typeof navigator !== "undefined") setIsOnline(navigator.onLine)`, навесить слушатели `window.addEventListener("online", ...)` и `"offline"`, вернуть cleanup со снятием обоих слушателей. Хук НЕ запускает useCatalogSync и НЕ обращается к IDB — это тонкая обёртка только над navigator.onLine. Комментарии на русском.

    Создать `lib/formatSyncTime.ts` — чистая утилита (без "use client", без внешних библиотек дат). Сигнатура: `export function formatSyncTime(syncedAt: number | null): string`. Логика по D-04: если `syncedAt === null` → «данные не загружены»; иначе построить `Date` из unix-ms и вернуть по календарной дате: сегодня (та же дата) → «данные за ЧЧ:ММ»; вчера (вчерашняя календарная дата) → «данные за вчера, ЧЧ:ММ»; раньше → «данные за D месяца, ЧЧ:ММ» (день + название месяца в родительном падеже, без года), например «данные за 10 июня, 08:30». Часы и минуты — с ведущими нулями (08:30, не 8:30). Сравнение дат — по календарным суткам (год+месяц+день), НЕ скользящим окном 24 ч. Названия месяцев в родительном падеже — массив-константа в модуле. Только нативный `Date`. Комментарии на русском.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - lib/useOnlineStatus.ts содержит "use client" первой строкой
    - lib/useOnlineStatus.ts экспортирует функцию useOnlineStatus с возвращаемым типом boolean
    - lib/useOnlineStatus.ts инициализирует состояние через useState(true) и читает navigator.onLine только внутри useEffect (typeof navigator !== "undefined")
    - lib/useOnlineStatus.ts навешивает и снимает слушатели событий "online" и "offline" (cleanup-функция присутствует)
    - lib/useOnlineStatus.ts НЕ импортирует useCatalogSync и НЕ обращается к lib/catalogDb
    - lib/formatSyncTime.ts экспортирует formatSyncTime(syncedAt: number | null): string
    - formatSyncTime(null) возвращает строку «данные не загружены»
    - formatSyncTime для сегодняшней даты возвращает строку формата «данные за ЧЧ:ММ» с ведущими нулями
    - formatSyncTime для вчерашней даты содержит слово «вчера»
    - formatSyncTime для более ранней даты содержит название месяца в родительном падеже (напр. «июня») без года
    - lib/formatSyncTime.ts не содержит "use client" и не импортирует dayjs/moment/date-fns
    - npx tsc --noEmit проходит без ошибок
  </acceptance_criteria>
  <done>Оба файла созданы, типобезопасны, formatSyncTime даёт три формата по D-04, useOnlineStatus SSR-safe и независим от useCatalogSync.</done>
</task>

<task type="auto">
  <name>Task 2: Компонент OfflineBar + вставка в layout</name>
  <files>components/OfflineBar.tsx, app/catalog/[secret]/layout.tsx</files>
  <read_first>
    - components/ScrollToTop.tsx (паттерн "use client" + useEffect для браузерного состояния + условный рендер `if (!visible) return null`)
    - components/CartIcon.tsx (паттерн клиентского компонента в шапке с Tailwind bg-/text-классами)
    - app/catalog/[secret]/layout.tsx (целевой файл — серверный async-компонент; точка вставки под `</header>` на строке 37, перед `<SubgroupFlyout />` на строке 40)
    - lib/catalogDb.ts (getMeta — строки 95-98; ключ "syncTimestamp")
    - lib/useOnlineStatus.ts (создан в Task 1)
    - lib/formatSyncTime.ts (создан в Task 1)
  </read_first>
  <action>
    Создать `components/OfflineBar.tsx` — клиентский компонент («use client", импорт `useState`, `useEffect` из "react"). Внутри: `const isOnline = useOnlineStatus()`; локальное состояние `syncedAt: number | null` (useState(null)), которое в `useEffect([])` заполняется через `await getMeta<number>("syncTimestamp")` из `@/lib/catalogDb` (обернуть в async-IIFE или .then, guard на размонтирование не обязателен, но getMeta может бросить — обернуть в try/catch, при ошибке оставить null). Вычислить «свежесть»: `isStale = syncedAt !== null && (Date.now() - syncedAt) > 24*60*60*1000`. Логика рендера (D-01): если `isOnline && !isStale` → `return null` (полоска скрыта, в т.ч. пока syncedAt === null и онлайн — не мигаем). Иначе рендерить полоску. Цвет (D-03, паттерн из 12-PATTERNS.md): устаревшие данные `isStale` → жёлтая (`bg-yellow-50 text-yellow-800`); офлайн со свежими данными → нейтральная серая (`bg-gray-100 text-gray-600`). Текст: офлайн → «Офлайн • {formatSyncTime(syncedAt)}»; онлайн+устарело → «{formatSyncTime(syncedAt)}» (без «Офлайн»). Позиционирование (D-02): внешний `<div>` со `sticky top-12 z-40 w-full` (top-12 = высота шапки h-12), внутренний контейнер `max-w-screen-2xl mx-auto w-full px-2 py-1 text-xs text-center` в тон шапке. НЕ `fixed`. Комментарии на русском.

    Изменить `app/catalog/[secret]/layout.tsx`: добавить импорт `import OfflineBar from "@/components/OfflineBar";` в начало файла (рядом с другими импортами компонентов). Вставить `<OfflineBar />` сразу после `</header>` (строка 37) и перед комментарием `{/* Полоса подгрупп ... */}` / `<SubgroupFlyout />` (строки 39-40). Добавить русский комментарий «Индикатор офлайн-режима и свежести данных — клиентский остров». Layout остаётся серверным async-компонентом — Next.js App Router сам делает OfflineBar клиентским островом (как CartIcon/TelegramButton), дополнительный "use client" в layout НЕ нужен.
  </action>
  <verify>
    <automated>npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - components/OfflineBar.tsx содержит "use client" первой строкой
    - components/OfflineBar.tsx импортирует useOnlineStatus из @/lib/useOnlineStatus
    - components/OfflineBar.tsx импортирует getMeta из @/lib/catalogDb и читает ключ "syncTimestamp"
    - components/OfflineBar.tsx импортирует formatSyncTime из @/lib/formatSyncTime
    - components/OfflineBar.tsx возвращает null когда isOnline === true и данные не старше 24 ч
    - components/OfflineBar.tsx использует bg-gray-100 (нейтральный) для офлайн со свежими данными и bg-yellow-50 для данных старше 24 ч
    - components/OfflineBar.tsx НЕ использует красный цвет (нет bg-red-/text-red-) — офлайн оформлен спокойно (D-03)
    - components/OfflineBar.tsx использует sticky (не fixed) и max-w-screen-2xl mx-auto во внутреннем контейнере
    - app/catalog/[secret]/layout.tsx импортирует OfflineBar и содержит <OfflineBar /> между </header> и <SubgroupFlyout />
    - app/catalog/[secret]/layout.tsx остаётся серверным компонентом (нет "use client" в начале файла)
    - npx tsc --noEmit и npm run build проходят без ошибок
  </acceptance_criteria>
  <done>OfflineBar рендерит три состояния по D-01/D-03, читает реальное syncedAt из IDB, вставлен под шапкой в layout; проект собирается.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB → клиентский UI | Чтение `syncTimestamp` из локальной IDB (данные, ранее записанные самим приложением — не пользовательский ввод) |
| navigator API → UI | Состояние сети из браузерного API (доверенный источник, не ввод) |

Новых серверных эндпоинтов, секретов, обработки внешнего пользовательского ввода в этом плане нет. Поверхность атаки — чисто клиентский рендер из уже доверенных локальных источников.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | OfflineBar (метка времени) | accept | Время синхронизации не является секретом; отображается только владельцу устройства внутри защищённого UUID-каталога |
| T-12-02 | Denial of Service | getMeta() бросает при недоступной IDB (приватный режим iOS) | mitigate | Обернуть getMeta в try/catch, при ошибке оставить syncedAt=null — полоска не ломает рендер шапки |
| T-12-03 | Tampering | npm-зависимости | accept | План не добавляет новых npm-пакетов (только нативный Date и существующий idb через catalogDb); package legitimacy gate не применим |
</threat_model>

<verification>
- `npx tsc --noEmit` — типобезопасность всех новых файлов
- `npm run build` — production-сборка проходит (layout остаётся серверным, OfflineBar — корректный клиентский остров)
- Ручная проверка в браузере (опционально, для исполнителя): DevTools → офлайн-режим → полоска становится серой; перевод системных часов / правка syncTimestamp в IDB на >24ч назад → полоска жёлтая; онлайн+свежие данные → полоски нет
</verification>

<success_criteria>
- Полоска скрыта при онлайн + данные свежее 24 ч (критерий успеха ROADMAP №1: «при подключении полоска исчезает»)
- Полоска серая «Офлайн • данные за ЧЧ:ММ» в офлайне (критерий №1)
- Полоска жёлтая при данных старше 24 ч (критерий №2)
- Метка времени читается из реального syncTimestamp в IDB, формат по D-04
</success_criteria>

<output>
Create `.planning/phases/12-ux/12-01-SUMMARY.md` when done
</output>
