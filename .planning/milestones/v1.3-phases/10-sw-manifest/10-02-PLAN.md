---
phase: 10-sw-manifest
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - public/icons/icon-192x192.png
  - public/icons/icon-512x512.png
  - public/icons/icon-512x512-maskable.png
  - public/icons/apple-touch-icon.png
  - app/manifest.ts
  - app/layout.tsx
autonomous: false
requirements: [PWA-01]
user_setup: []

must_haves:
  truths:
    - "Каталог устанавливается на домашний экран через штатное меню браузера и запускается в standalone-режиме без адресной строки (D-08 display:standalone; D-10 orientation:portrait)"
    - "На домашнем экране под иконкой отображается короткое имя «Вкусный Дом» (D-01 name «Каталог Вкусный Дом»; D-02 short_name «Вкусный Дом»), иконка — синий квадрат #2563eb с белой монограммой ВД (D-03 дизайн; D-04 размеры/maskable/apple-touch; D-05 временная иконка)"
    - "Splash-заставка и полоска статуса используют фирменные цвета: theme_color #2563eb (D-06), background_color #ffffff (D-07)"
    - "Тап по иконке открывает каталог напрямую по секретной ссылке без хардкода секрета в коде (D-09 start_url из CATALOG_SECRET)"
    - "Манифест отдаётся встроенным механизмом Next.js (app/manifest.ts), внешняя либа не нужна (D-15)"
  artifacts:
    - path: "app/manifest.ts"
      provides: "Web App Manifest (Next.js built-in) с именем, цветами, standalone, start_url из CATALOG_SECRET"
      contains: "MetadataRoute.Manifest"
    - path: "public/icons/icon-192x192.png"
      provides: "Иконка PWA 192×192"
    - path: "public/icons/icon-512x512.png"
      provides: "Иконка PWA 512×512"
    - path: "public/icons/icon-512x512-maskable.png"
      provides: "Maskable-иконка для Android adaptive icons"
    - path: "public/icons/apple-touch-icon.png"
      provides: "Иконка iOS 180×180 без прозрачности"
    - path: "app/layout.tsx"
      provides: "PWA/iOS-метаданные: appleWebApp, icons, theme-color"
      contains: "appleWebApp"
  key_links:
    - from: "app/manifest.ts"
      to: "CATALOG_SECRET"
      via: "process.env в start_url"
      pattern: "process.env.CATALOG_SECRET"
    - from: "app/layout.tsx"
      to: "public/icons/apple-touch-icon.png"
      via: "metadata.icons.apple"
      pattern: "apple-touch-icon"
---

<objective>
Сделать каталог устанавливаемым PWA-приложением: сгенерировать иконки (синий #2563eb квадрат + белая монограмма «ВД»), создать app/manifest.ts (имя, цвета, standalone, start_url из CATALOG_SECRET), добавить PWA/iOS-метаданные в app/layout.tsx.

Purpose: Закрывает требование PWA-01 — установка на домашний экран и запуск без браузерной строки. Опирается на активный SW из плана 10-01 (браузер не предложит установку без зарегистрированного SW + manifest).
Output: Каталог устанавливается через меню браузера, запускается в standalone с иконкой «ВД» и именем «Вкусный Дом».
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sw-manifest/10-CONTEXT.md
@.planning/phases/10-sw-manifest/10-PATTERNS.md

<interfaces>
<!-- Контракты для исполнителя. Кодовую базу исследовать не нужно. -->

Текущий app/layout.tsx (полностью):
```tsx
import type { Metadata } from "next";
import "./globals.css";
import { CartProvider } from "@/components/CartProvider";

export const metadata: Metadata = {
  title: "Каталог товаров",
  description: "B2B-каталог товаров для владельцев магазинов",
  robots: "noindex, nofollow",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ru">
      <body className="bg-gray-50 antialiased">
        <CartProvider>{children}</CartProvider>
      </body>
    </html>
  );
}
```

Паттерн чтения секрета (из app/catalog/[secret]/page.tsx:18 и lib/sheets.ts:4-5):
`process.env.CATALOG_SECRET` — секрет НЕ хардкодить (D-09). В manifest.ts формировать start_url как `` `/catalog/${process.env.CATALOG_SECRET ?? ""}` ``.

Тип манифеста (Next.js built-in): `import type { MetadataRoute } from "next"`, `export default function manifest(): MetadataRoute.Manifest`. Next.js сам выставит Content-Type: application/manifest+json — внешняя либа не нужна (D-15).

Фирменный синий: `#2563eb` (= Tailwind bg-blue-600, из app/catalog/[secret]/layout.tsx:28).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Сгенерировать иконки PWA</name>
  <files>public/icons/icon-192x192.png, public/icons/icon-512x512.png, public/icons/icon-512x512-maskable.png, public/icons/apple-touch-icon.png</files>
  <read_first>
    - .planning/phases/10-sw-manifest/10-CONTEXT.md (D-03, D-04, D-05 — параметры иконки, форматы, временность)
    - .planning/phases/10-sw-manifest/10-PATTERNS.md (раздел «public/ иконки» — имена файлов и подпапка public/icons/)
  </read_first>
  <action>
    Создать папку public/icons/ (папки public/ ещё нет — создать с нуля). Сгенерировать 4 PNG-иконки: синий квадрат `#2563eb` + белая монограмма «ВД» по центру, со скруглением (D-03):
    - icon-192x192.png — 192×192 px, скруглённые углы;
    - icon-512x512.png — 512×512 px, скруглённые углы;
    - icon-512x512-maskable.png — 512×512 px, монограмма в safe-zone ~80% от центра (буквы меньше и ближе к центру, чтобы Android adaptive-маска не обрезала их), фон синий заполняет весь квадрат без прозрачности (D-04);
    - apple-touch-icon.png — 180×180 px, БЕЗ прозрачности, фон синий #2563eb, без скругления (iOS скругляет сам) (D-04).

    Способ генерации — на усмотрение исполнителя (Claude's Discretion, D-05): Node-скрипт с подходящей графической библиотекой (например sharp с SVG-исходником монограммы), либо чистый SVG→PNG конвертер. Если ставится временная dev-зависимость для генерации — это не часть рантайма, в package.json dependencies не добавлять без необходимости. Шрифт монограммы — жирный, белый, читаемый. Иконка временная и легко заменяемая (D-05): сложную дизайн-работу не делать.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f=['public/icons/icon-192x192.png','public/icons/icon-512x512.png','public/icons/icon-512x512-maskable.png','public/icons/apple-touch-icon.png'];for(const p of f){if(!fs.existsSync(p)||fs.statSync(p).size<500){console.error('missing/empty '+p);process.exit(1)}}console.log('icons ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Существуют 4 непустых PNG: public/icons/icon-192x192.png, icon-512x512.png, icon-512x512-maskable.png, apple-touch-icon.png (D-04).
    - Иконки — синий #2563eb фон с белой монограммой «ВД» (D-03).
    - apple-touch-icon.png без альфа-прозрачности (фон сплошной синий) (D-04).
    - maskable-вариант имеет safe-zone (монограмма не у краёв) (D-04).
    - Иконки временные и заменяемые без правки кода (D-05).
  </acceptance_criteria>
  <done>4 иконки сгенерированы в public/icons/.</done>
</task>

<task type="auto">
  <name>Task 2: Создать app/manifest.ts и добавить PWA-метаданные в app/layout.tsx</name>
  <files>app/manifest.ts, app/layout.tsx</files>
  <read_first>
    - .planning/phases/10-sw-manifest/10-PATTERNS.md (разделы «app/manifest.ts» и «app/layout.tsx» — скелеты и поля)
    - .planning/phases/10-sw-manifest/10-CONTEXT.md (D-01..D-10, D-15 — все поля манифеста и метаданные)
    - app/layout.tsx (текущий файл — расширяется)
  </read_first>
  <action>
    Создать app/manifest.ts — Next.js built-in (D-15): `import type { MetadataRoute } from "next"`, `export default function manifest(): MetadataRoute.Manifest`. Комментарии на русском. Поля:
    - `name`: "Каталог Вкусный Дом" (D-01);
    - `short_name`: "Вкусный Дом" (D-02);
    - `start_url`: `` `/catalog/${process.env.CATALOG_SECRET ?? ""}` `` (D-09 — секрет из env, НЕ хардкод);
    - `display`: "standalone" (D-08);
    - `orientation`: "portrait" (D-10);
    - `theme_color`: "#2563eb" (D-06);
    - `background_color`: "#ffffff" (D-07);
    - `icons`: массив из 3 записей — icon-192x192.png (192x192, purpose "any"), icon-512x512.png (512x512, purpose "any"), icon-512x512-maskable.png (512x512, purpose "maskable"); type "image/png", src — пути от корня (/icons/...).

    Расширить app/layout.tsx — добавить в объект metadata (НЕ ломая существующие title/description/robots, НЕ трогая RootLayout-функцию и CartProvider):
    - `appleWebApp: { capable: true, statusBarStyle: "default", title: "Вкусный Дом" }` (D-02 short_name для iOS);
    - `icons: { icon: [{url:"/icons/icon-192x192.png", sizes:"192x192", type:"image/png"}, {url:"/icons/icon-512x512.png", sizes:"512x512", type:"image/png"}], apple: [{url:"/icons/apple-touch-icon.png", sizes:"180x180"}] }`;
    - theme-color #2563eb — через `export const viewport: Viewport = { themeColor: "#2563eb" }` (D-06; Next.js 14: themeColor выносится в viewport, не в metadata; импортировать тип Viewport из "next"). Комментарии на русском.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('app/manifest.ts','utf8');const l=fs.readFileSync('app/layout.tsx','utf8');const ok=m.includes('MetadataRoute.Manifest')&&m.includes('Вкусный Дом')&&m.includes('standalone')&&m.includes('#2563eb')&&m.includes('process.env.CATALOG_SECRET')&&m.includes('maskable')&&l.includes('appleWebApp')&&l.includes('apple-touch-icon')&&l.includes('#2563eb');if(!ok){console.error('manifest/layout incomplete');process.exit(1)}console.log('manifest+layout ok')"</automated>
  </verify>
  <acceptance_criteria>
    - app/manifest.ts экспортирует функцию типа MetadataRoute.Manifest (D-15) с name "Каталог Вкусный Дом" (D-01), short_name "Вкусный Дом" (D-02), display standalone (D-08), theme_color #2563eb (D-06), background_color #ffffff (D-07), orientation portrait (D-10).
    - start_url использует process.env.CATALOG_SECRET (секрет не захардкожен) (D-09).
    - manifest.icons включает запись с purpose "maskable" (D-04).
    - app/layout.tsx содержит appleWebApp с title "Вкусный Дом" (D-02), icons с apple-touch-icon (D-04), theme-color #2563eb через viewport (D-06).
    - Существующие title/description/robots и RootLayout/CartProvider не сломаны.
    - `npm run build` завершается с кодом 0; манифест доступен по /manifest.webmanifest.
  </acceptance_criteria>
  <done>app/manifest.ts создан, layout.tsx содержит PWA/iOS-метаданные; build проходит.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Проверить установку PWA и standalone-режим на устройстве</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 10 — критерий успеха №3: standalone-режим)
  </read_first>
  <what-built>
    Автоматизировано: manifest.ts (имя/цвета/standalone/start_url из секрета), 4 иконки, PWA/iOS-метаданные в layout.tsx. SW активен из плана 10-01. PWA-поведение нельзя проверить в dev (D-13) — только через продакшн-сборку и реальный браузер/устройство.
  </what-built>
  <how-to-verify>
    1. `npm run build` затем `npm start` (НЕ npm run dev — в dev SW отключён, D-13).
    2. Открыть секретный URL каталога в Chrome (десктоп): DevTools → Application → Manifest — поля Name «Каталог Вкусный Дом», Short name «Вкусный Дом», theme_color #2563eb, иконки (включая maskable) загружены без ошибок.
    3. DevTools → Application → Service Workers — статус `activated and running`.
    4. В адресной строке Chrome появляется иконка установки (или меню ⋮ → «Установить приложение»). Установить.
    5. Запустить установленное приложение — открывается БЕЗ адресной строки браузера (standalone), сразу на каталоге по секретной ссылке (критерий успеха №3).
    6. Под иконкой на домашнем экране/в списке приложений — «Вкусный Дом», иконка синяя с «ВД».
    7. Желательно: проверить на реальном Android-телефоне (Chrome) — установка через меню, standalone-запуск.
  </how-to-verify>
  <acceptance_criteria>
    - DevTools → Manifest без ошибок, все иконки и поля корректны.
    - Приложение устанавливается через штатное меню браузера.
    - После установки каталог запускается в standalone (без адресной строки) и открывается на нужной секретной ссылке.
    - Короткое имя и иконка отображаются верно.
  </acceptance_criteria>
  <resume-signal>Напишите «approved», если установка и standalone-режим работают, либо опишите расхождения.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest.webmanifest → публичный доступ | Манифест содержит start_url с секретом каталога |
| Иконки public/icons/ → публичный доступ | Статические ассеты доступны по прямым URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | CATALOG_SECRET в start_url манифеста | accept | Манифест /manifest.webmanifest отдаётся по тому же секретному origin, что и каталог; кто видит каталог — уже знает секрет. Секрет НЕ хардкодится, берётся из env (D-09). Новых публичных эндпоинтов с секретом не добавляется. Модель безопасности «секрет в URL» не меняется этим этапом. Приемлемо. |
| T-10-05 | Spoofing | Иконки/имя приложения подменяют бренд | accept | Иконки — временная самодельная монограмма (D-05); фишинг-риск незначим для B2B-каталога по приватной ссылке. |
| T-10-06 | Tampering | maskable-иконка обрезается Android-маской | mitigate | Safe-zone ~80% в icon-512x512-maskable.png (D-04) — монограмма не у краёв. |
</threat_model>

<verification>
Проверка ТОЛЬКО через продакшн-сборку (`npm run build && npm start`, НЕ dev — D-13):
1. `npm run build` — код выхода 0.
2. /manifest.webmanifest отдаётся с корректными полями (DevTools → Application → Manifest).
3. Установка через меню браузера → standalone-запуск без адресной строки (критерий успеха №3).
4. Часть критериев (отображение иконки/имени на домашнем экране, реальный standalone) проверяется на реальном устройстве (Android Chrome; iOS-установка — этап 14).
</verification>

<success_criteria>
- 4 иконки в public/icons/ (синий #2563eb + «ВД», включая maskable и apple-touch-icon).
- app/manifest.ts: имя/цвета/standalone/portrait, start_url из CATALOG_SECRET (не хардкод).
- app/layout.tsx: appleWebApp, icons, theme-color #2563eb.
- `npm run build` проходит; приложение устанавливается и запускается в standalone (PWA-01).
</success_criteria>

<output>
Create `.planning/phases/10-sw-manifest/10-02-SUMMARY.md` when done
</output>
</content>
