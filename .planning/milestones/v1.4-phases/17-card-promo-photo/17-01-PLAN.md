---
phase: 17-card-promo-photo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/CardCornerButton.tsx
autonomous: false
requirements: [CARD-01]
must_haves:
  truths:
    - "Клиент видит на карточке с фото вторую угловую кнопку со стрелками под сердечком избранного"
    - "Нажатие на стрелки клиента открывает тот же Lightbox, что и у агента"
    - "На карточке без фото клиенту показывается только сердечко (кнопки стрелок нет)"
    - "Роль sales (Агент) выглядит и работает ровно как раньше — без изменений"
    - "До готовности роли (ready=false) на месте кнопок держится пустое место того же размера (нет мелькания)"
  artifacts:
    - path: "components/CardCornerButton.tsx"
      provides: "Вторая угловая кнопка-стрелки для роли client под сердечком"
      contains: "onPhotoOpen"
  key_links:
    - from: "components/CardCornerButton.tsx (ветка client)"
      to: "onPhotoOpen"
      via: "onClick со stopPropagation"
      pattern: "onPhotoOpen\\?\\.\\(\\)"
---

<objective>
Дать роли «Клиент» возможность открыть фото товара на весь экран прямо с карточки —
второй угловой кнопкой со стрелками (arrows-pointing-out), расположенной ПОД сердечком
избранного, в дополнение к нему. Кнопка показывается только при наличии фото.

Назначение: закрыть реальный пробел — сейчас клиент не может открыть фото крупно
(тап по фото переворачивает карточку, стрелки раскрытия есть только у роли «Агент»).
Многие не догадаются развести фото пальцами — делаем действие явным и нативным (D-03).

Выход: правка `components/CardCornerButton.tsx` — клиент получает две кнопки (сердечко +
стрелки) вертикальным стеком; агент не затрагивается.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-card-promo-photo/17-CONTEXT.md
@.planning/phases/17-card-promo-photo/17-PATTERNS.md

<interfaces>
<!-- Контракты, которые исполнителю нужны напрямую. Извлечены из кода — без доисследования. -->

Пропсы компонента (уже существуют, новых вводить не нужно) — components/CardCornerButton.tsx:
```typescript
interface CardCornerButtonProps {
  productId: string;
  onPhotoOpen?: () => void;   // открыть Lightbox (проброшен из CatalogView)
  canOpenPhoto?: boolean;     // !!product.imageUrl — есть ли что разворачивать
  size?: "sm" | "md";
}
```

Роль (SSR-safe) — lib/useRole.tsx:
```typescript
const { role, ready } = useRole(); // role: "client" | "sales"; ready: boolean
```

Избранное — components/FavoritesProvider.tsx:
```typescript
const { isFavorite, toggleFavorite } = useFavoritesContext();
```

Готовая кнопка-эталон стрелок — это ветка `role === "sales" && canOpenPhoto`
(строки 45–66 текущего файла): SVG arrows-pointing-out + onClick со stopPropagation →
onPhotoOpen. Её паттерн копируется в ветку client.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить клиенту вторую кнопку-стрелки под сердечком</name>
  <files>components/CardCornerButton.tsx</files>
  <read_first>
    - components/CardCornerButton.tsx (изменяемый файл; эталон стрелок — строки 45–66, сердечко клиента — строки 70–98, SSR-заглушка — строки 39–42)
    - .planning/phases/17-card-promo-photo/17-PATTERNS.md (точные строки-эталоны: SVG стрелок, классы угловой кнопки, stopPropagation, размеры box/icon)
    - .planning/phases/17-card-promo-photo/17-CONTEXT.md (решения D-03..D-07: стрелки как у агента, под сердечком, только при canOpenPhoto, агент без изменений)
  </read_first>
  <action>
    В ветке `role === "client"` рендерить вертикальный стек из двух угловых кнопок вместо
    одиночного сердечка. Сердечко остаётся сверху (текущая разметка строк 70–98 без
    изменений по стилям/действию `toggleFavorite`/`isFavorite`). Под ним — вторая кнопка
    со стрелками, показываемая ТОЛЬКО при `canOpenPhoto` (D-05).

    Вторую кнопку собрать копированием эталона из ветки sales (строки 45–66): тот же
    `<svg>` arrows-pointing-out (дословно path из строки 61), тот же `onClick` со
    `e.stopPropagation()` → `onPhotoOpen?.()` (строки 49–52, D-07), `aria-label="Открыть
    фото на весь экран"`, тот же тип `<button type="button">`. Действие открывает тот же
    Lightbox — новой логики просмотрщика НЕ писать (D-07).

    Раскладка стека (Claude's Discretion по точным значениям, см. CONTEXT строки 99–104):
    обернуть обе кнопки в контейнер `absolute top-1 right-1 z-10 flex flex-col gap-1`
    (вертикальный стек) и убрать `absolute top-1 right-1 z-10` с самих кнопок, оставив у
    каждой `flex ${box} items-center justify-center rounded-full bg-white/80 shadow-sm
    backdrop-blur-sm active:bg-white` и цветовые классы. Так кнопки не налезают друг на
    друга и сохраняют единый шаблон угловой кнопки. Переиспользовать существующие `box`/
    `icon` (строки 33–37) для обеих кнопок — новых размеров не вводить.

    SSR-safe: ветку `if (!ready)` (строки 39–42) НЕ ломать — пустое место того же размера
    остаётся; стек кнопок ждёт `ready` так же, как сейчас.

    Ветку `role === "sales" && canOpenPhoto` (строки 44–66) НЕ трогать вообще (D-06) —
    у агента стрелки уже есть, избранное ему не нужно.

    Решение по режиму списка (size="sm", миниатюра 14×14): дать ли клиенту стрелки там —
    на усмотрение по эргономике (Discretion, CONTEXT строки 102–104). Если две кнопки на
    мелкой миниатюре не помещаются, допустимо показывать стрелки клиента только при
    `size !== "sm"`; в сетке/презентации стрелки обязательны. Выбранное поведение
    зафиксировать комментарием на русском.

    Комментарии к новому коду — на русском (правило проекта).
  </action>
  <acceptance_criteria>
    - В `components/CardCornerButton.tsx` ветка `role === "client"` рендерит сердечко И,
      при `canOpenPhoto`, вторую кнопку со стрелками; стрелочная кнопка имеет
      `onClick` → `e.stopPropagation()` затем `onPhotoOpen?.()`.
    - SVG-иконка второй кнопки — arrows-pointing-out (тот же `d=...` path, что в ветке sales).
    - Ветка `role === "sales"` в файле текстуально не изменена (агент без изменений, D-06).
    - Ветка `if (!ready)` сохранена (SSR-safe заглушка не удалена).
    - При `canOpenPhoto === false` в ветке client стрелочная кнопка не рендерится (только сердечко).
    - Роль `sales` без фото (`canOpenPhoto === false`) по-прежнему показывает сердечко: общий fall-through НЕ удалён, а добавлена явная ветка `role === "client"` выше него (защита от регрессии — не схлопывать fall-through в client-стек).
    - Новых пропсов в `CardCornerButtonProps` не добавлено (используются productId, onPhotoOpen, canOpenPhoto, size).
    - `npm run build` завершается с кодом 0 (типы и сборка проходят).
  </acceptance_criteria>
  <verify>
    <automated>cd /c/catalog && npm run build</automated>
  </verify>
  <done>
    Клиент на карточке с фото видит сердечко и под ним стрелки; стрелки открывают Lightbox;
    без фото — только сердечко; агент не изменён; build зелёный.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    В роли «Клиент» на карточке товара с фото добавлена вторая угловая кнопка со стрелками
    (раскрытие фото на весь экран) под сердечком избранного. Кнопка открывает тот же
    Lightbox, что и у агента. На карточках без фото показывается только сердечко.
    Роль «Агент» не изменена.
  </what-built>
  <how-to-verify>
    Проверка ТОЛЬКО на проде (Vercel) после `git push` — локального dev у владельца нет.
    1. Зайти в каталог в роли «Клиент» (телефон/ПК-пресет).
    2. На карточке товара С фото в правом верхнем углу видны сердечко (сверху) и стрелки (под ним), не налезают друг на друга и на стикер слева.
    3. Нажать стрелки → открывается полноэкранный просмотрщик (Lightbox) этого товара.
    4. Нажать сердечко → товар добавляется/убирается из избранного (карточка НЕ переворачивается).
    5. Найти карточку БЕЗ фото → у неё только сердечко, стрелок нет.
    6. Переключиться в роль «Агент» → у него по-прежнему одни стрелки раскрытия (как раньше), сердечка нет.
    7. Включить режим «Список»: стрелки клиента либо аккуратно помещаются на миниатюре рядом с сердечком, либо сознательно скрыты (`size==='sm'`) — миниатюра не сломана, кнопки не налезают.
  </how-to-verify>
  <resume-signal>Напишите «принято» или опишите, что поправить (размер/отступ/режим списка)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (нет новых) | Правка чисто клиентская по фронту карточки: нет нового ввода, сети, секретов, аутентификации |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | Кнопка стрелок клиента | accept | Открывает тот же Lightbox с уже отображаемым публичным фото товара — новых данных не раскрывается |
| T-17-02 | Tampering | npm/pip/cargo installs | mitigate | Установок пакетов нет; новых зависимостей не добавляется |
</threat_model>

<verification>
- `npm run build` — код 0.
- Ветка sales в `CardCornerButton.tsx` не изменена (diff показывает правки только в ветке client / SSR-каркасе обёртки).
- Ручная приёмка на проде по чек-листу выше.
</verification>

<success_criteria>
Клиент открывает фото на весь экран отдельной кнопкой-стрелками под сердечком; кнопка есть
только при наличии фото; агент без изменений; используется существующий Lightbox; сборка зелёная.
</success_criteria>

<output>
Создать `.planning/phases/17-card-promo-photo/17-01-SUMMARY.md` по завершении.
</output>
