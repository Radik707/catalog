# Дорожная карта: Каталог «Вкусный Дом»

## Вехи (Milestones)

- ✅ **v1.0 Автоматизация и админ-управление** — Этапы 1–4 (отгружено 2026-06-07)
- ✅ **v1.1 Двухуровневая навигация** — Этапы 5–7 (отгружено 2026-06-10)
- ✅ **v1.2 Скрытие товаров и десктоп-вид** — Этапы 8–9 (отгружено 2026-06-11)
- ✅ **v1.3 Оффлайн-режим (PWA)** — Этапы 10–14 (отгружено 2026-06-13, ⏸ iPhone-приёмка офлайна отложена)
- ✅ **v1.4 Фундамент разделения аудиторий** — Этапы 15–17 (отгружено 2026-06-28)
- ✅ **v1.5 Опыт клиента (телефон)** — Этапы 18–20 (отгружено 2026-06-30)
- 🔄 **v1.6 Опыт торгового (планшет)** — Этапы 21–23 (в планировании)

## Этапы

<details>
<summary>✅ v1.0 Автоматизация и админ-управление (Этапы 1–4) — отгружено 2026-06-07</summary>

- [x] Этап 1: Боевой прогон нового формата 1С (3/3 планов)
- [x] Этап 2: Переделка страницы загрузчика (2/2 планов)
- [x] Этап 3: Память правок (2/2 планов)
- [x] Этап 4: Админ-панель владельца (4/4 планов)

</details>

<details>
<summary>✅ v1.1 Двухуровневая навигация (Этапы 5–7) — отгружено 2026-06-10</summary>

- [x] Этап 5: Структура данных двухуровневой навигации (2/2 планов)
- [x] Этап 6: Двухуровневая навигация на витрине (2/2 планов)
- [x] Этап 7: Подгруппы в админ-панели (1/1, бэкфилл)

Полные детали этапов вехи v1.1 — в `.planning/milestones/v1.1-ROADMAP.md`.

</details>

<details>
<summary>✅ v1.2 Скрытие товаров и десктоп-вид (Этапы 8–9) — отгружено 2026-06-11</summary>

- [x] **Этап 8: Скрытие товаров «глазиком»** — владелец скрывает/возвращает товары кнопкой в админ-панели ✅ 2026-06-11
- [x] **Этап 9: Десктопный вид витрины** — витрина удобна и красива на большом экране ✅ 2026-06-11

> Этап «Десктопный вид админ-панели» исключён из v1.2 (2026-06-11): панель и так выглядит и работает хорошо. Возможная доводка под мобильный/десктоп для пользователя-агента перенесена в бэклог.

</details>

<details>
<summary>✅ v1.3 Оффлайн-режим (PWA) (Этапы 10–14) — отгружено 2026-06-13 (⏸ iPhone-приёмка офлайна отложена)</summary>

- [x] **Этап 10: Фундамент SW и Manifest** — service worker подключён, каталог устанавливается как PWA-приложение ✅ 2026-06-12
- [x] **Этап 11: Слой данных (IndexedDB)** — товары и структура хранятся локально, каталог открывается без сети ✅ 2026-06-12
- [x] **Этап 12: Офлайн-UX (индикаторы и корзина)** — агент видит состояние сети и работает с корзиной офлайн ✅ 2026-06-12
- [x] **Этап 13: Синхронизация фото** — фото кэшируются умно, кнопка ↻ обновляет только новые ✅ 2026-06-13
- [x] **Этап 14: Install Prompt** — подсказка об установке (Android/iOS) + скачиваемый .apk для любого браузера ✅ 2026-06-13

> ⏸ Перенесено: ручная iPhone-приёмка офлайна (OFF-01..03, CART-01..02, IMG-01..03, PWA-02 #2/#5) — код проверен, на реальном iPhone не тестировалось. Прогнать перед опорой на офлайн в полях.
> Деплой вехи — только `git push` на Vercel; серверная часть (daniella) не затрагивалась. Полные детали — `.planning/milestones/v1.3-ROADMAP.md`.

</details>

<details>
<summary>✅ v1.4 Фундамент разделения аудиторий (Этапы 15–17) — отгружено 2026-06-28</summary>

- [x] Этап 15: Роль «Торговый / Клиент» (2/2 планов) — 2026-06-27
- [x] Этап 16: Локальная история заказов (2/2 планов) — 2026-06-27
- [x] Этап 17: Карточка витрины — стикер акции + раскрытие фото клиентом (2/2 планов) — 2026-06-28

Полные детали этапов — в архиве `milestones/v1.4-ROADMAP.md`.

</details>

<details>
<summary>✅ v1.5 Опыт клиента (телефон) (Этапы 18–20) — отгружено 2026-06-30</summary>

Телефонный опыт владельца магазина поверх фундамента v1.4 (роль `client` + локальная история заказов). Все три этапа — только для роли «Клиент»; роль «Торговый» (планшет) этой вехой не задевалась. Чистый фронт (деплой = git push → Vercel), сервер daniella не трогали.

- [x] **Этап 18: Нижняя навигация-табы для роли «Клиент»** — панель Каталог · Избранное · Корзина со счётчиками; у роли «Торговый» табов нет (TABS-01..04) ✅ 2026-06-28
- [x] **Этап 19: «Повторить заказ» + экран «Мои заказы»** — повтор прошлого заказа в один тап с актуальной ценой/наличием, честные пометки исчезнувших товаров (REORD-01..04) ✅ 2026-06-29
- [x] **Этап 20: Чистый вход клиента + история поиска** — разгрузка шапки от дублей с табами, разделы на первый план, компактный повтор последнего заказа + история запросов в поиске (HOME-01..03, SRCH-01) ✅ 2026-06-30. Пост-приёмка: 4 доработки витрины (макс. 2 перевёрнутые карточки, память места прокрутки, выезжающий поиск, три чипа-фильтра «Все · Хиты · Новинки»)

Полные детали этапов вехи v1.5 — в архиве `milestones/v1.5-ROADMAP.md`.

</details>

### v1.6 Опыт торгового (планшет) (Этапы 21–23) — в планировании

Опыт торгового представителя на планшете поверх фундамента v1.4 (роль `sales`). Все три этапа ветвятся по **роли «Торговый»** (`role==='sales'`), а не по типу устройства; клиентский опыт v1.4/v1.5 (телефон) не задеваем. Чистый фронт (деплой = git push → Vercel), сервер daniella не трогаем.

- [ ] **Этап 21: Quick-order набор + кратность упаковки** — плотные строки с прямым вводом числа и шагом кратно фасовке (блок/коробка/ящик), без флипа/галереи (QORD-01..05) — закрывает T1, T2
- [ ] **Этап 22: Наличие числом + режим «Показ клиенту»** — остаток виден числом + фильтр «в наличии»; одна кнопка → чистый показ (крупное фото, цены скрыты принудительно) (STOCK-01..02, SHOW-01..02) — закрывает T3, T5
- [ ] **Этап 23: Корзина с разбивкой по поставщику** — группировка позиций по поставщику с подытогами + комментарий к заказу; структурированный текст заказа Telegram/MAX (SUPCART-01..04) — закрывает T4

Целевая раскладка: 21 → QORD; 22 → STOCK + SHOW; 23 → SUPCART. Опциональное SUPCART-05 (минималка по поставщику) — вне MVP, добавляется только при наличии данных минималок.

## Детали этапов

### Phase 8: Скрытие товаров «глазиком»

**Цель:** Владелец одним нажатием скрывает ненужный товар с витрины (и так же возвращает) — без правки кода и таблиц; скрытие переживает перепрогон прайсов.

**Зависит от:** Этапа 7 (память правок и типы правок — уже реализованы)

**Требования:** HIDE-01, HIDE-02, HIDE-03, HIDE-04, HIDE-05

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. Владелец нажимает глазик в карточке админ-панели — товар исчезает с витрины; повторный клик возвращает товар обратно.
2. Скрытая карточка в панели визуально тускнеет, иконка меняется — сразу видно, что товар скрыт.
3. После перепрогона прайсов (`upload.py`) скрытые товары по-прежнему остаются скрытыми.
4. Запрос `/api/products` не возвращает скрытые товары — браузер клиента их не получает вообще.
5. Товар остаётся в листе «Товары» Google Sheet (не удаляется) — владелец может вернуть его в любой момент.

**Планы:** 4 plans

Plans:

- [x] 08-01-PLAN.md — Память правок + upload.py: тип `скрыт` в белых списках, колонка «Скрыт» (L), разворот в p['hidden'] (HIDE-04) ✅ 2026-06-11
- [x] 08-02-PLAN.md — Витрина: Product.hidden, диапазон A2:L, фильтр скрытых на сервере (HIDE-05) ✅ 2026-06-11
- [x] 08-03-PLAN.md — Админ-панель: /save type=`скрыт`, кнопка-глазик, optimistic toggle, тусклая карточка + чип, фильтр видимости (HIDE-01..03) ✅ 2026-06-11
- [x] 08-04-PLAN.md — Раздельный деплой (Vercel + сисадмин) + сквозная проверка цикла скрыть/вернуть (HIDE-01..05) ✅ 2026-06-11

**Заметки по реализации** (из бэклога, проработан 2026-06-10):

- Новый тип правки `скрыт` добавить в ТРИ белых списка: `sheet_helper.py`, `upload.py`, `admin.py`
- Колонка «Скрыт» (L) добавляется В КОНЕЦ строки Sheet — паттерн expand-contract, не сдвигает существующие колонки
- `lib/sheets.ts`: расширить диапазон до `A2:L`, фильтровать скрытые на сервере
- `admin.py`: кнопка-глазик, оптимистичный UI, POST `/save` type=`скрыт` value=`1`/``

### Phase 9: Десктопный вид витрины

**Цель:** Витрина каталога удобна и читаема на большом экране — карточки, сетка и навигация используют доступное пространство, не выглядят «растянутыми».

**Зависит от:** Этапа 6 (двухуровневая навигация витрины — уже реализована)

**Требования:** DESK-01, DESK-02, DESK-03, DESK-04

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. На широком экране (≥1280px) витрина показывает больше колонок или просторнее расставляет карточки — пространство не пустует.
2. Карточка товара на десктопе выглядит пропорционально: фото не слишком маленькое, название читается без обрезания, цена и кнопка корзины не «разъезжаются».
3. Двухуровневая навигация (разделы → подгруппы) на десктопе удобна для клика мышью — зоны нажатия достаточные, нет мелких нечитаемых элементов.
4. На мобильном экране (≤768px) вид не ухудшается по сравнению с текущим состоянием.

**Планы:** 1 план

Plans:

- [x] 09-01-PLAN.md — Десктоп-адаптация витрины: брейкпоинты сетки xl/2xl, контейнер max-w-screen-2xl, hover-состояния карточек и навигации (DESK-01..04)

**UI hint:** yes

---

### Phase 10: Фундамент SW и Manifest

**Цель:** Service worker подключён и работает корректно; каталог отвечает критериям PWA — устанавливается как приложение с иконкой и запускается без адресной строки браузера.

**Зависит от:** Ничего (первый этап вехи v1.3)

**Требования:** PWA-01

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. В DevTools → Application → Service Workers статус SW — `activated and running`; при перезагрузке страницы SW не падает с ошибкой.
2. Заголовок ответа для `/sw.js` содержит `Cache-Control: max-age=0, must-revalidate` — Vercel не кэширует SW с `immutable`.
3. Каталог открывается в standalone-режиме (без адресной строки браузера) после установки через браузерное меню.
4. В dev-режиме (`npm run dev`) SW отключён — разработка не ломается из-за кэша; PWA-поведение проверяется только через `npm run build && npm start`.
5. `public/sw.js` и `public/swe-worker*.js` добавлены в `.gitignore` — сгенерированные файлы не попадают в репозиторий.

**Планы:** 2 plans

Plans:
**Wave 1**

- [x] 10-01-PLAN.md — Инфраструктура SW: @serwist/next + serwist + idb, app/sw.ts (precache + каркас стратегий), withSerwist + заголовки Vercel в next.config.mjs, .gitignore (PWA-01)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 10-02-PLAN.md — Идентичность PWA: иконки ВД, app/manifest.ts (standalone, start_url из CATALOG_SECRET), PWA/iOS-метаданные в layout.tsx (PWA-01)

**UI hint:** yes

---

### Phase 11: Слой данных (IndexedDB + useCatalogSync)

**Цель:** Товары, цены, остатки и структура навигации хранятся в IndexedDB на устройстве агента и переживают закрытие вкладки; при офлайн-запуске каталог показывает данные из локального хранилища.

**Зависит от:** Этапа 10 (SW активен — без него офлайн-стратегия не работает)

**Требования:** OFF-01, OFF-02, OFF-03

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. После однократной загрузки каталога агент переводит телефон в авиарежим — каталог открывается и показывает весь список товаров с ценами, остатками и бейджами.
2. В авиарежиме работает навигация по разделам и подгруппам, поиск и фильтры — по данным, загруженным ранее.
3. В DevTools → Application → IndexedDB видна база `catalog-db` со stores `products` и `meta`; данные присутствуют после закрытия и повторного открытия вкладки.
4. После первой синхронизации `navigator.storage.persist()` вызван — браузер уведомлён, что данные не должны вытесняться (особенно важно на iOS с 7-дневным eviction).
5. Проверка на реальном iPhone: авиарежим → открыть каталог → товары видны (тест обязателен, эмулятор недостаточен).

**Планы:** 3 plans

Plans:
**Wave 1**

- [x] 11-01-PLAN.md — lib/catalogDb.ts: обёртка IndexedDB на idb (база catalog-db, stores products/meta, upgrade-колбэк) (OFF-03)

**Wave 2** *(blocked on Wave 1)*

- [x] 11-02-PLAN.md — lib/useCatalogSync.ts: хук stale-while-revalidate (IDB → fetch → подмена), persist(), статусы loading/ready/empty-offline (OFF-01, OFF-03) ✅ 2026-06-12

**Wave 3** *(blocked on Wave 2)*

- [x] 11-03-PLAN.md — Витрина на офлайн-источник: CatalogView из useCatalogSync + скелетон/заглушка, page.tsx без SSR-данных (OFF-01, OFF-02)

---

### Phase 12: Офлайн-UX (индикаторы и корзина)

**Цель:** Агент всегда понимает, онлайн он или офлайн и насколько свежи данные; корзина работает без сети, но попытка отправить заказ офлайн не теряет данные молча.

**Зависит от:** Этапа 11 (данные в IndexedDB — есть что показывать и с чем работать)

**Требования:** SYNC-03, CART-01, CART-02

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. В шапке каталога отображается полоска «Офлайн • Данные за 08:30» — агент видит режим и время последней синхронизации; при подключении к сети полоска исчезает или меняет цвет.
2. Если данные старше 24 часов, индикатор окрашивается в жёлтый — агент понимает, что каталог может быть устаревшим.
3. В авиарежиме товары добавляются в корзину и удаляются из неё — корзина работает полностью офлайн.
4. Кнопка «Отправить заказ» в авиарежиме заблокирована и содержит понятное пояснение («Нужен интернет для отправки»); заказ в корзине при этом сохраняется и не теряется.
5. Проверка на реальном iPhone: авиарежим → добавить товар в корзину → убедиться, что корзина сохранилась и кнопка отправки заблокирована.

**Планы:** 2 plans

Plans:
**Wave 1**

- [x] 12-01-PLAN.md — Индикатор сети и свежести: хук useOnlineStatus, утилита formatSyncTime, компонент OfflineBar + вставка в layout (SYNC-03)

**Wave 2** *(blocked on Wave 1)*

- [x] 12-02-PLAN.md — Офлайн-блокировка обеих кнопок отправки заказа + проверка офлайн-корзины + iPhone-приёмка (CART-01, CART-02)

**UI hint:** yes

---

### Phase 13: Синхронизация фото

**Цель:** Фото просмотренных товаров доступны офлайн; ежедневное обновление докачивает только новые фото, не перекачивая весь набор (~900 штук); кэш фото не переполняет хранилище телефона.

**Зависит от:** Этапа 11 (IndexedDB с `meta.prevImageUrls` для diff; данные каталога — источник URL фото для сравнения)

**Требования:** IMG-01, IMG-02, IMG-03, SYNC-01, SYNC-02

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. Фото товара, открытого ранее при наличии сети, отображается в авиарежиме — в том числе в полноэкранном просмотрщике с зумом.
2. Нажатие кнопки «Обновить» при наличии сети подтягивает свежий каталог; в Cache Storage появляются только URL новых фото — старые не перекачиваются заново.
3. В DevTools → Application → Cache Storage → Cloudinary-кэш не превышает `maxEntries: 450` записей — лимит соблюдается автоматически (CacheFirst + ExpirationPlugin).
4. На iPhone кэш фото не вызывает ошибку `QuotaExceededError` и не блокирует загрузку каталога; суммарный объём кэша остаётся в пределах ~50 МБ.
5. Проверка на реальном iPhone: просмотреть 5–10 товаров → авиарежим → открыть те же карточки → фото видны без сети.

**Планы:** 2 plans

Plans:
**Wave 1**

- [x] 13-01-PLAN.md — Кэш-слой фото: raw Cloudinary URL (unoptimized) + onError-заглушка в ProductCard, CacheableResponsePlugin(200) в sw.ts, lib/syncPhotos.ts (diff+prefetch новых), syncPhotos+refetch в useCatalogSync (IMG-01..03, SYNC-02)

**Wave 2** *(blocked on Wave 1)*

- [x] 13-02-PLAN.md — Кнопка ↻ в шапке: CatalogSyncProvider (единый sync), SyncButton (spin→галочка, disabled офлайн), встройка в layout + перевод CatalogView на контекст; iPhone-приёмка IMG-01..03 отложена на конец вехи v1.3 (SYNC-01, IMG-01..03) ✅ 2026-06-13

---

### Phase 14: Install Prompt (Android + iOS)

**Цель:** Агент узнаёт о возможности установить каталог как приложение и умеет это сделать на своём телефоне — на Android получает баннер, на iPhone — понятную пошаговую инструкцию.

**Зависит от:** Этапа 10 (manifest + SW активны — без них промпт не появится), Этапа 11 (офлайн-каталог работает — логично предлагать установку когда продукт уже полезен)

**Требования:** PWA-02

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. На Android-устройстве после просмотра каталога появляется баннер «Установить приложение»; после установки каталог открывается с домашнего экрана без браузерной строки.
2. На iPhone в Safari появляется bottom-sheet с пошаговой инструкцией «Нажмите "Поделиться" → "На экран Домой"»; инструкция понятна без технических знаний.
3. Промпт показывается однократно — повторные визиты не надоедают агенту баннером при каждом открытии.
4. В standalone-режиме (после установки) каталог отображается корректно: нет лишних отступов, нет адресной строки, интерфейс занимает весь экран.
5. Проверка на реальном iPhone: Safari → открыть каталог → увидеть bottom-sheet → следовать инструкции → каталог установлен на домашний экран → открывается без браузера.

**Планы:** 3 plans

Plans:
**Wave 1**

- [x] 14-01-PLAN.md — Хук-фундамент: lib/useInstallPrompt.ts (детект android/ios/standalone, beforeinstallprompt-перехват, localStorage-флаг, сигнал вовлечённости) + InstallPromptProvider (единый экземпляр через контекст) (PWA-02)

**Wave 2** *(blocked on Wave 1)*

- [x] 14-02-PLAN.md — UI установки: InstallPrompt (Android-баннер + iOS bottom-sheet), пункт «Установить приложение» в SettingsPanel, монтирование в layout под провайдером (PWA-02)

**Wave 3** *(blocked on Wave 2)*

- [x] 14-03-PLAN.md — Standalone-вид: viewport-fit=cover в layout (safe-area) + ручная iPhone-приёмка отложена на конец вехи v1.3 (PWA-02) ✅ 2026-06-13

**UI hint:** yes

---

### Phase 15: Роль «Торговый / Клиент»

**Цель:** В каталоге появляется явная роль (`sales` | `client`), которая по умолчанию — «Клиент» (на всех устройствах), запоминается на устройстве и переключается вручную в настройках; все текущие ветвления по устройству переезжают на роль.

**Зависит от:** Ничего (первый этап вехи v1.4)

**Требования:** ROLE-01, ROLE-02, ROLE-03, ROLE-04

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. При первом заходе на любом устройстве роль по умолчанию — «Клиент»; «Агент» выбирается вручную в настройках (осознанный выбор, без автоопределения по устройству).
2. Пользователь может сменить роль в настройках; выбор сохраняется в localStorage и переживает перезагрузку страницы и работу в офлайн-режиме.
3. Поведение интерфейса — угловое действие карточки (стрелки vs ♥), главное действие в шапке (↻ Обновить vs ♥ Избранное), дефолтная плотность сетки — следует роли, а не типу устройства; кейс «торговый с телефона» / «клиент с планшета» решается сменой роли.
4. До монтирования компонента (SSR) роль нейтральна — нет скачка вёрстки и ошибки гидратации (тот же паттерн, что в текущем `useDeviceClass`).

**Планы:** 2 plans

Plans:
**Wave 1**

- [x] 15-01-PLAN.md — Ядро роли: lib/useRole.ts (контекст+persist+SSR-safe ready, дефолт client), монтаж RoleProvider в layout, переключатель «Клиент | Агент» в настройках (ROLE-01, ROLE-02, ROLE-04)

**Wave 2** *(blocked on Wave 1)*

- [x] 15-02-PLAN.md — Перенос ветвлений device→role: HeaderPrimaryAction, CardCornerButton, дефолт gridPreset по роли в CatalogSettings; решение по useDeviceClass (ROLE-03)

**UI hint:** yes

---

### Phase 16: Локальная история заказов

**Цель:** При отправке заказа (Telegram или MAX) каталог записывает его в локальную историю на устройстве — что, сколько, на какую сумму, когда, каким каналом; фундамент под «Повторить заказ» и «Мои заказы» в веху v1.5.

**Зависит от:** Ничего (можно выполнять параллельно с этапом 15)

**Требования:** HIST-01, HIST-02, HIST-03, HIST-04

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. После успешной отправки заказа через Telegram или MAX в локальной истории появляется запись: позиции (название, количество, цена на момент заказа), итоговая сумма, дата-время, канал отправки.
2. История переживает закрытие вкладки и браузера и доступна офлайн — данные в localStorage согласованы с офлайн-моделью v1.3.
3. История честно подписана «Отправленные заказы» — нигде не написано «статус», «принято», «в доставке»; пользователь понимает, что это «что я отправил», а не подтверждение от поставщика.
4. Пользователь может очистить историю одним действием; битая или неполная запись не ломает отображение всего списка — деградация мягкая.

**Планы:** 2 плана

Plans:
**Wave 1**

- [x] 16-01-PLAN.md — Тип OrderHistoryEntry + хук useOrderHistory (localStorage, FIFO 20, мягкая деградация) (HIST-01, HIST-02, HIST-04)

**Wave 2**

- [x] 16-02-PLAN.md — Страница /orders «Отправленные заказы» + запись в обе кнопки корзины + ссылки (HIST-01, HIST-03, HIST-04) ✅ 2026-06-27

---

### Phase 17: Карточка витрины — стикер акции + раскрытие фото клиентом

> ⚠️ **Этап пересмотрен при обсуждении 2026-06-28.** Исходно — «Сортировка витрины +
> стикеры акций». **Сортировка (SORT-01..03) снята** по решению владельца (разноплановый
> каталог, несопоставимые единицы фасовки, «по наличию» бессмысленна — детали в
> `phases/17-card-promo-photo/17-CONTEXT.md` D-01). Вместо неё в этап добавлено
> **раскрытие фото клиентом** (CARD-01). Текущая группировка по разделам остаётся
> единственным порядком витрины.

**Цель:** Две доработки карточки витрины, полезные обеим ролям: (1) заметный стикер
акции поверх фото и (2) кнопка раскрытия фото на весь экран для роли «Клиент».

**Зависит от:** Этапа 15 (роль `client`/`sales` — кнопка раскрытия фото у клиента; можно
было выполнять параллельно с 16)

**Требования:** MERCH-01, CARD-01 *(SORT-01..03 — отклонены)*

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. Товар с меткой «акция» показывает **заметный стикер** (яркая пилюля с иконкой, напр.
   «🔥 Акция») в углу фото, не перекрывая название и цену; заметно крупнее/ярче нынешнего
   маленького бейджа. Согласован с приоритетом бейджей (у товара одна метка — конфликта нет).

2. **Клиент** может открыть фото товара на весь экран прямо с карточки — отдельной угловой
   кнопкой со **стрелками** (как у агента), расположенной **под сердечком** избранного;
   кнопка показывается только при наличии фото.

3. **Агент** без изменений — у него стрелки раскрытия фото уже есть; избранное ему не нужно.
4. Стикер и стрелки работают во всех режимах сетки (сетка/презентация); раскрытие открывает
   тот же существующий Lightbox; офлайн-модель v1.3 и `force-dynamic` витрины не затронуты.

**Планы:** 2 плана

Планы:

- [x] 17-01-PLAN.md — раскрытие фото клиентом: вторая кнопка-стрелки под сердечком (CARD-01)
- [x] 17-02-PLAN.md — заметный стикер акции во всех режимах карточки (MERCH-01)

**UI hint:** yes

### Phase 21: Quick-order набор + кратность упаковки

**Цель:** В роли «Торговый» появляется плотный режим «Быстрый набор» — строка товара с инлайн-полем количества (прямой ввод числа с клавиатуры и кнопки шага «+/−»), где шаг кратен фасовке товара (за шт / блок / коробку / ящик). Крупный заказ набирается за минимум тапов, без флипа карточки и галереи.

**Зависит от:** Этапа 15 (роль `sales` — фундамент вехи v1.4, уже в проде). Использует существующий `QuantityInput`, режим вывода `list` и ~40 правил фасовки.

**Требования:** QORD-01, QORD-02, QORD-03, QORD-04, QORD-05 *(закрывает T1, T2)*

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. В роли «Торговый» доступен режим «Быстрый набор»: товары идут плотными строками, в каждой — поле количества с прямым вводом числа с клавиатуры и кнопками шага «+/−»; товар добавляется в корзину прямо из строки, без перехода, флипа карточки или открытия галереи.
2. Кнопки шага и быстрого добавления учитывают фасовку товара: для штучного товара шаг 1, для блочного/коробочного/ящичного — кратно упаковке (например «+1 блок»); единица фасовки подписана у поля.
3. Введённое в строке количество мгновенно отражается в счётчике корзины и в итоговой сумме; количество нельзя задрать выше остатка — ввод капится по `stock`.
4. В роли «Клиент» режим «Быстрый набор» скрыт и не является дефолтом — телефонный опыт v1.4/v1.5 (нижние табы, «Повторить заказ») не меняется и визуально не затрагивается.
5. На полном каталоге (~800 видимых строк) прямой ввод и пересчёт остаются отзывчивыми — без заметных подвисаний при наборе (виртуализацию списка вводим только если на практике появятся тормоза).

**Планы:** 2 plans

Plans:
**Wave 1**

- [x] 21-01-PLAN.md — Хелпер getUnit (шов под 21b) + компонент QuickOrderRow: плотная строка с мини-фото, ценой+единицей, инлайн-количеством и капом по stock (QORD-01..03)

**Wave 2** *(blocked on Wave 1)*

- [ ] 21-02-PLAN.md — Подключение 4-го режима «quick»: ViewMode+localStorage, пункт меню под гейтом роли sales, ветка рендера в CatalogView с SSR-safe откатом + опц. единица в снимке заказа (QORD-01, QORD-04, QORD-05)

**UI hint:** yes

---

### Phase 22: Наличие числом + режим «Показ клиенту»

**Цель:** Две связанные доработки опыта торгового. (1) Остаток показан числом (а не только «есть/нет»), с фильтром «только в наличии» поверх разделов и поиска — торговый сразу видит, что и сколько можно отгрузить. (2) Одной кнопкой включается чистый режим показа товара клиенту: крупное фото на весь экран без цен и служебного UI — чтобы случайно не показать закупочную цену.

**Зависит от:** Этапа 15 (роль `sales`). Использует существующий полноэкранный просмотрщик (Lightbox) и режим вывода «без цен».

**Требования:** STOCK-01, STOCK-02, SHOW-01, SHOW-02 *(закрывает T3, T5)*

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. В роли «Торговый» у каждого товара виден остаток числом (например «остаток: 24»), а не только признак наличия; в роли «Клиент» отображение остатка не навязывается.
2. Фильтр «только в наличии» работает поверх текущей структуры: он сужает и список внутри разделов/подгрупп, и результаты поиска — товары не в наличии скрываются.
3. Отображение остатка числом согласовано с текущим правилом скрытия товара с остатком ≤1: нет двусмысленности (товары, скрытые по этому правилу, не «всплывают» с нулём, и наоборот) — поведение однозначно и описано.
4. Кнопка «Показ клиенту» заметным действием (в роли «Торговый») переводит в чистый режим показа: крупное фото в Lightbox, без цен и без служебных кнопок; выход из режима — одним жестом/тапом. Режим доступен и в роли «Клиент», но не выделен.
5. В режиме показа цены скрыты принудительно — независимо от настройки «С ценами»: торговый не может случайно показать клиенту закупочную цену.

**Планы:** TBD

**UI hint:** yes

---

### Phase 23: Корзина с разбивкой по поставщику

**Цель:** В роли «Торговый» корзина группирует позиции по поставщику с подытогом по каждому — заказ удобно комплектовать и сверять по поставщикам. К заказу можно добавить комментарий. Текст заказа в Telegram/MAX для торгового структурирован по поставщику; для клиента корзина и формат заказа остаются простыми, как сейчас.

**Зависит от:** Этапа 15 (роль `sales`). Использует существующую корзину (`cart/page.tsx`) и формирование текста заказа на фронте; серверная часть (max_bot.py/Telegram) не трогается.

**Требования:** SUPCART-01, SUPCART-02, SUPCART-03, SUPCART-04 *(закрывает T4; SUPCART-05 — минималка по поставщику — вне MVP/опционально)*

**Критерии успеха** (что должно быть ПРАВДОЙ по завершении):

1. В роли «Торговый» корзина показывает позиции, сгруппированные по поставщику, с подытогом суммы по каждому поставщику и общим итогом.
2. В роли «Клиент» корзина остаётся простой: плоский список позиций + один общий итог, без разбивки по поставщику — телефонный опыт v1.4/v1.5 не меняется.
3. К заказу можно добавить текстовый комментарий перед отправкой; комментарий попадает в текст заказа, отправляемый в Telegram и MAX.
4. Текст заказа для роли «Торговый» структурирован по поставщику (позиции под заголовком поставщика, подытоги + общий итог + комментарий); для роли «Клиент» формат текста заказа остаётся прежним.

**Планы:** TBD

**UI hint:** yes

---

## Прогресс

| Этап | Веха | Планы | Статус | Завершён |
|------|------|-------|--------|----------|
| 1. Боевой прогон нового формата 1С | v1.0 | 3/3 | Complete | 2026-06-07 |
| 2. Переделка страницы загрузчика | v1.0 | 2/2 | Complete | 2026-06-07 |
| 3. Память правок | v1.0 | 2/2 | Complete | 2026-06-07 |
| 4. Админ-панель владельца | v1.0 | 4/4 | Complete | 2026-06-07 |
| 5. Структура данных | v1.1 | 2/2 | Complete | 2026-06-09 |
| 6. Навигация витрины | v1.1 | 2/2 | Complete | 2026-06-09 |
| 7. Подгруппы в админ-панели | v1.1 | 1/1 | Complete (бэкфилл) | 2026-06-10 |
| 8. Скрытие товаров «глазиком» | v1.2 | 4/4 | Complete | 2026-06-11 |
| 9. Десктопный вид витрины | v1.2 | 1/1 | Complete | 2026-06-11 |
| 10. Фундамент SW и Manifest | v1.3 | 2/2 | Complete    | 2026-06-12 |
| 11. Слой данных (IndexedDB) | v1.3 | 3/3 | Complete    | 2026-06-12 |
| 12. Офлайн-UX (индикаторы и корзина) | v1.3 | 2/2 | Complete    | 2026-06-12 |
| 13. Синхронизация фото | v1.3 | 2/2 | Complete (⏸ iPhone-UAT отложена) | 2026-06-13 |
| 14. Install Prompt | v1.3 | 3/3 | Complete    | 2026-06-13 |
| 15. Роль «Торговый / Клиент» | v1.4 | 2/2 | Complete    | 2026-06-27 |
| 16. Локальная история заказов | v1.4 | 2/2 | Complete    | 2026-06-27 |
| 17. Стикер акции + раскрытие фото | v1.4 | 2/2 | Complete    | 2026-06-28 |
| 18. Нижние табы (роль Клиент) | v1.5 | 2/2 | Complete | 2026-06-28 |
| 19. Повторить заказ + Мои заказы | v1.5 | 2/2 | Complete    | 2026-06-29 |
| 20. Чистый вход клиента + история поиска | v1.5 | 3/3 | Complete   | 2026-06-30 |
| 21. Quick-order набор + кратность упаковки | v1.6 | 1/2 | In Progress|  |
| 22. Наличие числом + «Показ клиенту» | v1.6 | 0/? | Not started | - |
| 23. Корзина по поставщику | v1.6 | 0/? | Not started | - |

---

## Инварианты вехи v1.6 (нельзя сломать)

> Памятка для планировщиков этапов 21–23 — учитывать при написании PLAN.md (источник — мастер-план §9).

- **Ветвление по РОЛИ, не по устройству:** весь новый UI вехи v1.6 включается по роли «Торговый» (`role==='sales'`), а не по типу экрана. До монтирования (SSR) — нейтральный дефолт, иначе ошибка гидратации (паттерн `useRole`/`useDeviceClass`); новые блоки рендерятся только после `ready`.
- **Клиентский опыт v1.4/v1.5 не задеваем:** нижние табы, «Повторить заказ», «Мои заказы», история поиска роли «Клиент» (телефон) остаются как есть — quick-order, наличие числом, режим показа и разбивка корзины видны только торговому.
- **`force-dynamic` + `cache: "no-store"`** на витрине и `/api/products` не трогать — любая ISR/кэш-обёртка снова сломает «живое» управление из админ-панели (грабля этапа 8).
- **Офлайн-модель v1.3:** новые экраны (быстрый набор, наличие, корзина по поставщику) читать офлайн-безопасно (IndexedDB/localStorage), без сетевых зависимостей на старте; standalone-PWA + safe-area не ломать (как в этапе 14).
- **Чистый фронт:** вся веха v1.6 — git push → Vercel; серверную часть daniella (upload.py/admin.py/max_bot.py) не трогаем. Структурированный текст заказа по поставщику формируется на фронте перед открытием Telegram/MAX deep-link.
- **Любой индикатор «времени/свежести» — со своим тиком** (грабля этапа 12 — `OfflineBar`); актуально, если этап 22 покажет свежесть остатков.
- **Кап по остатку:** прямой ввод количества (этап 21) не должен позволять заказать больше `stock`; согласовать с правилом скрытия остатка ≤1 (этап 22).

---

*Веха v1.6 «Опыт торгового (планшет)» — в планировании (этапы 21–23): quick-order набор + кратность упаковки (21), наличие числом + режим «Показ клиенту» (22), корзина с разбивкой по поставщику (23). Финальная веха мастер-плана разделения аудиторий (v1.4→v1.5→v1.6) — `.planning/audience-split-roadmap.md`, §6. Все этапы ветвятся по роли «Торговый»; клиентский опыт v1.4/v1.5 не задеваем. Деплой — git push → Vercel (серверная часть daniella не затрагивается). Покрытие требований: 13/13 MVP-требований привязаны к этапам (см. REQUIREMENTS.md → Прослеживаемость). Следующий шаг — `/gsd-plan-phase 21`.*
