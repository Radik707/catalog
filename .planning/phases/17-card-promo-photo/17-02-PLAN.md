---
phase: 17-card-promo-photo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ProductCard.tsx
autonomous: false
requirements: [MERCH-01]
must_haves:
  truths:
    - "Товар с меткой акция показывает заметный стикер (яркая пилюля с иконкой) в углу фото"
    - "Стикер акции заметно крупнее/ярче нынешнего маленького оранжевого бейджа"
    - "Стикер не перекрывает название и цену — живёт в углу фото (top-left)"
    - "Метки хит и новинка (NEW) выводятся как раньше — без изменений"
    - "Стикер акции работает во всех режимах вывода бейджа: сетка, презентация, без фото, список"
  artifacts:
    - path: "components/ProductCard.tsx"
      provides: "Заметный стикер для product.badge === 'акция' во всех режимах"
      contains: "акция"
  key_links:
    - from: "components/ProductCard.tsx"
      to: "product.badge === 'акция'"
      via: "условный вывод усиленного стикера (источник прежний — badges.json)"
      pattern: "акция"
---

<objective>
Прокачать вывод метки «акция» в `components/ProductCard.tsx`: вместо нынешнего маленького
оранжевого бейджа — заметная яркая «пилюля» с иконкой (например красная «🔥 Акция»),
крупнее и ярче, в углу фото, не перекрывая название и цену (D-08, D-09).

Назначение: акция должна реально бросаться в глаза (MERCH-01). Это усиление существующего
вывода `BADGE_STYLES.акция` / `badgeLabel()`, НЕ новый канал данных — источник прежний:
`product.badge === "акция"` из `scripts/badges.json`.

Выход: правка `components/ProductCard.tsx` — усиленный стикер акции во всех трёх режимах
вывода бейджа (сетка/презентация, режим «без фото», список).

Forward-looking (D-11): ключ `"акция"` в `badges.json` сейчас пуст — визуально стикер
появится, когда владелец отметит товар; логику можно проверить временной меткой.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-card-promo-photo/17-CONTEXT.md
@.planning/phases/17-card-promo-photo/17-PATTERNS.md

<interfaces>
<!-- Текущие точки вывода бейджа — извлечены из кода, исследовать дополнительно не нужно. -->

components/ProductCard.tsx:
```typescript
const BADGE_STYLES: Record<string, string> = {
  хит: "bg-red-500 text-white",
  новинка: "bg-green-500 text-white",
  акция: "bg-orange-500 text-white",        // ← усилить именно акцию
};
function badgeLabel(badge?: string): string | undefined {
  // "новинка" → "NEW"; остальное как есть
}
const badgeStyle = product.badge ? BADGE_STYLES[product.badge] : null;
```

Три точки вывода бейджа (все три обновить для акции):
- grid/presentation поверх фото — строки 181–187 (позиция `badgePos = top-left`, размер `badgeCls`)
- режим «без фото» — строки 199–203 (`self-start`, без угловой привязки)
- режим списка — строки 311–315 (`absolute top-1.5 right-1.5`)

Угловое размещение: бейджи — `top-left`, угловые кнопки (сердечко/стрелки) — `top-right`,
конфликта по углам нет (D-09, CONTEXT строки 99–101).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Усилить стикер акции во всех режимах вывода бейджа</name>
  <files>components/ProductCard.tsx</files>
  <read_first>
    - components/ProductCard.tsx (изменяемый файл; BADGE_STYLES — строки 53–57, badgeLabel — строки 61–64, badgeStyle — строка 106, вывод бейджа: grid/presentation 181–187, без фото 199–203, список 311–315; позиции badgePos/badgeCls 130–131)
    - .planning/phases/17-card-promo-photo/17-PATTERNS.md (точные строки-эталоны вывода бейджа и решения D-08..D-11)
    - .planning/phases/17-card-promo-photo/17-CONTEXT.md (решения D-08..D-11 и Claude's Discretion по тексту/цвету/эмодзи/размеру)
  </read_first>
  <action>
    Сделать вывод метки `акция` заметно ярче/крупнее остальных бейджей, оставив хит/новинку
    без изменений. Реализовать как частный случай вывода бейджа: когда
    `product.badge === "акция"`, рисовать усиленный стикер-вариант (краснее/ярче фон,
    иконка-эмодзи 🔥, текст «Акция», крупнее текущего `text-[9px]`/`text-xs`); иначе —
    прежний `<span>` бейджа как сейчас.

    Конкретный текст («Акция» / «🔥 Акция»), цвет (красный/оранжевый), размер и анимация —
    Claude's Discretion (CONTEXT строки 95–96, D-08); подобрать в духе каталога,
    финально согласуется визуально на приёмке — НЕ блокировать на точных значениях.
    Рекомендация по умолчанию: яркий красный фон (например `bg-red-600 text-white`),
    эмодзи 🔥 + «Акция», скругление `rounded-full`, чуть крупнее обычного бейджа
    (например `text-[11px] px-2 py-0.5` в сетке, `text-xs` в презентации), лёгкая тень
    `shadow`. Допустимо ввести отдельную константу стиля стикера акции рядом с
    `BADGE_STYLES` и/или отдельный фрагмент с иконкой — не плодить дублирование, вынести
    общий рендер метки в локальную функцию-хелпер, если так чище.

    Применить усиленный стикер во ВСЕХ трёх точках вывода (D-08, критерий 4):
    1. grid/presentation поверх фото (строки 181–187): стикер живёт в углу `badgePos`
       (`top-left`, D-09) — НЕ перекрывает название/цену и не конфликтует с угловыми
       кнопками справа.
    2. режим «без фото» (строки 199–203): стикер уместен, но без угловой привязки
       (`self-start`), как сейчас.
    3. режим списка (строки 311–315): усиленный вариант на месте текущего `absolute
       top-1.5 right-1.5`; следить, чтобы не перекрыл миниатюру/кнопки списка.

    У товара ровно одна метка `badge` (D-10) — конфликта «акция + хит» на одной карточке
    нет, приоритет реализовывать не нужно. Источник данных НЕ менять: по-прежнему
    `product.badge` из `badges.json`; ключ `"акция"` сейчас пуст — это нормально (D-11).

    Инварианты НЕ трогать: `force-dynamic`/`cache: "no-store"` витрины и `/api/products`,
    офлайн-модель v1.3 — правка чисто клиентская по разметке карточки.

    Комментарии к новому коду — на русском (правило проекта).
  </action>
  <acceptance_criteria>
    - В `components/ProductCard.tsx` при `product.badge === "акция"` рендерится усиленный
      стикер (иконка/эмодзи + текст), визуально крупнее/ярче прежнего `bg-orange-500` бейджа.
    - Усиленный стикер применён во всех трёх точках вывода: grid/presentation, «без фото», список.
    - Метки `хит` и `новинка` (вывод «NEW») остаются прежними по стилю и размеру.
    - Стикер в режимах с фото размещён в углу `top-left` (badgePos), не перекрывает название/цену.
    - Источник данных не изменён: используется `product.badge`; правок в lib/sheets / API / badges.json нет.
    - `npm run build` завершается с кодом 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/catalog && npm run build</automated>
  </verify>
  <done>
    Товар с меткой «акция» показывает яркий заметный стикер с иконкой во всех режимах, не
    перекрывая название/цену; хит и новинка без изменений; build зелёный.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Вывод метки «акция» на карточке витрины усилен: вместо маленького оранжевого бейджа —
    заметная яркая пилюля с иконкой (по умолчанию красная «🔥 Акция») в углу фото,
    во всех режимах сетки, не перекрывая название и цену. Хит и NEW не изменены.
  </what-built>
  <how-to-verify>
    Проверка ТОЛЬКО на проде (Vercel) после `git push` — локального dev нет.
    Поскольку `badges.json` ключ «акция» сейчас пуст, для приёмки временно отметьте
    один-два товара акцией (или попросите ассистента подставить временную метку), затем:
    1. Открыть каталог, найти товар с меткой «акция».
    2. В сетке и презентации стикер «акция» заметно крупнее/ярче прежнего, виден в углу фото слева.
    3. Стикер НЕ перекрывает название и цену; не налезает на угловые кнопки справа.
    4. Проверить режим «без фото» — стикер тоже виден, размещён над названием.
    5. Проверить режим списка — стикер виден, не ломает строку.
    6. Товары с «хит» и «новинка» (NEW) выглядят как раньше.
    Согласовать визуально текст/цвет/эмодзи/размер; при необходимости — подправить на месте.
  </how-to-verify>
  <resume-signal>Напишите «принято» или укажите желаемый текст/цвет/размер стикера</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (нет новых) | Правка чисто клиентская по разметке карточки: нет нового ввода, сети, секретов, аутентификации |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | Источник метки `product.badge` | accept | Источник прежний (badges.json через API), новый канал данных не вводится; правка только визуальная |
| T-17-04 | Tampering | npm/pip/cargo installs | mitigate | Установок пакетов нет; новых зависимостей не добавляется |
</threat_model>

<verification>
- `npm run build` — код 0.
- Метки хит/новинка в `ProductCard.tsx` не изменены (diff затрагивает только ветку акции и общий рендер метки).
- Ручная приёмка на проде по чек-листу выше (с временной меткой акции).
</verification>

<success_criteria>
Метка «акция» показывается заметным ярким стикером с иконкой во всех режимах сетки, не
перекрывая название/цену; хит и новинка не изменены; источник данных прежний; сборка зелёная.
</success_criteria>

<output>
Создать `.planning/phases/17-card-promo-photo/17-02-SUMMARY.md` по завершении.
</output>
