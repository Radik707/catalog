---
phase: 09-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/catalog/[secret]/layout.tsx
  - components/CatalogView.tsx
  - components/CatalogSettings.tsx
  - components/ProductCard.tsx
  - components/CatalogNav.tsx
  - components/SubgroupFlyout.tsx
autonomous: false
requirements: [DESK-01, DESK-02, DESK-03, DESK-04]

must_haves:
  truths:
    - "На экране ≥1280px (xl) витрина в режиме grid показывает 5 колонок"
    - "На экране ≥1536px (2xl) витрина в режиме grid показывает 6 колонок"
    - "На экране ≤768px витрина по-прежнему показывает 2 колонки (мобильный вид не изменился)"
    - "Контент витрины и шапки центрирован и ограничен по ширине (max-w-screen-2xl) — не растягивается на весь ультраширокий монитор"
    - "При наведении мыши карточка товара (grid/presentation) подсвечивается тенью, строка списка — фоном"
    - "При наведении мыши кнопки навигации (разделы, режимы) и чипы подгрупп подсвечиваются"
    - "npm run build завершается успешно (код 0)"
  artifacts:
    - path: "components/CatalogView.tsx"
      provides: "grid-режим с десктопными брейкпоинтами xl/2xl"
      contains: "xl:grid-cols-5 2xl:grid-cols-6"
    - path: "components/CatalogSettings.tsx"
      provides: "пресеты плотности с расширением xl/2xl"
      contains: "xl:grid-cols-3 2xl:grid-cols-4"
    - path: "app/catalog/[secret]/layout.tsx"
      provides: "контейнер max-w-screen-2xl для main и шапки"
      contains: "max-w-screen-2xl"
    - path: "components/ProductCard.tsx"
      provides: "hover-состояния карточки и строки списка"
      contains: "hover:shadow-md"
    - path: "components/CatalogNav.tsx"
      provides: "hover-состояния кнопок навигации"
      contains: "hover:bg-blue-500"
    - path: "components/SubgroupFlyout.tsx"
      provides: "hover-состояния чипов подгрупп"
      contains: "hover:bg-gray-200"
  key_links:
    - from: "components/CatalogView.tsx (containerClass, grid-ветка)"
      to: "сетка витрины на десктопе"
      via: "Tailwind responsive grid-cols"
      pattern: "xl:grid-cols-5 2xl:grid-cols-6"
    - from: "components/CatalogSettings.tsx (PRESENTATION_PRESETS.cols)"
      to: "сетка режима presentation"
      via: "поле cols подставляется в CatalogView через preset.cols"
      pattern: "grid-cols-\\d xl:grid-cols-\\d 2xl:grid-cols-\\d"
---

<objective>
Адаптировать витрину каталога под большие экраны (десктоп) — без изменения мобильного вида.

Чисто фронтенд: правка Tailwind-классов в 6 существующих файлах. Никаких новых компонентов, стека, данных или API. Семь атомарных правок из дизайн-контракта 09-UI-SPEC.md:
1. Брейкпоинты сетки xl/2xl (режим grid) + контейнер max-w-screen-2xl (DESK-01)
2. Расширение пресетов плотности на xl/2xl (DESK-01)
3. Hover-состояния карточки и строки списка (DESK-02)
4. Hover-состояния навигации и подгрупп (DESK-03)
Всё под защитой инварианта DESK-04: мобильный вид (≤768px) не меняется.

Purpose: На широком мониторе витрина использует доступную ширину (5–6 колонок вместо 4), контент центрируется и не растягивается, элементы реагируют на курсор мыши hover-эффектами.
Output: Изменённые классы Tailwind в 6 файлах, успешная сборка, визуально подтверждённая адаптация на широком экране при неизменном мобильном виде.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-1/09-UI-SPEC.md
@CLAUDE.md

<notes>
- Номера строк в 09-UI-SPEC.md приблизительны. Перед каждой правкой ОБЯЗАТЕЛЬНО прочитай файл и найди актуальную строку по якорному классу.
- Проект: только Tailwind CSS, TypeScript, комментарии в коде на русском.
- Метод правки grid-классов: НЕ заменять существующие мобильные классы, а ДОБАВЛЯТЬ суффиксы xl:/2xl: и hover:. Hover не срабатывает на тач-устройствах — мобильный вид не затрагивается.
- После всех правок и перед сборкой почистить кэш: Remove-Item -Recurse -Force C:\catalog\.next (PowerShell, правило проекта).
</notes>

<current_anchors>
<!-- Фактические якоря, прочитаны планировщиком из текущего кода. Используй их, но всё равно перечитай файл. -->
- app/catalog/[secret]/layout.tsx: header-div строка 29 (`flex items-center justify-between px-2 h-12 gap-2`); `<main>{children}</main>` строка 44 (без className).
- components/CatalogView.tsx: строка 107 — grid-ветка `"flex-1 grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-2 p-2"`.
- components/CatalogSettings.tsx: PRESENTATION_PRESETS — `cols: "grid-cols-2"` (строка 20, пресет 2x3), `cols: "grid-cols-3"` (строка 33, пресет 3x4), `cols: "grid-cols-4"` (строка 46, пресет 4x6).
- components/ProductCard.tsx: обёртка grid/presentation строка 115 (`relative rounded-lg overflow-hidden border border-gray-100 shadow-sm`); строка списка строка 264 (`flex items-center gap-3 px-4 py-3`).
- components/CatalogNav.tsx: кнопка режима строка 51 (неактив. `bg-blue-500 text-white active:bg-blue-400`); кнопка раздела строка 81 (неактив. `text-white active:bg-blue-500`).
- components/SubgroupFlyout.tsx: чип «Все» неактив. строка 25 (`bg-gray-100 text-gray-600 active:bg-gray-200`), актив. строка 24 (`bg-blue-600 text-white`); чипы подгрупп неактив. строка 37 (`bg-gray-100 text-gray-600 active:bg-gray-200`), актив. строка 36 (`bg-blue-600 text-white`).
</current_anchors>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Десктопная сетка витрины и контейнер ширины (DESK-01)</name>
  <files>app/catalog/[secret]/layout.tsx, components/CatalogView.tsx, components/CatalogSettings.tsx</files>
  <read_first>
    - app/catalog/[secret]/layout.tsx (читать целиком — найти header-div и main)
    - components/CatalogView.tsx (читать строки ~95–125 — найти containerClass и grid-ветку)
    - components/CatalogSettings.tsx (читать строки ~14–57 — найти поля cols в PRESENTATION_PRESETS)
    - .planning/phases/09-1/09-UI-SPEC.md (разделы «Сетка витрины» и «Точный список изменений по файлам», пункты 1–3)
  </read_first>
  <action>
    Три правки, добавляющие только десктопные суффиксы (мобильные классы НЕ трогать):

    1. components/CatalogView.tsx — в grid-ветке containerClass (viewMode grid, по якорю строки 107) добавить `xl:grid-cols-5 2xl:grid-cols-6` ПОСЛЕ `lg:grid-cols-4`. Итог: `"flex-1 grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6 gap-2 p-2"`.

    2. components/CatalogSettings.tsx — расширить поле `cols` в трёх пресетах PRESENTATION_PRESETS, добавляя десктопные суффиксы к существующему значению:
       - пресет "2x3": `cols: "grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4"`
       - пресет "3x4": `cols: "grid-cols-3 xl:grid-cols-4 2xl:grid-cols-5"`
       - пресет "4x6": `cols: "grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6"`
       Поля sizes, label и compactCart не менять.

    3. app/catalog/[secret]/layout.tsx — центрировать контент по max-width:
       - header-внутренний div (якорь строки 29): добавить к className `max-w-screen-2xl mx-auto w-full` → `"flex items-center justify-between px-2 h-12 gap-2 max-w-screen-2xl mx-auto w-full"`.
       - `<main>{children}</main>` (якорь строки 44): добавить className → `<main className="max-w-screen-2xl mx-auto w-full">{children}</main>`.

    Комментарии к новым/изменённым местам — на русском (если поясняешь правку рядом в коде).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; if (-not (Select-String -Path components/CatalogView.tsx -SimpleMatch 'xl:grid-cols-5 2xl:grid-cols-6')) {$ok=$false}; if (-not (Select-String -Path components/CatalogView.tsx -SimpleMatch 'grid-cols-2 sm:grid-cols-3 lg:grid-cols-4')) {$ok=$false}; if (-not (Select-String -Path components/CatalogSettings.tsx -SimpleMatch 'grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4')) {$ok=$false}; if (-not (Select-String -Path components/CatalogSettings.tsx -SimpleMatch 'grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6')) {$ok=$false}; if ((Select-String -Path 'app/catalog/[secret]/layout.tsx' -SimpleMatch 'max-w-screen-2xl').Count -lt 2) {$ok=$false}; if ($ok) {exit 0} else {exit 1}"</automated>
  </verify>
  <acceptance_criteria>
    - components/CatalogView.tsx содержит строку `xl:grid-cols-5 2xl:grid-cols-6` в grid-ветке containerClass.
    - components/CatalogView.tsx ВСЁ ЕЩЁ содержит `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4` (мобильные/планшетные классы сохранены — DESK-04).
    - components/CatalogSettings.tsx: cols пресетов = `grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4`, `grid-cols-3 xl:grid-cols-4 2xl:grid-cols-5`, `grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6`. Базовые `grid-cols-2/3/4` сохранены первыми (DESK-04).
    - app/catalog/[secret]/layout.tsx содержит `max-w-screen-2xl` минимум в двух местах (header-div и main).
    - Поля sizes в PRESENTATION_PRESETS не изменены.
  </acceptance_criteria>
  <done>Сетка grid получила xl/2xl-колонки, пресеты плотности расширены на десктоп, контент центрирован контейнером max-w-screen-2xl; мобильные grid-классы нетронуты.</done>
</task>

<task type="auto">
  <name>Задача 2: Hover-состояния (карточки, список, навигация, подгруппы) (DESK-02, DESK-03)</name>
  <files>components/ProductCard.tsx, components/CatalogNav.tsx, components/SubgroupFlyout.tsx</files>
  <read_first>
    - components/ProductCard.tsx (читать строки ~110–130 для обёртки grid/presentation и ~255–270 для строки списка)
    - components/CatalogNav.tsx (читать целиком — найти кнопку режима и кнопку раздела)
    - components/SubgroupFlyout.tsx (читать целиком — найти чипы «Все» и подгрупп)
    - .planning/phases/09-1/09-UI-SPEC.md (разделы «Hover-состояние карточки», «CatalogNav», «SubgroupFlyout» и пункты 4–7 «Точного списка изменений»)
  </read_first>
  <action>
    Четыре правки — добавить hover-классы (не трогать существующие active:/bg- классы; hover не срабатывает на тач-устройствах, поэтому мобильный вид не затрагивается — DESK-04):

    1. ProductCard.tsx — обёртка карточки grid/presentation (якорь строки 115, className начинается с `relative rounded-lg overflow-hidden border border-gray-100 shadow-sm`): добавить `hover:shadow-md transition-shadow duration-150`. Сохрани шаблонную литералку и условие opacity-60 (вставляй hover-классы в статическую часть строки, до выражения `${inStock ...}`).

    2. ProductCard.tsx — строка списка (якорь строки 264, className `flex items-center gap-3 px-4 py-3` с условием `${inStock ...}`): добавить `hover:bg-gray-50 transition-colors duration-100` в статическую часть.

    3. CatalogNav.tsx:
       - кнопка режима (якорь строки 51): к неактивному состоянию `bg-blue-500 text-white active:bg-blue-400` добавить `hover:bg-blue-400`.
       - кнопка раздела (якорь строки 81): к неактивному состоянию `text-white active:bg-blue-500` добавить `hover:bg-blue-500`.

    4. SubgroupFlyout.tsx (две кнопки — «Все» и .map подгрупп, в обеих один паттерн классов):
       - неактивное состояние `bg-gray-100 text-gray-600 active:bg-gray-200` → добавить `hover:bg-gray-200`.
       - активное состояние `bg-blue-600 text-white` → добавить `hover:bg-blue-700`.
       Применить к ОБЕИМ кнопкам (чип «Все» и чипы подгрупп).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; if (-not (Select-String -Path components/ProductCard.tsx -SimpleMatch 'hover:shadow-md')) {$ok=$false}; if (-not (Select-String -Path components/ProductCard.tsx -SimpleMatch 'hover:bg-gray-50')) {$ok=$false}; if (-not (Select-String -Path components/CatalogNav.tsx -SimpleMatch 'hover:bg-blue-400')) {$ok=$false}; if (-not (Select-String -Path components/CatalogNav.tsx -SimpleMatch 'hover:bg-blue-500')) {$ok=$false}; if ((Select-String -Path components/SubgroupFlyout.tsx -SimpleMatch 'hover:bg-gray-200').Count -lt 2) {$ok=$false}; if ((Select-String -Path components/SubgroupFlyout.tsx -SimpleMatch 'hover:bg-blue-700').Count -lt 2) {$ok=$false}; if ($ok) {exit 0} else {exit 1}"</automated>
  </verify>
  <acceptance_criteria>
    - components/ProductCard.tsx содержит `hover:shadow-md transition-shadow duration-150` (обёртка карточки) и `hover:bg-gray-50 transition-colors duration-100` (строка списка).
    - components/CatalogNav.tsx содержит `hover:bg-blue-400` (кнопка режима) и `hover:bg-blue-500` (кнопка раздела); существующие `active:bg-blue-400`/`active:bg-blue-500` сохранены.
    - components/SubgroupFlyout.tsx содержит `hover:bg-gray-200` минимум 2 раза (оба чипа) и `hover:bg-blue-700` минимум 2 раза (активные состояния обоих чипов).
    - Существующие `active:bg-gray-200` в SubgroupFlyout сохранены (мобильный tap-эффект — DESK-04).
  </acceptance_criteria>
  <done>Карточки, строки списка, кнопки навигации и чипы подгрупп получили hover-эффекты для мыши; active-классы для тача сохранены.</done>
</task>

<task type="auto">
  <name>Задача 3: Чистка кэша и сборка (DESK-04 — целостность)</name>
  <files>.next (кэш сборки, удаляется и пересоздаётся)</files>
  <read_first>
    - CLAUDE.md (раздел «Важные правила» — про чистку .next перед build)
  </read_first>
  <action>
    Применить правило проекта: после правок удалить кэш Next.js и собрать проект.
    1. Удалить кэш: Remove-Item -Recurse -Force C:\catalog\.next (если папки нет — игнорировать ошибку).
    2. Запустить npm run build.
    Если сборка падает — исправить (вероятная причина: опечатка в Tailwind-классе или сломанная шаблонная литералка). Не менять при этом мобильные классы.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path C:\catalog\.next) { Remove-Item -Recurse -Force C:\catalog\.next }; npm run build; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build завершается с кодом 0 (без ошибок типов и сборки).
    - Папка .next пересоздана сборкой.
  </acceptance_criteria>
  <done>Кэш почищен, проект собирается без ошибок — изменения готовы к визуальной проверке.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 4: Визуальная проверка десктоп/мобильного вида (DESK-01..04)</name>
  <files>нет (проверка в браузере, файлы не меняются)</files>
  <action>
    Это checkpoint — Claude не пишет код. Запусти dev-сервер (npm run dev) и предложи владельцу проверить адаптацию в браузере по шагам ниже, затем дождись подтверждения.
  </action>
  <what-built>
    Витрина адаптирована под десктоп: на широком экране grid показывает 5 колонок (≥1280px) и 6 колонок (≥1536px), контент центрирован (max-w-screen-2xl), карточки/навигация реагируют на наведение мыши. Мобильный вид (≤768px) — без изменений (2 колонки).
  </what-built>
  <how-to-verify>
    1. Запустить dev-сервер: npm run dev. Открыть каталог по секретной ссылке (/catalog/<секрет>) в браузере.
    2. Переключиться в режим «Сетка» (grid) через шестерёнку настроек.
    3. Растянуть окно ≥1280px — убедиться: 5 колонок товаров.
    4. Растянуть окно ≥1536px (или открыть на большом мониторе) — убедиться: 6 колонок; контент центрирован, по краям симметричные поля, не прижат к левому краю.
    5. Сузить окно до ≤768px (или DevTools, мобильный профиль) — убедиться: 2 колонки, вид как раньше, ничего не сломалось.
    6. Навести мышь на карточку — появляется тень (hover:shadow-md). В режиме «Список» — навести на строку, появляется серый фон.
    7. Навести на кнопки разделов/режимов в шапке и на чипы подгрупп — подсветка при наведении.
    8. Проверить режим «Презентация» с разными пресетами плотности (2×3, 3×4, 4×6) на широком экране — колонок становится больше согласно контракту.
  </how-to-verify>
  <verify>
    <human-check>Владелец подтверждает в браузере: ≥1280px → 5 колонок, ≥1536px → 6 колонок и центрирование, ≤768px → 2 колонки (без регресса), hover работает на мыши.</human-check>
  </verify>
  <resume-signal>Напиши «approved», если десктоп даёт 5/6 колонок и центрирован, мобильный остался 2 колонки, hover работает. Иначе опиши, что не так.</resume-signal>
  <done>Владелец визуально подтвердил десктоп-адаптацию (5/6 колонок, центрирование, hover) и неизменность мобильного вида.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Новых границ доверия не вводится. Этап — правка статических Tailwind CSS-классов в клиентских компонентах. Нет нового пользовательского ввода, нет новых эндпоинтов, нет изменения обработки данных, аутентификации или сети.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | (none) | Tailwind-классы в 6 компонентах | accept | Поверхность атаки не расширяется: нет ввода, эндпоинтов, данных, доступа. Severity: none. |

Установок пакетов (npm/pip/cargo) в этапе нет — package legitimacy gate не применяется.
severity: none — обоснование: чисто визуальная CSS-адаптация существующих компонентов.
</threat_model>

<verification>
- Задача 1 verify: grep подтверждает наличие xl/2xl классов и сохранение мобильных классов.
- Задача 2 verify: grep подтверждает наличие hover-классов и сохранение active-классов.
- Задача 3 verify: npm run build → код 0 после чистки .next.
- Задача 4 (человек): на ≥1280px — 5 колонок, на ≥1536px — 6 колонок, центрирование; на ≤768px — 2 колонки (без регресса); hover работает на мыши.
</verification>

<success_criteria>
- DESK-01: на широком экране grid даёт 5 (xl) / 6 (2xl) колонок, контент центрирован max-w-screen-2xl. ✓ если задачи 1, 4 пройдены.
- DESK-02: карточка соразмерна и реагирует на hover (тень / фон строки). ✓ если задачи 2, 4 пройдены.
- DESK-03: навигация и подгруппы кликабельны и подсвечиваются на десктопе. ✓ если задачи 2, 4 пройдены.
- DESK-04: мобильный вид (≤768px) не изменился — базовые grid-cols-2/sm/lg и active-классы сохранены. ✓ acceptance_criteria задач 1–2 + проверка п.5 задачи 4.
- npm run build завершается успешно. ✓ задача 3.
</success_criteria>

<output>
Create `.planning/phases/09-1/09-01-SUMMARY.md` when done
</output>
