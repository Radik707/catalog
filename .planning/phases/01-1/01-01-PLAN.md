---
phase: 01-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/test_upload_new_format.py
  - scripts/upload.py
  - components/CatalogView.tsx
autonomous: true
requirements: [FMT-01, FMT-02, FMT-03, FMT-04, GRP-01, GRP-02, GRP-03]

must_haves:
  truths:
    - "Парсер нового формата извлекает название из колонки D, цену/остаток из дальних колонок и пропускает строку-дубль с названием в колонке A"
    - "Старый формат (A=название/категории, B=цена, C=остаток) разбирается без изменений"
    - "Если текущие группы каталога недоступны — обновление прерывается с ненулевым кодом возврата, а не сваливает товары в «Новинки»"
    - "Реально новые товары получают группу «Новинки» и бейдж «новинка», уже известные — свою группу из текущего каталога"
    - "Группа «Новинки» появляется в фильтре витрины в детерминированной позиции, вкладка «Новинка» показывает товары с бейджем «новинка»"
  artifacts:
    - path: "scripts/test_upload_new_format.py"
      provides: "Pytest-проверки парсера нового/старого формата и логики новинок на синтетических .xlsx-фикстурах"
      contains: "def test_"
    - path: "scripts/upload.py"
      provides: "Проверенный и точечно укреплённый парсер двух форматов"
      contains: "parse_new_format"
    - path: "components/CatalogView.tsx"
      provides: "GROUP_ORDER с группой «Новинки»"
      contains: "Новинки"
  key_links:
    - from: "scripts/upload.py:main"
      to: "load_current_groups"
      via: "прерывание при пустом результате (sys.exit(1))"
      pattern: "if not current_groups"
    - from: "app/catalog/[secret]/page.tsx"
      to: "badge === новинка"
      via: "фильтр ?filter=new"
      pattern: "filter === \"new\""
---

<objective>
Довести уже написанную поддержку нового формата 1С в `scripts/upload.py` до доверенного состояния перед боевым прогоном: закрепить поведение парсера тестами на синтетических Excel-фикстурах, точечно починить найденные пробелы (НЕ переписывать), и убрать единственный фронтенд-пробел для GRP-03 — детерминированный показ группы «Новинки» в витрине.

Это локальная задача без доступа к серверу: безопасно править код и гонять тесты на машине разработчика. Боевой прогон и деплой — в следующих волнах.

Purpose: Снизить риск первого боевого прогона. Сейчас весь новый код (`parse_new_format`, `is_new_format`, `find_header_cols`, `load_current_groups`, блок новинок) ни разу не запускался на структуре нового формата и не покрыт тестами (см. CONCERNS.md). До деплоя нужно закрепить контракт парсера.
Output: pytest-файл с проходящими тестами, точечно укреплённый `upload.py`, фронтенд-фикс «Новинки» в GROUP_ORDER.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Ключевые функции и контракты из scripts/upload.py. Исполнитель использует их напрямую — без повторного исследования кодовой базы. -->

Из scripts/upload.py (текущая реализация, НЕ переписывать — только проверять/точечно чинить):
- NEW_FORMAT_NAME_COL = 3  # колонка D — название в новом формате
- find_header_cols(ws) -> tuple[int|None, int|None]  # ищет индексы «Цена»/«Остаток» в первых 10 строках
- is_new_format(price_col) -> bool  # True если price_col is not None and price_col >= 5
- clean_new_name(name) -> str  # убирает хвостовую ", <ед.>"
- normalize_name(name) -> str  # clean_new_name + схлоп пробелов + lower
- parse_new_format(filepath, price_col, stock_col) -> list[dict]  # source_category=""
- parse_excel_file(filepath) -> list[dict]  # старый формат, source_category из строк-категорий
- load_current_groups() -> dict  # {normalize_name(name): group} из листа «Товары»; при сбое чтения возвращает {}
- apply_group_mapping(products, category_map) -> list[dict]  # добавляет display_group
- products_to_rows(products, badges, photo_data, new_names) -> list[list]  # бейдж «новинка» для new_names
- main() строки ~673–689: если new_format_used и не удалось прочитать current_groups → log.error + sys.exit(1);
  иначе товары с source_category=="" получают группу из current_groups либо «Новинки»+new_names

Формат словаря товара: {name, price, stock, source_category, supplier_file, display_group?}
Схема листа «Товары» (A:I): Наименование | Цена | Остаток | Категория | Группа | Поставщик | Badge | ImageUrl | Description
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pytest-фикстуры и тесты парсера двух форматов</name>
  <read_first>
    - scripts/upload.py (целиком — текущая реализация обоих парсеров и блока новинок)
    - .planning/codebase/CONVENTIONS.md (Python-конвенции: snake_case, docstring на русском, encoding utf-8)
    - .planning/codebase/TESTING.md (раздел «If Tests Were Added» — pytest, расположение scripts/tests; здесь кладём в scripts/ ради простоты запуска)
  </read_first>
  <behavior>
    - Тест нового формата: .xlsx, где строка-заголовок имеет «Цена» в колонке N (индекс 13) и «Остаток» в O (индекс 14), название в колонке D (индекс 3); строка-дубль с названием в колонке A пропускается → parse_new_format возвращает только товарные строки с верными name/price/stock.
    - Тест is_new_format: price_col=13 → True; price_col=1 (старый формат, «Цена» в B) → False; price_col=None → False.
    - Тест find_header_cols: на новой фикстуре возвращает (13, 14); на старой фикстуре (B=Цена, C=Остаток) возвращает (1, 2).
    - Тест старого формата: parse_excel_file на фикстуре со строками-категориями (A заполнена, B/C пусты) и товарами (A=название, B=цена, C=остаток) → товары получают source_category из ближайшей строки-категории, strip_category_prefix снимает префикс «а».
    - Тест clean_new_name/normalize_name: "Конфеты Ромашка, шт" → "Конфеты Ромашка" / "конфеты ромашка".
    - Тест логики новинок (на чистых функциях, без сети): для товара с source_category=="" и known normalize_name в current_groups → берётся группа из current_groups; для неизвестного → display_group "Новинки" и name в new_names.
  </behavior>
  <action>Создай `scripts/test_upload_new_format.py`. Импортируй функции из `upload` (тот же каталог). Фикстуры .xlsx генерируй в памяти/во временной папке через `openpyxl.Workbook()` внутри тестов (pytest `tmp_path`), не коммить бинарные .xlsx. Для теста логики новинок вынеси проверяемое поведение в чистый вызов: если в `upload.py` логика новинок зашита только внутри `main()`, НЕ рефактори её ради теста — вместо этого протестируй её косвенно через `normalize_name` + словарь current_groups, воспроизведя ту же ветку в тесте (известное название → группа; неизвестное → «Новинки»). Комментарии и docstring — на русском. Запуск из каталога scripts: `python -m pytest test_upload_new_format.py -q`. Если pytest не установлен — добавь его в `scripts/requirements.txt` (строка `pytest>=8`), но не запускай установку глобально без необходимости (используй уже доступный python -m pytest).</action>
  <verify>
    <automated>cd scripts && python -m pytest test_upload_new_format.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest scripts/test_upload_new_format.py -q` завершается без падений (все тесты PASSED).
    - Файл `scripts/test_upload_new_format.py` содержит минимум 5 функций `def test_` покрывающих: новый формат, старый формат, is_new_format, find_header_cols, normalize_name/новинки.
    - Тесты НЕ обращаются к сети и к Google Sheets (load_current_groups не вызывается напрямую — логика новинок проверяется на синтетическом словаре).
  </acceptance_criteria>
  <done>Тесты парсера обоих форматов и логики новинок написаны и проходят локально; контракт парсера зафиксирован.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Точечное укрепление upload.py по результатам тестов (FMT-01..04, GRP-01/02)</name>
  <read_first>
    - scripts/upload.py (целиком)
    - scripts/test_upload_new_format.py (только что созданные тесты — они фиксируют ожидаемое поведение)
    - scripts/sheet_tool.py (механизм подушки безопасности — НЕ менять, но понимать, что прод-прогон подстрахован бэкапом через uploader)
    - .planning/codebase/CONCERNS.md (раздел «Новый формат 1С», «Категоризация по точному совпадению», «Автодетекция по номеру колонки»)
  </read_first>
  <action>На основе падений/пограничных случаев из Task 1 точечно почини `upload.py`. НЕ переписывай существующие парсеры. Конкретные точки внимания (правь только если тест выявил реальную проблему):
1) FMT-04: убедись, что ветка в `main()` (строки ~673–689) при `new_format_used and not current_groups` делает `log.error(...)` с понятным русским текстом и `sys.exit(1)` ДО любой записи в Sheet. Если `load_current_groups()` маскирует сбой чтения под легитимно пустой каталог — добавь различение: при исключении чтения логируй причину (уже есть `log.warning` на строке ~275) и при пустом mapping в режиме нового формата всегда прерывайся (это безопасное поведение — оставить как есть).
2) FMT-01: смешанная партия (часть файлов старого формата, часть нового) — проверь, что `new_format_used` ставится в True, если хоть один файл нового формата, и товары старого формата сохраняют свою группу (их `source_category` непустой, ветка новинок их не трогает). Если тест показал, что старые товары случайно попадают в новинки — почини условие `if p["source_category"] == ""`.
3) GRP-02: убедись, что товар, попавший в «Новинки», получает И `display_group="Новинки"` И добавление в `new_names` (для бейджа «новинка» в `products_to_rows`). 
4) Добавь в начало docstring модуля строку про режим dry-run на сервере и про то, что новый формат поддержан (актуализируй заголовок, который сейчас says «Этап 0»).
Все комментарии новых правок — на русском. После правок перезапусти тесты.</action>
  <verify>
    <automated>python -m py_compile scripts/upload.py && cd scripts && python -m pytest test_upload_new_format.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile scripts/upload.py` завершается без ошибок.
    - `python -m pytest scripts/test_upload_new_format.py -q` — все тесты PASSED.
    - В `scripts/upload.py` ветка нового формата содержит прерывание при пустых группах: grep-проверка `grep -v '^#' scripts/upload.py | grep -c "if not current_groups"` возвращает 1.
    - Товары старого формата (source_category != "") не попадают в new_names ни в одном тесте.
  </acceptance_criteria>
  <done>Найденные тестами пробелы в парсере/логике новинок устранены точечно; оба формата и FMT-04 подтверждены тестами; существующая реализация не переписана.</done>
</task>

<task type="auto">
  <name>Task 3: Детерминированный показ группы «Новинки» в витрине (GRP-03)</name>
  <read_first>
    - components/CatalogView.tsx (строки 17–66: GROUP_ORDER, сборка groups, фильтрация)
    - app/catalog/[secret]/page.tsx (фильтр ?filter=new по badge === "новинка")
    - .planning/codebase/CONCERNS.md (баг «Новинки не включены в GROUP_ORDER», строка ~141)
    - .planning/codebase/CONVENTIONS.md (TS: GROUP_ORDER — SCREAMING_SNAKE_CASE константа, без редеплой-хардкода по возможности)
  </read_first>
  <action>В `components/CatalogView.tsx` добавь строку `"Новинки"` в массив `GROUP_ORDER` — помести её первой (индекс 0), чтобы новые товары были заметны владельцу вверху списка фильтра. Это убирает поведение «индекс 999 / произвольный порядок» из CONCERNS.md. НЕ трогай логику фильтрации `?filter=new` в `page.tsx` — она уже корректно фильтрует по `badge === "новинка"` и работает. Никаких других групп не добавляй в этой задаче (бакалея/прикассовое — вне scope этапа). Проверь типобезопасность сборкой.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` проходит без ошибок.
    - В `components/CatalogView.tsx` массив `GROUP_ORDER` содержит элемент `"Новинки"` (grep `grep -c '"Новинки"' components/CatalogView.tsx` >= 1).
    - Логика `?filter=new` в `app/catalog/[secret]/page.tsx` не изменена (фильтр по `badge === "новинка"` сохранён).
  </acceptance_criteria>
  <done>Группа «Новинки» отображается в фильтре витрины в детерминированной (первой) позиции; вкладка «Новинка» по-прежнему фильтрует по бейджу — GRP-03 закрыт на фронтенде.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Excel-файл → upload.py | Недоверенный вход: структура файла от поставщика 1С может отличаться от ожидаемой |
| upload.py → Google Sheet «Товары» | Запись перезаписывает весь каталог (деструктивно) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | parse_new_format на неожиданной структуре .xlsx | mitigate | Тесты на синтетических фикстурах фиксируют контракт колонок D/N/O и пропуск строк-дублей до боевого прогона |
| T-01-02 | Denial of Service | load_current_groups сбой → все товары в «Новинки» | mitigate | FMT-04: при пустых группах в режиме нового формата — sys.exit(1) до записи; подтверждено тестом |
| T-01-03 | Information Disclosure | credentials.json / .env в git | accept | Эта задача локальная, новые файлы (тест, фронтенд) секретов не содержат; .gitignore уже исключает credentials.json и .env |
| T-01-SC | Tampering | установка pytest (pip) | mitigate | pytest — известный публичный пакет (pypi.org/project/pytest); установка не выполняется глобально, используется уже доступный python -m pytest; новых сторонних зависимостей в прод-код не добавляется |
</threat_model>

<verification>
- `python -m pytest scripts/test_upload_new_format.py -q` — все тесты проходят
- `python -m py_compile scripts/upload.py` — без ошибок
- `npx tsc --noEmit` — без ошибок типов
- Существующие функции парсера (`parse_new_format`, `parse_excel_file`, `is_new_format`) сохранены, не переписаны
</verification>

<success_criteria>
- Контракт парсера обоих форматов закреплён проходящими тестами
- FMT-04 (прерывание при недоступных группах) подтверждён тестом и кодом
- Группа «Новинки» детерминированно отображается в витрине (GRP-03, фронтенд)
- Никакой существующий рабочий код не переписан — только тесты + точечные правки + одна строка в GROUP_ORDER
</success_criteria>

<output>
Create `.planning/phases/01-1/01-01-SUMMARY.md` when done
</output>
