---
phase: 01-1
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: []
autonomous: false
requirements: [FMT-05, FMT-03, GRP-03]

user_setup:
  - service: daniella (сервер) + веб-загрузчик
    why: "Реальный боевой прогон выполняется через страницу загрузчика (с бэкапом) на боевом сервере"
    dashboard_config:
      - task: "Открыть секретную ссылку загрузчика и нажать «Обновить каталог»"
        location: "https://uploader.zhukoleg.ru/<secret> (или ssh-запуск пути обновления)"

must_haves:
  truths:
    - "Боевой прогон через путь uploader (с бэкапом «Товары_BACKUP») обновил каталог ≈871 товаром"
    - "Перед перезаписью каталога создан бэкап, доступен откат — витрина не остаётся битой"
    - "Группа «Новинки» и бейдж «новинка» видны в реальной витрине: и как группа, и во вкладке «Новинка»"
    - "Файл старого формата (если подан) по-прежнему обрабатывается корректно — оба формата работают"
  artifacts: []
  key_links:
    - from: "uploader/app.py update()"
      to: "sheet_tool.py backup → upload.py → notify"
      via: "путь обновления с подушкой безопасности"
      pattern: "backup_catalog"
    - from: "Google Sheet «Товары»"
      to: "витрина /catalog/<secret>"
      via: "getProducts() ISR 5 мин"
      pattern: "badge === \"новинка\""
---

<objective>
Выполнить реальный боевой прогон нового формата 1С через штатный путь веб-загрузчика (с обязательной подушкой безопасности: бэкап → upload → проверка счёта → при аномалии откат) и подтвердить сквозной результат в живой витрине: каталог обновлён ≈871 товаром, «Новинки» видны как группа и во вкладке «Новинка» (FMT-05, GRP-03). Это финальная волна вертикального среза: реальный файл → парсинг → группировка/новинки → запись в Sheet → показ в витрине.

Purpose: Закрыть цель этапа — каталог обновляется актуальными данными нового формата на проде, без риска для витрины.
Output: Обновлённый боевой каталог; подтверждённое отображение новинок; зафиксированный итог прогона.

ВАЖНО (autonomous: false): прогон перезаписывает боевой каталог. Выполняется ТОЛЬКО через путь uploader (бэкап обязателен) и под наблюдением владельца. Исполнитель НЕ запускает `upload.py` напрямую с записью в обход бэкапа. Если что-то идёт не так — используется штатный откат (кнопка/командa rollback).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md

<runbook>
<!-- Штатный путь обновления с подушкой безопасности (из uploader/app.py) -->
- Веб-путь: оператор/владелец открывает секретную ссылку загрузчика, файлы из last_batch уже там → «Обновить каталог».
  app.py делает: backup_catalog() (Товары→Товары_BACKUP) → run_upload() → если мало товаров (< WARN_RATIO*было) то stash_new+rollback+notify decision; иначе notify plain.
- Откат: кнопка «↩ Откатить к прошлой версии» или `ssh my-vps "cd /opt/apps/catalog/scripts && python3 sheet_tool.py rollback"`.
- Счёт товаров: `ssh my-vps "cd /opt/apps/catalog/scripts && python3 sheet_tool.py count"` → COUNT rows=N.
- Витрина (Vercel, ISR 5 мин): /catalog/<CATALOG_SECRET>; вкладка «Новинка» = ?filter=new (badge === "новинка"); фильтр группы «Новинки».
- Telegram-уведомление владельцу идёт через SOCKS-прокси — итог прогона придёт в бот.
</runbook>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Готовность к боевому прогону: код доставлен и dry-run подтверждён (Wave 2), допуск получен.</what-built>
  <how-to-verify>
    1. Подтвердите, что в Wave 2 dry-run дал счёт ≈871 и владелец выбрал «proceed».
    2. Зафиксируйте текущий счёт каталога ДО прогона: `ssh my-vps "cd /opt/apps/catalog/scripts && python3 sheet_tool.py count"` → запишите COUNT (это значение пойдёт в «Товары_BACKUP» при бэкапе).
    3. Подтвердите, что файлы нового формата находятся в очереди загрузчика (last_batch / incoming) и владелец у экрана.
  </how-to-verify>
  <resume-signal>Напишите «готов к прогону, COUNT до = N» или причину паузы.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Боевой прогон через путь uploader (с бэкапом)</name>
  <read_first>
    - uploader/app.py (маршрут update(): backup_catalog → run_upload → warn/rollback → notify; строки ~284–330)
    - scripts/sheet_tool.py (backup/rollback/count — подушка безопасности)
    - .planning/codebase/ARCHITECTURE.md (основной путь обновления каталога, шаги 4–9)
  </read_first>
  <action>Запусти боевое обновление ТОЛЬКО штатным путём с подушкой безопасности. Предпочтительно — через веб-страницу загрузчика (владелец нажимает «Обновить каталог»), что гарантирует backup_catalog() перед записью. Если веб-путь недоступен и нужен ssh-запуск — воспроизведи последовательность app.py вручную, НЕ пропуская бэкап: сначала `ssh my-vps "cd /opt/apps/catalog/scripts && python3 sheet_tool.py backup"` (должно напечатать BACKUP_OK rows=N, где N≈прежний счёт), затем `ssh my-vps "cd /opt/apps/catalog/scripts && python3 upload.py --path /opt/apps/catalog/uploader/last_batch"` (реальная запись). После записи сними новый счёт: `ssh my-vps "cd /opt/apps/catalog/scripts && python3 sheet_tool.py count"`. Сверь: новый COUNT ≈871 и не меньше WARN_RATIO (0.5) от прежнего. Если COUNT подозрительно мал — НЕ оставляй битый каталог: выполни `python3 sheet_tool.py rollback` и переходи к checkpoint с отчётом. НЕ трогай credentials.json/.env. Зафиксируй: BACKUP rows, итоговый COUNT, текст Telegram-уведомления (если пришло).</action>
  <verify>
    <automated>ssh my-vps "cd /opt/apps/catalog/scripts && python3 sheet_tool.py count"</automated>
  </verify>
  <acceptance_criteria>
    - Перед записью выполнен бэкап: вывод содержит `BACKUP_OK rows=` с числом ≈ прежнего счёта (бэкап доступен для отката).
    - После прогона `sheet_tool.py count` печатает COUNT rows=N с N в диапазоне ~800–950 (целевое ≈871).
    - Новый COUNT не меньше 0.5 × прежнего (порог WARN_RATIO не сработал); если сработал — выполнен откат и каталог восстановлен (COUNT вернулся к прежнему).
    - credentials.json/.env на сервере не изменены и не выведены в лог.
  </acceptance_criteria>
  <done>Каталог обновлён актуальными данными нового формата через защищённый путь; бэкап создан; счёт ≈871 подтверждён (или выполнен штатный откат).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Каталог обновлён ≈871 товаром нового формата (с бэкапом). Витрина читает Google Sheet с ISR 5 мин.</what-built>
  <how-to-verify>
    0. ВАЖНО: убедитесь, что фронтенд-фикс из Wave 1 (строка «Новинки» в GROUP_ORDER, `components/CatalogView.tsx`) уже задеплоен на Vercel — то есть коммит волны 1 запушен в `origin/main` и Vercel завершил сборку. Без этого группа «Новинки» НЕ появится в фильтре витрины, даже если данные обновились. Проверка: `git log origin/main --oneline -5` содержит коммит с правкой CatalogView, либо в дашборде Vercel последний деплой — Ready.
    1. Подождите до 5 минут (ISR-кэш Vercel) либо обновите страницу каталога несколько раз.
    2. Откройте `/catalog/<CATALOG_SECRET>` — каталог отображается, товаров много (актуальные данные, не «пусто»).
    3. Откройте вкладку «Новинка» (или `?filter=new`) — показываются товары с бейджем «новинка» (реально новые из этого прогона).
    4. В фильтре групп найдите группу «Новинки» (в начале списка после фикса Wave 1) — выберите её, убедитесь, что товары соответствуют новинкам.
    5. Проверьте 2–3 знакомых товара старого ассортимента — у них сохранилась корректная группа (не свалились в «Новинки») → подтверждает GRP-01 и FMT-03 на проде.
    6. Если в этом прогоне был и файл старого формата — убедитесь, что его товары тоже на месте с категориями (FMT-03).
  </how-to-verify>
  <resume-signal>Напишите «новинки видны, группы корректны, каталог актуален» — или опишите проблему (пусто / все в новинках / нет вкладки), тогда инициируйте откат.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Веб-загрузчик/ssh → Google Sheet «Товары» | Деструктивная перезапись всего каталога |
| Google Sheet → витрина (Vercel) | Чтение; ошибка чтения возвращает [] (пустой каталог) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | прогон сбил каталог (мало/0 товаров) | mitigate | backup_catalog ДО записи + порог WARN_RATIO + штатный rollback; checkpoint владельца после прогона |
| T-03-02 | Tampering | запуск upload.py в обход бэкапа | mitigate | прогон только через uploader или ручную последовательность backup→upload→count; прямой write без бэкапа запрещён планом |
| T-03-03 | Information Disclosure | секреты в ssh-выводе/уведомлениях | accept | команды не печатают credentials/.env; Telegram-уведомление идёт владельцу через приватный бот |
| T-03-04 | Repudiation | непонятный итог прогона | mitigate | фиксируются BACKUP rows, итоговый COUNT, текст уведомления в SUMMARY |
</threat_model>

<verification>
- Бэкап создан до записи (BACKUP_OK rows=N)
- Итоговый COUNT ≈871, порог WARN_RATIO не нарушен (или выполнен откат)
- Витрина показывает актуальный каталог; «Новинки» видны как группа и во вкладке «Новинка»
- Товары старого ассортимента сохранили группы (GRP-01, FMT-03)
</verification>

<success_criteria>
- FMT-05 выполнен: новый формат проверен реальным боевым прогоном (≈871 товар, каталог обновлён корректно)
- GRP-03 выполнен сквозно: «Новинки» показываются в живой витрине
- Оба формата работают одновременно (FMT-03 подтверждён на проде, если подан старый файл)
- Каталог не остался битым: подушка безопасности отработала
</success_criteria>

<output>
Create `.planning/phases/01-1/01-03-SUMMARY.md` when done
</output>
