---
phase: 19-reorder-my-orders
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - app/catalog/[secret]/orders/page.tsx
  - components/SettingsPanel.tsx
autonomous: true
requirements: [REORD-01, REORD-02, REORD-04]
must_haves:
  truths:
    - "На карточке каждого заказа в «Мои заказы» есть кнопка «Повторить»"
    - "После нажатия «Повторить» позиции прошлого заказа кладутся в корзину с актуальной ценой и наличием"
    - "Показывается понятная сводка: сколько добавлено + что пропущено/изменено и почему (нет в наличии / цена изменилась / товар больше недоступен)"
    - "Если добавить нечего — честно сообщается, корзина не открывается"
    - "Из сводки можно перейти в корзину"
    - "Роль «Клиент» имеет вход на «Мои заказы» из меню (SettingsPanel) и из корзины"
    - "Экран «Мои заказы» показывает дату-время, сумму, число позиций, канал; пустое состояние оформлено"
  artifacts:
    - path: "app/catalog/[secret]/orders/page.tsx"
      provides: "Кнопка «Повторить» на карточке заказа + сводка результата повтора"
      contains: "Повторить"
    - path: "components/SettingsPanel.tsx"
      provides: "Пункт входа «Мои заказы» в меню настроек для роли client"
      contains: "Мои заказы"
  key_links:
    - from: "app/catalog/[secret]/orders/page.tsx"
      to: "lib/reorder.ts"
      via: "import classifyReorder"
      pattern: "classifyReorder"
    - from: "app/catalog/[secret]/orders/page.tsx"
      to: "components/CartProvider.tsx"
      via: "addToCartWithQuantity из useCartContext"
      pattern: "addToCartWithQuantity"
    - from: "app/catalog/[secret]/orders/page.tsx"
      to: "components/CatalogSyncProvider.tsx"
      via: "products из useCatalogSyncContext"
      pattern: "useCatalogSyncContext"
---

<objective>
Дать клиенту повтор прошлого заказа в один тап: кнопка «Повторить» на карточке заказа, честная сводка результата (что добавлено / что пропущено и почему) с переходом в корзину, и вход на экран «Мои заказы» из меню настроек (вход из корзины уже есть). Расширяем существующий экран этапа 16 — НЕ переписываем с нуля.

Purpose: «Повторить заказ» — высший ROI вехи v1.5. Клиент-владелец магазина повторяет привычную закупку без ручного перебора, видя честную картину изменений цен и наличия.
Output: расширенный `app/catalog/[secret]/orders/page.tsx` (кнопка «Повторить» + сводка, D-06/D-07) и пункт входа в `components/SettingsPanel.tsx` (D-08). Покрывает REORD-01 (проверка/полировка существующего экрана), REORD-02, REORD-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-reorder-my-orders/19-CONTEXT.md
@.planning/phases/19-reorder-my-orders/19-01-SUMMARY.md

<interfaces>
<!-- Контракты из плана 19-01 и провайдеров — использовать напрямую. -->

Из lib/reorder.ts (создан в 19-01):
```typescript
export type ReorderOutcome = 'added' | 'price_changed' | 'out_of_stock' | 'unavailable';
export interface ReorderLineResult {
  outcome: ReorderOutcome;
  historyItem: OrderHistoryItem;
  product?: Product;
  currentPrice?: number;
  oldPrice: number;
}
export interface ReorderResult { lines: ReorderLineResult[]; addedCount: number; }
export function classifyReorder(historyItems: OrderHistoryItem[], catalog: Product[], priceForm: PriceForm): ReorderResult;
```

Из components/CartProvider.tsx (расширен в 19-01):
```typescript
// useCartContext() предоставляет addToCartWithQuantity(product, quantity) — кап по остатку, накопление для существующих
```

Из components/CatalogSyncProvider.tsx:
```typescript
// useCatalogSyncContext(): { products: Product[], isOnline, syncedAt, status, refetch }
// products — актуальный каталог из IDB (офлайн-безопасно)
```

Из components/CatalogSettings.tsx:
```typescript
// useCatalogSettings(): { priceForm: "1" | "2", panelOpen, setPanelOpen, ... }
```

Из lib/useRole (паттерн SSR-safe гейта):
```typescript
// useRole(): { role: 'client' | 'sales', setRole, ready }
// Любой завязанный на роль UI рендерить только после ready (D-10)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Кнопка «Повторить» + сводка результата на экране заказов</name>
  <read_first>
    - app/catalog/[secret]/orders/page.tsx (существующий экран — OrderCard, OrderItem, структура карточки; расширяем, не переписываем)
    - lib/reorder.ts (classifyReorder, типы исходов — из 19-01)
    - components/CartProvider.tsx (addToCartWithQuantity — из 19-01)
    - components/CatalogSyncProvider.tsx (useCatalogSyncContext — источник products)
    - components/CatalogSettings.tsx (useCatalogSettings — priceForm)
    - app/catalog/[secret]/cart/page.tsx (формат ссылок, паттерн перехода в корзину)
    - .planning/phases/19-reorder-my-orders/19-CONTEXT.md (D-06, D-07, D-09..D-11)
  </read_first>
  <action>
    Расширить `app/catalog/[secret]/orders/page.tsx` (комментарии на русском, только Tailwind, функциональные компоненты). Подключить `useCartContext` (метод `addToCartWithQuantity`), `useCatalogSyncContext` (поле `products`), `useCatalogSettings` (`priceForm`) и `classifyReorder` из `lib/reorder.ts`.

    D-06 — кнопка «Повторить»: добавить на каждую карточку `OrderCard` кнопку «Повторить» (заметная, в тон синей теме каталога). Передать в OrderCard через пропсы либо колбэк `onRepeat(entry)`, либо нужные контексты — на усмотрение исполнителя, но логику повтора держать в одном месте (родительский компонент или общий обработчик). При нажатии:
    1. Вызвать `classifyReorder(entry.items, products, priceForm)` → получить `ReorderResult`.
    2. Для каждой строки с исходом `added` или `price_changed` вызвать `addToCartWithQuantity(line.product!, line.historyItem.quantity)`.
    3. Открыть сводку результата (D-07).

    D-07 — сводка результата (модалка или инлайн-панель — на усмотрение, но мобильно-дружелюбно, с учётом safe-area снизу и нижних табов клиента — не прятать кнопки под панелью): показать «Добавлено N товаров» (склонение «товар/товара/товаров»), затем список пропущенных/изменённых с причиной:
      - `price_changed` — «<имя>: цена изменилась, было <oldPrice> ₽ → стало <currentPrice> ₽» (добавлен);
      - `out_of_stock` — «<имя>: нет в наличии» (не добавлен);
      - `unavailable` — «<имя>: товар больше недоступен» (не добавлен).
    Кнопка «Перейти в корзину →» ведёт на `/catalog/${params.secret}/cart`. Если `addedCount === 0` (добавить нечего) — честно сказать об этом (например «Ни одного товара из заказа сейчас нельзя добавить») и НЕ показывать кнопку перехода в корзину (D-07). Сводку можно закрыть (крестик/«Закрыть»/тап по подложке).

    Тексты пометок и микрокопирайт — на усмотрение исполнителя в рамках формулировок выше. Инварианты (D-11): нигде не вводить слова «статус», «принято», «подтверждено», «в доставке»; заголовок экрана остаётся «Отправленные заказы». D-09: никаких новых сетевых вызовов при старте/повторе — данные только из контекстов (IDB/localStorage); `force-dynamic` и `/api/products` не трогать. Если `products` пуст (каталог ещё не подгрузился офлайн) — повтор всё равно отработает (все позиции уйдут в `unavailable`), сводка честно это покажет.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - На карточке заказа в orders/page.tsx есть кнопка «Повторить»
    - Нажатие вызывает classifyReorder(entry.items, products, priceForm) и кладёт added/price_changed позиции через addToCartWithQuantity
    - Сводка показывает число добавленных + список пропущенных/изменённых с причинами (нет в наличии / цена изменилась было→стало / товар больше недоступен)
    - При addedCount === 0 показывается честное сообщение и кнопка «Перейти в корзину» НЕ выводится
    - При addedCount > 0 есть кнопка «Перейти в корзину →» на /catalog/{secret}/cart
    - Нет слов «статус/принято/подтверждено/в доставке»; заголовок экрана «Отправленные заказы» сохранён
    - Нет новых сетевых вызовов в логике повтора; данные берутся из useCatalogSyncContext и useOrderHistoryContext
    - `npx tsc --noEmit` завершается с кодом 0
  </acceptance_criteria>
  <done>Экран заказов получил кнопку «Повторить» и честную сводку результата с переходом в корзину; повтор использует актуальный каталог и форму цен; инварианты v1.5 соблюдены; типизация проходит.</done>
</task>

<task type="auto">
  <name>Task 2: Вход на «Мои заказы» из меню настроек (REORD-04)</name>
  <read_first>
    - components/SettingsPanel.tsx (структура панели, паттерн пунктов, useRole, setPanelOpen)
    - app/catalog/[secret]/cart/page.tsx (существующая ссылка на /orders — проверить, что вход из корзины уже есть)
    - lib/useRole.tsx (role, ready — SSR-safe гейт)
    - .planning/phases/19-reorder-my-orders/19-CONTEXT.md (D-08, D-10)
  </read_first>
  <action>
    D-08 — добавить в `components/SettingsPanel.tsx` пункт-ссылку «Мои заказы», ведущий на экран истории заказов. Без 4-й вкладки в нижних табах (табы остаются 3). Путь к экрану: `/catalog/${secret}/orders`. SettingsPanel сейчас не получает `secret` — определить путь надёжно: либо взять базовый путь каталога из `usePathname()` (срезать хвосты `/cart`/`/orders`, как делает MaxOrderButton через replace), либо принять `secret` пропом, если компонент монтируется с доступным секретом — выбрать менее инвазивный вариант после чтения места монтирования SettingsPanel. Ссылка оформляется в стиле существующих пунктов панели (Tailwind, как кнопка «Установить приложение»). При клике закрывать панель (`setPanelOpen(false)`).

    D-10 — пункт «Мои заказы» как клиентский акцент: показывать его всегда (экран доступен в обеих ролях, D-06), но если нужна завязка на роль — читать роль только после `ready` из `useRole`, до этого нейтральный дефолт, чтобы не было ошибки гидратации. Поскольку экран безвреден в обеих ролях, допустимо показывать пункт безусловно — тогда завязка на роль не требуется (предпочтительно: меньше риска гидратации). Проверить, что вход из корзины (ссылка/таблетка «История заказов» на `/orders`) уже присутствует в cart/page.tsx — если да, не дублировать, REORD-04 закрывается связкой «меню + корзина».
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - В SettingsPanel есть пункт «Мои заказы», ведущий на /catalog/{secret}/orders
    - Путь к экрану формируется корректно (через usePathname-срезку хвоста или проброшенный secret), без захардкоженного секрета
    - Клик по пункту закрывает панель настроек
    - Вход из корзины на /orders подтверждён существующим (не дублируется лишний раз)
    - Если используется роль для условного показа — она читается только после ready (нет ошибки гидратации); иначе пункт показывается безусловно
    - `npx tsc --noEmit` завершается с кодом 0
  </acceptance_criteria>
  <done>В меню настроек появился вход «Мои заказы»; вместе с входом из корзины REORD-04 закрыт для роли «Клиент»; 4-я вкладка не добавлена; гидратация не нарушена; типизация проходит.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage/IndexedDB → UI | История заказов (localStorage) и каталог (IDB) — доверенный источник того же происхождения, читаются через провайдеры этапов 11/16 |
| UI рендер → пользователь | Имена товаров и причины из истории/каталога выводятся в сводку повтора |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | сводка повтора (рендер имён товаров) | accept | React по умолчанию экранирует текстовое содержимое; имена выводятся как текст ({item.name}), без dangerouslySetInnerHTML — XSS не вносится |
| T-19-04 | Information Disclosure | содержимое заказа | mitigate | Логика повтора не отправляет содержимое заказа во внешние сервисы и не логирует его; всё в памяти клиента |
| T-19-05 | Spoofing/Hydration | вход на «Мои заказы» (роль) | mitigate | Любая завязка на роль — только после `ready` из useRole (D-10); предпочтительно показывать пункт безусловно (экран безвреден в обеих ролях) |

Новой серверной поверхности атаки этап не вводит: чистый клиент. Секретная UUID-ссылка и серверная проверка `params.secret` не меняются (D-09).
</threat_model>

<verification>
- `npx tsc --noEmit` — типизация всего проекта проходит (код 0)
- Кнопка «Повторить» присутствует на карточке заказа; сводка показывает 4 исхода честно
- Пункт «Мои заказы» присутствует в SettingsPanel; вход из корзины не сломан
- В коде экрана заказов нет слов «статус/принято/подтверждено/в доставке» (D-11)
</verification>

<success_criteria>
- Клиент повторяет прошлый заказ в один тап: added/price_changed уходят в корзину, out_of_stock/unavailable честно помечены
- Сводка результата понятна, при addedCount===0 корзина не открывается
- Вход на «Мои заказы» доступен из меню и из корзины (REORD-04)
- Экран «Мои заказы» (REORD-01) проверен и работает с существующими полями (дата-время, сумма, позиции, канал, пустое состояние)
- Инварианты v1.5 (офлайн-безопасность, force-dynamic, SSR-safe роль, история ≠ статус) соблюдены
</success_criteria>

<output>
Create `.planning/phases/19-reorder-my-orders/19-02-SUMMARY.md` when done
</output>
