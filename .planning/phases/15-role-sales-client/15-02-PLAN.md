---
phase: 15-role-sales-client
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - components/HeaderPrimaryAction.tsx
  - components/CardCornerButton.tsx
  - components/CatalogSettings.tsx
  - lib/useDeviceClass.ts
autonomous: true
requirements: [ROLE-03]

must_haves:
  truths:
    - "Главное действие в шапке следует роли: sales → ↻ SyncButton, client → ♥ FavoritesIcon"
    - "Угловое действие карточки следует роли: sales (+есть фото) → стрелки/Lightbox, иначе → ♥ избранное"
    - "Дефолтная плотность сетки по роли: sales → 3x4, client → 2x3; сохранённый пользователем gridPreset остаётся главнее"
    - "Кейс «торговый с телефона» / «клиент с планшета» решается сменой роли, а не типом устройства"
    - "Ни один из трёх компонентов больше не использует isTabletLike для ветвления опыта"
  artifacts:
    - path: "components/HeaderPrimaryAction.tsx"
      provides: "Ветвление шапки по роли"
      contains: "role === \"sales\""
    - path: "components/CardCornerButton.tsx"
      provides: "Ветвление углового действия карточки по роли"
      contains: "role === \"sales\""
    - path: "components/CatalogSettings.tsx"
      provides: "Дефолт gridPreset по роли"
      contains: "userRole"
  key_links:
    - from: "components/HeaderPrimaryAction.tsx"
      to: "lib/useRole.ts"
      via: "useRole() вместо useDeviceClass()"
      pattern: "useRole\\(\\)"
    - from: "components/CardCornerButton.tsx"
      to: "lib/useRole.ts"
      via: "useRole() вместо useDeviceClass()"
      pattern: "useRole\\(\\)"
    - from: "components/CatalogSettings.tsx"
      to: "localStorage(userRole)"
      via: "чтение роли для дефолта gridPreset"
      pattern: "userRole"
---

<objective>
Перевести три точки ветвления опыта с типа устройства (`isTabletLike`) на роль (`role === 'sales'`): главное действие в шапке (`HeaderPrimaryAction.tsx`), угловое действие карточки (`CardCornerButton.tsx`), дефолт плотности сетки (`CatalogSettings.tsx`). Решить судьбу `useDeviceClass` после переноса.

Purpose: Завершить цель этапа — поведение интерфейса следует роли, а не устройству (ROLE-03). После этого «торговый с телефона» и «клиент с планшета» получают свой опыт сменой роли в настройках.
Output: изменённые `HeaderPrimaryAction.tsx`, `CardCornerButton.tsx`, `CatalogSettings.tsx`; решение по `lib/useDeviceClass.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-role-sales-client/15-CONTEXT.md
@.planning/phases/15-role-sales-client/15-PATTERNS.md

# Источник правды контракта роли (создан в плане 15-01):
@lib/useRole.ts

# Изменяемые файлы и эталоны:
@components/HeaderPrimaryAction.tsx
@components/CardCornerButton.tsx
@components/CatalogSettings.tsx
@lib/useDeviceClass.ts

<interfaces>
<!-- Контракт хука роли, реализованный в плане 15-01. Используйте напрямую. -->

Из lib/useRole.ts:
```typescript
export type UserRole = 'sales' | 'client';
export function useRole(): { role: UserRole; setRole: (r: UserRole) => void; ready: boolean };
```
Ключ localStorage роли: `"userRole"` (значения `"sales"` | `"client"`).

Текущий контракт useDeviceClass (то, что заменяем):
```typescript
export function useDeviceClass(): { isTabletLike: boolean; ready: boolean };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Перевести HeaderPrimaryAction и CardCornerButton на роль</name>
  <read_first>
    - components/HeaderPrimaryAction.tsx — текущее ветвление isTabletLike (весь файл, 17 строк)
    - components/CardCornerButton.tsx — текущее ветвление isTabletLike (строки 28, 38)
    - lib/useRole.ts — контракт useRole() (создан в 15-01)
    - 15-PATTERNS.md секции «HeaderPrimaryAction.tsx» и «CardCornerButton.tsx» (готовые целевые фрагменты)
  </read_first>
  <action>
    В components/HeaderPrimaryAction.tsx: заменить импорт `useDeviceClass` из "@/lib/useDeviceClass" на `import { useRole } from "@/lib/useRole"`. Заменить `const { isTabletLike, ready } = useDeviceClass();` на `const { role, ready } = useRole();`. Сохранить ветку `if (!ready) return <div className="h-9 w-9" />;` без изменений (ready-флаг здесь нужен, т.к. SyncButton и FavoritesIcon заметно разные). Заменить тернарник на `return role === "sales" ? <SyncButton /> : <FavoritesIcon secret={secret} />;`. Обновить комментарий-шапку: вместо «планшет/телефон» — «sales → ↻ Обновить, client → ♥ Избранное».

    В components/CardCornerButton.tsx: заменить импорт `useDeviceClass` на `import { useRole } from "@/lib/useRole"`. Заменить `const { isTabletLike } = useDeviceClass();` на `const { role } = useRole();` — ready НЕ деструктурировать и НЕ использовать: дефолт client до монтирования = сердечко, что является корректным безопасным поведением при гидратации (см. 15-PATTERNS.md). Заменить условие `if (isTabletLike && canOpenPhoto)` на `if (role === "sales" && canOpenPhoto)`. Обновить комментарии-шапку и метки веток: «Планшет с фото» → «Агент (sales) с фото», «Телефон/ПК» → «Клиент». Логику кнопок (стрелки/сердечко) и классы не трогать.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - components/HeaderPrimaryAction.tsx содержит `import { useRole } from "@/lib/useRole"`
    - components/HeaderPrimaryAction.tsx НЕ содержит `isTabletLike` и НЕ содержит `useDeviceClass`
    - components/HeaderPrimaryAction.tsx содержит `role === "sales"` в тернарнике возврата
    - components/HeaderPrimaryAction.tsx сохраняет `if (!ready) return`
    - components/CardCornerButton.tsx содержит `import { useRole } from "@/lib/useRole"`
    - components/CardCornerButton.tsx НЕ содержит `isTabletLike` и НЕ содержит `useDeviceClass`
    - components/CardCornerButton.tsx содержит `role === "sales" && canOpenPhoto`
    - npm run build завершается с кодом 0
  </acceptance_criteria>
  <done>Шапка и угол карточки ветвятся по роли: sales→SyncButton/стрелки, client→FavoritesIcon/сердечко; isTabletLike удалён из обоих компонентов; сборка проходит.</done>
</task>

<task type="auto">
  <name>Task 2: Дефолт gridPreset по роли + решение по useDeviceClass</name>
  <read_first>
    - components/CatalogSettings.tsx — текущая эвристика дефолта gridPreset по ширине экрана (строки 107–115)
    - lib/useRole.ts — ключ localStorage "userRole" (создан в 15-01)
    - lib/useDeviceClass.ts — кандидат на удаление/спящий статус
    - 15-PATTERNS.md секция «components/CatalogSettings.tsx» (готовый целевой фрагмент)
  </read_first>
  <action>
    В components/CatalogSettings.tsx в useEffect загрузки сохранённых настроек заменить блок дефолта gridPreset (строки 107–115). Сохранённый пользователем выбор остаётся главнее: если localStorage.getItem("gridPreset") равен "2x3" | "3x4" | "4x6" — применять его через setGridPreset(p) как раньше (D-10). Иначе (нет сохранённого выбора) вычислять дефолт ПО РОЛИ: прочитать `const savedRole = localStorage.getItem("userRole");` и вызвать `setGridPreset(savedRole === "sales" ? "3x4" : "2x3");` — sales→3x4, client/прочее→2x3. НЕ записывать этот дефолт обратно в localStorage (как и текущая адаптивная ветка — остаётся «не зафиксированным», пока пользователь не выберет сам). Удалить старую ветку с window.matchMedia("(max-width: 767px)"). Читать роль НАПРЯМУЮ из localStorage (не через useRole()), потому что этот useEffect синхронно выполняется при монтировании независимо от контекста роли — комментарий об этом на русском.

    Решение по lib/useDeviceClass.ts (D-05, Claude's Discretion): после этого плана у useDeviceClass не остаётся потребителей. Проверить grep'ом отсутствие импортов useDeviceClass по всему проекту; если потребителей нет — УДАЛИТЬ файл lib/useDeviceClass.ts (чистота кода предпочтительнее «спящего» мёртвого файла; восстановим из git при необходимости). Если grep неожиданно найдёт оставшегося потребителя — оставить файл и зафиксировать это в SUMMARY.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - components/CatalogSettings.tsx содержит `localStorage.getItem("userRole")`
    - components/CatalogSettings.tsx содержит `setGridPreset(` с веткой `=== "sales" ? "3x4" : "2x3"` (или эквивалентной логикой sales→3x4, иначе 2x3)
    - components/CatalogSettings.tsx сохраняет приоритет сохранённого gridPreset (ветка `p === "2x3" || p === "3x4" || p === "4x6"` остаётся перед дефолтом по роли)
    - components/CatalogSettings.tsx НЕ содержит `matchMedia("(max-width: 767px)")` в блоке дефолта gridPreset
    - `grep -rn "useDeviceClass" --include="*.ts" --include="*.tsx" .` (исключая node_modules/.next) не находит активных импортов; если файл удалён — он отсутствует
    - npm run build завершается с кодом 0
  </acceptance_criteria>
  <done>Дефолт сетки вычисляется по роли (sales→3x4, client→2x3), сохранённый выбор главнее; useDeviceClass удалён (или оставлен с фиксацией причины); сборка проходит.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → приложение | CatalogSettings читает роль из localStorage("userRole") для дефолта сетки; источник локальный, не сетевой |

Поверхность атаки минимальна и идентична плану 15-01: роль — локальная настройка отображения. Этот план лишь переносит ветвление UI с устройства на роль; новых сетевых поверхностей, серверной логики, персональных данных или проверок прав не вводится. Ветвление по роли не открывает доступ к защищаемым ресурсам (их нет).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | CatalogSettings чтение userRole | mitigate | Дефолт gridPreset: любое значение userRole кроме "sales" трактуется как client (2x3) — повреждённое значение безопасно деградирует |
| T-15-05 | Elevation of Privilege | ветвление UI по role==='sales' | accept | Ветка sales показывает SyncButton/стрелки — это UI-пресет, не привилегия; защищаемого ресурса нет (логин агента → v2.0) |
</threat_model>

<verification>
- `npm run build` проходит без ошибок
- grep подтверждает: `isTabletLike` отсутствует в HeaderPrimaryAction.tsx и CardCornerButton.tsx; активных импортов useDeviceClass в проекте нет
- Ручная проверка (опционально, UI hint): в настройках переключить роль на «Агент» → в шапке появляется ↻ (SyncButton), на карточке с фото — стрелки; переключить на «Клиент» → ♥ в шапке и сердечко на карточке. На телефоне с ролью «Агент» дефолт сетки 3x4 (если пользователь ранее не выбирал плотность вручную)
</verification>

<success_criteria>
- ROLE-03: угловое действие карточки, главное действие в шапке и дефолтная плотность сетки следуют роли, а не типу устройства
- Кейс «торговый с телефона» / «клиент с планшета» решается сменой роли в настройках
- Сохранённый пользователем gridPreset остаётся приоритетнее дефолта по роли
- useDeviceClass больше не определяет опыт (удалён или спящий с фиксацией причины)
</success_criteria>

<output>
Создать `.planning/phases/15-role-sales-client/15-02-SUMMARY.md` по завершении.
</output>
