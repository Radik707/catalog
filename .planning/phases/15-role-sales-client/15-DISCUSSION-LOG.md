# Phase 15: Роль «Торговый / Клиент» - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-27
**Phase:** 15-role-sales-client
**Areas discussed:** Вид переключателя роли, Видимость текущей роли, «Клиент с большого экрана» (плотность), Первый заход, Логин агента / дефолт роли

---

## Вид переключателя роли

| Option | Description | Selected |
|--------|-------------|----------|
| Сегмент «Торговый \| Клиент», первым, подпись «Роль» | Стиль как «Форма 1-я/2-я», первым в панели | ✓ (с правкой подписи) |
| Такой же сегмент, но в конце панели | Не выпячивать роль | |
| Иконки вместо слов | Компактнее, менее очевидно | |

**User's choice:** Сегмент, но с подписью **«Агент»** вместо «Торговый» → «Клиент | Агент». По умолчанию — клиент; агент переключается в настройках вручную.
**Notes:** Пользователь добавил видение: переключение на агента в будущем требует личного логина и перестраивает кабинет под него (персональные картинки и т.п.). Разобрано отдельным вопросом — отнесено к v2.0 (аккаунты).

---

## Видимость текущей роли

| Option | Description | Selected |
|--------|-------------|----------|
| Только в настройках, без ярлыка в шапке | Интерфейс и так сигналит ролью | ✓ |
| Маленький ярлык в шапке всегда виден | Ясно всегда, но занимает место | |
| Ярлык только для торгового | Клиенту не нужен | |

**User's choice:** Только в настройках, ярлыка в шапке нет.
**Notes:** —

---

## «Клиент с большого экрана» (дефолт плотности сетки)

| Option | Description | Selected |
|--------|-------------|----------|
| По роли: агент 3×4, клиент 2×3 | Дефолт по роли; сохранённый выбор главнее | ✓ |
| Оставить адаптив по ширине (как сейчас) | Проще, но обещание «по роли» не работает | |
| По роли, но клиенту на ПК — 3×4 | Гибрид, сложнее объяснить | |

**User's choice:** По роли: агент 3×4, клиент 2×3.
**Notes:** Сохранённый пользователем `gridPreset` по-прежнему перекрывает дефолт.

---

## Первый заход

| Option | Description | Selected |
|--------|-------------|----------|
| Тихо угадать/открыть, без подсказок | Ноль трения, смена — в настройках | ✓ |
| Разовая ненавязчивая подсказка | Помогает найти переключатель, но шум | |
| Спросить роль явно при первом заходе | Барьер на входе для B2B-каталога | |

**User's choice:** Тихо открыть клиентский вид, без окон и подсказок.
**Notes:** Идею с экраном-выбором «Клиент/Агент» при входе пользователь рассмотрел в ответе про переключатель и сознательно отклонил («усложняет, может запутать»).

---

## Логин агента / дефолт роли (уточняющий раунд)

| Option | Description | Selected |
|--------|-------------|----------|
| Сейчас без логина — логин в v2.0 | Роль = вид интерфейса; аккаунты/логин/персонализация — v2.0 | ✓ |
| Простой пароль уже сейчас | Бутафорская защита, раздувает объём этапа | |

**User's choice:** Сейчас без логина — логин в v2.0.

| Option | Description | Selected |
|--------|-------------|----------|
| Всегда «Клиент», агент — ручной выбор | Меняет ROLE-01 (убирает планшет→Агент) | ✓ |
| Планшет→Агент автоматически (как ROLE-01) | Агенту не надо переключать, но клиент на планшете попадёт в агентский вид | |

**User's choice:** Всегда «Клиент», агент — ручной выбор. (ROLE-01 и критерий №1 в ROADMAP обновлены.)

---

## Claude's Discretion

- Точные классы/отступы сегмента «Клиент | Агент» (по образцу существующих сегментов панели).
- Имена ключа localStorage и форма хука `useRole` (по аналогии с `viewMode`/`gridPreset`).
- Судьба `useDeviceClass` после переноса ветвлений (удалить vs оставить спящим).

## Deferred Ideas

- Логин/аккаунт агента + персонализация кабинета (свои картинки/настройки) → веха v2.0 (аккаунты).
- Экран-выбор «Клиент/Агент» при первом входе — рассмотрен и отклонён.
- Роль-зависимый дефолт сортировки (SORT-04) — будущее улучшение.
