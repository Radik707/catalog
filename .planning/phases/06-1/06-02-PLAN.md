---
phase: 06-1
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - components/SectionBar.tsx
  - components/CatalogView.tsx
  - app/catalog/[secret]/page.tsx
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05]
user_setup: []
must_haves:
  truths:
    - "На витрине вместо плоских кнопок-групп показывается прилипающая полоса из 6 разделов (Новинки + 5)"
    - "Все товары видны сразу одной длинной прокручиваемой страницей, сгруппированные заголовками раздел → подгруппа"
    - "Тап по разделу в полосе плавно перематывает страницу к заголовку этого раздела"
    - "Активный раздел в полосе автоматически подсвечивается при прокрутке (scroll-spy)"
    - "Разделы и подгруппы идут в порядке появления в данных (Новинки первым, Энергетики последними в Напитках)"
    - "При непустом поиске ИЛИ фильтре Хит/Новинка показывается плоский список, полоса и группировка скрыты"
    - "Товары с пустым полем «Раздел» попадают в раздел «Новинки» — ничего не теряется"
  artifacts:
    - path: "components/SectionBar.tsx"
      provides: "Прилипающая полоса разделов со scroll-spy и плавной перемоткой к якорям"
      min_lines: 30
      contains: "IntersectionObserver"
    - path: "components/CatalogView.tsx"
      provides: "Группировка раздел→подгруппа из данных, рендер заголовков-якорей, ветка плоского списка"
      contains: "section-"
    - path: "app/catalog/[secret]/page.tsx"
      provides: "Передача products в CatalogView без initialCategory"
  key_links:
    - from: "components/CatalogView.tsx"
      to: "components/SectionBar.tsx"
      via: "import + рендер SectionBar"
      pattern: "import SectionBar"
    - from: "components/SectionBar.tsx"
      to: "заголовки разделов в CatalogView"
      via: "id=section-${section} + scrollIntoView/IntersectionObserver"
      pattern: "section-"
    - from: "components/CatalogView.tsx"
      to: "Product.section / Product.subgroup"
      via: "группировка useMemo"
      pattern: "p\\.(section|subgroup)"
---

<objective>
Перестроить навигацию витрины с плоских 14 групп на двухуровневую модель «меню-якорь»: единая длинная прокручиваемая страница, сгруппированная заголовками **раздел → подгруппа** в порядке из данных; прилипающая сверху полоса из 6 разделов (Новинки + 5) с плавной перемоткой по тапу и автоподсветкой активного раздела (scroll-spy). Поиск и фильтры Хит/Новинка переключают витрину в плоский список (D-06).

Purpose: Это центральная ценность Этапа 6 — покупатель «видит весь магазин, разложенный по полкам, листает». Реализует NAV-01..05. Использует поля `section`/`subgroup`, добавленные планом 01.
Output: Новый `SectionBar.tsx`; перестроенный `CatalogView.tsx`; очищенный `page.tsx`; `npm run build` exits 0; checkpoint визуальной проверки на телефоне.

## Phase Goal

**As a** покупатель-владелец магазина, **I want to** видеть весь каталог разложенным по 6 крупным разделам с прилипающей полосой навигации и плавной перемоткой, **so that** я быстро нахожу нужную полку и оцениваю ассортимент, не теряясь в 14 плоских кнопках.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-1/06-CONTEXT.md
@.planning/phases/06-1/06-PATTERNS.md
@.planning/phases/06-1/06-01-SUMMARY.md

<interfaces>
<!-- Контракты, которые исполнитель использует напрямую. -->

Тип (после плана 01) — lib/types.ts:
```typescript
export interface Product {
  id: string; name: string; price: number; stock: number;
  category: string; group: string; supplier: string;
  badge?: string; imageUrl?: string; description?: string;
  subgroup?: string; // J «Подгруппа»
  section?: string;  // K «Раздел»
}
```

Текущий CatalogView (что перестраиваем) — components/CatalogView.tsx:
- "use client"; useState/useMemo; импортирует ProductCard, CategoryFilter, SearchBar, ScrollToTop, Lightbox, useCatalogSettings/PRESENTATION_PRESETS из ./CatalogSettings
- props: { products: Product[]; initialCategory?: string }
- состояние: activeGroup, search, lightboxIndex
- фильтр stock>1 + по группе + по поиску; photoProducts = filtered с imageUrl; openLightbox(product)
- containerClass зависит от viewMode (list/presentation/grid) и preset.cols
- рендер: ScrollToTop, CategoryFilter, SearchBar (count={filtered.length}), сетка ProductCard, Lightbox

ProductCard используется как есть (НЕ менять) — текущий вызов:
```tsx
<ProductCard
  key={product.id}
  product={product}
  showPhotos={showPhotos}
  showPrices={showPrices}
  viewMode={viewMode}
  onPhotoOpen={() => openLightbox(product)}
  presentationSizes={viewMode === "presentation" ? preset.sizes : undefined}
/>
```

SearchBar (как есть): props { value, onChange, count }.
ScrollToTop (как есть): props { viewMode: "list" | "grid" }.
Lightbox (как есть): props { products, index, onIndexChange, onClose }.

Текущая CategoryFilter (sticky top-0) и текущий page.tsx (фильтр ?filter=hit|new, передаёт initialCategory) — см. 06-PATTERNS.md.

Шапка layout.tsx: `sticky top-0 z-50 ... h-12` (48px) — полоса разделов прилипает под ней через `top-12`.

Порядок разделов на витрине (D-05 + structure_map.json):
Новинки → Сладкое → Напитки → Крупы, лапша, бакалея → Соусы и консервация → Снэки и прикассовое.
Порядок приезжает из данных (товары уже отсортированы upload.py) — витрина выводит разделы/подгруппы в порядке ПЕРВОГО появления, плюс «Новинки» искусственно первым.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Создать компонент SectionBar (прилипающая полоса + scroll-spy)</name>
  <read_first>
    - components/CategoryFilter.tsx (аналог: sticky-контейнер строки 14-16, стили кнопок-таблеток строки 27-39 — копировать стили как есть)
    - components/ScrollToTop.tsx (аналог: useEffect + window-подписка + ОБЯЗАТЕЛЬНАЯ очистка в return, строки 16-41)
    - .planning/phases/06-1/06-PATTERNS.md §«SectionBar.tsx» и §«Sticky-полоса под шапкой» (top-12), §«Якорные id»
    - app/catalog/[secret]/layout.tsx (высота шапки h-12 — почему top-12)
  </read_first>
  <files>components/SectionBar.tsx</files>
  <action>
    Создать новый клиентский компонент `SectionBar` (PascalCase, default export, директива `"use client"`, комментарии на русском).
    Пропсы: `sections: string[]` (разделы в порядке появления), `activeSection: string` (текущий активный), `onSectionChange: (section: string) => void` (клик = плавная перемотка).
    Разметка: sticky-контейнер с классами по аналогу CategoryFilter, НО `sticky top-12 z-10` (под синей шапкой h-12, не top-0), `bg-white border-b border-gray-200 shadow-sm`, внутренний `flex gap-2 px-4 py-2.5 overflow-x-auto scrollbar-hide`. Кнопка на каждый раздел: классы-таблетки из CategoryFilter (`flex-shrink-0 px-3.5 py-1.5 rounded-full text-sm font-medium transition-colors whitespace-nowrap`), активная = `bg-blue-600 text-white`, неактивная = `bg-gray-100 text-gray-600 active:bg-gray-200`. Активность определяется сравнением `activeSection === section`. По клику кнопки вызывать `onSectionChange(section)` И выполнять плавную перемотку: найти `document.getElementById(\`section-${section}\`)` и `el?.scrollIntoView({ behavior: "smooth", block: "start" })`.
    Scroll-spy: в `useEffect` создать `IntersectionObserver` с `{ rootMargin: "-50% 0px -50% 0px", threshold: 0 }`; в колбэке для пересекающих entries вызывать `onSectionChange(id)` где id = `entry.target.id` без префикса `section-`. Наблюдать все элементы `document.getElementById(\`section-${s}\`)` для каждого s из `sections`. В `return` обязательно `observer.disconnect()`. Зависимость эффекта: `[sections, onSectionChange]` (или мемоизированный колбэк — на усмотрение исполнителя, без лишних ре-подписок).
    Якорный id-контракт: `section-${section}` — ровно тот же формат, что используется в Task 2 для заголовков. Никаких новых пропсов кроме перечисленных.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: файл начинается с `"use client"` и экспортирует default-функцию `SectionBar`
    - Source: содержит `sticky top-12` (НЕ `top-0`)
    - Source: содержит `IntersectionObserver` и `disconnect()` в cleanup
    - Source: содержит `scrollIntoView` с `behavior: "smooth"`
    - Source: использует id-формат `section-`
    - Build: `npx tsc --noEmit` без ошибок
  </acceptance_criteria>
  <done>SectionBar существует, типизируется; sticky под шапкой; клик перематывает к якорю, scroll-spy через IntersectionObserver с очисткой.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Перестроить CatalogView — группировка раздел→подгруппа + плоский список</name>
  <read_first>
    - components/CatalogView.tsx (файл, который полностью перестраиваем — весь, 137 строк)
    - components/SectionBar.tsx (созданный в Task 1 — его пропсы)
    - .planning/phases/06-1/06-PATTERNS.md §«CatalogView.tsx», §«Стили заголовков разделов/подгрупп», §«Якорные id», §«useMemo для вычислений»
    - lib/types.ts (поля section/subgroup из плана 01)
  </read_first>
  <files>components/CatalogView.tsx</files>
  <action>
    Перестроить CatalogView, сохранив структуру (use client, useCatalogSettings, Lightbox, ScrollToTop, SearchBar, ProductCard, containerClass) и заменив навигацию.

    Импорты: заменить `import CategoryFilter from "./CategoryFilter"` на `import SectionBar from "./SectionBar"`. Добавить `useState` для `activeSection`.
    Пропсы: убрать `initialCategory` — оставить только `{ products: Product[] }`. Удалить состояние `activeGroup`.
    Удалить массив `GROUP_ORDER` (строки 18-32) и `groups`-useMemo полностью (порядок теперь из данных, D-04).

    Группировка (useMemo, зависимость `[products]`): построить `Map<string, Map<string, Product[]>>` — раздел → подгруппа → товары. Перебирать `products.filter(p => p.stock > 1)` В ИСХОДНОМ ПОРЯДКЕ (порядок = из данных). Для каждого товара: `sec = p.section || "Новинки"` (fallback по D-05 — пустой раздел → Новинки); `sub = p.subgroup || p.category || "—"`. Map сохраняет порядок вставки ключей = порядок первого появления = требуемый порядок (NAV-04). Дополнительно гарантировать «Новинки» первым разделом: если раздел «Новинки» присутствует, он должен идти первым в итоговом порядке (например, собрать порядок ключей так, чтобы «Новинки» был спереди; на усмотрение исполнителя — пересоздать Map с «Новинки» в начале либо упорядочить entries при рендере).
    `sections` для полосы: массив ключей верхнего уровня в этом порядке.

    Ветка плоского списка (D-06): `isFlat = Boolean(search.trim())` (фильтр Хит/Новинка приходит через `props.products`, уже отфильтрованные в page.tsx — поэтому badge-фильтр НЕ дублировать здесь; именно поиск переключает в плоский режим, а при активном фильтре товаров в одном разделе и так мало — но чтобы фильтр Хит/Новинка тоже давал плоский список, см. ниже). Чтобы выполнить D-06 полностью (плоский список и при поиске, И при фильтре Хит/Новинка): добавить в пропсы опциональный `isFiltered?: boolean` и считать `isFlat = Boolean(search.trim()) || Boolean(isFiltered)`. `flatFiltered` (useMemo, `[products, search]`): `products.filter(p => p.stock > 1 && p.name.toLowerCase().includes(search.trim().toLowerCase()))`.

    photoProducts/openLightbox/Lightbox: сохранить, но источник — текущий видимый набор. Проще: считать `photoProducts` из плоско-развёрнутого видимого списка (в плоском режиме — flatFiltered; в групповом — все видимые товары stock>1 в порядке данных). openLightbox по `findIndex` id. Lightbox-блок скопировать как есть.

    containerClass — скопировать без изменений (list/presentation/grid).

    Рендер:
    - `<ScrollToTop viewMode={...} />` — как есть.
    - `<SearchBar value={search} onChange={setSearch} count={...} />` — count = (isFlat ? flatFiltered.length : общее число видимых товаров).
    - Если `!isFlat`: `<SectionBar sections={sections} activeSection={activeSection} onSectionChange={setActiveSection} />`. В плоском режиме SectionBar НЕ рендерить (D-06 — полоса скрыта).
    - Контент:
      - `isFlat` → один `<div className={containerClass}>` с `flatFiltered.map(p => <ProductCard ... />)`; пустой результат → блок «Ничего не найдено» (скопировать существующий).
      - иначе → перебор разделов: для каждого `[section, subMap]` рендерить `<h2 id={\`section-${section}\`} className="text-lg font-bold text-gray-800 px-4 pt-5 pb-2">{section}</h2>`; внутри перебор подгрупп `[subgroup, items]`: `<h3 className="text-sm font-semibold text-gray-500 px-4 pt-3 pb-1.5 uppercase tracking-wide">{subgroup} <span className="font-normal text-gray-400">({items.length})</span></h3>` и `<div className={containerClass}>{items.map(p => <ProductCard ... />)}</div>`. ProductCard вызывать ТЕМИ ЖЕ пропсами, что сейчас (showPhotos/showPrices/viewMode/onPhotoOpen/presentationSizes). Пустые подгруппы/разделы не возникают (в Map попадают только товары stock>1).
    - id-якорь заголовка раздела — строго `section-${section}`, совпадает с SectionBar.
    Комментарии на русском. НЕ менять ProductCard, CatalogSettings, Lightbox, SearchBar, ScrollToTop.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: импортирует `SectionBar`, НЕ импортирует `CategoryFilter`
    - Source: НЕ содержит `GROUP_ORDER` и `initialCategory`
    - Source: строит группировку по `p.section` и `p.subgroup` с fallback на «Новинки»
    - Source: заголовки разделов имеют `id={\`section-${section}\`}`
    - Source: подгруппы показывают счётчик `({items.length})` или эквивалент
    - Source: ветка `isFlat` рендерит плоский список и скрывает SectionBar
    - Build: `npx tsc --noEmit` без ошибок
  </acceptance_criteria>
  <done>CatalogView группирует товары раздел→подгруппа в порядке данных с «Новинки» первым, рендерит SectionBar и заголовки-якоря, переключается в плоский список при поиске/фильтре; типизация проходит.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Очистить page.tsx и собрать проект</name>
  <read_first>
    - app/catalog/[secret]/page.tsx (файл, который меняем — весь, 28 строк)
    - components/CatalogView.tsx (обновлённые пропсы из Task 2 — initialCategory убран, добавлен isFiltered)
    - .planning/phases/06-1/06-PATTERNS.md §«page.tsx»
  </read_first>
  <files>app/catalog/[secret]/page.tsx</files>
  <action>
    Сохранить серверную логику: проверку `params.secret !== process.env.CATALOG_SECRET → notFound()`, `revalidate = 300`, `getProducts()`, фильтр `?filter=hit|new` по badge. Убрать передачу `initialCategory` в `<CatalogView>` (навигация теперь через scroll-to-anchor, не через URL). Вычислить флаг активного фильтра: `const isFiltered = searchParams.filter === "hit" || searchParams.filter === "new"` и передать `<CatalogView products={products} isFiltered={isFiltered} />` (D-06 — фильтр Хит/Новинка даёт плоский список). Убрать `category` из типа `searchParams` если он больше не используется (текущий тип: `{ filter?: string; category?: string }` → `{ filter?: string }`). Комментарии на русском.
    После правки запустить полную сборку (PowerShell): `npm run build`. Устранить ошибки сборки в рамках файлов этого плана (SectionBar/CatalogView/page).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `page.tsx` НЕ передаёт `initialCategory` в CatalogView
    - Source: передаёт `isFiltered` в CatalogView
    - Source: проверка `CATALOG_SECRET` и `notFound()` сохранены
    - Build: `npm run build` завершается с кодом 0
  </acceptance_criteria>
  <done>page.tsx передаёт products и isFiltered без initialCategory; gate секрета цел; npm run build проходит.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Визуальная проверка двухуровневой навигации на телефоне</name>
  <what-built>
    Двухуровневая навигация витрины: прилипающая полоса из 6 разделов (Новинки + Сладкое + Напитки + Крупы/лапша/бакалея + Соусы и консервация + Снэки и прикассовое), длинная прокручиваемая страница с заголовками раздел → подгруппа, плавная перемотка по тапу, scroll-spy подсветка, плоский список при поиске/фильтре Хит/Новинка.
  </what-built>
  <action>Запустить локально и вручную проверить навигацию по шагам ниже; дождаться сигнала владельца перед продолжением.</action>
  <how-to-verify>
    1. Запустить локально: `npm run dev`, открыть каталог по секретной ссылке (`/catalog/<CATALOG_SECRET>`) на телефоне или в мобильной эмуляции браузера.
    2. Полоса разделов: сверху под синей шапкой видна прилипающая полоса; первая кнопка — «Новинки», далее Сладкое → Напитки → Крупы, лапша, бакалея → Соусы и консервация → Снэки и прикассовое (именно в этом порядке).
    3. Прокрутка: листая страницу вниз, видно крупные заголовки разделов и под ними подгруппы со счётчиками (например «Конфеты (15)»); товары — обычными карточками/в текущем режиме отображения.
    4. Тап по разделу в полосе → страница плавно перематывается к этому разделу.
    5. Scroll-spy: при ручной прокрутке активная кнопка в полосе подсвечивается (синяя) в такт тому, какой раздел сейчас на экране.
    6. Поиск: ввести слово в строку поиска → полоса разделов и заголовки исчезают, показывается плоский список найденного; очистить поиск → группировка возвращается.
    7. Вкладки в шапке: нажать «★ Хит» и «✦ Новинка» → плоский список товаров с бейджем; «Каталог» → возврат к группировке.
    8. Проверить, что переключатели вид (сетка/список/презентация) и просмотрщик фото работают как раньше.
  </how-to-verify>
  <resume-signal>Напишите «одобрено» или опишите, что выглядит/работает не так.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер клиента → Next.js страница | URL содержит CATALOG_SECRET; проверка на сервере |
| Поисковый ввод пользователя → клиентская фильтрация | строка поиска используется только для `.includes()` по name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Elevation of Privilege | page.tsx (CATALOG_SECRET gate) | mitigate | Проверка `params.secret !== process.env.CATALOG_SECRET → notFound()` сохранена без изменений; новых маршрутов не добавляется |
| T-06-04 | Injection (XSS) | поиск + заголовки section/subgroup | accept | React экранирует значения по умолчанию; section/subgroup рендерятся как текст в JSX (`{section}`), не через dangerouslySetInnerHTML; поиск используется только в `.includes()`, не в DOM-API/eval |
| T-06-05 | Tampering | id-якоря `section-${section}` | accept | id формируется из доверенных данных Sheet, используется только для scrollIntoView/IntersectionObserver внутри страницы |

Новой поверхности атаки нет: эндпоинты не добавляются, единственный пользовательский вход (поиск) уже существовал, секрет CATALOG_SECRET и серверный ключ API не меняются.
</threat_model>

<verification>
- `npx tsc --noEmit` без ошибок
- `npm run build` exits 0
- `components/SectionBar.tsx` существует, содержит `IntersectionObserver`, `top-12`, `section-`
- `components/CatalogView.tsx` импортирует SectionBar, не содержит `GROUP_ORDER`/`initialCategory`, группирует по section/subgroup
- `page.tsx` не передаёт initialCategory, передаёт isFiltered, gate секрета цел
- Checkpoint визуальной проверки одобрен
</verification>

<success_criteria>
Витрина показывает 6 разделов в прилипающей полосе; единая прокручиваемая страница раздел→подгруппа в порядке данных (Новинки первым); тап = плавная перемотка; scroll-spy подсветка; поиск/фильтр Хит/Новинка → плоский список; пустой раздел → Новинки; карточки/режимы/поиск работают как раньше. NAV-01..05 выполнены.
</success_criteria>

<output>
Create `.planning/phases/06-1/06-02-SUMMARY.md` when done
</output>
