---
phase: 05-структура-данных
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scripts/upload.py
  - scripts/test_upload_structure.py
autonomous: false
requirements: [STRUCT-01, STRUCT-02, STRUCT-03, STRUCT-04, STRUCT-05]

must_haves:
  truths:
    - "После прогона upload.py каждый товар получает непустые поля «Подгруппа» и «Раздел» (STRUCT-04, Success #1)"
    - "Старая колонка «Группа» в заголовке и строках Sheet не изменена (D-04 — нулевой риск для живой витрины)"
    - "Товары отсортированы по (раздел, подгруппа, категория) — кластеры «Подарочные» и «Энергетики» собираются автоматически (STRUCT-05/D-05)"
    - "Категория, отсутствующая в structure_map.json, попадает в «Прочее» с предупреждением в лог — ничего не теряется молча (D-12)"
    - "python scripts/upload.py --dry-run печатает человекочитаемый предпросмотр раздел→подгруппа→категории со счётчиками товаров (D-13)"
  artifacts:
    - path: "scripts/upload.py"
      provides: "load_structure_map(), build_category_index(), apply_structure_mapping(); две новые колонки Sheet; сортировка D-05; сводка D-12; предпросмотр D-13 в --dry-run"
      contains: "def load_structure_map"
    - path: "scripts/test_upload_structure.py"
      provides: "Pytest: индекс категорий, проставление подгруппы/раздела, fallback «Прочее», ключ сортировки, неизменность колонки «Группа»"
  key_links:
    - from: "scripts/upload.py main()"
      to: "scripts/structure_map.json"
      via: "load_structure_map() → build_category_index() → apply_structure_mapping() после apply_edit_memory(), перед products_to_rows()"
      pattern: "load_structure_map\\(\\)"
    - from: "apply_structure_mapping()"
      to: "строка Sheet"
      via: "p['display_subgroup']/p['display_section'] → два новых столбца в products_to_rows()"
      pattern: "display_subgroup|display_section"
---

<objective>
Дополнить `scripts/upload.py` так, чтобы он прочитал `structure_map.json` (план 05-01) и для КАЖДОГО товара проставил два НОВЫХ поля Sheet — «Подгруппа» и «Раздел» — рядом с нетронутой колонкой «Группа» (D-04, expand-contract: витрина переключится на новые поля только на этапе 6). Это исполняющий (end-to-end) ломтик вертикального среза: данные (05-01) → пайплайн читает их → пишет колонки → владелец сверяет предпросмотр (D-13) → боевой прогон. Сюда же входит сортировка товаров по структуре (кластеры D-05), валидация покрытия (D-12) и человекочитаемый предпросмотр через существующий флаг `--dry-run`.

Назначение: закрывает STRUCT-04 (запись полей) и доводит STRUCT-01/02/03/05 до реальных данных в Sheet. После этого этап 6 сможет строить навигацию по готовым полям.

Выход: модифицированный `upload.py` + `scripts/test_upload_structure.py` (зелёный pytest) + подтверждённый владельцем предпросмотр.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-1/05-CONTEXT.md
@.planning/phases/05-1/05-PATTERNS.md
@scripts/structure_map.json

<interfaces>
<!-- Точки модификации upload.py (номера строк выверены в 05-PATTERNS.md):
     :82  load_category_map()      — образец загрузки JSON-конфига (graceful-fallback)
     :404 apply_group_mapping()    — образец проставления display_group + лог непокрытых
     :645 header = [...]           — заголовок Sheet (добавить «Подгруппа», «Раздел» В КОНЕЦ)
     :666 rows.append([...])       — формирование строки товара (добавить два значения В КОНЕЦ)
     :789 main()                   — порядок шагов пайплайна
     :853 apply_group_mapping()    — старый маппинг (НЕ трогать, D-04)
     :876 apply_edit_memory()      — память правок (НОВЫЙ блок структуры — ПОСЛЕ него)
     :882 products_to_rows()       — сборка строк (НОВЫЙ блок структуры — ПЕРЕД ним)
     :893 if args.dry_run:         — существующий dry-run (расширить предпросмотром D-13)

     Копировать-готовые реализации load_structure_map / build_category_index /
     apply_structure_mapping / блока main / предпросмотра D-13 — в 05-PATTERNS.md,
     секция "scripts/upload.py — точки модификации", пункты 1–8. Имена полей фиксированы:
       p["display_subgroup"], p["display_section"], p["_sort_key"]
     Заголовки колонок фиксированы: "Подгруппа", "Раздел". Fallback-метки: "Прочее"/"Прочее". -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Функции структуры в upload.py + две новые колонки Sheet</name>
  <files>scripts/upload.py, scripts/test_upload_structure.py</files>
  <read_first>
    - scripts/upload.py (строки 82-89 load_category_map; 404-426 apply_group_mapping; 638-677 products_to_rows/header; 789-905 main)
    - scripts/structure_map.json (карта, которую читаем)
    - .planning/phases/05-1/05-PATTERNS.md (пункты 1–4 — копировать-готовый код load_structure_map, build_category_index, apply_structure_mapping, правка header/строки)
    - scripts/test_upload_new_format.py (стиль pytest: import upload, фикстуры в памяти)
  </read_first>
  <behavior>
    - build_category_index: для карты {"Сладкое":{"Конфеты":["Акконд","Коробочные"]}} возвращает индекс, где index["Акконд"] == ("Сладкое","Конфеты",0,0,0) и index["Коробочные"][4]==1 (cat_idx).
    - apply_structure_mapping назначает покрытой категории p["display_section"]/p["display_subgroup"] из карты и p["_sort_key"]=(sec,sub,cat).
    - apply_structure_mapping для НЕпокрытой категории ставит section="Прочее", subgroup="Прочее", _sort_key=(9999,9999,9999) и НЕ падает (D-12).
    - products_to_rows: заголовок заканчивается на ["...","Подгруппа","Раздел"]; колонка «Группа» остаётся на своей прежней позиции (индекс 4), её значение == p["display_group"] (D-04 не нарушено).
    - products_to_rows: последние два значения строки товара == p.get("display_subgroup","") и p.get("display_section","").
  </behavior>
  <action>
    Добавить в `upload.py` три функции по образцам из 05-PATTERNS.md (пункты 1–3): `load_structure_map()` (копия паттерна `load_category_map` с graceful-fallback и warn при отсутствии файла), `build_category_index(structure_map)` (обратный индекс `{категория: (раздел, подгруппа, idx_раздела, idx_подгруппы, idx_кат)}`), `apply_structure_mapping(products, category_index)` (проставляет `display_section`/`display_subgroup`/`_sort_key`; непокрытые → «Прочее»/«Прочее»/(9999,9999,9999) с `log.warning("  ! %s", cat)`, D-12).
    В `products_to_rows()` (строка 645 и 666): добавить в КОНЕЦ массива `header` два элемента — `"Подгруппа"`, `"Раздел"`; в `rows.append([...])` добавить в КОНЕЦ `p.get("display_subgroup", "")` и `p.get("display_section", "")`. Существующие 9 колонок и позицию «Группа» (индекс 4 = `p["display_group"]`) НЕ менять (D-04). Комментарии к новому коду — на русском.
    Параллельно создать `scripts/test_upload_structure.py` (стиль test_upload_new_format.py, `import upload`, без сети) с тестами из блока <behavior>: индекс, проставление, fallback «Прочее», порядок колонок заголовка, неизменность колонки «Группа».
  </action>
  <verify>
    <automated>cd scripts && python -m pytest test_upload_structure.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import scripts.upload" `-эквивалент: `cd scripts && python -c "import upload"` импортируется без ошибок.
    - pytest `test_upload_structure.py` — все тесты PASSED.
    - Тест подтверждает: `products_to_rows` header[4]=="Группа", header[-2]=="Подгруппа", header[-1]=="Раздел".
    - Тест подтверждает: непокрытая категория → display_section=="Прочее" без исключения (D-12).
  </acceptance_criteria>
  <done>
    Три функции добавлены, две колонки в Sheet, колонка «Группа» нетронута, pytest зелёный.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Встроить структуру в main() — сортировка D-05, сводка D-12, предпросмотр D-13</name>
  <files>scripts/upload.py</files>
  <read_first>
    - scripts/upload.py (строки 789-905 main: порядок apply_group_mapping → new_format → apply_edit_memory → products_to_rows; блок --dry-run 893-901)
    - .planning/phases/05-1/05-PATTERNS.md (пункты 5–8 — копировать-готовый блок встраивания в main, сортировка, сводка D-12, предпросмотр D-13 в --dry-run)
  </read_first>
  <action>
    В `main()` ПОСЛЕ `apply_edit_memory(...)` (строка ~878) и ПЕРЕД `products_to_rows(...)` (строка ~882) вставить блок по 05-PATTERNS.md (пункт 7): `structure_map = load_structure_map()`; если карта непуста — `category_index = build_category_index(structure_map)`, `all_products = apply_structure_mapping(all_products, category_index)`, затем сортировка `all_products.sort(key=lambda p: p.get("_sort_key", (9999, 9999, 9999)))` (D-05) с `log.info`, затем сводка D-12: собрать товары с `display_section=="Прочее"`, если есть — `log.warning("D-12: %d товаров (%d кат.) не покрыты structure_map.json → «Прочее»", ...)` с перечнем категорий. Если карта пуста — `log.info`, поля остаются пустыми (поведение не ломается). Существующие шаги (apply_group_mapping, блок new_format, apply_edit_memory) НЕ переставлять.
    Расширить существующий блок `if args.dry_run:` (строка 893) предпросмотром D-13 по 05-PATTERNS.md (пункт 8): если `structure_map` непуста — напечатать заголовок «ПРЕДПРОСМОТР ДВУХУРОВНЕВОЙ СТРУКТУРЫ (для сверки владельцем)», затем для каждого раздела и подгруппы — счётчик товаров и список категорий с их количеством; в конце — блок «Прочее» при наличии непокрытых. Старый вывод первых 5 строк оставить как ветку else (когда карта не загружена). Комментарии — на русском.
  </action>
  <verify>
    <automated>cd scripts && python -m pytest test_upload_structure.py test_upload_new_format.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python -c "import upload"` — без ошибок импорта (синтаксис main валиден).
    - Существующий набор `test_upload_new_format.py` остаётся зелёным (регрессии в пайплайне нет).
    - Порядок шагов в main: apply_group_mapping → (new_format) → apply_edit_memory → [НОВОЕ: structure] → products_to_rows — подтверждается чтением кода.
    - В коде main присутствует ветка предпросмотра D-13 внутри `if args.dry_run:` с печатью «ПРЕДПРОСМОТР».
  </acceptance_criteria>
  <done>
    main() читает structure_map, проставляет/сортирует/валидирует; --dry-run печатает предпросмотр D-13; старые тесты зелёные.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    upload.py теперь читает structure_map.json и в режиме `--dry-run` печатает человекочитаемый предпросмотр «раздел → подгруппа → категории (+ счётчики товаров)» (D-13). Запись в Google Sheet в dry-run НЕ выполняется — это безопасная сверка перед боевым прогоном. Боевой прогон на сервере daniella (через субагента sysadmin) делается ТОЛЬКО после одобрения владельцем — отдельным шагом за рамками этого плана.
  </what-built>
  <how-to-verify>
    1. Выполнить локально (PowerShell): `python scripts/upload.py --dry-run`
       (нужны реальные .xlsx в C:\price или укажите `--path` к папке с прайсами).
    2. В выводе найти блок «ПРЕДПРОСМОТР ДВУХУРОВНЕВОЙ СТРУКТУРЫ».
    3. Проверить глазами по 5 Success-критериям этапа:
       - Все 5 разделов на месте, порядок: Сладкое → Напитки → Крупы/лапша/бакалея → Соусы/консервация → Снэки/прикассовое.
       - «Стоевъ и Сэнсой» как группы НЕТ; её товары видны в Соусах/Консервации/Лапше/Соках.
       - «Конфеты» и «Печенье и вафли» — отдельные подгруппы; Сладонеж в «Печенье и вафли».
       - «Подарочные»/коробочные идут кучей в конце «Конфеты»; «Энергетики» — последняя подгруппа «Напитки».
       - Нет неожиданно большого блока «Прочее» (если есть — значит категория не покрыта; сообщите, какие).
    4. Если что-то лежит не там — назвать категорию и желаемую подгруппу: правка делается в scripts/structure_map.json (план 05-01), без изменения кода (D-02).
  </how-to-verify>
  <resume-signal>Напишите «одобрено», если предпросмотр верен, либо перечислите категории, которые нужно переложить.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| диск → Python | `structure_map.json` читается `load_structure_map()` — локальный конфиг, не внешний ввод. |
| Python → Google Sheet | `upload.py` перезаписывает лист «Товары» (деструктивно, `worksheet.clear()`); подушка безопасности (Товары_BACKUP + авто-откат при «подозрительно мало товаров») в `sheet_tool.py` НЕ трогается этим планом. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering / Denial | две новые колонки ломают порог/бэкап sheet_tool.py | mitigate | Колонки добавляются В КОНЕЦ строки; число СТРОК (товаров) не меняется — порог «подозрительно мало товаров» работает по строкам, не колонкам. План не правит sheet_tool.py; проверка — старые тесты зелёные + dry-run перед боевым. |
| T-05-05 | Information loss | категория молча теряется (пустые «Подгруппа»/«Раздел») | mitigate | D-12: непокрытые → «Прочее» с `log.warning`; pytest fallback-теста (план 05-01 покрытие + 05-02 fallback) гарантирует поведение. |
| T-05-06 | Denial (обнуление каталога при сбое записи) | деструктивная `worksheet.clear()` в upload.py | accept | Известный тех-долг (CONCERNS.md); вне scope этапа — пайплайн и подушка безопасности не меняются. Боевой прогон — через sysadmin с бэкапом, отдельным шагом после D-13. |
| T-05-07 | — | новых эндпоинтов/auth/внешнего ввода нет | accept | Этап правит офлайн-скрипт; поверхность атаки не расширяется. |
</threat_model>

<verification>
- `cd scripts && python -c "import upload"` — без ошибок.
- `cd scripts && python -m pytest test_upload_structure.py test_upload_new_format.py -q` — всё зелёное.
- `python scripts/upload.py --dry-run` печатает блок «ПРЕДПРОСМОТР ДВУХУРОВНЕВОЙ СТРУКТУРЫ» со счётчиками; запись в Sheet пропущена.
- Чтение кода: заголовок Sheet оканчивается «Подгруппа», «Раздел»; «Группа» на прежней позиции (D-04).
</verification>

<success_criteria>
- upload.py проставляет «Подгруппа»/«Раздел» каждому товару (STRUCT-04, Success #1) — пустых среди активных нет (D-12 ловит остаток).
- Колонка «Группа» нетронута (D-04 — живая витрина не ломается между этапами 5 и 6).
- Сортировка даёт кластеры «Подарочные»/«Энергетики» (STRUCT-05/03, D-05).
- `--dry-run` даёт предпросмотр для сверки владельцем (D-13); владелец одобрил.
</success_criteria>

<output>
Создать `.planning/phases/05-1/05-02-SUMMARY.md` по завершении.
</output>
