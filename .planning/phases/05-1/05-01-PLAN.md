---
phase: 05-структура-данных
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/structure_map.json
  - scripts/test_structure_map.py
autonomous: true
requirements: [STRUCT-01, STRUCT-02, STRUCT-03, STRUCT-05, REGRP-01, REGRP-02, REGRP-03]

must_haves:
  truths:
    - "Файл scripts/structure_map.json существует и парсится как JSON"
    - "Все 117 ключей category_map.json присутствуют в structure_map.json (ни одна категория не потеряна)"
    - "Группа «Стоевъ и Сэнсой» как раздел/подгруппа не существует — её 12 категорий разложены по смыслу (REGRP-01)"
    - "«Конфеты» и «Печенье и вафли» — две отдельные подгруппы; Сладонеж в «Печенье и вафли» (REGRP-02)"
    - "Категория «Мистраль Крупы варпак» лежит в подгруппе «Крупы и бакалея» (REGRP-03)"
    - "5 разделов идут в порядке: Сладкое, Напитки, Крупы лапша бакалея, Соусы и консервация, Снэки и прикассовое (STRUCT-02)"
    - "«Коробочные конфеты» (7 категорий) идут последними в подгруппе «Конфеты» (STRUCT-05); «Энергетики» — последняя подгруппа в «Напитки» (STRUCT-03)"
  artifacts:
    - path: "scripts/structure_map.json"
      provides: "Карта разделы → подгруппы → список категорий (вложенный JSON, порядок = позиция в массиве, D-01/D-02)"
      contains: "Шоколад и батончики"
    - path: "scripts/test_structure_map.py"
      provides: "Pytest: покрытие всех 117 категорий + структурные инварианты (5 разделов, порядок, кластеры)"
  key_links:
    - from: "scripts/structure_map.json"
      to: "scripts/category_map.json"
      via: "каждый ключ-категория из значений массивов structure_map покрывает ключ из category_map"
      pattern: "set(category_map keys) == set(все строки в массивах structure_map)"
---

<objective>
Создать единый источник правды двухуровневой структуры — файл `scripts/structure_map.json` (D-01): вложенный JSON «5 разделов → подгруппы → список категорий», где порядок везде задан позицией в массиве (D-02, без числовых полей сортировки). Это первый (data-contract) ломтик вертикального среза этапа: сам файл данных + pytest-проверка, что КАЖДАЯ из 117 категорий `category_map.json` покрыта (защита Success-критерия №1 «ни одного пустого среди активных товаров») и что выполнены требования перекладки (REGRP-01..03) и кластеризации (STRUCT-03/05).

Назначение: без этого файла `upload.py` (план 05-02) не сможет проставить «Подгруппа»/«Раздел». Файл правит владелец-непрограммист руками (переставить строку = переставить кластер), поэтому контракт — простой вложенный массив без служебных числовых полей.

Выход: `scripts/structure_map.json` + `scripts/test_structure_map.py` (зелёный прогон pytest).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-1/05-CONTEXT.md
@.planning/phases/05-1/05-PATTERNS.md
@scripts/category_map.json

<interfaces>
<!-- Контракт целевого файла. structure_map.json — вложенный объект:
     { "Раздел": { "Подгруппа": ["КатегорияA", "КатегорияB", ...] } }
     Строки в массивах — ДОСЛОВНЫЕ ключи из category_map.json (то же, что product["source_category"]).
     Порядок раздела/подгруппы/категории = позиция в JSON (D-02). Числовых полей order НЕТ.

     Согласованная структура (из 05-CONTEXT.md <domain>, НЕ переспрашивать):
       1. Сладкое            → Шоколад и батончики · Конфеты (Коробочные кучей в конце) · Печенье и вафли · Детское
       2. Напитки            → Воды и газировки · Соки · Чай и кофе · Энергетики (последними)
       3. Крупы, лапша, бакалея → Крупы и бакалея (+ варпаки Мистраль) · Лапша и каши б/п
       4. Соусы и консервация  → Соусы/кетчуп/майонез/приправы · Консервация
       5. Снэки и прикассовое  → Чипсы/сухарики/орехи/крекеры · Жвачка/Холс/цикорий

     Полная целевая раскладка всех категорий — в 05-PATTERNS.md, секция
     "Целевая форма structure_map.json" (копировать как основу, затем выверить покрытие). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Создать scripts/structure_map.json по согласованной структуре</name>
  <read_first>
    - scripts/category_map.json (источник 117 ключей-категорий — каждый ДОЛЖЕН попасть в structure_map)
    - .planning/phases/05-1/05-PATTERNS.md (секция «Целевая форма structure_map.json» — копировать как черновик)
    - .planning/phases/05-1/05-CONTEXT.md (раздел <domain> — согласованная структура 5 разделов; D-06..D-11 — спорные раскладки)
  </read_first>
  <action>
    Создать файл `scripts/structure_map.json` — вложенный JSON ровно с 5 разделами верхнего уровня в порядке: «Сладкое», «Напитки», «Крупы, лапша, бакалея», «Соусы и консервация», «Снэки и прикассовое». Каждый раздел — объект подгрупп; каждая подгруппа — массив строк-категорий, дословно совпадающих с ключами `category_map.json` (используются как `product["source_category"]`, без нормализации).
    Взять за основу готовую раскладку из 05-PATTERNS.md и выверить её против ВСЕХ 117 ключей `category_map.json`. Обязательно соблюсти locked-решения:
    - REGRP-01: категории «Стоевъ …» и «СэнСой …» разложить по смыслу — `СэнСой Соусы`/`СэнСой Соусы Соевые`/`СэнСой Разное`/`Стоевъ`/`Стоевъ Кетчуп/соусы/пасты/горчица`/`Стоевъ Приправа`/`Стоевъ Уксус` → подгруппа «Соусы/кетчуп/майонез/приправы»; `Стоевъ Консервация овощная`/`Стоевъ Консервация фруктовая` → «Консервация»; `СэнСой Б/П`/`СэнСой Лапша` → «Лапша и каши б/п»; `Стоевъ Сок` → «Соки». Подгруппы/раздела «Стоевъ и Сэнсой» в файле быть НЕ должно.
    - REGRP-02 / D-10: `Сладонеж` → «Печенье и вафли»; конфетные категории → «Конфеты».
    - REGRP-03 / D-11: `Мистраль Крупы варпак` → «Крупы и бакалея».
    - D-07/D-08/D-09: `Let's Be`, `Продако`, `Квас` → «Воды и газировки».
    - STRUCT-05 / D-06: 7 категорий бывшей группы «Коробочные конфеты» (`Коробочные`, `Подарки`, `Торты`, `Набор конфет ЛЮСИ`, `Набор конфет МВН`, `Набор конфет Сонуар`, `Фас. кор. конфеты`) идут ПОДРЯД в КОНЦЕ массива подгруппы «Конфеты» — это кластер «Подарочные кучей».
    - STRUCT-03 / D-05: подгруппа «Энергетики» — ПОСЛЕДНЯЯ в массиве раздела «Напитки».
    Числовых полей `order` НЕ добавлять (D-02). Файл с отступом 2 пробела, кодировка UTF-8 без BOM.
    Если при сверке найдётся ключ category_map.json, не упомянутый в черновике PATTERNS — добавить его в наиболее подходящую по смыслу подгруппу (категории уже осмысленны по названию). Цель задачи 2 — формально доказать, что не осталось ни одного непокрытого ключа.
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('scripts/structure_map.json', encoding='utf-8')); assert len(d)==5, len(d); print('OK 5 разделов:', list(d.keys()))"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/structure_map.json` парсится как JSON и имеет ровно 5 ключей верхнего уровня в заданном порядке.
    - Ни одного ключа верхнего/второго уровня «Стоевъ и Сэнсой».
    - Подгруппы «Конфеты» и «Печенье и вафли» существуют как отдельные; `Сладонеж` присутствует в массиве «Печенье и вафли», отсутствует в «Конфеты».
    - В массиве подгруппы «Конфеты» последними 7 элементами идут категории «Коробочных конфет» (D-06).
    - В разделе «Напитки» подгруппа «Энергетики» — последний ключ.
  </acceptance_criteria>
  <done>
    Файл создан, парсится, 5 разделов в нужном порядке, перекладка REGRP-01/02/03 и кластеры D-05/D-06 соблюдены.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Pytest — покрытие всех 117 категорий и структурные инварианты</name>
  <files>scripts/test_structure_map.py</files>
  <read_first>
    - scripts/test_upload_new_format.py (образец стиля pytest проекта: sys.path.insert(SCRIPTS_DIR), docstring на русском, фикстуры в памяти)
    - scripts/category_map.json (множество эталонных ключей)
    - scripts/structure_map.json (созданный в задаче 1)
  </read_first>
  <behavior>
    - test_файл_парсится: structure_map.json загружается как dict, ровно 5 разделов.
    - test_полное_покрытие: множество всех строк во всех массивах structure_map == множество ключей category_map.json (равенство множеств; тест печатает разницу в обе стороны при провале). Это формальная защита Success-критерия №1 (D-12).
    - test_нет_дублей: ни одна категория не встречается в двух подгруппах (длина плоского списка == размеру множества).
    - test_стоевъ_сэнсой_расформирована: строки «Стоевъ и Сэнсой» нет среди разделов и подгрупп (REGRP-01).
    - test_конфеты_и_печенье_разделены: «Конфеты» и «Печенье и вафли» — разные подгруппы; «Сладонеж» в «Печенье и вафли», не в «Конфеты» (REGRP-02).
    - test_варпак_мистраль_в_крупах: «Мистраль Крупы варпак» в подгруппе «Крупы и бакалея» (REGRP-03).
    - test_порядок_разделов: список ключей верхнего уровня == ["Сладкое","Напитки","Крупы, лапша, бакалея","Соусы и консервация","Снэки и прикассовое"] (STRUCT-02).
    - test_коробочные_кучей: последние 7 элементов массива подгруппы «Конфеты» — ровно набор коробочных категорий D-06 (STRUCT-05).
    - test_энергетики_последние: «Энергетики» — последний ключ раздела «Напитки» (STRUCT-03).
  </behavior>
  <action>
    Создать `scripts/test_structure_map.py` по стилю существующего `test_upload_new_format.py` (docstring на русском, `SCRIPTS_DIR = Path(__file__).resolve().parent`, загрузка обоих JSON через `SCRIPTS_DIR / "...json"`). Реализовать тесты из блока <behavior>. Ключевой тест — `test_полное_покрытие`: собрать `covered = {cat for sub in structure_map.values() for cats in sub.values() for cat in cats}` и сравнить с `set(category_map.keys())`; при расхождении assert-сообщение печатает «лишние в structure_map» и «отсутствуют в structure_map». Сетевых вызовов и обращений к Google Sheet нет — только два локальных JSON.
  </action>
  <verify>
    <automated>cd scripts && python -m pytest test_structure_map.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest scripts/test_structure_map.py -q` — все тесты PASSED.
    - `test_полное_покрытие` зелёный, то есть `set` категорий structure_map точно равно `set` ключей category_map.json (0 пропущенных, 0 лишних).
    - Тесты не делают сетевых запросов (выполняются < 5 c).
  </acceptance_criteria>
  <done>
    Тест-файл создан, все тесты зелёные; покрытие 117/117 категорий доказано автоматически.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| диск → Python | `structure_map.json` читается `upload.py` (план 05-02). Файл не из внешнего ввода — локальный конфиг рядом с кодом, как `category_map.json`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | structure_map.json (битый/неполный JSON) | mitigate | `test_structure_map.py` валидирует парсинг и полное покрытие 117 категорий; на этапе 05-02 `load_structure_map()` имеет graceful-fallback при отсутствии/ошибке файла. |
| T-05-02 | Information loss (Denial) | потеря категории при ручной правке владельцем | mitigate | `test_полное_покрытие` падает, если множество категорий разошлось; в рантайме D-12 (план 05-02) логирует непокрытые → «Прочее». |
| T-05-03 | — | новых внешних вводов / auth / эндпоинтов нет | accept | Этап — только локальный data-файл; поверхность атаки не расширяется. |
</threat_model>

<verification>
- `python -c "import json; json.load(open('scripts/structure_map.json', encoding='utf-8'))"` — без ошибок.
- `python -m pytest scripts/test_structure_map.py -q` — все тесты зелёные, покрытие 117/117.
- Ручной просмотр: «Стоевъ и Сэнсой» отсутствует, «Конфеты»/«Печенье и вафли» разделены.
</verification>

<success_criteria>
- structure_map.json существует, валиден, 5 разделов в заданном порядке (STRUCT-02).
- Все 117 категорий покрыты — доказано тестом (защита Success #1).
- REGRP-01/02/03 и кластеры STRUCT-03/05 реализованы порядком массивов (D-02/D-05/D-06).
</success_criteria>

<output>
Создать `.planning/phases/05-1/05-01-SUMMARY.md` по завершении.
</output>
