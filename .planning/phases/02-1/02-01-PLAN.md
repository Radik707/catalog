---
phase: 02-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - uploader/app.py
autonomous: true
requirements: [UPL-01, UPL-02, UPL-03, UPL-04]

must_haves:
  truths:
    - "После «Обновить» оператор мгновенно получает нейтральное подтверждение, браузер не ждёт обработку"
    - "Вся обработка (бэкап → upload.py → warn/stash/rollback → notify → запись истории) идёт в фоновом потоке"
    - "Повторное «Обновить» во время идущей обработки не запускает второй upload.py — возвращает нейтральное «обработка уже идёт»"
    - "Подробный итог (успех/ошибка/мало товаров) уходит только владельцу в Telegram; оператор видит лишь нейтральные статусы без трассировок"
    - "На странице есть таблица истории; сразу после «Обновить» появляется строка «обрабатывается…», затем превращается в финальный нейтральный итог"
    - "История хранится персистентно в JSON-файле (последние 10 записей) и переживает перезапуск"
  artifacts:
    - path: "uploader/app.py"
      provides: "Асинхронный /update, защита двойного запуска, endpoint /history, запись истории, таблица истории в HTML"
      contains: "def _process_async"
  key_links:
    - from: "POST /<token>/update"
      to: "_process_async (threading.Thread daemon)"
      via: "немедленный возврат после старта потока"
      pattern: "threading\\.Thread"
    - from: "_process_async"
      to: "history.json"
      via: "append_history / update_history (atomic temp + os.replace)"
      pattern: "os\\.replace"
    - from: "GET /<token>/history"
      to: "страница (поллинг fetch)"
      via: "JSON массив записей истории"
      pattern: "/history"
    - from: "_process_async"
      to: "notify(...)"
      via: "подробный итог только владельцу"
      pattern: "notify\\("
---

<objective>
Переделать `uploader/app.py`: синхронный `/update` (оператор ждёт несколько минут и видит весь результат) заменить на асинхронный поток — мгновенный нейтральный ответ оператору, вся обработка в фоновом потоке того же gunicorn-воркера, подробный итог только владельцу в Telegram, плюс персистентная история загрузок с таблицей на странице и поллингом.

Это вертикальный сквозной срез (MVP): после плана локально работает полный путь «нажал «Обновить» → мгновенно «файлы отправлены» → фон обрабатывает → история показывает «обрабатывается…» → финальный нейтральный итог; детали — владельцу в Telegram». Реальный деплой и проверка на сервере — план 02-02 (Wave 2).

Purpose: Оператор работает с телефона через MAX — он не должен ждать, видеть технические детали или случайно запустить второй прогон. Владелец получает полную картину в Telegram.
Output: Переделанный `uploader/app.py` (один файл, стиль сохранён) с асинхронной обработкой, защитой двойного запуска, журналом истории и endpoint `/history`.

Граница: НЕ трогать `scripts/upload.py`, `scripts/sheet_tool.py`, `scripts/notify_tg.py`, admin_bot. Переиспользовать существующие функции `backup_catalog`, `run_upload`, `stash_new`, `rollback_catalog`, `archive_incoming`, `notify` — менять только их оркестровку (перенос в фон) и слой ответа оператору.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-1/02-CONTEXT.md

<interfaces>
<!-- Контракты из текущего uploader/app.py — исполнитель использует их напрямую, без разведки. -->

Существующие функции-оркестраторы (переиспользовать как есть, НЕ менять сигнатуры):
- backup_catalog() -> tuple[bool, int | None]   # (успех, число_строк_до)
- run_upload() -> tuple[bool, str, int | None]   # (успех, текст_ошибки, число_товаров)
- stash_new() -> bool
- rollback_catalog() -> tuple[bool, str]
- archive_incoming() -> list[str]                # перемещает партию в LAST_BATCH_DIR, очищает INCOMING_DIR
- notify(mode: str, text: str) -> None           # mode: "plain" | "error" | "decision"
- list_files() -> list[str]
- check(token: str) -> None                      # abort(404) если токен неверный

Текущая синхронная логика /update (строки ~284–330) — её надо разорвать:
  1) проверка наличия файлов
  2) backup_catalog(); при провале — отказ
  3) run_upload(); при провале — rollback_catalog() + archive_incoming() + notify("error", ...)
  4) archive_incoming(); warn = count < b_rows * WARN_RATIO
  5) если warn — stash_new() + rollback_catalog() + notify("decision", ...)
  6) иначе — notify("plain", "✅ Каталог обновлён: {count} товаров.")

Константы уже определены: INCOMING_DIR, LAST_BATCH_DIR, WARN_RATIO, SCRIPT_DIR, PYTHON_BIN.
Существующий endpoint POST /<token>/rollback вызывает rollback_catalog() — оставить БЕЗ изменений (D-06).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Журнал истории и фоновая обработка с защитой двойного запуска</name>
  <files>uploader/app.py</files>
  <read_first>
    - uploader/app.py (функции backup_catalog, run_upload, stash_new, rollback_catalog, archive_incoming, notify, list_files; синхронный update() строки ~284–330)
    - .planning/phases/02-1/02-CONTEXT.md (решения D-01, D-02, D-03, D-04, D-07)
    - scripts/notify_tg.py (режимы plain/error/decision — что получает владелец; НЕ менять файл)
  </read_first>
  <action>
    Добавь в `uploader/app.py` журнал истории и фоновую обработку.

    Константа пути истории: `HISTORY_FILE = Path(os.environ.get("HISTORY_FILE", str(SCRIPT_DIR / "history.json")))` (env с дефолтом — D-04; имя/путь — Claude's discretion).

    Защита двойного запуска (D-02): модульный `PROCESS_LOCK = threading.Lock()`. Захват не блокируя через `acquire(blocking=False)` — единственный источник правды «идёт обработка». gunicorn `-w 1` гарантирует один процесс, Lock корректен.

    Функции истории (атомарная запись temp + `os.replace`; формат — Claude's discretion):
    - `load_history() -> list[dict]` — читает HISTORY_FILE; при отсутствии/битом JSON возвращает `[]` (не падать).
    - `append_history(entry: dict) -> str` — добавляет запись в НАЧАЛО списка, обрезает до последних 10, пишет атомарно, возвращает уникальный `id` записи (например `secrets.token_hex(8)`) для последующего обновления.
    - `update_history(entry_id: str, **fields) -> None` — находит запись по `id`, обновляет переданные поля (`status`, `count`, `result_text`), пишет атомарно.

    Поля записи (D-04): `id`, `ts` (время старта, ISO-строка), `status` (`processing` | `ok` | `warn` | `error`), `count` (число товаров или null), `result_text` (нейтральный текст итога для оператора, БЕЗ трассировок — D-07), `files` (список имён файлов партии).

    Фоновая функция `_process_async(entry_id: str) -> None`:
    - Выполняет ТОЧНО ту же оркестровку, что сейчас в синхронном update() (backup → run_upload → warn/stash/rollback → notify), НО:
      - все подробные сообщения (`notify("error"/"decision"/"plain", ...)`) уходят ТОЛЬКО владельцу через `notify(...)` как сейчас (D-03);
      - по завершении вызывает `update_history(entry_id, status=..., count=..., result_text=...)` с НЕЙТРАЛЬНЫМ текстом для оператора (например «Загружено N товаров», «Ошибка обработки — администратор уведомлён», «Мало товаров — отправлено на проверку»), БЕЗ технических деталей (D-07).
    - В блоке `finally` ОБЯЗАТЕЛЬНО освобождает `PROCESS_LOCK` (чтобы зависший поток не заблокировал навсегда).
    - Любое необработанное исключение внутри потока — поймать, записать `status=error` нейтрально, освободить Lock в finally.

    Импорты: добавь `import threading`, `import json`, `import secrets`, `import tempfile`, `from datetime import datetime` (если ещё нет).
  </action>
  <verify>
    <automated>python -c "import ast,sys; t=ast.parse(open('uploader/app.py',encoding='utf-8').read()); names={n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)}; req={'_process_async','load_history','append_history','update_history'}; m=req-names; sys.exit('MISSING: '+','.join(m) if m else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `uploader/app.py` определяет функции `_process_async`, `load_history`, `append_history`, `update_history` (AST-проверка выше проходит).
    - В модуле присутствует `threading.Lock` (grep `threading\.Lock` >=1) и атомарная запись истории через `os.replace` (grep `os\.replace` >=1 помимо archive_incoming).
    - Поле `status` использует значения из набора processing/ok/warn/error, есть `result_text` без трассировок: grep `processing` и `result_text` присутствуют.
    - `python -m py_compile uploader/app.py` проходит без ошибок.
    - Функции `backup_catalog`, `run_upload`, `stash_new`, `rollback_catalog`, `archive_incoming`, `notify` не изменены по сигнатуре (тот же `def ...` остаётся).
  </acceptance_criteria>
  <done>В app.py есть персистентный журнал истории (10 записей, атомарная запись), фоновая обработка `_process_async` с защитой двойного запуска через Lock и нейтральным итогом для оператора; подробности уходят владельцу через notify.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Асинхронный /update, endpoint /history и таблица истории на странице</name>
  <files>uploader/app.py</files>
  <read_first>
    - uploader/app.py (после Task 1: функции истории и _process_async; маршруты index/files/upload/update/rollback; строка PAGE с HTML+CSS+JS, строки ~341–510)
    - .planning/phases/02-1/02-CONTEXT.md (решения D-01, D-05, D-06, D-07; specifics — стиль страницы, mobile-first, нейтральный тон)
  </read_first>
  <action>
    Перепиши маршрут `POST /<token>/update` и страницу под асинхронный сценарий.

    Маршрут `update(token)` (D-01, D-02):
    - `check(token)`; если `not list_files()` → вернуть `jsonify(ok=False, message="Нет файлов для обновления.")`.
    - Захватить Lock не блокируя: `PROCESS_LOCK.acquire(blocking=False)`. Если НЕ удалось → вернуть `jsonify(ok=False, message="Обработка уже идёт, подождите.")` БЕЗ запуска второго потока (D-02).
    - Если удалось: снять снимок имён файлов через `list_files()`, вызвать `append_history({status:"processing", ts:now, count:null, result_text:"обрабатывается…", files:[...]})` → получить `entry_id`. Стартовать `threading.Thread(target=_process_async, args=(entry_id,), daemon=True).start()`. Немедленно вернуть `jsonify(ok=True, message="Файлы отправлены, обработка идёт.")` — нейтрально, БЕЗ числа товаров и деталей (UPL-01, D-07). Внимание: Lock освобождает `_process_async` в finally — НЕ освобождать в маршруте.

    Новый маршрут `GET /<token>/history` (D-05): `check(token)`; вернуть `jsonify(history=load_history())` — массив записей с полями ts/status/count/result_text/files. Никаких трассировок в выдаче (D-07).

    Страница PAGE (HTML+CSS+JS внутри строки, стиль сохранить — mobile-first, русский, тон существующих нейтральных формулировок):
    - Добавить блок таблицы истории (компактная, последние записи сверху): колонки «время · файлы · итог». Контейнер `<div id="history">`.
    - JS `loadHistory()` — `fetch('/${TOKEN}/history')`, рендер строк; строка со `status==='processing'` отображается как «⏳ обрабатывается…», иначе показывается `result_text`.
    - Обработчик кнопки «Обновить»: после `fetch('/${TOKEN}/update')` показать оператору нейтральный `d.message`, вызвать `loadHistory()` и запустить поллинг `/history` каждые ~3–5 c (интервал/условие остановки — Claude's discretion, D-05), пока в истории есть строка `processing`; как только processing исчезла — остановить поллинг.
    - Кнопку и обработчик «Откатить к прошлой версии» (`/rollback`) оставить БЕЗ изменений (D-06).
    - Вызвать `loadHistory()` при старте (рядом с существующим `refresh()`).
    - НЕ показывать оператору технические детали, числа при ошибке, трассировки — только нейтральные тексты `result_text`/`message` (D-07).
  </action>
  <verify>
    <automated>python -m py_compile uploader/app.py && python -c "import re,sys; s=open('uploader/app.py',encoding='utf-8').read(); ok=('acquire(blocking=False)' in s) and ('Файлы отправлены' in s) and ('Обработка уже идёт' in s) and bool(re.search(r'@app\.get\([^)]*/history', s)) and ('loadHistory' in s); sys.exit(0 if ok else 'MISSING async/history wiring')"</automated>
  </verify>
  <acceptance_criteria>
    - `POST /<token>/update` возвращает `ok=true` с нейтральным `message` «Файлы отправлены…» и НЕ содержит число товаров в ответе оператору (UPL-01, D-07).
    - При уже идущей обработке повторный `POST /<token>/update` возвращает `ok=false`, `message` «Обработка уже идёт…» и второй `threading.Thread` НЕ стартует (grep `acquire(blocking=False)` присутствует; ветка отказа до старта потока).
    - `GET /<token>/history` зарегистрирован (`@app.get` с `/history`) и возвращает `history` — массив записей с полями ts/status/count/result_text/files.
    - В PAGE есть `loadHistory` и поллинг `/history`; строка `processing` рендерится как «обрабатывается…».
    - Обработчик `/rollback` и его кнопка не изменены (D-06): grep `/rollback` присутствует.
    - `python -m py_compile uploader/app.py` проходит; automated-проверка выше возвращает 0.
  </acceptance_criteria>
  <done>Маршрут /update мгновенно отвечает нейтрально и стартует фон под Lock; есть endpoint /history; страница рисует таблицу истории с поллингом «обрабатывается…» → итог; кнопка отката не тронута.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер оператора → Flask (/<token>/*) | Доступ только по секретному токену; оператор не должен получать технические детали |
| /update → фоновый поток | Запуск subprocess upload.py; защита от параллельного второго запуска |
| Фоновый поток → history.json | Конкурентная/прерванная запись файла истории |
| Фоновый поток → Telegram (notify) | Подробности уходят владельцу, не оператору |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02a-01 | Information Disclosure | ответ /update и /history оператору | mitigate | оператор получает только нейтральные result_text/message; трассировки и числа при ошибке — только владельцу через notify (D-03, D-07); /history не отдаёт техдетали |
| T-02a-02 | Denial of Service / Tampering | двойной запуск upload.py (race) | mitigate | PROCESS_LOCK.acquire(blocking=False); второй /update получает отказ без старта потока (D-02); finally гарантированно освобождает Lock |
| T-02a-03 | Tampering | повреждение history.json при сбое записи | mitigate | атомарная запись temp + os.replace; load_history при битом JSON возвращает [] (не падает) |
| T-02a-04 | Information Disclosure | /history доступен без секрета | mitigate | endpoint под тем же сегментом /<token>, check(token) → abort(404) |
| T-02a-05 | Denial of Service | зависший поток оставляет Lock занятым | mitigate | освобождение Lock в finally + любое исключение пишет status=error нейтрально; «зависшие processing» — отложено (CONTEXT deferred), подушка безопасности страхует каталог |
| T-02a-06 | Tampering | непроверенный путь HISTORY_FILE через env | accept | путь задаёт владелец сервера через env; дефолт внутри SCRIPT_DIR; не пользовательский ввод |
</threat_model>

<verification>
- `python -m py_compile uploader/app.py` проходит без ошибок
- AST содержит `_process_async`, `load_history`, `append_history`, `update_history`
- `/update` отвечает мгновенно нейтрально; второй вызов при занятом Lock — отказ без второго потока
- `/history` зарегистрирован под токеном, отдаёт массив записей с нужными полями
- PAGE содержит таблицу истории, `loadHistory`, поллинг; кнопка отката не тронута
- Подробные итоги идут через notify(...) (grep notify в _process_async)
</verification>

<success_criteria>
- Success Criteria #1: оператор немедленно видит подтверждение «файлы отправлены» — страница не зависает (UPL-01, UPL-02)
- Success Criteria #2: владелец получает Telegram-итог после фоновой обработки, оператор его не видит (UPL-03)
- Success Criteria #3: на странице таблица истории — дата/время, файлы, нейтральный итог (UPL-04)
- Success Criteria #4: при ошибке оператор видит только нейтральное, детали — владельцу (D-07)
- Защита двойного запуска работает (D-02); история персистентна, 10 записей (D-04)
</success_criteria>

<output>
Create `.planning/phases/02-1/02-01-SUMMARY.md` when done
</output>
