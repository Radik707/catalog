---
phase: 02-1
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements: [UPL-01, UPL-02, UPL-03, UPL-04]

user_setup:
  - service: daniella (сервер)
    why: "Деплой переделанного загрузчика и сквозная проверка идут на боевом сервере"
    dashboard_config:
      - task: "Все серверные операции — только через субагента sysadmin (правило проекта)"
        location: "/opt/apps/catalog, служба catalog-uploader (gunicorn -w 1)"

must_haves:
  truths:
    - "Переделанный uploader/app.py доставлен на daniella в /opt/apps/catalog и служба catalog-uploader перезапущена"
    - "После «Обновить» оператор на боевой странице мгновенно видит «файлы отправлены», страница не зависает"
    - "Обработка идёт в фоне; владелец получает Telegram-итог по завершении; оператор деталей не видит"
    - "На боевой странице видна таблица истории: строка «обрабатывается…» превращается в нейтральный итог"
    - "Повторное «Обновить» во время обработки не запускает второй upload.py — страница отвечает «обработка уже идёт»"
    - "history.json создаётся и переживает перезапуск службы; каталог при проверке не пострадал (подушка безопасности)"
  artifacts:
    - path: "uploader/app.py"
      provides: "Серверная версия идентична проверенной локальной (Wave 1)"
      contains: "_process_async"
  key_links:
    - from: "git pull / scp на сервере"
      to: "systemctl restart catalog-uploader"
      via: "деплой через субагента sysadmin"
      pattern: "catalog-uploader"
    - from: "боевой /<token>/update"
      to: "Telegram владельцу + строка истории"
      via: "фоновый поток на сервере"
      pattern: "history"
---

<objective>
Доставить переделанный в Wave 1 `uploader/app.py` на сервер daniella и проверить сквозной асинхронный сценарий на боевой странице: оператор жмёт «Обновить» → мгновенно «файлы отправлены» → фон обрабатывает → владелец получает Telegram-итог → таблица истории на странице показывает «обрабатывается…» и затем нейтральный итог; повторное нажатие во время обработки не запускает второй прогон.

ВАЖНО (autonomous: false): все серверные операции — ТОЛЬКО через субагента `sysadmin` (правило проекта, см. память server-via-sysadmin). Исполнитель НЕ ходит по ssh напрямую. Владелец наблюдает. Деплой переписывает рабочий загрузчик — подушка безопасности (бэкап/откат каталога) остаётся защитой; запись в каталог при контрольном прогоне страхуется бэкапом.

Purpose: Подтвердить, что переделка действительно работает в боевом окружении (gunicorn -w 1, реальный Telegram через SOCKS5, реальный лист «Товары»), а не только локально.
Output: Переделанный загрузчик на сервере, перезапущенная служба, подтверждённый сквозной сценарий и история на боевой странице.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/02-1/02-CONTEXT.md

<server_facts>
<!-- Из CONTEXT.md, README загрузчика, памяти проекта, 01-02-PLAN.md -->
- Все серверные операции — ТОЛЬКО через субагента sysadmin (НЕ ssh напрямую)
- Корень проекта на сервере: /opt/apps/catalog
- Загрузчик на сервере: /opt/apps/catalog/uploader/app.py
- Служба systemd: catalog-uploader (gunicorn -w 1 -b 127.0.0.1:8000 app:app), WorkingDirectory=/opt/apps/catalog/uploader
- Деплой кода: git pull на сервере → sudo systemctl restart catalog-uploader (рестарт нужен — gunicorn держит app.py в памяти)
- venv: /opt/apps/catalog/.venv (системный python3 без gspread)
- Доступ к странице: https://uploader.zhukoleg.ru/<APP_SECRET> (секрет — в uploader/.env на сервере)
- Telegram идёт через SOCKS5-прокси 127.0.0.1:1080; Google Sheets — напрямую
- credentials.json, .env, uploader/.env — ТОЛЬКО на сервере (.gitignore), не трогать и не выводить
- history.json создастся в uploader/ при первом запуске (HISTORY_FILE дефолт)
</server_facts>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Wave 1 завершён: переделанный uploader/app.py (асинхронный /update, история, /history) готов к доставке на сервер.</what-built>
  <how-to-verify>
    1. Подтвердите, что план 02-01 завершён и закоммичен (`uploader/app.py` с асинхронной обработкой в git).
    2. Подтвердите, что субагент `sysadmin` доступен для серверных операций на daniella.
    3. Подтвердите, что сейчас оператор НЕ грузит файлы и НЕ жмёт «Обновить» (чтобы деплой/рестарт не прервал реальную обработку).
    4. Решите, делаем ли контрольный реальный прогон с записью на боевом сервере: если делаем — нужна готовая партия .xlsx (можно повторно из last_batch) и подушка безопасности (бэкап) активна. Альтернатива — проверка без записи (отказ при пустой папке, защита двойного запуска).
  </how-to-verify>
  <resume-signal>Напишите «02-01 закоммичен, sysadmin доступен, оператор не активен» и укажите режим проверки (с реальным прогоном / без записи), либо опишите, что не так.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Деплой переделанного загрузчика на daniella через sysadmin</name>
  <read_first>
    - uploader/app.py (проверенная версия Wave 1 — её доставляем)
    - .planning/codebase/INTEGRATIONS.md (деплой кода, env сервера, служба catalog-uploader)
    - .planning/phases/02-1/02-CONTEXT.md (canonical_refs → раздел «Сервер»: корень /opt/apps/catalog, деплой git pull + restart)
  </read_first>
  <action>
    Через субагента `sysadmin` (НЕ напрямую по ssh) доставь переделанный `uploader/app.py` на сервер и перезапусти службу.
    - Предпочтительно: на сервере `cd /opt/apps/catalog && git pull` (если правки Wave 1 закоммичены и запушены). Если не в git — доставить только `uploader/app.py` через scp в `/opt/apps/catalog/uploader/app.py`.
    - НЕ трогать `credentials.json`, `.env`, `uploader/.env`, `scripts/*` на сервере и не выводить их содержимое.
    - Перед рестартом сверить синтаксис на сервере: `/opt/apps/catalog/.venv/bin/python -m py_compile /opt/apps/catalog/uploader/app.py` → должно пройти без ошибок.
    - Перезапустить службу: `sudo systemctl restart catalog-uploader`, затем `systemctl is-active catalog-uploader` → `active`.
    - Сверить, что серверная версия содержит переделку: на сервере grep `_process_async` в app.py возвращает >=1, grep `acquire(blocking=False)` возвращает >=1.
    - Убедиться, что служба отвечает: запрос главной страницы `/<APP_SECRET>` (секрет берёт sysadmin из uploader/.env, НЕ выводить в лог) возвращает HTTP 200; неверный токен — 404.
    Все команды и их вывод (без секретов) зафиксируй для SUMMARY.
  </action>
  <verify>
    <automated>Через sysadmin: `/opt/apps/catalog/.venv/bin/python -m py_compile /opt/apps/catalog/uploader/app.py && grep -c _process_async /opt/apps/catalog/uploader/app.py && systemctl is-active catalog-uploader`</automated>
  </verify>
  <acceptance_criteria>
    - `py_compile` серверного `uploader/app.py` проходит без ошибок.
    - `grep -c _process_async /opt/apps/catalog/uploader/app.py` >= 1 и `grep -c "acquire(blocking=False)"` >= 1 (переделка на сервере).
    - `systemctl is-active catalog-uploader` → `active`.
    - Главная страница по секретному URL отдаёт HTTP 200; неверный токен — 404.
    - `credentials.json`, `.env`, `uploader/.env` не изменены и не выводились в лог.
  </acceptance_criteria>
  <done>Переделанный загрузчик на сервере, служба перезапущена и активна, страница отвечает; секреты не тронуты.</done>
</task>

<task type="auto">
  <name>Task 2: Сквозная проверка асинхронного сценария на боевой странице через sysadmin</name>
  <read_first>
    - uploader/app.py (маршруты update/history, _process_async — поведение, которое проверяем)
    - .planning/phases/02-1/02-CONTEXT.md (D-01 мгновенный ответ, D-02 двойной запуск, D-03/D-07 итог владельцу, D-05 история/поллинг)
    - scripts/notify_tg.py (как выглядит Telegram-сообщение владельцу)
  </read_first>
  <action>
    Через субагента `sysadmin` проверь сквозной сценарий на боевом загрузчике. Режим (с реальной записью / без записи) — по решению из checkpoint.

    Базовые проверки (всегда, через curl на localhost сервера, токен берёт sysadmin из env, НЕ выводить):
    - `POST /<token>/update` при ПУСТОЙ папке входящих → `ok=false`, message «Нет файлов…».
    - `GET /<token>/history` → JSON с ключом `history` (массив); зафиксировать структуру записи (ts/status/count/result_text/files).
    - `GET /<token>/history` по НЕВЕРНОМУ токену → HTTP 404.

    Проверка асинхронности и двойного запуска:
    - Положить партию .xlsx во входящие (повторно из last_batch допустимо). Выполнить `POST /<token>/update` → ДОЛЖЕН вернуться немедленно `ok=true`, message «Файлы отправлены…», БЕЗ числа товаров (UPL-01, D-07). Замерить, что ответ пришёл за секунды, не за минуты.
    - Сразу (пока обработка идёт) повторить `POST /<token>/update` → `ok=false`, message «Обработка уже идёт…»; убедиться по логам службы, что второй upload.py НЕ запускался (в journalctl один «Запуск upload.py») (D-02).
    - Опросить `GET /<token>/history` несколько раз: сначала запись `status=processing` («обрабатывается…»), после завершения — `status` ok/warn/error с нейтральным `result_text` (D-05).

    Проверка изоляции деталей и Telegram (D-03, D-07):
    - Подтвердить (со слов владельца — наблюдает Telegram), что подробный итог пришёл владельцу; оператору (в ответах /update и /history) техдеталей/трассировок нет.

    Персистентность истории (D-04): после `sudo systemctl restart catalog-uploader` повторный `GET /<token>/history` всё ещё содержит прошлые записи (history.json на диске).

    Безопасность каталога: если делали реальный прогон — сверить, что подушка безопасности отработала (бэкап «Товары_BACKUP» есть; при «мало товаров» каталог откатился). Команда сверки числа: через sysadmin `/opt/apps/catalog/.venv/bin/python /opt/apps/catalog/scripts/sheet_tool.py count`.

    Всё зафиксировать для SUMMARY (без секретов).
  </action>
  <verify>
    <automated>Через sysadmin: `curl -s -o /dev/null -w "%{http_code}" https://uploader.zhukoleg.ru/НЕВЕРНЫЙ_ТОКЕН/history` → 404; и `journalctl -u catalog-uploader --since "-5 min" | grep -c "Запуск upload.py"` после двойного нажатия → 1</automated>
  </verify>
  <acceptance_criteria>
    - `POST /<token>/update` с файлами возвращается немедленно (секунды) с `ok=true`, message «Файлы отправлены…», без числа товаров (UPL-01, D-07).
    - Повторный `POST /<token>/update` во время обработки → `ok=false` «Обработка уже идёт…»; в journalctl за период один «Запуск upload.py» (D-02).
    - `GET /<token>/history` показывает запись `processing`, затем финальный `status` (ok/warn/error) с нейтральным `result_text`; по неверному токену — 404 (D-05, UPL-04).
    - Владелец подтвердил приход Telegram-итога; оператор техдеталей не видел (UPL-03, D-03/D-07).
    - После рестарта службы история сохранилась (D-04).
    - Если был реальный прогон — каталог не пострадал (бэкап есть; sheet_tool.py count осмыслен).
  </acceptance_criteria>
  <done>Сквозной асинхронный сценарий подтверждён на боевом сервере: мгновенный отклик, фон, Telegram владельцу, история с поллингом, защита двойного запуска, персистентность; каталог цел.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Переделанный загрузчик развёрнут и проверен на daniella: асинхронный отклик, история, Telegram-итог владельцу, защита двойного запуска.</what-built>
  <how-to-verify>
    1. Откройте боевую страницу загрузчика по своей секретной ссылке с телефона.
    2. Загрузите тестовую партию .xlsx и нажмите «Обновить» — убедитесь, что СРАЗУ появилось нейтральное «файлы отправлены», страница не висит.
    3. Убедитесь, что в таблице истории появилась строка «обрабатывается…», затем через некоторое время — нейтральный итог.
    4. Проверьте, что Telegram-сообщение с подробным итогом пришло ВАМ (владельцу), а на странице оператора деталей нет.
    5. (Опционально) Нажмите «Обновить» второй раз сразу — должно ответить «обработка уже идёт».
    6. Убедитесь, что кнопка «Откатить к прошлой версии» на месте и работает как раньше.
  </how-to-verify>
  <resume-signal>Напишите «всё работает как ожидал» или опишите расхождения (страница висит / нет истории / детали видны оператору / Telegram не пришёл / двойной запуск сработал).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Локальный репозиторий → сервер daniella | Доставка кода; нельзя затронуть секреты на сервере |
| Боевой /update → upload.py (реальная запись в Sheet) | Контрольный прогон может перезаписать каталог |
| Боевой /update → Telegram владельцу | Детали уходят владельцу, не оператору |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02b-01 | Tampering | непроверенный код на сервере перезапишет каталог | mitigate | деплоится только версия, прошедшая Wave 1; py_compile на сервере + подушка безопасности (бэкап перед записью) |
| T-02b-02 | Information Disclosure | вывод credentials.json/.env/APP_SECRET в лог при серверных командах | mitigate | секреты не читаются и не печатаются; sysadmin берёт токен из env, не выводит; деплой трогает только app.py |
| T-02b-03 | Denial of Service | реальный контрольный прогон даёт «мало товаров» | mitigate | подушка безопасности откатывает каталог при warn; контрольный прогон опционален (решение на checkpoint) |
| T-02b-04 | Tampering | прямой ssh в обход правил проекта | mitigate | все серверные операции через субагента sysadmin (autonomous: false), наблюдение владельца |
| T-02b-05 | Spoofing | доступ к /history без секрета | mitigate | проверка: неверный токен → 404 (endpoint под /<token>) |
</threat_model>

<verification>
- Серверный uploader/app.py компилируется и содержит _process_async + acquire(blocking=False)
- Служба catalog-uploader active после рестарта; страница отдаёт 200, неверный токен — 404
- /update отвечает мгновенно нейтрально; повторный вызов при обработке — «обработка уже идёт» (journalctl: один запуск upload.py)
- /history отдаёт записи с processing → финальным статусом; персистентность после рестарта
- Telegram-итог пришёл владельцу; оператор деталей не видел
- Каталог цел (бэкап доступен; при контрольном прогоне count осмыслен)
</verification>

<success_criteria>
- Success Criteria #1–4 подтверждены на боевом сервере (мгновенный отклик, Telegram владельцу, таблица истории, нейтральность для оператора)
- UPL-01..04 проверены сквозным сценарием в проде
- Защита двойного запуска и персистентность истории подтверждены
- Деплой выполнен через sysadmin, владелец подтвердил работу
</success_criteria>

<output>
Create `.planning/phases/02-1/02-02-SUMMARY.md` when done
</output>
