---
phase: 08-1
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified: []
autonomous: false
requirements: [HIDE-01, HIDE-02, HIDE-03, HIDE-04, HIDE-05]
must_haves:
  truths:
    - "Витрина задеплоена (Vercel по git push); сервер обновлён через сисадмина (pull + restart catalog-uploader + перепрогон)"
    - "Скрытие товара в админ-панели → товар отсутствует в /api/products после «Применить сейчас»"
    - "Возврат товара глазиком → товар снова появляется в /api/products после перепрогона"
    - "Товар остаётся в листе «Товары» Google Sheet при скрытии (не удалён)"
  artifacts: []
  key_links:
    - from: "git push main"
      to: "Vercel деплой витрины"
      via: "автоматический деплой lib/*"
      pattern: ""
    - from: "сисадмин pull + restart"
      to: "сервер daniella (catalog-uploader)"
      via: "обновление scripts/*.py, uploader/admin.py"
      pattern: ""
---

<objective>
Раздельно задеплоить изменения этапа и провести сквозную проверку скрытия: витрина — на Vercel через git push (`lib/*`), сервер — через субагента sysadmin (pull + restart службы `catalog-uploader` + перепрогон `upload.py`). Затем владелец проверяет цикл скрыть/вернуть в реальной админ-панели и на витрине.

Purpose: Замыкает все требования HIDE-01..05 в проде. Деплой раздельный (память проекта `server-via-sysadmin`): серверные операции — ТОЛЬКО через сисадмина, не напрямую. Поэтому план неавтономный.
Output: Изменения этапа в проде; подтверждённый цикл скрытия.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-1/08-CONTEXT.md
@.planning/phases/08-1/08-01-SUMMARY.md
@.planning/phases/08-1/08-02-SUMMARY.md
@.planning/phases/08-1/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Деплой витрины (Vercel) + обновление сервера через сисадмина</name>
  <read_first>
    - .planning/phases/08-1/08-01-SUMMARY.md, 08-02-SUMMARY.md, 08-03-SUMMARY.md (что изменено)
    - CLAUDE.md (раздел «Команды»: ежедневное обновление; правило «после upload.py удалять .next»)
    - MEMORY.md → server-via-sysadmin (серверные операции только через субагента sysadmin)
  </read_first>
  <action>
    Витрина (автоматизируемо локально): закоммитить и запушить изменения `lib/types.ts`, `lib/sheets.ts` в ветку main — Vercel задеплоит сам. Коммит-сообщение на русском в формате `feat(этап-8): ...`. PowerShell-синтаксис («;» вместо «&&»).
    Сервер (ТОЛЬКО через субагента sysadmin — НЕ выполнять ssh/перепрогон напрямую): поручить субагенту sysadmin на daniella выполнить от имени службы: git pull в каталоге приложения; restart службы `catalog-uploader`; перепрогон `upload.py` через venv `/opt/apps/catalog/.venv`. Сисадмин подтверждает, что админ-панель отвечает и колонка «Скрыт» (L) появилась в листе «Товары».
    НЕ запускать боевой upload.py локально — данные/ключи живут на сервере.
  </action>
  <verify>
    <automated>git log --oneline -1 -- lib/sheets.ts lib/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - Коммит с изменениями `lib/*` присутствует в истории main и запушен (git status чист по этим файлам).
    - Субагент sysadmin отчитался: pull выполнен, служба `catalog-uploader` перезапущена, перепрогон `upload.py` прошёл, лист «Товары» содержит колонку «Скрыт» (L).
    - Серверные операции выполнены ТОЛЬКО через субагента sysadmin (не прямым ssh из этого плана).
  </acceptance_criteria>
  <done>Витрина задеплоена на Vercel; сервер daniella обновлён через сисадмина; колонка «Скрыт» присутствует в листе «Товары».</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Сквозная проверка цикла скрыть/вернуть</name>
  <what-built>
    Кнопка-глазик в админ-панели (скрыть/вернуть), фильтрация скрытых на витрине, сохранение скрытия в памяти правок через перепрогон.
  </what-built>
  <how-to-verify>
    1. Открыть админ-панель (по ADMIN_SECRET-ссылке на daniella). Найти тестовый товар.
    2. Нажать глазик → карточка должна мгновенно потускнеть, глиф смениться на «скрытый», появиться чип «скрыт» (HIDE-01, HIDE-03).
    3. Нажать «Применить сейчас» (перепрогон). Дождаться завершения.
    4. Открыть витрину каталога и/или запрос `/api/products` (через секретную ссылку) → тестовый товар ОТСУТСТВУЕТ (HIDE-05).
    5. Проверить лист «Товары» Google Sheet → товар на месте, в колонке «Скрыт» (L) стоит `1` (HIDE-04, критерий «остаётся в листе»).
    6. В админ-панели включить фильтр «Скрытые» → тестовый товар находится в списке (HIDE-02 — возврат удобен при ~900 товарах).
    7. Нажать глазик повторно → карточка восстанавливается; «Применить сейчас»; на витрине товар снова появляется (HIDE-02).
    8. Бегло проверить вид в density-s и в режимах grid/list — глазик и чип читаемы, не перекрывают карандаш кадрирования.
  </how-to-verify>
  <resume-signal>Напишите «подтверждаю» если все 8 шагов прошли, либо опишите расхождения</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| локальный репозиторий → Vercel/прод | деплой витрины с серверной фильтрацией скрытых |
| оператор/сисадмин → сервер daniella | обновление кода службы и перепрогон |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | прод /api/products | mitigate | Шаг 4 чек-листа явно проверяет отсутствие скрытого товара в ответе /api/products перед закрытием этапа |
| T-08-09 | Tampering | боевой перепрогон | accept | Перепрогон выполняется только через сисадмина в контролируемой среде (venv, restart службы); подушка безопасности — товар остаётся в листе, скрытие обратимо |

Ни одна угроза не достигает порога high.
</threat_model>

<verification>
- git: коммит `lib/*` в main, запушен.
- Сисадмин: pull + restart + перепрогон выполнены, колонка «Скрыт» в листе.
- Человек-владелец: 8 шагов чек-листа пройдены (скрыть → нет на витрине → в листе остаётся → фильтр находит → вернуть → снова на витрине).
</verification>

<success_criteria>
Витрина и сервер задеплоены раздельным корректным путём; подтверждён полный цикл: скрытие убирает товар с витрины (и из /api/products) после «Применить сейчас», товар остаётся в листе, возврат глазиком возвращает его. Все HIDE-01..05 наблюдаемы в проде.
</success_criteria>

<output>
Create `.planning/phases/08-1/08-04-SUMMARY.md` when done
</output>
