---
phase: 08-1
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - uploader/admin.py
autonomous: true
requirements: [HIDE-01, HIDE-02, HIDE-03]
must_haves:
  truths:
    - "В карточке админ-панели есть кнопка-глазик в правом верхнем углу фото с зоной нажатия ≥44px"
    - "Клик по глазику оптимистично переключает состояние: карточка тускнеет, глиф меняется, чип «скрыт» появляется/исчезает — без диалога подтверждения"
    - "Повторный клик возвращает товар (правка type=скрыт value=пусто); при ошибке сети состояние откатывается"
    - "Эндпоинт /save принимает type=скрыт (value 1 или пусто) под защитой ADMIN_SECRET; пустое значение допускается"
    - "Фильтр видимости позволяет показать только скрытые товары совместно с поиском; по умолчанию видны все, скрытые тусклые"
  artifacts:
    - path: "uploader/admin.py"
      provides: "type=скрыт в SAVE_ALLOWED_TYPES; пустое значение разрешено; кнопка-глазик, optimistic toggleHidden, класс .pcard.hidden, чип «скрыт», фильтр видимости"
      contains: "скрыт"
  key_links:
    - from: "кнопка-глазик (JS toggleHidden)"
      to: "POST /<TOKEN>/save"
      via: "apiCall с type=скрыт, value 1/пусто"
      pattern: "type.*скрыт|toggleHidden"
    - from: "/save обработчик"
      to: "sheet_helper append_edit"
      via: "shell-out с type=скрыт"
      pattern: "скрыт"
---

<objective>
Добавить в админ-панель управление скрытием: бэкенд `/save` принимает новый тип `скрыт` (под существующей защитой ADMIN_SECRET, с разрешённым пустым значением), а в карточке появляется кнопка-глазик с оптимистичным переключением, тусклым видом скрытой карточки, чипом «скрыт» и фильтром видимости.

Purpose: Реализует HIDE-01 (скрыть), HIDE-02 (вернуть, в т.ч. фильтр для поиска скрытых), HIDE-03 (визуальное отличие скрытой карточки). Зависит от плана 01: `/save` пишет правку через `sheet_helper.append_edit`, который валидирует тип против `sheet_helper.ALLOWED_TYPES` — там тип `скрыт` должен уже присутствовать.
Output: `uploader/admin.py` с поддержкой скрытия (бэкенд + UI). Деплой на сервер — план 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-1/08-CONTEXT.md
@.planning/phases/08-1/08-PATTERNS.md

<interfaces>
<!-- Точные точки интеграции, подтверждены grep. -->

uploader/admin.py:228 — SAVE_ALLOWED_TYPES = {"group", "name", "photo", "badge", "подгруппа"}
uploader/admin.py:235-238 — if not value and edit_type not in ("photo", "badge"): return 400 "Значение не указано"
uploader/admin.py:240-278 — путь записи /save (нормализация ключа → shell-out sheet_helper append_edit) — типонезависим, не менять
uploader/admin.py:606+ / 771-783 — CSS .pcard-photo (position:relative) и .pcard-edit-photo (оверлей top:4px right:4px, 40px, density-s 34px)
uploader/admin.py:631 — секция .pcard-badges / образец чипа-индикатора .badge-attn
uploader/admin.py:872-877 — HTML filter-tabs (вкладки фильтра)
uploader/admin.py:945 — activeFilter = "all" по умолчанию
uploader/admin.py:1001-1008 — apiCall (POST + JSON + graceful null)
uploader/admin.py:1018-1031 — filteredProducts (клиентский фильтр + поиск)
uploader/admin.py:1082-1132 — buildCard: рендер .pcard-photo + editPhotoBtn оверлей
uploader/admin.py:1221-1254 — toggleBadge (ПРЯМОЙ образец optimistic toggle с откатом)
uploader/admin.py:1278-1281 — syncProduct (синхронизация товара в allProducts)
uploader/admin.py:1877-1882 — обработчик переключения вкладок фильтра
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Бэкенд /save — приём типа `скрыт`</name>
  <read_first>
    - uploader/admin.py (SAVE_ALLOWED_TYPES ~стр.228; проверка пустого значения ~стр.235-238; путь записи ~стр.240-278)
    - .planning/phases/08-1/08-PATTERNS.md (раздел «admin.py — обработчик /save»: SP-1, SP-2)
  </read_first>
  <action>
    Добавить `"скрыт"` в множество SAVE_ALLOWED_TYPES (~стр.228).
    Разрешить пустое значение для `скрыт`: в проверке `if not value and edit_type not in ("photo", "badge")` добавить `"скрыт"` в кортеж исключений → `("photo", "badge", "скрыт")`. Это позволяет value="" означать снятие скрытия (как у badge).
    Остальной путь записи (~стр.240-278: нормализация ключа на сервере → shell-out `sheet_helper append_edit` → graceful 500) НЕ менять — он типонезависим.
    Никаких изменений в проверке ADMIN_SECRET — новый тип проходит через тот же защищённый маршрут, неаутентифицированного пути записи не создаётся.
    Русские комментарии у новых строк.
  </action>
  <verify>
    <automated>python -m py_compile uploader/admin.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile uploader/admin.py` без ошибок.
    - SAVE_ALLOWED_TYPES содержит `"скрыт"`.
    - Кортеж исключений пустого значения содержит `"скрыт"` (grep находит `"скрыт"` рядом с `("photo", "badge"`).
    - Маршрут /save по-прежнему защищён ADMIN_SECRET (проверка токена не изменена — отдельного публичного пути для type=скрыт нет).
  </acceptance_criteria>
  <done>/save принимает type=скрыт с value «1» или пустым; защита ADMIN_SECRET не ослаблена; путь записи переиспользован без изменений.</done>
</task>

<task type="auto">
  <name>Task 2: UI карточки — кнопка-глазик, optimistic toggle, тусклый вид, чип, фильтр видимости</name>
  <read_first>
    - uploader/admin.py (CSS .pcard-photo/.pcard-edit-photo ~стр.771-783; чип .badge-attn ~стр.631; filter-tabs ~стр.872-877; buildCard ~стр.1082-1132; filteredProducts ~стр.1018-1031; toggleBadge ~стр.1221-1254; apiCall ~стр.1001-1008; syncProduct ~стр.1278-1281; activeFilter ~стр.945; обработчик вкладок ~стр.1877-1882)
    - .planning/phases/08-1/08-PATTERNS.md (раздел «admin.py — встроенный JS/CSS»: аналоги 1-5, «No Analog Found»)
  </read_first>
  <action>
    CSS:
    - Добавить правило `.pcard-eye` по образцу `.pcard-edit-photo`: position:absolute оверлеем в правом ВЕРХНЕМ углу области фото (D-04). Поскольку `.pcard-edit-photo` уже занимает right:4px, разместить глазик так, чтобы не перекрывать карандаш (например left:4px, либо сдвинуть по right). Зона нажатия ≥44px (D-05) — увеличить относительно образца 40px; компактный вариант для `.view-grid.density-s` по образцу последней строки .pcard-edit-photo.
    - Добавить правило `.pcard.hidden { opacity: 0.45; }` (D-01) — карточка ОСТАЁТСЯ интерактивной, НЕ ставить pointer-events:none.
    - Добавить чип «скрыт» по образцу `.badge-attn` (D-02) — маленькая метка в углу карточки, видна во всех режимах плотности (density-l/m/s) и в grid/list.
    JS рендер (buildCard ~стр.1082-1132):
    - Рядом с editPhotoBtn в `.pcard-photo` добавить кнопку-глазик. Глиф отражает состояние (D-03): открытый глаз (👁, `&#128065;`) когда товар видим (клик → скрыть); перечёркнутый/закрытый (🙈 `&#128584;` или 🚫) когда скрыт (клик → вернуть). При `p.hidden` навешивать на карточку класс `pcard hidden` и показывать чип «скрыт».
    JS логика — функция toggleHidden по ПРЯМОМУ образцу toggleBadge (~стр.1221-1254):
    - `const prev = p.hidden; const next = !p.hidden;` → оптимистично `p.hidden = next; syncProduct(p);` + переключить класс карточки `.hidden`, сменить глиф глазика, показать/скрыть чип; saveScrollPos перед перерисовкой.
    - `apiCall('/<TOKEN>/save', { type: "скрыт", value: next ? "1" : "" })`; при успехе toast/cardStatus ok; при ошибке откат `p.hidden = prev; syncProduct(p)` + восстановить класс/глиф/чип + toast err.
    - БЕЗ диалога подтверждения (D-06).
    Фильтр видимости (D-08/D-09) — клиентский, совместно с поиском:
    - В filteredProducts (~стр.1018-1031) добавить ветку фильтра видимости (например activeFilter === "hidden" → только `p.hidden`). Ветка `all` (по умолчанию) НЕ должна отфильтровывать скрытые — показывать ВСЕ, скрытые тусклыми (D-08).
    - В HTML filter-tabs (~стр.872-877) добавить вкладку видимости (data-filter="hidden", подпись «Скрытые»). Механика переключения вкладок (~стр.1877-1882) переиспользуется без изменений.
    Русские комментарии у нового кода.
  </action>
  <verify>
    <automated>python -m py_compile uploader/admin.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile uploader/admin.py` без ошибок.
    - В CSS присутствуют `.pcard-eye` (position:absolute, размер ≥44px) и `.pcard.hidden` (opacity ~0.45 без pointer-events:none).
    - В buildCard кнопка-глазик рендерится в `.pcard-photo`; при p.hidden карточка получает класс `hidden` и чип «скрыт».
    - Функция toggleHidden отправляет apiCall с `type: "скрыт"` и `value` «1»/пусто, делает оптимистичное обновление и откат при ошибке (grep находит `toggleHidden` и `скрыт`).
    - filteredProducts содержит ветку фильтра скрытых; ветка по умолчанию не отфильтровывает скрытые; в filter-tabs есть вкладка видимости.
  </acceptance_criteria>
  <done>Кнопка-глазик с состоянием-глифом, оптимистичный toggleHidden с откатом, тусклая карточка `.pcard.hidden` + чип «скрыт», фильтр видимости совместно с поиском. Работает в density-l/m/s и grid/list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер админ-панели → /save | запись правки `скрыт` пересекает границу аутентификации (ADMIN_SECRET в URL-токене) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Elevation of Privilege | /save type=скрыт | mitigate | Новый тип проходит через существующий маршрут /<TOKEN>/save под проверкой ADMIN_SECRET (hmac.compare_digest → 404); отдельного публичного пути записи не добавляется |
| T-08-06 | Tampering | value типа `скрыт` | mitigate | Бэкенд допускает только осмысленное использование: пустое значение = снятие, «1» = скрыт (трактуется витриной как row[11]==="1"); произвольное значение не вызывает скрытия на витрине (план 02) и не ломает append_edit |
| T-08-07 | Denial of Service | apiCall при сбое сети | accept | Оптимистичный UI с откатом по образцу toggleBadge — при ошибке состояние возвращается, данные не теряются; низкий риск |

Ни одна угроза не достигает порога high.
</threat_model>

<verification>
- `python -m py_compile uploader/admin.py` — без ошибок.
- grep: `"скрыт"` в SAVE_ALLOWED_TYPES и в кортеже исключений пустого значения; `toggleHidden`, `.pcard-eye`, `.pcard.hidden` присутствуют.
- Визуальная и функциональная проверка в браузере (клик глазика → тусклеет; возврат; фильтр) выполняется после деплоя на сервер — план 04 (checkpoint).
</verification>

<success_criteria>
/save принимает type=скрыт под ADMIN_SECRET с разрешённым пустым значением; в карточке кнопка-глазик с оптимистичным переключением, тусклым видом, чипом и сменой глифа; фильтр видимости совместно с поиском. Покрыто HIDE-01, HIDE-02, HIDE-03.
</success_criteria>

<output>
Create `.planning/phases/08-1/08-03-SUMMARY.md` when done
</output>
