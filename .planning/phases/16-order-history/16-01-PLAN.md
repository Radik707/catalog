---
phase: 16-order-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/useOrderHistory.ts
autonomous: true
requirements: [HIST-01, HIST-02, HIST-04]
must_haves:
  truths:
    - "Хук useOrderHistory загружает историю из localStorage при монтировании и сохраняет при изменении (по образцу useCart)"
    - "Добавление 21-й записи тихо вытесняет самую старую (FIFO, потолок 20)"
    - "Битая/неполная запись при чтении пропускается, валидные записи остаются (мягкая деградация)"
    - "Тип OrderHistoryEntry содержит items, total, createdAt, channel и уникальный id"
  artifacts:
    - path: "lib/types.ts"
      provides: "Типы OrderHistoryItem и OrderHistoryEntry"
      contains: "interface OrderHistoryEntry"
    - path: "lib/useOrderHistory.ts"
      provides: "Хук-store истории заказов на localStorage с CRUD"
      exports: ["useOrderHistory"]
      min_lines: 60
  key_links:
    - from: "lib/useOrderHistory.ts"
      to: "localStorage['catalog-order-history']"
      via: "loadHistory/saveHistory с тихим try/catch"
      pattern: "catalog-order-history"
    - from: "lib/useOrderHistory.ts"
      to: "lib/types.ts"
      via: "импорт типа OrderHistoryEntry"
      pattern: "OrderHistoryEntry"
---

<objective>
Создать фундамент локальной истории заказов: TypeScript-тип записи истории и хук-store
`useOrderHistory` на базе localStorage по образцу `useCart`/`useFavorites`.

Purpose: Это единственный источник данных истории заказов. План 02 (страница /orders и
встройка записи в корзину) строится поверх контракта этого хука. Сначала определяем
контракт (тип + API хука), затем потребители используют его без исследования кода.

Output: `lib/types.ts` дополнен типами `OrderHistoryItem` и `OrderHistoryEntry`;
создан `lib/useOrderHistory.ts` с CRUD (добавить запись с FIFO-вытеснением, удалить
запись, очистить всё) и мягкой деградацией битых записей при чтении.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-order-history/16-CONTEXT.md
@.planning/phases/16-order-history/16-PATTERNS.md

<interfaces>
<!-- Контракты из кодовой базы. Исполнитель использует их напрямую, без исследования кода. -->

From lib/useCart.ts (эталон localStorage-хука — копировать структуру):
```typescript
const CART_KEY = 'catalog-cart';
function loadCart(): CartItem[] { /* typeof window guard + try/catch + JSON.parse */ }
function saveCart(items: CartItem[]) { /* typeof window guard + try/catch + setItem */ }
export function useCart() {
  const [items, setItems] = useState<CartItem[]>([]);
  const [isLoaded, setIsLoaded] = useState(false);
  useEffect(() => { setItems(loadCart()); setIsLoaded(true); }, []);   // load-on-mount
  useEffect(() => { if (isLoaded) saveCart(items); }, [items, isLoaded]); // save-on-change
  const removeFromCart = useCallback((productId: string) => { /* filter */ }, []);
  const clearCart = useCallback(() => setItems([]), []);
}
```

From lib/types.ts (куда добавляются новые типы):
```typescript
export interface Product {
  id: string; name: string; price: number; stock: number;
  supplier: string; imageUrl?: string; /* ... */
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Объявить типы OrderHistoryItem и OrderHistoryEntry</name>
  <files>lib/types.ts</files>
  <read_first>
    - lib/types.ts (файл, который дополняем — точное место для нового типа после Product)
    - .planning/phases/16-order-history/16-PATTERNS.md (строки 115–149 — готовая форма типа по решениям D-07…D-11)
    - .planning/phases/16-order-history/16-CONTEXT.md (раздел «Форма снимка заказа», D-07..D-11)
  </read_first>
  <behavior>
    - OrderHistoryItem содержит поля: id (string), name (string), quantity (number), priceAtOrder (number), unit (string), imageUrl (string, опционально) — ровно по D-07
    - OrderHistoryEntry содержит: id (string), items (OrderHistoryItem[]), total (number), createdAt (string), channel ('telegram' | 'max') — по D-11
    - Оба типа экспортируются (export interface), доступны как `@/lib/types`
  </behavior>
  <action>
    Добавить в `lib/types.ts` (после интерфейса Product) два экспортируемых интерфейса с русскими комментариями.
    `OrderHistoryItem` — урезанный снимок позиции (НЕ целый Product, per D-07): поля `id: string` (id товара, задел под «Повторить заказ» per D-09), `name: string`, `quantity: number`, `priceAtOrder: number` (эффективная цена, которую видел клиент, per D-08), `unit: string`, `imageUrl?: string` (для мини-фото, per D-10).
    `OrderHistoryEntry` — одна запись отправленного заказа: `id: string` (уникальный id записи), `items: OrderHistoryItem[]`, `total: number` (сумма по priceAtOrder × quantity, per D-11), `createdAt: string` (ISO-строка), `channel: 'telegram' | 'max'` (per D-02/D-11).
    Не добавлять поля «статус»/«подтверждено»/«доставлено» — это запрещено инвариантом вехи v1.4 (per D-06, HIST-03).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' lib/types.ts | grep -c "export interface OrderHistoryEntry"` возвращает 1
    - `grep -v '^//' lib/types.ts | grep -c "export interface OrderHistoryItem"` возвращает 1
    - В OrderHistoryItem присутствуют идентификаторы: id, name, quantity, priceAtOrder, unit, imageUrl
    - В OrderHistoryEntry присутствуют идентификаторы: id, items, total, createdAt, channel
    - channel типизирован как `'telegram' | 'max'` (строковый литерал-юнион, не string)
    - `npx tsc --noEmit` завершается без ошибок
    - В файле нет полей со словами «status»/«статус»/«подтверж»/«достав»
  </acceptance_criteria>
  <done>Оба типа объявлены, экспортированы, проект проходит проверку типов.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать хук useOrderHistory (localStorage CRUD + FIFO 20 + мягкая деградация)</name>
  <files>lib/useOrderHistory.ts</files>
  <read_first>
    - lib/useCart.ts (эталон: ключ, loadCart/saveCart, useState+isLoaded+двойной useEffect, useCallback-мутации)
    - lib/useFavorites.ts (эталон валидации массива при чтении — Array.isArray + filter)
    - lib/types.ts (типы OrderHistoryEntry/OrderHistoryItem из Task 1)
    - .planning/phases/16-order-history/16-PATTERNS.md (строки 20–111 — готовые паттерны хука, FIFO, removeEntry)
  </read_first>
  <behavior>
    - loadHistory при typeof window === 'undefined' возвращает []
    - loadHistory при повреждённом JSON возвращает [] (try/catch), не бросает исключение
    - loadHistory отбрасывает невалидные элементы массива (нет id/items/createdAt) — мягкая деградация per D-14
    - addEntry добавляет запись в конец; при длине > 20 убирает самые старые (slice с начала), сохраняя последние 20 — FIFO per D-13
    - removeEntry(entryId) удаляет запись с совпадающим id
    - clearHistory очищает все записи
    - сохранение в localStorage не происходит до завершения первой загрузки (isLoaded-флаг)
  </behavior>
  <action>
    Создать `lib/useOrderHistory.ts` с директивой `'use client'` и русскими комментариями, по структуре `lib/useCart.ts`.
    Константа ключа: `const HISTORY_KEY = 'catalog-order-history';` (per Claude's Discretion — паттерн `catalog-*`).
    Константа потолка: `const MAX_HISTORY_ENTRIES = 20;` (per D-13).
    `loadHistory(): OrderHistoryEntry[]` — guard `typeof window === 'undefined'` → []; try `JSON.parse`; результат прогнать через `Array.isArray(arr) ? arr.filter(isValidEntry) : []` где `isValidEntry` проверяет наличие `id`, массива `items`, `createdAt` и `channel` (мягкая деградация per D-14); catch → []. Битые элементы молча пропускаются, валидные остаются.
    `saveHistory(entries)` — guard + try/catch (тихий, комментарий про приватный режим), `localStorage.setItem(HISTORY_KEY, JSON.stringify(entries))`.
    Хук `useOrderHistory()`: `useState<OrderHistoryEntry[]>([])` + `isLoaded`; первый useEffect — `setEntries(loadHistory()); setIsLoaded(true)`; второй useEffect — `if (isLoaded) saveHistory(entries)` с зависимостями `[entries, isLoaded]`.
    Мутации через `useCallback`: `addEntry(entry: OrderHistoryEntry)` — `setEntries(prev => { const next = [...prev, entry]; return next.length > MAX_HISTORY_ENTRIES ? next.slice(next.length - MAX_HISTORY_ENTRIES) : next; })` (FIFO per D-13); `removeEntry(entryId: string)` — filter по id (per D-12); `clearHistory()` — `setEntries([])` (per D-12).
    Возвращать объект: `{ entries, isLoaded, addEntry, removeEntry, clearHistory }`.
    Не использовать сетевые вызовы и не читать данные из API — только localStorage, чтобы работало офлайн (HIST-02).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "catalog-order-history" lib/useOrderHistory.ts` >= 1
    - `grep -c "MAX_HISTORY_ENTRIES = 20" lib/useOrderHistory.ts` == 1
    - `grep -c "export function useOrderHistory" lib/useOrderHistory.ts` == 1
    - Файл содержит идентификаторы addEntry, removeEntry, clearHistory, isLoaded, loadHistory, saveHistory
    - `grep -c "typeof window === 'undefined'" lib/useOrderHistory.ts` >= 1 (SSR-guard)
    - `grep -c "Array.isArray" lib/useOrderHistory.ts` >= 1 (мягкая деградация D-14)
    - `grep -c "slice" lib/useOrderHistory.ts` >= 1 (FIFO-вытеснение D-13)
    - Первая строка файла — `'use client';`
    - Файл импортирует OrderHistoryEntry из `./types` или `@/lib/types`
    - `npx tsc --noEmit` завершается без ошибок
  </acceptance_criteria>
  <done>Хук создан, экспортирует CRUD + FIFO 20 + мягкую деградацию, проект проходит проверку типов.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → приложение | Данные в localStorage пользователь/расширения могут испортить; читаются как недоверенный вход. Сети, аутентификации и серверных данных в этом плане НЕТ. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | loadHistory() парсинг JSON из localStorage | mitigate | try/catch вокруг JSON.parse + Array.isArray + filter(isValidEntry); битый JSON/запись не роняют хук (D-14, HIST-04) |
| T-16-02 | Denial of Service | localStorage недоступен (приватный режим iOS) | mitigate | typeof window guard + тихий try/catch в load/save — деградация к пустой истории без падения (паттерн useCart) |
| T-16-03 | Denial of Service | Разрастание localStorage при многих заказах | mitigate | FIFO-потолок MAX_HISTORY_ENTRIES = 20 (D-13) — размер хранилища ограничен |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Новых зависимостей нет — план использует только React и существующие модули; устанавливать нечего |
</threat_model>

<verification>
- `npx tsc --noEmit` — проект проходит проверку типов после добавления типа и хука.
- Хук читает/пишет только `localStorage['catalog-order-history']`, без сетевых вызовов (grep подтверждает отсутствие fetch).
- Структура хука повторяет useCart (load-on-mount, save-on-change, isLoaded-guard).
</verification>

<success_criteria>
- `lib/types.ts` содержит OrderHistoryItem и OrderHistoryEntry с полями по D-07/D-11.
- `lib/useOrderHistory.ts` экспортирует useOrderHistory с addEntry (FIFO 20), removeEntry, clearHistory.
- Битый JSON и неполные записи не ломают чтение (мягкая деградация D-14).
- Никаких сетевых зависимостей — история работает офлайн (HIST-02).
</success_criteria>

<output>
Create `.planning/phases/16-order-history/16-01-SUMMARY.md` when done
</output>
