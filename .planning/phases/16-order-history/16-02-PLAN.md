---
phase: 16-order-history
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - app/catalog/[secret]/cart/page.tsx
  - app/catalog/[secret]/orders/page.tsx
autonomous: false
requirements: [HIST-01, HIST-03, HIST-04]
user_setup: []
must_haves:
  truths:
    - "При нажатии «Отправить заказ в Telegram» в историю добавляется запись с channel: 'telegram'"
    - "При нажатии «Отправить заказ в MAX» в историю добавляется запись с channel: 'max'"
    - "Корзина после отправки НЕ очищается (поведение clearCart не вызывается при отправке)"
    - "Страница /orders показывает «Отправленные заказы», список записей с мини-фото, суммой, датой, каналом"
    - "Пользователь может очистить всю историю и удалить отдельную запись (с confirm)"
    - "Из пустой и заполненной корзины есть видимая ссылка на /orders"
    - "Нигде на странице нет слов «статус»/«принято»/«в доставке»"
  artifacts:
    - path: "app/catalog/[secret]/orders/page.tsx"
      provides: "Страница «Отправленные заказы» (список + очистка)"
      min_lines: 60
    - path: "app/catalog/[secret]/cart/page.tsx"
      provides: "Запись заказа в историю в обеих кнопках + ссылка на /orders"
      contains: "useOrderHistory"
  key_links:
    - from: "app/catalog/[secret]/cart/page.tsx"
      to: "useOrderHistory.addEntry"
      via: "вызов в handleSend обеих кнопок"
      pattern: "addEntry"
    - from: "app/catalog/[secret]/orders/page.tsx"
      to: "useOrderHistory"
      via: "чтение entries + removeEntry + clearHistory"
      pattern: "useOrderHistory"
    - from: "app/catalog/[secret]/cart/page.tsx"
      to: "/catalog/[secret]/orders"
      via: "ссылка в блоке пустой корзины и в шапке корзины"
      pattern: "/orders"
---

<objective>
Подключить историю к потоку отправки заказа и показать её пользователю: встроить запись
в обе кнопки отправки на странице корзины, создать страницу «Отправленные заказы»
(`/catalog/[secret]/orders`) и добавить ссылки на неё из корзины.

Purpose: Делает фундамент из плана 01 видимым и полезным. После этого плана заказ
реально попадает в историю и пользователь может её просмотреть и очистить.

Output: `cart/page.tsx` пишет снимок заказа в историю при отправке (оба канала, цены
через effectivePrice) и ведёт на /orders; новый `orders/page.tsx` отображает историю
с мини-фото, очисткой всё/по одной и честной подписью «Отправленные заказы».
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-order-history/16-CONTEXT.md
@.planning/phases/16-order-history/16-PATTERNS.md
@.planning/phases/16-order-history/16-01-PLAN.md

<interfaces>
<!-- Контракт хука из плана 16-01 (использовать напрямую) и точки встройки в корзине. -->

From lib/useOrderHistory.ts (создан в плане 16-01):
```typescript
export function useOrderHistory(): {
  entries: OrderHistoryEntry[];
  isLoaded: boolean;
  addEntry: (entry: OrderHistoryEntry) => void;   // FIFO-потолок 20 внутри
  removeEntry: (entryId: string) => void;
  clearHistory: () => void;
};
```

From lib/types.ts (создан в плане 16-01):
```typescript
export interface OrderHistoryItem {
  id: string; name: string; quantity: number;
  priceAtOrder: number; unit: string; imageUrl?: string;
}
export interface OrderHistoryEntry {
  id: string; items: OrderHistoryItem[]; total: number;
  createdAt: string; channel: 'telegram' | 'max';
}
```

From app/catalog/[secret]/cart/page.tsx (точки встройки):
```typescript
// TelegramButton (строки 183–231): const { items } = useCartContext();
//   const { priceForm } = useCatalogSettings();
//   const handleSend = () => { ... window.open(url, "_blank"); };  // ← добавить запись ПОСЛЕ window.open
// MaxOrderButton (строки 234–304): handleSend async; window.open после fetch/в catch  // ← добавить запись
// Пустая корзина (строки 35–60): ссылка «Вернуться в каталог» (строки 52–57)  // ← добавить ссылку на /orders
// Шапка заполненной корзины (строки 66–86): ссылка «← Каталог» слева
// effectivePrice(product, priceForm) — цена «как видит клиент» (D-08), уже импортирован
```

From lib/pricing.ts:
```typescript
export function effectivePrice(product: Product, form: PriceForm): number;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать страницу «Отправленные заказы» (/orders)</name>
  <files>app/catalog/[secret]/orders/page.tsx</files>
  <read_first>
    - app/catalog/[secret]/cart/page.tsx (образец каркаса: шапка + список + пустое состояние + мини-фото + confirm + pluralItems)
    - lib/useOrderHistory.ts (хук из плана 16-01 — entries, removeEntry, clearHistory)
    - lib/types.ts (типы OrderHistoryEntry / OrderHistoryItem)
    - .planning/phases/16-order-history/16-PATTERNS.md (строки 153–256 — готовые паттерны страницы)
    - .planning/phases/16-order-history/16-CONTEXT.md (D-04, D-05, D-06, D-12 + Claude's Discretion по текстам)
  </read_first>
  <action>
    Создать `app/catalog/[secret]/orders/page.tsx` с директивой `'use client'` и русскими комментариями, по каркасу `cart/page.tsx` (шапка + список + пустое состояние). Сигнатура: `export default function OrdersPage({ params }: { params: { secret: string } })`.
    Данные брать из `useOrderHistory()` (`entries`, `isLoaded`, `removeEntry`, `clearHistory`) — per D-04. До завершения загрузки (`!isLoaded`) можно не рендерить список (избежать мелькания).
    Заголовок страницы — строго «Отправленные заказы» (per D-06). НИГДЕ не использовать слова «статус», «принято», «подтверждено», «в доставке» (инвариант v1.4, HIST-03).
    Шапка: слева ссылка возврата «← Корзина» на `/catalog/${params.secret}/cart` (per PATTERNS строка 256); по центру заголовок; справа кнопка «Очистить» — `if (confirm("Удалить всю историю заказов?")) clearHistory();` (per D-12, паттерн confirm из корзины).
    Пустое состояние (`entries.length === 0`): блок по образцу пустой корзины с текстом «Вы пока не отправляли заказов» (per Discretion) и ссылкой «Вернуться в каталог» на `/catalog/${params.secret}`.
    Список: каждая `OrderHistoryEntry` — карточка/блок со сводкой заказа: дата-время (форматировать `createdAt` через `new Date(createdAt).toLocaleString('ru-RU')`), канал (подпись «Telegram» или «MAX» по `entry.channel`), число позиций со склонением, итоговая сумма `entry.total.toFixed(2)} ₽`. Внутри заказа перечислить позиции: для каждой `OrderHistoryItem` — мини-фото 12×12 с onError/SVG-заглушкой по `item.imageUrl` (паттерн корзины, per D-10), `item.name`, `item.quantity`, и `(item.priceAtOrder * item.quantity).toFixed(2)} ₽`. Конкретный вид (свёрнутая сводка с разворотом или плоский список) — на усмотрение исполнителя per Discretion, согласовать на приёмке.
    Удаление отдельной записи: кнопка у каждой записи — `if (confirm("Удалить эту запись?")) removeEntry(entry.id);` (per D-12).
    Мягкая деградация (D-14): если у позиции отсутствует `name`/`priceAtOrder`, не ронять рендер — показывать заглушку для этой позиции, остальные отрисовывать. Хук уже отфильтровал грубо-битые записи, но рендер должен быть устойчив к недостающим необязательным полям.
    Добавить локальную утилиту склонения слова «заказ» `pluralOrders(n)` (по образцу `pluralItems` из корзины) и/или переиспользовать склонение позиций.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Файл `app/catalog/[secret]/orders/page.tsx` существует, первая строка `'use client';`
    - `grep -c "Отправленные заказы" app/catalog/[secret]/orders/page.tsx` >= 1
    - `grep -ci "статус\|принято\|подтвержд\|в доставке" app/catalog/[secret]/orders/page.tsx` == 0 (HIST-03)
    - Файл содержит идентификаторы: useOrderHistory, removeEntry, clearHistory
    - `grep -c "confirm(" app/catalog/[secret]/orders/page.tsx` >= 2 (очистить всё + удалить одну, D-12)
    - Файл ссылается на `/catalog/${params.secret}/cart` (возврат в корзину)
    - Мини-фото рендерится из `imageUrl` с onError/SVG-заглушкой (паттерн корзины)
    - `npx tsc --noEmit` без ошибок
  </acceptance_criteria>
  <done>Страница /orders рендерит историю с честной подписью, очисткой всё/по одной и мини-фото; типы проходят.</done>
</task>

<task type="auto">
  <name>Task 2: Встроить запись заказа в обе кнопки отправки + ссылка на /orders в корзине</name>
  <files>app/catalog/[secret]/cart/page.tsx</files>
  <read_first>
    - app/catalog/[secret]/cart/page.tsx (файл, который дополняем — TelegramButton/MaxOrderButton handleSend, блок пустой корзины, шапка)
    - lib/useOrderHistory.ts (addEntry из плана 16-01)
    - lib/types.ts (OrderHistoryEntry / OrderHistoryItem)
    - lib/pricing.ts (effectivePrice — уже импортирован в cart/page.tsx)
    - .planning/phases/16-order-history/16-PATTERNS.md (строки 260–306 — точки встройки в handleSend и ссылка)
    - .planning/phases/16-order-history/16-CONTEXT.md (D-01, D-02, D-03, D-05, D-08)
  </read_first>
  <action>
    Дополнить `app/catalog/[secret]/cart/page.tsx`. Добавить русские комментарии к новому коду.
    Импортировать `useOrderHistory` из `@/lib/useOrderHistory` и тип `OrderHistoryEntry`/`OrderHistoryItem` из `@/lib/types`.
    Завести общий сборщик снимка (локальная функция или helper внутри файла), который из `items` и `priceForm` строит `OrderHistoryItem[]`: для каждой позиции `{ id: product.id, name: product.name, quantity, priceAtOrder: effectivePrice(product, priceForm), unit: 'шт', imageUrl: product.imageUrl }` (per D-07/D-08/D-10; единица — 'шт', как в подписи корзины «/ шт»). Итог `total` = сумма `priceAtOrder * quantity`. Уникальный id записи — `crypto.randomUUID()` (фолбэк `Date.now().toString()` если недоступен), `createdAt: new Date().toISOString()` (per D-11, Discretion).
    В `TelegramButton`: получить `addEntry` из `useOrderHistory()`; в `handleSend` ПОСЛЕ `window.open(url, "_blank")` вызвать `addEntry({ id, items: snapshot, total, createdAt, channel: 'telegram' })` (per D-01, D-02). НЕ вызывать clearCart — корзина не очищается (per D-03).
    В `MaxOrderButton`: получить `addEntry` из `useOrderHistory()`; в `handleSend` записать заказ в историю с `channel: 'max'` (per D-01, D-02) — записывать единожды на нажатие (например, перед/после `setSending`, в основном пути; в catch-фолбэке тоже считается отправкой, так что запись должна произойти один раз независимо от ветки fetch). НЕ очищать корзину (per D-03). Снимок строить из тех же `items`/`priceForm`, цены через effectivePrice (per D-08) — цифры истории совпадают с текстом заказа.
    Ссылка на /orders (per D-05): в блоке ПУСТОЙ корзины (после ссылки «Вернуться в каталог», строки ~52–57) добавить ссылку `Мои отправленные заказы →` на `/catalog/${params.secret}/orders` (видна при пустой корзине — обязательное требование D-05). Также добавить ссылку на /orders в шапке ЗАПОЛНЕННОЙ корзины (рядом с «← Каталог» или отдельной строкой), чтобы история была доступна и из непустой корзины.
    Не менять существующую логику сборки текста заказа и window.open — только добавить запись в историю и ссылки.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useOrderHistory" app/catalog/[secret]/cart/page.tsx` >= 2 (импорт + использование; либо в обоих компонентах)
    - `grep -c "addEntry" app/catalog/[secret]/cart/page.tsx` >= 2 (Telegram + MAX)
    - `grep -c "channel: 'telegram'\|channel: \"telegram\"" app/catalog/[secret]/cart/page.tsx` >= 1
    - `grep -c "channel: 'max'\|channel: \"max\"" app/catalog/[secret]/cart/page.tsx` >= 1
    - `grep -c "effectivePrice" app/catalog/[secret]/cart/page.tsx` >= 1 (priceAtOrder через effectivePrice, D-08)
    - `grep -c "/orders" app/catalog/[secret]/cart/page.tsx` >= 2 (ссылка в пустой корзине + в шапке)
    - clearCart НЕ добавлен в handleSend ни одной кнопки (корзина не очищается, D-03) — проверить вручную diff
    - `npx tsc --noEmit` без ошибок
  </acceptance_criteria>
  <done>Обе кнопки пишут заказ в историю с правильным каналом и effectivePrice-ценами; корзина не очищается; ссылки на /orders видны из пустой и заполненной корзины.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Локальная история заказов: запись при отправке (Telegram/MAX), страница «Отправленные
    заказы» (/orders) со списком, мини-фото, очисткой всё/по одной, ссылки из корзины.
    Цены в истории = effectivePrice (как видел клиент). Потолок 20 заказов, мягкая деградация.
  </what-built>
  <how-to-verify>
    Деплой: после `git push origin main` → автодеплой Vercel. Проверять ТОЛЬКО на проде
    (локального dev нет). Открыть каталог по секретной ссылке на телефоне.

    1. Набрать корзину (2–3 товара), открыть корзину.
    2. В шапке корзины и в пустой корзине (очистить корзину) — видна ссылка «Мои отправленные заказы →».
    3. Нажать «Отправить заказ в Telegram» → откроется Telegram. Вернуться, перейти на /orders.
       Ожидание: появилась запись — позиции с мини-фото, количества, итоговая сумма,
       дата-время, канал «Telegram». Цены совпадают с текстом заказа в Telegram.
    4. (Если включён MAX) Отправить заказ в MAX → запись с каналом «MAX».
    5. Проверить, что корзина НЕ очистилась после отправки (товары на месте).
    6. На /orders: заголовок «Отправленные заказы», НЕТ слов «статус/принято/в доставке».
    7. Удалить одну запись (подтверждение confirm) → исчезает только она.
    8. «Очистить» (подтверждение) → история пуста, текст «Вы пока не отправляли заказов».
    9. Офлайн: включить авиарежим, открыть /orders → история отображается (localStorage).
  </how-to-verify>
  <resume-signal>Напишите «принято» или опишите, что поправить.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → приложение | История читается из localStorage как недоверенный вход (см. план 16-01). Аутентификации, сети при чтении истории и серверных данных НЕТ. |
| пользователь → window.open | Отправка заказа открывает внешний мессенджер (t.me / max.ru) — существующее поведение, не меняется. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | Рендер записей истории на /orders | mitigate | Рендер устойчив к недостающим полям позиции (заглушка вместо падения); грубо-битые записи уже отфильтрованы хуком (D-14, HIST-04) |
| T-16-05 | Information Disclosure | Снимок заказа в localStorage | accept | История содержит ровно те данные, что уже есть в корзине (название/цена/кол-во) — новой PII не добавляется; данные локальны на устройстве клиента |
| T-16-06 | Repudiation | Запись «что я отправил» по клику, без серверного подтверждения | accept | Осознанное решение D-01/HIST-03: история честно = «что я отправил со своей стороны», не статус приёмки; подпись «Отправленные заказы» исключает ложное толкование |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Новых зависимостей нет — используются React, существующий хук и effectivePrice; устанавливать нечего |
</threat_model>

<verification>
- `npx tsc --noEmit` — проект проходит проверку типов.
- Запись в историю происходит в обеих кнопках с корректным каналом; clearCart при отправке не вызывается (D-03).
- Страница /orders подписана «Отправленные заказы», без запрещённых слов (HIST-03).
- Цены истории = effectivePrice (совпадают с текстом заказа, D-08).
- Ссылка на /orders видна из пустой и заполненной корзины (D-05).
- Живая приёмка на проде после деплоя (checkpoint).
</verification>

<success_criteria>
- После отправки (Telegram/MAX) запись появляется в локальной истории с позициями, суммой, датой, каналом (HIST-01).
- История доступна офлайн и переживает перезапуск браузера (HIST-02 — через хук 16-01).
- Подпись честная «Отправленные заказы», без слов о статусе (HIST-03).
- Очистка всё/по одной работает; битые записи не ломают список (HIST-04).
- Корзина после отправки не очищается (D-03).
</success_criteria>

<output>
Create `.planning/phases/16-order-history/16-02-SUMMARY.md` when done
</output>
