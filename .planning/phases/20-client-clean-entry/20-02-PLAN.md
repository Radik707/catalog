---
phase: 20-client-clean-entry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ReorderSummaryModal.tsx
  - lib/plural.ts
  - app/catalog/[secret]/orders/page.tsx
autonomous: true
requirements: [HOME-02]
must_haves:
  truths:
    - "ReorderSummaryModal — общий компонент, импортируемый и orders/page, и (далее) строкой повтора"
    - "Функции склонения pluralGoods/pluralOrders/pluralItems вынесены в общий lib/plural.ts"
    - "Экран orders/page работает как раньше: повтор → сводка с теми же текстами и safe-area"
    - "Нет дублирования модалки/склонений между orders/page и новым кодом этапа 20"
  artifacts:
    - path: "components/ReorderSummaryModal.tsx"
      provides: "Общая модалка-сводка повтора (bottom-sheet, safe-area + 5rem)"
      exports: ["default"]
      min_lines: 80
    - path: "lib/plural.ts"
      provides: "Утилиты склонения товар/заказ/позиция"
      exports: ["pluralGoods", "pluralOrders", "pluralItems"]
    - path: "app/catalog/[secret]/orders/page.tsx"
      provides: "Импорт общей модалки и склонений вместо локальных копий"
      contains: "from '@/components/ReorderSummaryModal'"
  key_links:
    - from: "app/catalog/[secret]/orders/page.tsx"
      to: "components/ReorderSummaryModal"
      via: "import default"
      pattern: "from '@/components/ReorderSummaryModal'"
    - from: "app/catalog/[secret]/orders/page.tsx"
      to: "lib/plural"
      via: "import pluralGoods/pluralOrders/pluralItems"
      pattern: "from '@/lib/plural'"
---

<objective>
Вынести `ReorderSummaryModal` и функции склонения (`pluralGoods`, `pluralOrders`, `pluralItems`)
из `app/catalog/[secret]/orders/page.tsx` в общие модули: `components/ReorderSummaryModal.tsx` и
`lib/plural.ts`. Заимпортить их обратно в `orders/page.tsx`. Это фундамент для строки быстрого
повтора этапа 20 (план 20-03): без выноса строка повтора продублировала бы модалку и склонения,
что прямо противоречит принципу этапа «без обвесов/нагромождений» (PATTERNS, долг рефакторинга).

Purpose: дать одну переиспользуемую модалку-сводку повтора и один набор склонений для двух точек
вызова (экран «Мои заказы» и новая строка повтора над каталогом). Поведение orders/page не меняется.
Output: 2 новых модуля + рефакторинг orders/page без изменения видимого поведения.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-client-clean-entry/20-CONTEXT.md
@.planning/phases/20-client-clean-entry/20-UI-SPEC.md
@.planning/phases/20-client-clean-entry/20-PATTERNS.md

<interfaces>
<!-- Контракты для прямого использования. Источник — текущий orders/page.tsx и reorder.ts. -->

Из lib/reorder.ts:
export type ReorderOutcome = 'added' | 'price_changed' | 'out_of_stock' | 'unavailable';
export interface ReorderLineResult {
  outcome: ReorderOutcome;
  historyItem: OrderHistoryItem;        // имеет name, id?, quantity
  product?: Product;
  currentPrice?: number;
  oldPrice: number;
  requestedQty?: number;
  addedQty?: number;
  capped?: boolean;
}
export interface ReorderResult { lines: ReorderLineResult[]; addedCount: number; }

Текущая сигнатура модалки (внутри orders/page.tsx, строки 311-475):
  function ReorderSummaryModal({
    result: ReorderResult; secret: string; onClose: () => void;
  })
// bottom-sheet: оверлей `fixed inset-0 z-50 ... bg-black/40`, панель
// `w-full bg-white rounded-t-2xl ... pb-safe` со style
// paddingBottom: 'calc(env(safe-area-inset-bottom, 0px) + 5rem)';
// показывает addedCount (зелёная галочка) ИЛИ «Ни одного товара… нельзя добавить» (янтарная),
// notableLines (price_changed/out_of_stock/unavailable/capped) с семантическими иконками
// (text-green-500/text-blue-500/text-gray-400/text-red-400/text-amber-500 — НЕ менять),
// кнопку «Перейти в корзину →» (bg-blue-600, только при addedCount>0), кнопку «Закрыть».

Текущие склонения (orders/page.tsx строки 480-501):
  pluralOrders(n) → заказ/заказа/заказов
  pluralItems(n)  → позиция/позиции/позиций
  pluralGoods(n)  → товар/товара/товаров
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: lib/plural.ts — общий модуль склонений</name>
  <files>lib/plural.ts</files>
  <read_first>
    - app/catalog/[secret]/orders/page.tsx (строки 477-501 — три функции склонения, исходник для переноса)
  </read_first>
  <action>
    Создать новый файл `lib/plural.ts` с тремя именованными экспортами `pluralOrders`, `pluralItems`,
    `pluralGoods` — перенести тела ДОСЛОВНО из `orders/page.tsx` (правило русского склонения по
    `n % 10`/`n % 100`, исключения 11-14). Сигнатура каждой: `(n: number): string`. Добавить
    комментарии на русском (по правилу проекта для нового кода). Никакой логики не менять — это
    чистый перенос, чтобы один и тот же набор склонений использовали и orders/page, и строка повтора.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/plural.ts','utf8'); for(const f of ['pluralOrders','pluralItems','pluralGoods']){ if(!new RegExp('export function '+f).test(s)){console.error('нет '+f);process.exit(1)} }"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `lib/plural.ts` экспортирует ровно `pluralOrders`, `pluralItems`, `pluralGoods`.
    - Каждая функция `(n: number): string`; правила склонения совпадают с прежними (11-14 → форма «-ов/-ий»).
    - Нет побочных импортов/состояния — чистые функции.
  </acceptance_criteria>
  <done>Общий модуль склонений готов; orders/page и строка повтора смогут импортировать одни функции.</done>
</task>

<task type="auto">
  <name>Задача 2: components/ReorderSummaryModal.tsx — вынос общей модалки</name>
  <files>components/ReorderSummaryModal.tsx</files>
  <read_first>
    - app/catalog/[secret]/orders/page.tsx (строки 307-475 — текущая ReorderSummaryModal, исходник)
    - lib/reorder.ts (типы ReorderResult/ReorderLineResult/ReorderOutcome)
    - lib/plural.ts (созданный в задаче 1 — источник pluralGoods)
    - .planning/phases/20-client-clean-entry/20-UI-SPEC.md (§Color: палитру исходов НЕ менять; §Spacing: safe-area + 5rem)
  </read_first>
  <action>
    Создать клиентский компонент (`"use client"`) `components/ReorderSummaryModal.tsx` с дефолтным
    экспортом `ReorderSummaryModal`, перенеся тело из `orders/page.tsx` ДОСЛОВНО (та же вёрстка,
    те же классы). Сигнатура пропсов: `{ result: ReorderResult; secret: string; onClose: () => void }`.
    Импортировать `ReorderResult` из `@/lib/reorder` и `pluralGoods` из `@/lib/plural`. Сохранить
    БЕЗ изменений: bottom-sheet (`fixed inset-0 z-50 ... bg-black/40` оверлей; панель
    `w-full bg-white rounded-t-2xl ... pb-safe` со `style` paddingBottom
    `calc(env(safe-area-inset-bottom, 0px) + 5rem)` — обязательный резерв под BottomTabBar),
    ветку addedCount>0 (зелёная галочка) / нечего добавить (янтарная), `notableLines`-фильтр,
    семантические цвета иконок исходов (`text-green-500`/`text-blue-500`/`text-gray-400`/`text-red-400`/
    `text-amber-500` — UI-SPEC запрещает менять), кнопку «Перейти в корзину →» (`bg-blue-600`, только
    при addedCount>0) и «Закрыть». Тексты копирайта не менять (D-11: без «статус/принято»).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('components/ReorderSummaryModal.tsx','utf8'); if(!/use client/.test(s)){process.exit(1)} if(!/export default function ReorderSummaryModal/.test(s)){process.exit(2)} if(!/from '@\/lib\/plural'/.test(s)){process.exit(3)} if(!/from '@\/lib\/reorder'/.test(s)){process.exit(4)} if(!/env\(safe-area-inset-bottom, 0px\) \+ 5rem/.test(s)){process.exit(5)}"</automated>
  </verify>
  <acceptance_criteria>
    - Файл — клиентский компонент с дефолтным экспортом `ReorderSummaryModal({ result, secret, onClose })`.
    - Импортирует `ReorderResult` из `@/lib/reorder` и `pluralGoods` из `@/lib/plural`.
    - Bottom-sheet сохраняет `paddingBottom: calc(env(safe-area-inset-bottom, 0px) + 5rem)`.
    - Семантические цвета иконок исходов не изменены; тексты копирайта прежние.
    - Кнопка «Перейти в корзину →» рендерится только при `addedCount > 0`.
  </acceptance_criteria>
  <done>Общая модалка-сводка повтора готова к переиспользованию в orders/page и строке повтора.</done>
</task>

<task type="auto">
  <name>Задача 3: orders/page.tsx — перейти на общие модули, удалить локальные копии</name>
  <files>app/catalog/[secret]/orders/page.tsx</files>
  <read_first>
    - app/catalog/[secret]/orders/page.tsx (весь файл — удаляем локальные ReorderSummaryModal и склонения)
    - components/ReorderSummaryModal.tsx (созданный в задаче 2 — новый импорт)
    - lib/plural.ts (созданный в задаче 1 — новый импорт)
  </read_first>
  <action>
    В `orders/page.tsx` добавить импорты: `ReorderSummaryModal` из `@/components/ReorderSummaryModal`
    (default) и `pluralOrders`, `pluralItems` из `@/lib/plural` (`pluralGoods` теперь живёт внутри
    модалки, в page он не используется напрямую — проверить и не импортировать лишнее). Удалить из
    файла локальные определения функции `ReorderSummaryModal` (строки ~307-475) и трёх функций
    склонения (строки ~477-501). Использование `<ReorderSummaryModal result=... secret=... onClose=... />`
    и вызовы `pluralOrders`/`pluralItems` в `OrderCard`/`OrderItem` остаются прежними — теперь они
    резолвятся из импортов. `handleRepeat`, `classifyReorder`, `addToCartWithQuantity`, провайдеры —
    НЕ трогать. Поведение экрана «Мои заказы» не меняется.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/catalog/[secret]/orders/page.tsx','utf8'); if(!/from '@\/components\/ReorderSummaryModal'/.test(s)){console.error('нет импорта модалки');process.exit(1)} if(!/from '@\/lib\/plural'/.test(s)){console.error('нет импорта plural');process.exit(2)} if(/function ReorderSummaryModal/.test(s)){console.error('локальная модалка не удалена');process.exit(3)} if(/function pluralOrders/.test(s)){console.error('локальные склонения не удалены');process.exit(4)}"</automated>
  </verify>
  <acceptance_criteria>
    - `orders/page.tsx` импортирует модалку из `@/components/ReorderSummaryModal` и склонения из `@/lib/plural`.
    - Локальные `function ReorderSummaryModal` и `function pluralOrders/pluralItems/pluralGoods` удалены из файла.
    - Использование модалки и склонений в JSX не изменилось (те же пропсы/вызовы).
    - `npm run build` проходит без ошибок; экран «Мои заказы» рендерится и повтор открывает сводку как раньше.
  </acceptance_criteria>
  <done>orders/page использует общие модули; дублирование устранено; видимое поведение прежнее.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (нет новых) | Чистый рефакторинг: перенос кода между модулями без новых вводов данных |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering | ReorderSummaryModal рендер истории | mitigate | Модалка показывает только имена/цены из локальной истории и каталога; без `dangerouslySetInnerHTML`/`eval` — React экранирует текст. Перенос дословный, поверхность не расширяется. |
| T-20-05 | Information Disclosure | — | accept | Этап-рефакторинг не добавляет сетевых поверхностей, секретов, PII. |
</threat_model>

<verification>
- `npm run build` проходит без ошибок типов/линта.
- Экран «Мои заказы» работает идентично прежнему: повтор → сводка с теми же текстами, safe-area, иконками.
- В `orders/page.tsx` нет локальных копий модалки/склонений (нет дублирования с этапом 20).
- Никаких сетевых/серверных изменений; `force-dynamic` витрины не затронут.
</verification>

<success_criteria>
- `components/ReorderSummaryModal.tsx` и `lib/plural.ts` существуют и экспортируют нужное.
- `orders/page.tsx` импортирует их, локальные дубли удалены.
- Фундамент для строки повтора (план 20-03) готов — переиспользование без дублирования.
</success_criteria>

<output>
Создать `.planning/phases/20-client-clean-entry/20-02-SUMMARY.md` по завершении.
</output>
