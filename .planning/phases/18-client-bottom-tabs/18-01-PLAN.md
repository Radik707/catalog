---
phase: 18-client-bottom-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/BottomTabBar.tsx
autonomous: true
requirements: [TABS-01, TABS-02]
must_haves:
  truths:
    - "В роли «Клиент» снизу видна панель с 3 вкладками: Каталог · Избранное · Корзина"
    - "Активная вкладка подсвечена (синий), неактивные приглушены (серый)"
    - "Зона нажатия каждой вкладки дотягивается большим пальцем (вся колонка, высота полосы 56–64 px)"
    - "На «Избранное» и «Корзина» показываются бейджи-счётчики при значении > 0"
    - "В роли «Торговый» панель не монтируется вовсе"
  artifacts:
    - path: "components/BottomTabBar.tsx"
      provides: "Клиентский компонент нижней панели вкладок с гейтом роли, счётчиками и активным состоянием"
      contains: "BottomTabBar"
      min_lines: 60
  key_links:
    - from: "components/BottomTabBar.tsx"
      to: "useRole (lib/useRole)"
      via: "гейт рендера: role === 'client' && ready === true"
      pattern: "useRole"
    - from: "components/BottomTabBar.tsx"
      to: "useCartContext / useFavoritesContext"
      via: "счётчики бейджей (items.length / count)"
      pattern: "useCartContext|useFavoritesContext"
    - from: "components/BottomTabBar.tsx"
      to: "useNav (NavProvider)"
      via: "вкладки Каталог/Избранное переключают mode, активное состояние читается из mode"
      pattern: "useNav"
---

<objective>
Создать клиентский компонент `components/BottomTabBar.tsx` — нижнюю панель вкладок
(Каталог · Избранное · Корзина) для роли «Клиент». Компонент содержит весь UI и логику
вкладок: гейт по роли, переключение режимов витрины/навигацию, активное состояние,
бейджи-счётчики, safe-area снизу. Монтирование в layout — отдельный план 18-02.

Purpose: Закрыть TABS-01 (3 вкладки, активная подсвечена, тап-зона под большой палец) и
TABS-02 (бейджи-счётчики в стиле существующего CartIcon). Это нативный мобильный паттерн
нижних табов, к которому клиент привык по маркетплейсам (Ozon/WB).
Output: Новый файл `components/BottomTabBar.tsx` (экспортирует функциональный компонент).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-client-bottom-tabs/18-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Контракты хуков/контекстов, которые компонент потребляет. Извлечено из кодовой базы.
     Исполнитель использует их напрямую — обхода кодовой базы не требуется. -->

Из lib/useRole.tsx:
  export function useRole(): { role: 'sales' | 'client'; setRole; ready: boolean }
  — гейт: рендерить панель только при role === 'client' && ready === true.

Из components/CartProvider.tsx (провайдер смонтирован в КОРНЕВОМ app/layout.tsx, выше каталога):
  export function useCartContext(): { items: CartItem[]; totalItems; isLoaded; ... }
  — счётчик корзины = items.length (число позиций), НЕ totalItems (D-06, как CartIcon).

Из components/FavoritesProvider.tsx (провайдер смонтирован в КОРНЕВОМ app/layout.tsx):
  export function useFavoritesContext(): { count: number; favoriteIds; isLoaded; ... }
  — счётчик избранного = count.

Из components/NavProvider.tsx:
  export function useNav(): { mode: 'catalog'|'hit'|'new'|'fav'; setMode; section; ... }
  — Каталог → setMode('catalog'); Избранное → setMode('fav'); активное состояние читается из mode.

Из next/navigation:
  usePathname(): string  — для активной «Корзины» (оканчивается на '/cart').
  useRouter(): { push }  — навигация на каталог/корзину.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Создать компонент BottomTabBar — каркас, гейт роли, 3 вкладки, навигация, активное состояние</name>
  <files>components/BottomTabBar.tsx</files>
  <read_first>
    - components/BottomTabBar.tsx (создаётся — убедиться, что файла ещё нет)
    - lib/useRole.tsx (сигнатура useRole → { role, ready }; SSR-safe паттерн «не рисуем до ready»)
    - components/FavoritesIcon.tsx (ОБРАЗЕЦ поведения «Избранное»: setMode(active ? 'catalog' : 'fav') + router.push(catalogPath) если не на витрине; usePathname/useRouter)
    - components/CartIcon.tsx (ОБРАЗЕЦ маршрута корзины `/catalog/${secret}/cart` и `<a href>`)
    - components/NavProvider.tsx (useNav: mode 'catalog'|'hit'|'new'|'fav', setMode)
  </read_first>
  <action>
    Создать `components/BottomTabBar.tsx` с директивой "use client". Компонент
    `BottomTabBar({ secret }: { secret: string })` — функциональный, default-экспорт.
    Комментарии в коде — на русском (правило проекта).

    ГЕЙТ РОЛИ (D-11): прочитать `const { role, ready } = useRole()`. Вернуть `null`, если
    `!ready || role !== 'client'` — тот же SSR-safe паттерн, что в HeaderPrimaryAction/
    CardCornerButton (до ready ничего не рисуем, чтобы не мелькала раскладка при гидратации).
    Для роли 'sales' панель не отрисовывается.

    ТРИ ВКЛАДКИ (D-01), порядок слева-направо: Каталог · Избранное · Корзина. Контейнер —
    `fixed bottom-0 left-0 right-0 z-50` (D-07), `bg-white border-t border-gray-200`,
    три равные колонки на всю ширину (`grid grid-cols-3`). Высота полосы 56–64 px (D-05) —
    задать через высоту строки (например `h-16` ≈ 64px); точное значение — Discretion.
    Каждая вкладка — отдельный `<button>` на всю колонку (тап-зона = вся колонка целиком,
    не только иконка), `flex flex-col items-center justify-center` (иконка сверху + подпись
    снизу, D-04). Подписи: «Каталог», «Избранное», «Корзина».

    ПОВЕДЕНИЕ ВКЛАДОК (D-02), прочитать `const { mode, setMode } = useNav()`,
    `const pathname = usePathname()`, `const router = useRouter()`,
    `const catalogPath = \`/catalog/\${secret}\``:
      - Каталог → если pathname !== catalogPath, сначала router.push(catalogPath), затем
        setMode('catalog'); если уже на витрине — просто setMode('catalog').
      - Избранное → аналогично: при необходимости router.push(catalogPath), затем
        setMode('fav') (тот же механизм, что FavoritesIcon).
      - Корзина → router.push(`/catalog/${secret}/cart`).

    АКТИВНОЕ СОСТОЯНИЕ (D-03): вычислить активную вкладку —
      cart активна, если pathname оканчивается на '/cart';
      иначе fav активна при mode === 'fav';
      иначе активна Каталог (mode 'catalog'/'hit'/'new').
    Активная вкладка — `text-blue-600` (в тон шапке bg-blue-600), неактивная — `text-gray-500`
    (D-03). По желанию добавить `transition-colors` (Discretion). Подпись — мелкий шрифт
    (`text-xs`), иконка — inline-SVG ~24px (`w-6 h-6`).

    ИКОНКИ (D-04, Discretion по точным SVG): inline-SVG в духе heroicons — для «Каталог»
    сетка/дом, для «Избранное» — то же сердечко, что в FavoritesIcon (тот же `d`-path), для
    «Корзина» — та же иконка, что в CartIcon (тот же `d`-path). НЕ добавлять внешних
    зависимостей на иконки.

    БЕЙДЖИ-СЧЁТЧИКИ в этой задаче НЕ обязательны (выносятся в Task 2), но если удобно —
    оставить место под бейдж у верх-правого угла иконок «Избранное»/«Корзина».
  </action>
  <verify>
    <automated>cd /c/catalog && node -e "const s=require('fs').readFileSync('components/BottomTabBar.tsx','utf8'); const need=['use client','useRole','useNav','usePathname','BottomTabBar','grid-cols-3','fixed bottom-0','text-blue-600','text-gray-500']; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING:',miss);process.exit(1)} console.log('OK base')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/BottomTabBar.tsx` существует и экспортирует функциональный компонент по умолчанию
    - Компонент возвращает `null` при `role !== 'client'` ИЛИ `ready === false`
    - Панель содержит ровно 3 вкладки: Каталог, Избранное, Корзина (три равные колонки grid-cols-3)
    - Контейнер использует `fixed bottom-0 left-0 right-0 z-50`
    - Тап-зона каждой вкладки — `<button>` на всю колонку (не только иконка)
    - Вкладка «Каталог» вызывает setMode('catalog') (с переходом на витрину при необходимости)
    - Вкладка «Избранное» вызывает setMode('fav') (с переходом на витрину при необходимости)
    - Вкладка «Корзина» делает router.push на `/catalog/${secret}/cart`
    - Активная вкладка использует класс `text-blue-600`, неактивная — `text-gray-500`
    - «Корзина» активна, когда pathname оканчивается на '/cart'; иначе «Избранное» при mode==='fav'; иначе «Каталог»
  </acceptance_criteria>
  <done>BottomTabBar отрисовывает 3 рабочие вкладки с корректным гейтом роли, навигацией и активным состоянием; верификация base проходит.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Бейджи-счётчики (Избранное/Корзина) + тап-зона/safe-area</name>
  <files>components/BottomTabBar.tsx</files>
  <read_first>
    - components/BottomTabBar.tsx (дополняемый файл из Task 1)
    - components/CartIcon.tsx (ОБРАЗЕЦ бейджа: count = items.length из useCartContext; классы красной «пилюли»; условие count > 0; обрезка 99)
    - components/FavoritesIcon.tsx (ОБРАЗЕЦ бейджа избранного: count из useFavoritesContext)
    - components/InstallPrompt.tsx (ОБРАЗЕЦ safe-area: paddingBottom: 'env(safe-area-inset-bottom)')
    - lib/useCart.ts (items.length vs totalItems — для бейджа берём items.length)
    - lib/useFavorites.ts (count)
  </read_first>
  <action>
    Дополнить `components/BottomTabBar.tsx` бейджами-счётчиками и эргономикой.

    СЧЁТЧИКИ (D-06): прочитать `const { items } = useCartContext()` (из
    components/CartProvider — смонтирован в корневом app/layout.tsx) и
    `const { count } = useFavoritesContext()` (из components/FavoritesProvider).
    Бейдж корзины = `items.length` (число позиций, НЕ totalItems — для согласованности с
    нынешним CartIcon). Бейдж избранного = `count` из useFavorites.

    СТИЛЬ БЕЙДЖА — ровно как у CartIcon: красная «пилюля»
    `absolute -top-0.5 -right-0.5 w-5 h-5 bg-red-500 text-white text-xs font-bold rounded-full
    flex items-center justify-center`, позиционируется у верх-правого угла иконки вкладки
    (иконка-обёртка должна быть `relative`). Показывать только при значении `> 0`. Обрезка
    больших чисел как в CartIcon (`count > 99 ? "99" : count`).

    ТАП-ЗОНА (D-05, TABS-01): убедиться, что высота полосы в диапазоне 56–64 px и кликабельная
    область каждой вкладки ≥48 px по высоте — за счёт того, что `<button>` занимает всю колонку
    и всю высоту полосы.

    SAFE-AREA (D-08, TABS-04): добавить нижний паддинг панели через
    `pb-[env(safe-area-inset-bottom)]` ИЛИ inline `style={{ paddingBottom: 'env(safe-area-inset-bottom)' }}`
    (как в InstallPrompt — inline надёжнее на Safari). `viewportFit: "cover"` уже включён в
    app/layout.tsx — дополнительно ничего не настраивать.

    Aria: каждой вкладке-кнопке задать `aria-label` (Каталог/Избранное/Корзина) и `aria-current`
    или `aria-pressed` для активной (по образцу FavoritesIcon `aria-pressed`).
  </action>
  <verify>
    <automated>cd /c/catalog && node -e "const s=require('fs').readFileSync('components/BottomTabBar.tsx','utf8'); const need=['useCartContext','useFavoritesContext','items.length','bg-red-500','rounded-full','safe-area-inset-bottom']; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING:',miss);process.exit(1)} if(!/count\s*>\s*0|>\s*0\s*&&/.test(s)){console.error('MISSING badge >0 guard');process.exit(1)} console.log('OK badges')"</automated>
    <automated>cd /c/catalog && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "BottomTabBar" && echo "TS ERROR in BottomTabBar" && exit 1 || echo "no TS errors in BottomTabBar"</automated>
  </verify>
  <acceptance_criteria>
    - Бейдж корзины использует `items.length` из useCartContext (как CartIcon), НЕ totalItems
    - Бейдж избранного использует `count` из useFavoritesContext
    - Бейджи используют классы красной пилюли `bg-red-500 ... rounded-full` (стиль CartIcon)
    - Бейдж показывается только при значении > 0
    - Панель имеет нижний паддинг `env(safe-area-inset-bottom)` (Tailwind или inline style)
    - Высота полосы в диапазоне 56–64 px, тап-зона вкладки ≥48 px
    - `npx tsc --noEmit` не выдаёт ошибок в BottomTabBar.tsx
  </acceptance_criteria>
  <done>Вкладки «Избранное» и «Корзина» показывают бейджи-счётчики в стиле CartIcon; учтены safe-area и эргономика тап-зоны; типы корректны.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Новой поверхности атаки нет: нет новых входов, сетевых эндпойнтов или секретов. Компонент
читает только уже используемые приложением localStorage-данные (роль, корзина, избранное)
через существующие провайдеры/контексты и навигирует по существующим маршрутам.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | localStorage (роль/корзина/избранное) | accept | Чтение через существующие хуки/контексты, уже провалидированные (useRole отбрасывает не 'sales'/'client'); BottomTabBar новых записей в storage не делает. Нет новой поверхности атаки. |
</threat_model>

<verification>
- `components/BottomTabBar.tsx` существует, экспортирует функциональный компонент
- Гейт роли: `null` при role !== 'client' или ready === false
- 3 вкладки (Каталог/Избранное/Корзина), активная — text-blue-600, неактивная — text-gray-500
- Бейджи из items.length / count, стиль красной пилюли, только при > 0
- safe-area-inset-bottom учтён
- `npx tsc --noEmit` без ошибок по этому файлу
</verification>

<success_criteria>
TABS-01: панель из 3 вкладок, активная подсвечена, тап-зона под большой палец — реализованы в компоненте.
TABS-02: бейджи-счётчики на «Избранное»/«Корзина» в стиле существующего CartIcon — реализованы.
Компонент готов к монтированию (план 18-02).
</success_criteria>

<output>
Create `.planning/phases/18-client-bottom-tabs/18-01-SUMMARY.md` when done
</output>
