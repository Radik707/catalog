---
phase: 18-client-bottom-tabs
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - app/catalog/[secret]/layout.tsx
  - components/ContactFab.tsx
  - components/ClientBottomSpacer.tsx
autonomous: false
requirements: [TABS-03, TABS-04]
must_haves:
  truths:
    - "В роли «Клиент» нижняя панель видна и работает в standalone-PWA с учётом safe-area"
    - "В роли «Торговый» нижней панели НЕТ — навигация остаётся в шапке как сейчас"
    - "Нижняя панель не перекрывает контент витрины (зарезервирован нижний отступ только для client)"
    - "ContactFab для роли «Клиент» поднят выше панели и не перекрывается ею"
    - "ScrollToTop с панелью не конфликтует (трогать не требуется)"
  artifacts:
    - path: "app/catalog/[secret]/layout.tsx"
      provides: "Монтирование BottomTabBar рядом с ContactFab + резерв нижнего отступа контента для client"
      contains: "BottomTabBar"
    - path: "components/ContactFab.tsx"
      provides: "Подъём основной кнопки выше панели для роли client (useRole)"
      contains: "useRole"
  key_links:
    - from: "app/catalog/[secret]/layout.tsx"
      to: "components/BottomTabBar.tsx"
      via: "монтирование внутри NavProvider/RoleProvider с передачей secret"
      pattern: "BottomTabBar"
    - from: "components/ContactFab.tsx"
      to: "useRole (lib/useRole)"
      via: "подъём bottom для role === 'client'"
      pattern: "useRole"
---

<objective>
Смонтировать `BottomTabBar` (из плана 18-01) в каталожный layout и развести раскладку:
зарезервировать нижний отступ контента под фиксированной панелью (только для роли client),
поднять `ContactFab` выше панели для роли client, проверить роль sales (без панели),
standalone/офлайн и safe-area.

Purpose: Закрыть TABS-03 (у роли «Торговый» нижних табов нет — панель не монтируется) и
TABS-04 (панель не перекрывает контент и плавающие кнопки ContactFab/ScrollToTop; работает
офлайн и в standalone-PWA, safe-area учтён). Это «возня вёрстки» вокруг уже готового компонента.
Output: правка `app/catalog/[secret]/layout.tsx`, `components/ContactFab.tsx` и новый
клиентский spacer-компонент `components/ClientBottomSpacer.tsx` (резерв отступа).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-client-bottom-tabs/18-CONTEXT.md
@.planning/phases/18-client-bottom-tabs/18-01-PLAN.md

<interfaces>
<!-- Контракты для встройки. Извлечено из кодовой базы. -->

Из components/BottomTabBar.tsx (создан в плане 18-01):
  export default function BottomTabBar({ secret }: { secret: string })
  — сам делает гейт по роли (рендерит null при role !== 'client' или !ready).

Из app/catalog/[secret]/layout.tsx (серверный компонент, params.secret доступен):
  Структура провайдеров: RoleProvider > CatalogSettingsProvider > NavProvider >
  CatalogSyncProvider > InstallPromptProvider. <ContactFab /> монтируется внутри
  InstallPromptProvider, сразу после <main>{children}</main> (строка ~82).
  <main className="max-w-screen-2xl mx-auto w-full">{children}</main> — сейчас БЕЗ нижнего паддинга.

Из lib/useRole.tsx:
  useRole(): { role: 'sales'|'client'; ready }

Из components/ContactFab.tsx:
  Контейнер плавающих кнопок: `fixed bottom-6 right-4 z-50`. Дочерние иконки —
  `bottom-[5.5rem]` (Telegram) и `bottom-[10.5rem]` (MAX) ОТНОСИТЕЛЬНО контейнера,
  поэтому смещаются вместе с подъёмом контейнера. Компонент уже "use client".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Смонтировать BottomTabBar в layout + резерв нижнего отступа контента (только client)</name>
  <files>app/catalog/[secret]/layout.tsx, components/ClientBottomSpacer.tsx</files>
  <read_first>
    - app/catalog/[secret]/layout.tsx (точка монтирования рядом с ContactFab, ~строка 82; структура провайдеров; main без нижнего паддинга)
    - components/BottomTabBar.tsx (готовый компонент из 18-01; принимает { secret })
    - components/ContactFab.tsx (как сосед монтируется внутри InstallPromptProvider)
    - lib/useRole.tsx (для spacer: рендер только при role === 'client' && ready)
    - components/InstallPrompt.tsx (образец клиентского острова + safe-area)
  </read_first>
  <action>
    МОНТИРОВАНИЕ (D-12): в `app/catalog/[secret]/layout.tsx` импортировать BottomTabBar и
    смонтировать его рядом с `<ContactFab />` (внутри `InstallPromptProvider`, чтобы читались
    NavProvider/RoleProvider/корзина/избранное), передав `secret={params.secret}`. Layout —
    серверный; гейт роли внутри BottomTabBar уже клиентский, поэтому дополнительных «use client»
    в layout не нужно. Комментарий-пояснение к строке монтирования — на русском.

    РЕЗЕРВ ОТСТУПА КОНТЕНТА (D-09, TABS-04): создать новый клиентский компонент
    `components/ClientBottomSpacer.tsx` ("use client", default-экспорт). Он читает
    `const { role, ready } = useRole()` и рендерит null при `!ready || role !== 'client'`;
    для client рендерит пустой `<div>` высотой = высота полосы табов + safe-area, например
    `h-16` плюс `pb-[env(safe-area-inset-bottom)]` (или явный `style` с
    `height: calc(4rem + env(safe-area-inset-bottom))`). Точную высоту согласовать с высотой
    полосы из 18-01 (56–64 px). Смонтировать `<ClientBottomSpacer />` ВНУТРИ `<main>` после
    `{children}` — так отступ резервируется в потоке прокрутки только когда панель есть (для
    sales spacer вернёт null, лишнего отступа не будет). Комментарии — на русском.

    Не трогать: `<main className="max-w-screen-2xl mx-auto w-full">` базовые классы (только
    добавить spacer внутрь), `force-dynamic`/кэш витрины, ScrollToTop (с панелью не конфликтует).
  </action>
  <verify>
    <automated>cd /c/catalog && node -e "const s=require('fs').readFileSync('app/catalog/[secret]/layout.tsx','utf8'); if(!s.includes('BottomTabBar')){console.error('BottomTabBar not mounted');process.exit(1)} if(!/secret=\{params\.secret\}|secret=\{ *params\.secret *\}/.test(s)){console.error('secret prop not passed');process.exit(1)} console.log('OK layout mount')"</automated>
    <automated>cd /c/catalog && node -e "const s=require('fs').readFileSync('components/ClientBottomSpacer.tsx','utf8'); const need=['use client','useRole','safe-area-inset-bottom']; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING:',miss);process.exit(1)} console.log('OK spacer')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/catalog/[secret]/layout.tsx` монтирует `<BottomTabBar secret={params.secret} />` рядом с `<ContactFab />`
    - `components/ClientBottomSpacer.tsx` существует, "use client", возвращает null при role !== 'client' или !ready
    - Spacer резервирует высоту = высота полосы табов + env(safe-area-inset-bottom), смонтирован внутри `<main>` после children
    - Базовые классы `<main>` не изменены; ScrollToTop не тронут
    - При role==='sales' ни панель, ни spacer не дают лишнего отступа (оба возвращают null)
  </acceptance_criteria>
  <done>BottomTabBar смонтирован для клиента; контент не перекрывается панелью (зарезервирован отступ только для client); sales не получает ни панели, ни лишнего отступа.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Поднять ContactFab выше панели для роли client + сборка</name>
  <files>components/ContactFab.tsx</files>
  <read_first>
    - components/ContactFab.tsx (контейнер `fixed bottom-6 right-4 z-50`; дочерние bottom-[5.5rem]/[10.5rem] относительны контейнеру)
    - lib/useRole.tsx (useRole → role; добавить чтение роли в ContactFab)
    - components/BottomTabBar.tsx (высота полосы — согласовать величину подъёма)
  </read_first>
  <action>
    ПОДЪЁМ ContactFab (D-10, TABS-04): в `components/ContactFab.tsx` прочитать
    `const { role } = useRole()` (компонент уже "use client"). Для `role === 'client'`
    поднять контейнер плавающих кнопок выше панели табов: заменить статический `bottom-6` на
    условный — для client, например, `bottom-24` (≈ выше полосы 64px + запас) с учётом
    safe-area; для sales оставить `bottom-6` без изменений. Дочерние иконки
    (`bottom-[5.5rem]`/`[10.5rem]`) заданы ОТНОСИТЕЛЬНО контейнера — они сместятся
    автоматически, их трогать не нужно. Точную величину поднятого `bottom-*` подобрать по
    эргономике (Discretion), главное — основная кнопка не перекрывается панелью. Для надёжности
    на standalone iPhone допустимо учесть safe-area в позиционировании (Discretion).
    Комментарий-пояснение — на русском.

    Не вводить новых сетевых зависимостей; ContactFab остаётся клиентским островом, офлайн-
    поведение (isOnline) не менять.

    СБОРКА: после правок прогнать `npm run build` — должна завершиться успешно (Next.js типы +
    линт). Это и есть автоматическая проверка интеграции.
  </action>
  <verify>
    <automated>cd /c/catalog && node -e "const s=require('fs').readFileSync('components/ContactFab.tsx','utf8'); if(!s.includes('useRole')){console.error('useRole not used');process.exit(1)} if(!/role *=== *['\"]client['\"]|role *!== *['\"]sales['\"]/.test(s)){console.error('no client-role branch for bottom');process.exit(1)} console.log('OK contactfab')"</automated>
    <automated>cd /c/catalog && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `components/ContactFab.tsx` читает `useRole()` и для role==='client' поднимает контейнер выше панели (изменён `bottom-*`)
    - Для роли sales позиционирование ContactFab не изменилось (остаётся `bottom-6`)
    - Дочерние иконки (Telegram/MAX) не правились (смещаются вместе с контейнером)
    - `npm run build` завершается успешно
  </acceptance_criteria>
  <done>ContactFab для клиента поднят выше панели и не перекрывается ею; sales без изменений; сборка проходит.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Приёмка нижних табов на проде (client/sales, бейджи, перекрытия, safe-area/офлайн)</name>
  <action>Ручная приёмка после деплоя на Vercel (правило проекта: проверка только на проде). Исполнитель не пишет код — координирует деплой и ждёт подтверждения владельца по шагам ниже.</action>
  <what-built>
    Нижняя панель вкладок (Каталог · Избранное · Корзина) для роли «Клиент»: смонтирована в
    каталожный layout, контент не перекрывается (зарезервирован нижний отступ), ContactFab
    поднят выше панели. Для роли «Торговый» панели нет. Деплой на Vercel — после `git push`
    (см. правило проекта: приёмка только на проде).
  </what-built>
  <how-to-verify>
    После деплоя на Vercel (git push → автодеплой):
    1. Открыть каталог на телефоне в роли «Клиент» (дефолтная роль). Внизу видна панель из
       3 вкладок: Каталог · Избранное · Корзина; активная вкладка подсвечена синим (TABS-01).
    2. Добавить товар в избранное (♥) и в корзину — на вкладках «Избранное» и «Корзина»
       появляются красные бейджи-счётчики (TABS-02).
    3. Прокрутить витрину вниз до конца — нижние карточки НЕ перекрыты панелью; ContactFab
       (кнопка связи) не закрыта панелью (TABS-04).
    4. Тапнуть «Корзина» → открывается страница корзины; «Избранное» → режим избранного;
       «Каталог» → обычная витрина. Активная вкладка переключается соответственно.
    5. В настройках сменить роль на «Агент» (sales) → нижней панели НЕТ, навигация в шапке
       как раньше (TABS-03).
    6. Установить как PWA (standalone) и проверить, что панель не наезжает на системную
       нижнюю зону (safe-area, TABS-04); включить авиарежим — каталог и панель работают офлайн.
  </how-to-verify>
  <resume-signal>Напишите «принято» или опишите проблемы (перекрытие, бейджи, роль sales, safe-area).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Новой поверхности атаки нет: монтирование готового компонента и правки раскладки. Нет новых
входов, сетевых эндпойнтов, секретов или хранилищ. ContactFab читает роль через уже
существующий useRole; `force-dynamic`/кэш витрины и `/api/products` не затрагиваются.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Tampering | localStorage (роль) через useRole в ContactFab/spacer | accept | Чтение через существующий провалидированный useRole; новых записей нет. Нет новой поверхности атаки. |
</threat_model>

<verification>
- `app/catalog/[secret]/layout.tsx` монтирует BottomTabBar с secret={params.secret}
- `components/ClientBottomSpacer.tsx` резервирует отступ только для client (null для sales)
- `components/ContactFab.tsx` поднимает кнопку выше панели для client, sales без изменений
- `npm run build` проходит
- Ручная приёмка на проде: панель видна у client, нет у sales, бейджи, нет перекрытий, safe-area/офлайн
</verification>

<success_criteria>
TABS-03: у роли «Торговый» нижней панели нет (BottomTabBar возвращает null, spacer null) — навигация в шапке как сейчас.
TABS-04: панель не перекрывает контент (резерв отступа) и ContactFab/ScrollToTop; работает офлайн и в standalone-PWA (safe-area).
</success_criteria>

<output>
Create `.planning/phases/18-client-bottom-tabs/18-02-SUMMARY.md` when done
</output>
