---
phase: 13-photo-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ProductCard.tsx
  - app/sw.ts
  - lib/syncPhotos.ts
  - lib/useCatalogSync.ts
autonomous: true
requirements: [IMG-01, IMG-02, IMG-03, SYNC-02]
must_haves:
  truths:
    - "Фото каталога запрашивается браузером по прямому URL res.cloudinary.com (не через /_next/image)"
    - "SW кэширует только успешные (200) ответы Cloudinary, не 4xx/5xx"
    - "D-04: При нажатии Обновить онлайн докачиваются только новые фото (diff против prevImageUrls), старые не перекачиваются"
    - "D-04: Первая синхронизация (prevImageUrls пуст) НЕ качает все ~900 фото — кэш наполняется лениво по просмотру"
    - "D-06: Незакэшированное фото офлайн показывает иконку-заглушку нет фото, а не битое изображение"
    - "Хук useCatalogSync экспонирует refetch() в контракте UseCatalogSyncResult"
  artifacts:
    - path: "lib/syncPhotos.ts"
      provides: "Diff списка imageUrl + fire-and-forget prefetch новых URL"
      exports: ["syncPhotos"]
    - path: "components/ProductCard.tsx"
      provides: "Рендер фото через raw Cloudinary URL (unoptimized) + onError-фолбэк на PhotoPlaceholder"
      contains: "unoptimized"
    - path: "app/sw.ts"
      provides: "CacheFirst Cloudinary с CacheableResponsePlugin statuses:[200] + maxEntries:450/7д"
      contains: "CacheableResponsePlugin"
    - path: "lib/useCatalogSync.ts"
      provides: "Вызов syncPhotos после saveProducts + экспонирование refetch"
      contains: "refetch"
  key_links:
    - from: "components/ProductCard.tsx"
      to: "res.cloudinary.com"
      via: "next/image unoptimized → реальный запрос raw URL"
      pattern: "unoptimized"
    - from: "lib/useCatalogSync.ts"
      to: "lib/syncPhotos.ts"
      via: "вызов syncPhotos(fresh) после saveProducts в isCurrent()"
      pattern: "syncPhotos"
    - from: "lib/syncPhotos.ts"
      to: "lib/catalogDb.ts"
      via: "getMeta/saveMeta prevImageUrls"
      pattern: "prevImageUrls"
---

<objective>
Оживить кэш-слой фото: согласовать URL фото между рендером, SW-matcher и prefetch (зафиксированный подход 1 — raw Cloudinary URL без оптимизатора Next), добавить умную докачку только новых фото (diff по prevImageUrls) и экспонировать ручной refetch для кнопки «Обновить» (создаётся в плане 02).

Purpose: фото просмотренных товаров доступны офлайн (IMG-01/IMG-02), обновление умное (SYNC-02), кэш ограничен maxEntries:450 (IMG-03), фундамент под кнопку «Обновить» (SYNC-01, план 02).
Output: правки components/ProductCard.tsx, app/sw.ts, lib/useCatalogSync.ts + новый lib/syncPhotos.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-photo-sync/13-CONTEXT.md
@.planning/phases/13-photo-sync/13-PATTERNS.md

<locked_decision>
Стыковка next/image ↔ SW-кэш — ПОДХОД 1 (НЕ переоткрывать):
Фото каталога рендерятся НАПРЯМУЮ из Cloudinary без оптимизатора Next.js Image.
- ProductCard.tsx: оба <Image> (сетка строки 139-144, список 284-290) получают атрибут unoptimized → реально запрашиваемый URL становится https://res.cloudinary.com/... (а не /_next/image?url=...).
- Lightbox.tsx уже использует raw <img src={getHiResUrl(...)}> → текущий matcher SW его ловит. НЕ трогаем Lightbox. Учесть: hi-res URL (.../w_1600/...) — ОТДЕЛЬНАЯ запись кэша от карточки.
- Цель: matcher /^https:\/\/res\.cloudinary\.com\// в app/sw.ts ловит ровно те URL, что показываются И что предзагружаются. Одно фото карточки = одна запись → потолок maxEntries:450 честный.
- Обоснование: Cloudinary уже отдаёт сжатый WebP, оптимизатор Next избыточен; raw-URL делает офлайн-кэш предсказуемым.
</locked_decision>

<interfaces>
<!-- Контракты, которые исполнитель использует напрямую — без исследования кодовой базы. -->

lib/catalogDb.ts (готово, этап 11):
  export async function getMeta&lt;T = unknown&gt;(key: string): Promise&lt;T | undefined&gt;;
  export async function saveMeta(key: string, value: unknown): Promise&lt;void&gt;;
  // Ключ "prevImageUrls" зарезервирован, ранее не заполнялся — заполняется на этом этапе.

lib/types.ts: Product имеет опциональное поле imageUrl?: string.

lib/useCatalogSync.ts (текущий контракт, расширяется):
  UseCatalogSyncResult = { products, isOnline, syncedAt, status } — ДОБАВИТЬ refetch.
  sync() — стабильная useCallback([]) (строки 72-174).
  Точка вставки prefetch: внутри if (isCurrent()) ПОСЛЕ saveProducts(fresh) +
  saveMeta("syncTimestamp", now) (строки 143-148).
  Порядок строго по Потоку 4 ARCHITECTURE.md: fetch → setProducts → saveProducts →
  saveMeta(syncTimestamp) → syncPhotos(fresh) → setStatus("ready").
  syncPhotos сам читает/пишет prevImageUrls — отдельный saveMeta(prevImageUrls) в хуке НЕ нужен.

app/sw.ts (текущая CacheFirst Cloudinary, строки 61-72):
  matcher: /^https:\/\/res\.cloudinary\.com\//  → ОСТАВИТЬ как есть (подход 1).
  handler: CacheFirst cacheName "cloudinary-images",
    plugins: [ ExpirationPlugin({ maxEntries: 450, maxAgeSeconds: 7*24*60*60 }) ].
  ДОБАВИТЬ в plugins: CacheableResponsePlugin({ statuses: [200] }), импорт из "serwist".
  Якорь ^ (комментарий sw.ts:45-47): Serwist прогоняет regex по url.href; для Cloudinary ^https:// валиден. matcher НЕ менять.

components/ProductCard.tsx:
  PhotoPlaceholder (строки 52-70) — готовая иконка «нет фото», переиспользовать для D-06.
  <Image> сетки — строки 138-147; <Image> списка — строки 283-293.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raw Cloudinary URL в карточках + onError-фолбэк на заглушку (подход 1)</name>
  <read_first>
    - components/ProductCard.tsx (оба блока Image: сетка 138-147, список 283-293; PhotoPlaceholder 52-70)
    - .planning/phases/13-photo-sync/13-PATTERNS.md (раздел «КРИТИЧЕСКАЯ НАХОДКА», подход 1; раздел ProductCard про onError)
    - next.config.mjs (images.remotePatterns res.cloudinary.com уже разрешён)
  </read_first>
  <action>
    Добавить атрибут unoptimized к обоим экземплярам next/image в ProductCard.tsx (сетка и список) — тогда браузер запрашивает прямой product.imageUrl (https://res.cloudinary.com/...), а не /_next/image?url=... Это согласует реальный URL с matcher SW и с prefetch (подход 1, зафиксирован).
    Для D-06 (незакэшированное фото офлайн): добавить локальное состояние ошибки загрузки фото (useState imgError, начальное false); навесить onError-хендлер на оба Image, выставляющий imgError=true; в рендере при imgError показывать существующий PhotoPlaceholder вместо Image. Размер иконки заглушки в сетке — placeholderIcon (как сейчас), в списке — дефолтный. Никаких пометок «фото появится при сети» (D-06: лишний визуальный шум).
    Не менять Lightbox.tsx, не менять getHiResUrl. Не трогать badge/AddToCartButton/flip-логику. Новый код — с русскими комментариями.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - В components/ProductCard.tsx оба Image содержат атрибут unoptimized (grep: unoptimized встречается ≥1 раз).
    - Есть onError-хендлер на Image, переключающий рендер на PhotoPlaceholder (grep: onError).
    - npx tsc --noEmit завершается без ошибок типов в ProductCard.tsx.
    - PhotoPlaceholder НЕ дублируется — переиспользована существующая функция (строки 52-70).
    - Поведение (проверяется в плане 02 на устройстве): онлайн карточка показывает фото; офлайн незакэшированное фото → иконка-заглушка, не битая картинка.
  </acceptance_criteria>
  <done>Карточки рендерят raw Cloudinary URL через unoptimized; при ошибке загрузки фото офлайн показывается PhotoPlaceholder; tsc чист.</done>
</task>

<task type="auto">
  <name>Task 2: CacheableResponsePlugin (только 200) в CacheFirst Cloudinary</name>
  <read_first>
    - app/sw.ts (блок CacheFirst Cloudinary 61-72; импорты 5-12; комментарий про якорь ^ 45-47)
    - .planning/research/PITFALLS.md (Security Mistakes → «SW кэширует 401/403» → CacheableResponsePlugin statuses:[200])
  </read_first>
  <action>
    В app/sw.ts добавить в массив plugins CacheFirst-стратегии Cloudinary new CacheableResponsePlugin({ statuses: [200] }) рядом с существующим ExpirationPlugin. Импортировать CacheableResponsePlugin из "serwist" (дополнить существующий import-список, где уже CacheFirst/ExpirationPlugin/NetworkFirst/Serwist). НЕ менять matcher (/^https:\/\/res\.cloudinary\.com\// остаётся — подход 1). НЕ менять maxEntries:450 / maxAgeSeconds:7д (D-05). Добавить русский комментарий: кэшируем только успешные ответы — 4xx/5xx (включая возможные 401/403) в кэш не попадают.
    Примечание для исполнителя: Cloudinary отдаёт фото cross-origin → ответ может быть opaque (status 0). statuses:[200] кэширует только status===200. Если на устройстве-приёмке (план 02) обнаружится, что opaque-ответы не кэшируются — зафиксировать в SUMMARY как известный риск (альтернатива — crossOrigin на img); в рамках этого плана statuses:[200] ставим как защиту threat model от кэширования ошибок.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/sw.ts импортирует CacheableResponsePlugin из "serwist" (grep: CacheableResponsePlugin).
    - CacheFirst Cloudinary имеет в plugins И ExpirationPlugin, И CacheableResponsePlugin со statuses:[200].
    - matcher остался /^https:\/\/res\.cloudinary\.com\// (не изменён).
    - maxEntries:450 и maxAgeSeconds:7*24*60*60 не изменены.
    - npx tsc --noEmit без ошибок в sw.ts.
  </acceptance_criteria>
  <done>SW кэширует фото Cloudinary только при ответе 200; лимит 450/7д сохранён; matcher не тронут.</done>
</task>

<task type="auto">
  <name>Task 3: lib/syncPhotos.ts (diff + prefetch новых) + интеграция в useCatalogSync + refetch</name>
  <read_first>
    - lib/useCatalogSync.ts (весь файл; sync() 72-174, контракт 30-39, return 216, isCurrent() 76, вставка после saveProducts 143-148)
    - lib/catalogDb.ts (getMeta/saveMeta 95-107; ключ prevImageUrls — комментарий 92)
    - .planning/research/ARCHITECTURE.md (Pattern 3 «CacheFirst + умный diff», Поток 4 «Обновление по требованию» — порядок шагов)
    - .planning/phases/13-photo-sync/13-PATTERNS.md (раздел lib/syncPhotos.ts — псевдокод diff; раздел useCatalogSync — точка вставки и экспонирование refetch)
  </read_first>
  <action>
    Создать lib/syncPhotos.ts с клиентской функцией export async function syncPhotos(products: Product[]): Promise&lt;void&gt; (русские комментарии):
    1. Собрать newUrls = products.map(p =&gt; p.imageUrl).filter((u): u is string =&gt; Boolean(u)).
    2. prevUrls = (await getMeta&lt;string[]&gt;("prevImageUrls")) ?? [].
    3. D-04 гибрид: ТОЛЬКО если prevUrls.length &gt; 0 — вычислить toFetch = URL из newUrls, которых нет в prevUrls (использовать new Set(prevUrls) для O(1) проверки, НЕ includes в цикле), и для каждого сделать fire-and-forget fetch(url).catch(() =&gt; {}). URL — сырой product.imageUrl (подход 1: совпадает с тем, что запрашивает карточка через unoptimized и что ловит matcher SW). При первой синхронизации (prevUrls пуст) — НЕ качать ничего (анти-паттерн №1, взрыв квоты iOS).
    4. После любой синхронизации: await saveMeta("prevImageUrls", newUrls) — для следующего diff.
    Ошибки prefetch — молча (.catch(() =&gt; {})), в духе D-04 этапа 11.
    Затем модифицировать lib/useCatalogSync.ts:
    - Импортировать syncPhotos из @/lib/syncPhotos.
    - Внутри sync(), в ветке if (isCurrent()) ПОСЛЕ saveProducts(fresh) и saveMeta("syncTimestamp", now): вызвать await syncPhotos(fresh) (порядок Потока 4). syncPhotos сам пишет prevImageUrls — отдельного saveMeta(prevImageUrls) в хуке НЕ добавлять.
    - Расширить UseCatalogSyncResult: добавить refetch: () =&gt; Promise&lt;void&gt;; (JSDoc: ручная синхронизация для кнопки «Обновить», D-01/SYNC-01).
    - В return добавить refetch: sync (sync уже стабилен через useCallback([]); защита от гонки syncGenRef/isCurrent() уже встроена — ничего нового не изобретать).
    Не менять стратегию stale-while-revalidate, persist(), обработку таймаута/abort.
  </action>
  <verify>
    <automated>npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - lib/syncPhotos.ts существует, экспортирует syncPhotos, использует getMeta/saveMeta с ключом "prevImageUrls".
    - syncPhotos: при пустом prevUrls НЕ вызывает fetch (grep-логика: ветка if (prevUrls.length &gt; 0) оборачивает fetch).
    - syncPhotos использует Set для diff (grep: new Set), не includes в цикле.
    - lib/useCatalogSync.ts импортирует и вызывает syncPhotos(fresh) после saveProducts (grep: syncPhotos).
    - UseCatalogSyncResult содержит refetch: () => Promise<void>; и return содержит refetch: sync (grep: refetch).
    - npx tsc --noEmit без ошибок; npm run build — Compiled successfully.
  </acceptance_criteria>
  <done>syncPhotos делает diff только новых URL и lazy при первой загрузке; useCatalogSync вызывает его и отдаёт refetch; сборка зелёная.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → res.cloudinary.com | Внешний CDN; ответы попадают в SW Cache Storage |
| SW ↔ Cache Storage | Записи фото на устройстве агента (квота iOS ~50 МБ) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information disclosure | CacheFirst Cloudinary (app/sw.ts) | mitigate | CacheableResponsePlugin({ statuses: [200] }) — не кэшировать 4xx/5xx/ошибочные ответы Cloudinary (Security Mistakes PITFALLS.md) |
| T-13-02 | Denial of service | Cache Storage (квота iOS) | mitigate | ExpirationPlugin maxEntries:450 + 7д сохранён (D-05); prefetch только diff новых URL, первая загрузка lazy (D-04) — не precache 900 фото (анти-паттерн №1) |
| T-13-03 | Tampering | prefetch fetch(url) в syncPhotos | accept | Только GET к res.cloudinary.com (тот же домен, что в remotePatterns); ошибки молча .catch — нет записи мусора в кэш (statuses:[200] страхует) |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | План НЕ устанавливает новых пакетов: serwist/next/image уже в зависимостях (этап 10). Новых install-задач нет → package legitimacy gate неприменим |
</threat_model>

<verification>
- npx tsc --noEmit — без ошибок типов.
- npm run build — Compiled successfully (SW компилируется, app/sw.ts валиден).
- grep: unoptimized (ProductCard), CacheableResponsePlugin + statuses:[200] (sw.ts), syncPhotos + refetch (useCatalogSync), prevImageUrls + new Set (syncPhotos).
- Поведенческая верификация офлайн-фото и diff — в плане 02 (на устройстве, HUMAN-UAT).
</verification>

<success_criteria>
- Фото карточки запрашивается по raw Cloudinary URL (unoptimized), совпадает с matcher SW и prefetch.
- SW кэширует только 200-ответы Cloudinary; лимит 450/7д сохранён.
- syncPhotos докачивает только новые URL (diff), первая загрузка — lazy.
- useCatalogSync вызывает syncPhotos после успешного fetch и отдаёт refetch.
- Незакэшированное фото офлайн → PhotoPlaceholder (onError).
</success_criteria>

<output>
Create `.planning/phases/13-photo-sync/13-01-SUMMARY.md` when done.
</output>
