---
phase: 21-quick-order
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - components/CatalogSettings.tsx
  - components/SettingsPanel.tsx
  - components/CatalogView.tsx
  - app/catalog/[secret]/cart/page.tsx
autonomous: true
requirements: [QORD-01, QORD-04, QORD-05]
must_haves:
  truths:
    - "Тип ViewMode включает значение quick и оно валидируется при чтении из localStorage"
    - "В роли Торговый в панели настроек есть пункт «Быстрый набор», в роли Клиент его нет"
    - "Выбор режима quick рендерит на витрине плотные строки QuickOrderRow вместо карточек"
    - "Режим quick недоступен клиенту и не является дефолтом — при роли не sales откатывается к презентации"
    - "Дефолт режима остаётся presentation; клиентский опыт визуально не затронут"
  artifacts:
    - path: "components/CatalogSettings.tsx"
      provides: "Тип ViewMode с quick + валидация localStorage"
      contains: "quick"
    - path: "components/SettingsPanel.tsx"
      provides: "Пункт меню «Быстрый набор» под гейтом роли sales"
      contains: "quick"
    - path: "components/CatalogView.tsx"
      provides: "Ветка рендера quick + SSR-safe гейт роли"
      contains: "QuickOrderRow"
  key_links:
    - from: "components/CatalogView.tsx"
      to: "components/QuickOrderRow"
      via: "рендер в режиме quick"
      pattern: "QuickOrderRow"
    - from: "components/SettingsPanel.tsx"
      to: "lib/useRole"
      via: "гейт ready && role === sales"
      pattern: "role === \"sales\""
---

<objective>
Подключить готовые из плана 01 блоки как 4-й режим вывода «Быстрый набор»:

1. `CatalogSettings.tsx` — расширить тип `ViewMode` значением `"quick"` и добавить его в валидацию
   при чтении из localStorage. Дефолт остаётся `"presentation"` (D-01/D-02 — набор НЕ дефолт).
2. `SettingsPanel.tsx` — добавить пункт «Быстрый набор» в переключатель режимов, видимый ТОЛЬКО
   роли `sales` после `ready` (SSR-safe гейт), реализуя QORD-04.
3. `CatalogView.tsx` — ветка рендера режима `quick` (плотный список из `QuickOrderRow`) + SSR-safe
   гейт: если сохранён `quick`, но роль не `sales`/ещё не `ready` — откат к `presentation`.
4. (Опционально, low-prio, D-10) `cart/page.tsx` — подставить реальную единицу через `getUnit`
   в снимок заказа вместо жёсткого `'шт'`.

Purpose: Сделать режим видимым и переключаемым только для торгового, не задев клиента.
Output: Правки 3 обязательных файлов + 1 опциональная.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-quick-order/21-CONTEXT.md
@.planning/phases/21-quick-order/21-PATTERNS.md

<interfaces>
<!-- Точки правки уже размечены в PATTERNS.md с номерами строк. Ключевые контракты: -->

components/CatalogSettings.tsx:10 — `export type ViewMode = "list" | "grid" | "presentation";`
components/CatalogSettings.tsx:110–111 — валидация: `if (v === "list" || v === "grid" || v === "presentation") setViewMode(v);`
components/CatalogSettings.tsx:91 — дефолт `useState<ViewMode>("presentation")` (НЕ менять).

lib/useRole (useRole()):
```typescript
const { role, ready } = useRole();   // role: "sales" | "client"; ready: boolean (SSR-safe)
// Инвариант: ветвиться ТОЛЬКО по (ready && role === "sales"). До ready role всегда "client".
```

components/CatalogView.tsx:
  - 17,49: useRole уже импортирован и вызван — `const { role, ready } = useRole();`
  - 322–327: containerClass по viewMode (list => flex-1; presentation => grid preset; иначе grid).
  - 331–346: renderCard(product) — сейчас всегда <ProductCard/>.
  - 348–351: паттерн гейта showRepeatRow = `ready && role === "client" && ...` (образец SSR-safe гейта).
  - 355: <ScrollToTop viewMode={viewMode === "list" ? "list" : "grid"} />.

components/SettingsPanel.tsx:
  - 40: `const { role, setRole, ready } = useRole();`
  - 56–60: массив `views: { key: ViewMode; label: string }[]`.
  - 88: образец гейта подсветки `ready && role === "sales"`.
  - 112–122: рендер кнопок `views.map(...)`, активная подсветка `viewMode === v.key`.

lib/getUnit.ts (создан в плане 01): `export function getUnit(product: Product): string` — строка с предлогом «за …».

app/catalog/[secret]/cart/page.tsx:
  - ~208–225: buildOrderSnapshot; строка ~217 — жёсткий `unit: 'шт'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewMode "quick" + валидация localStorage (QORD-01)</name>
  <files>components/CatalogSettings.tsx</files>
  <read_first>
    - components/CatalogSettings.tsx (строки 10 — тип ViewMode; 91 — дефолт; 109–158 — чтение и persist)
    - .planning/phases/21-quick-order/21-PATTERNS.md (раздел «components/CatalogSettings.tsx», строки 215–232)
  </read_first>
  <action>
    В `CatalogSettings.tsx:10` расширь тип: `export type ViewMode = "list" | "grid" | "presentation" | "quick";`
    с русским комментарием «+ быстрый набор (роль sales)». В валидации чтения из localStorage (строки 110–111)
    добавь `|| v === "quick"` в условие, чтобы сохранённое значение `"quick"` восстанавливалось. Persist-обёртку
    `updateViewMode` (строки 151–154) и ключ `"viewMode"` НЕ менять — они работают по произвольной строке ViewMode.
    Дефолт `useState<ViewMode>("presentation")` (строка 91) НЕ менять — набор не дефолт (D-01/D-02).
  </action>
  <verify>
    <automated>grep -Eq 'ViewMode = .*"quick"' components/CatalogSettings.tsx && grep -Eq 'v === "quick"' components/CatalogSettings.tsx && grep -Eq 'useState<ViewMode>\("presentation"\)' components/CatalogSettings.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Тип `ViewMode` содержит `"quick"` (строка с `export type ViewMode`).
    - В блоке чтения localStorage (около строк 110–111) присутствует `v === "quick"` в условии валидации.
    - Дефолт остаётся `useState<ViewMode>("presentation")` (значение не изменено на quick).
    - Ключ localStorage остаётся `"viewMode"`; обёртка updateViewMode не меняет сигнатуру.
    - `npx tsc --noEmit` без новых ошибок.
  </acceptance_criteria>
  <done>ViewMode включает quick, значение валидируется и восстанавливается из localStorage, дефолт по-прежнему presentation.</done>
</task>

<task type="auto">
  <name>Task 2: Пункт меню «Быстрый набор» под гейтом роли sales (QORD-04)</name>
  <files>components/SettingsPanel.tsx</files>
  <read_first>
    - components/SettingsPanel.tsx (строки 40 — useRole с ready; 56–60 — массив views; 88 — образец гейта роли; 110–123 — рендер кнопок)
    - .planning/phases/21-quick-order/21-PATTERNS.md (раздел «components/SettingsPanel.tsx», строки 235–258)
  </read_first>
  <action>
    Оставь базовый массив `views` (Список/Сетка/Презентация, строки 56–60) без пункта набора. Сформируй производный
    список `visibleViews`: если `ready && role === "sales"` — это `[...views, { key: "quick" as ViewMode, label: "⚡ Набор" }]`,
    иначе — просто `views`. В блоке рендера кнопок режимов (строки 112–122) маппи `visibleViews` вместо `views`
    (активная подсветка `viewMode === v.key` работает без изменений). КРИТИЧНО: гейт — строго `ready && role === "sales"`
    (тот же инвариант, что подсветка роли на строке 88); до `ready` пункт не показывать (иначе клиент на первом кадре
    увидит режим агента / риск гидратации). Иконку/подпись пункта можно уточнить (Claude's Discretion), гейт менять нельзя.
    Комментарии — на русском.
  </action>
  <verify>
    <automated>grep -Eq "visibleViews" components/SettingsPanel.tsx && grep -Eq 'ready && role === "sales"' components/SettingsPanel.tsx && grep -q "quick" components/SettingsPanel.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Определён `visibleViews`, включающий элемент с `key: "quick"` только при `ready && role === "sales"`.
    - Рендер кнопок режимов маппит `visibleViews` (не исходный `views`).
    - Пункт «Быстрый набор» отсутствует в списке, когда `role !== "sales"` или `!ready` (нет безусловной вставки quick).
    - Базовый массив `views` не содержит пункта quick напрямую (гейт вынесен в visibleViews).
    - `npx tsc --noEmit` без новых ошибок.
  </acceptance_criteria>
  <done>Пункт «Быстрый набор» виден в панели только роли sales после ready; клиент его не видит.</done>
</task>

<task type="auto">
  <name>Task 3: Ветка рендера quick в CatalogView + SSR-safe гейт (QORD-01, QORD-04, QORD-05)</name>
  <files>components/CatalogView.tsx</files>
  <read_first>
    - components/CatalogView.tsx (строки 17,49 — useRole; 314–346 — containerClass и renderCard; 348–351 — образец гейта showRepeatRow; 355 — ScrollToTop; 386–429 — плоская/групповая ветки списка)
    - components/QuickOrderRow.tsx (создан в плане 01 — пропсы product, priceForm)
    - .planning/phases/21-quick-order/21-PATTERNS.md (раздел «components/CatalogView.tsx», строки 168–212)
  </read_first>
  <action>
    Импортируй `QuickOrderRow` из `@/components/QuickOrderRow`. Введи `effectiveMode`: если `viewMode === "quick"`
    и НЕ `(ready && role === "sales")` — используем `"presentation"`, иначе `viewMode` (D-02/QORD-04: клиент или
    незагруженная роль не видят режим агента, откат к дефолту). Далее по всему рендеру использовать `effectiveMode`
    вместо `viewMode`: в `containerClass` (322–327) добавь ветку `effectiveMode === "quick"` → `"flex-1"` (плотный
    список без сетки, как list); в `renderCard` (331–346) при `effectiveMode === "quick"` рендери
    `<QuickOrderRow key={product.id} product={product} priceForm={priceForm} />` вместо `<ProductCard/>`; в
    `<ScrollToTop>` (355) для quick передавай `"list"` (то есть `effectiveMode === "list" || effectiveMode === "quick" ? "list" : "grid"`).
    Обе ветки списка (плоская 386–408 и групповая 413–428) уже используют `renderCard`/`containerClass` — менять их структуру
    не нужно, только источники (effectiveMode). force-dynamic и route.ts НЕ трогать. Производительность (QORD-05):
    виртуализацию НЕ вводить (D-09) — рендерим тот же плоский маппинг, что и режим «Список»; оставь короткий русский
    комментарий-заметку, что при реальных тормозах на ~800 строк следующий шаг — виртуализация. Комментарии — на русском.
  </action>
  <verify>
    <automated>grep -q "QuickOrderRow" components/CatalogView.tsx && grep -q "effectiveMode" components/CatalogView.tsx && grep -Eq 'ready && role === "sales"' components/CatalogView.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `CatalogView.tsx` импортирует `QuickOrderRow`.
    - Определён `effectiveMode`, который при `viewMode === "quick"` и не (`ready && role === "sales"`) равен `"presentation"`.
    - `containerClass` имеет ветку для `effectiveMode === "quick"` (значение `"flex-1"`).
    - `renderCard` при `effectiveMode === "quick"` возвращает `<QuickOrderRow ... priceForm={priceForm} />`.
    - `<ScrollToTop>` получает `"list"` в режиме quick.
    - Виртуализация НЕ введена; есть комментарий-заметка про будущий шаг (D-09, QORD-05).
    - Файл не затрагивает force-dynamic/route.ts. `npm run build` завершается кодом 0.
  </acceptance_criteria>
  <done>Выбор режима quick показывает плотные строки QuickOrderRow; клиент/незагруженная роль видят презентацию (откат); build зелёный.</done>
</task>

<task type="auto">
  <name>Task 4 (опционально, low-prio D-10): реальная единица в снимке заказа</name>
  <files>app/catalog/[secret]/cart/page.tsx</files>
  <read_first>
    - app/catalog/[secret]/cart/page.tsx (buildOrderSnapshot, строки ~208–225; жёсткий `unit: 'шт'` ~217; тип product в сигнатуре ~209)
    - lib/getUnit.ts (создан в плане 01)
    - .planning/phases/21-quick-order/21-PATTERNS.md (раздел «cart/page.tsx», строки 262–279)
  </read_first>
  <action>
    Это НЕОБЯЗАТЕЛЬНАЯ мелочь (D-10), не блокирует этап — делать только если Tasks 1–3 завершены и остаётся бюджет.
    В `buildOrderSnapshot` (`cart/page.tsx`) заменить жёсткий `unit: 'шт'` на реальную единицу через `getUnit`:
    значение = `getUnit(product).replace(/^за\s+/, "") || 'шт'` (getUnit возвращает строку с предлогом «за …» — срезаем
    префикс «за », fallback `'шт'` при отсутствии правила). Проверь сигнатуру `buildOrderSnapshot`: параметр `product`
    сейчас типизирован как `Parameters<typeof effectivePrice>[0]`; для `getUnit(product)` нужны поля `group`/`name` —
    если тип это не гарантирует, расширь параметр до полного `Product` (в items они доступны). Импортируй `getUnit`
    из `@/lib/getUnit`. Русские комментарии.
  </action>
  <verify>
    <automated>grep -q "getUnit" "app/catalog/[secret]/cart/page.tsx" && echo OK || echo "SKIPPED (опциональная задача, допустимо)"</automated>
  </verify>
  <acceptance_criteria>
    - Если задача выполнена: `cart/page.tsx` импортирует `getUnit`; в buildOrderSnapshot `unit` вычисляется через `getUnit(product).replace(/^за\s+/, "") || 'шт'`.
    - Тип параметра product в buildOrderSnapshot допускает доступ к group/name (расширен до Product при необходимости).
    - `npm run build` завершается кодом 0.
    - Если задача пропущена по бюджету — это допустимо (low-prio), зафиксировать в SUMMARY как «отложено».
  </acceptance_criteria>
  <done>Снимок заказа использует реальную единицу товара (или задача осознанно пропущена как low-prio с отметкой в SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| нет новых | Правки только клиентские: тип режима, пункт меню, ветка рендера, снимок заказа. Серверных поверхностей не добавляется. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Information Disclosure | гейт роли (quick виден клиенту?) | mitigate | Пункт меню и ветка рендера строго под `ready && role === "sales"`; при роли не sales режим откатывается к presentation. Но роль — UX-пресет, не граница безопасности: клиент и так видит те же товары/цены во всех режимах, нового раскрытия данных режим quick не даёт. |
| T-21-04 | Elevation of Privilege | подмена роли в localStorage | accept | Роль хранится в localStorage и может быть изменена пользователем на своём устройстве — но роль не даёт доступа к скрытым данным (реальная защита каталога — секретный UUID в URL + серверная проверка, не трогаем). Режим quick — лишь другой вид тех же данных. |

Пакетных установок (npm/pip/cargo) в этом плане нет — Package Legitimacy Gate не требуется.
</threat_model>

<verification>
- `npm run build` завершается кодом 0.
- `CatalogView.tsx` рендерит QuickOrderRow только в effectiveMode quick; effectiveMode откатывает к presentation при не-sales/не-ready.
- `SettingsPanel.tsx`: пункт quick появляется только при `ready && role === "sales"`.
- `CatalogSettings.tsx`: ViewMode содержит quick, валидация localStorage принимает quick, дефолт presentation.
- Клиентские экраны (нижние табы, «Повторить заказ») визуально не затронуты — правки касаются только режима вывода витрины.
</verification>

<success_criteria>
- Режим «Быстрый набор» виден и переключаем ТОЛЬКО в роли Торговый, не дефолт (QORD-01, QORD-04).
- Выбор quick показывает плотные строки QuickOrderRow на витрине.
- Клиент/незагруженная роль видят презентацию — клиентский опыт не затронут (QORD-04).
- Виртуализация не введена; рендер отзывчив как режим «Список» (QORD-05, D-09).
- Опционально: снимок заказа несёт реальную единицу (D-10).
</success_criteria>

<output>
Create `.planning/phases/21-quick-order/21-02-SUMMARY.md` when done
</output>
