---
phase: 21-quick-order
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/getUnit.ts
  - components/QuickOrderRow.tsx
autonomous: true
requirements: [QORD-01, QORD-02, QORD-03]
must_haves:
  truths:
    - "Существует хелпер getUnit(product), возвращающий подпись единицы товара"
    - "Строка быстрого набора показывает мини-фото, название, цену с подписью единицы и остаток"
    - "В строке можно прямым вводом задать количество и менять его кнопками «+/−»"
    - "Введённое количество пишется в общую корзину и капится по остатку stock"
  artifacts:
    - path: "lib/getUnit.ts"
      provides: "Единая точка чтения единицы товара (шов под этап 21b)"
      contains: "export function getUnit"
    - path: "components/QuickOrderRow.tsx"
      provides: "Плотная строка быстрого набора с инлайн-количеством"
      contains: "QuickOrderRow"
  key_links:
    - from: "components/QuickOrderRow.tsx"
      to: "lib/getUnit.ts"
      via: "import getUnit"
      pattern: "getUnit\\("
    - from: "components/QuickOrderRow.tsx"
      to: "components/CartProvider"
      via: "useCartContext / AddToCartButton"
      pattern: "useCartContext|AddToCartButton"
---

<objective>
Создать два новых артефакта, на которых стоит весь этап 21:

1. `lib/getUnit.ts` — тонкий хелпер-обёртка над `getPackaging()` (D-06). Это «шов» под будущую
   ручную правку единицы в админ-панели (этап 21b). Весь фронт должен читать единицу товара
   ТОЛЬКО через `getUnit`, никогда напрямую через `getPackaging`.
2. `components/QuickOrderRow.tsx` — плотная строка режима «Быстрый набор»: мини-фото 56×56,
   название, цена с подписью единицы, скромный остаток и инлайн-количество (прямой ввод + «+/−»),
   пишущее в существующую корзину. Без флипа и без открытия галереи (D-03).

Purpose: Это самодостаточные строительные блоки. Волна 2 (план 02) подключит их в витрину.
Interface-first: сначала контракты/новые компоненты, потом провод.
Output: Два новых файла. Витрину и настройки этот план НЕ трогает.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-quick-order/21-CONTEXT.md
@.planning/phases/21-quick-order/21-PATTERNS.md

<interfaces>
<!-- Контракты, которые исполнитель использует напрямую — без разведки по кодовой базе. -->

lib/packaging.ts:
```typescript
// Возвращает подпись единицы С предлогом: «за шт» | «за блок» | «за коробку» | «за кг» | "" (нет правила)
export function getPackaging(group: string, name: string): string
```

lib/types.ts — Product содержит как минимум: id, name, group, price, stock, imageUrl?. Поля единицы/множителя НЕТ.

lib/useCart.ts (через components/CartProvider → useCartContext):
```typescript
getQuantity(productId: string): number
updateQuantity(productId: string, quantity: number): void   // quantity<=0 => удаление; кап Math.min(quantity, stock)
addToCart(product: Product): void
addToCartWithQuantity(product: Product, quantity: number): void  // накопление + кап по stock
isInCart(productId: string): boolean
```

components/AddToCartButton.tsx (переиспользуемый готовый блок):
```typescript
// props: { product: Product; compact?: boolean }
// Сам показывает: «Нет» если stock<=0; «+» если не в корзине; «− N +» (QuantityInput + кап) если в корзине.
export default function AddToCartButton(props): JSX.Element
```

components/QuantityInput.tsx:
```typescript
// props: value: number; max: number; onCommit: (v:number)=>void; className?: string
// inputMode="numeric", капит ввод по max, stopPropagation на клике.
```

lib/pricing.ts:
```typescript
export function effectivePrice(product, priceForm: PriceForm): number
export type PriceForm = "1" | "2"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Хелпер getUnit — единая точка чтения единицы товара (D-06)</name>
  <files>lib/getUnit.ts</files>
  <read_first>
    - lib/packaging.ts (сигнатура и формат getPackaging — строки 1–40, возвращает «за шт»/«за блок»/…/"")
    - lib/types.ts (тип Product: убедиться, что есть поля group и name)
    - .planning/phases/21-quick-order/21-PATTERNS.md (раздел «lib/getUnit.ts», строки 29–63 — целевая форма и заметка про предлог «за »)
  </read_first>
  <action>
    Создай `lib/getUnit.ts` с единственной экспортируемой чистой функцией `export function getUnit(product: Product): string`,
    которая делегирует `getPackaging(product.group, product.name)` и возвращает результат КАК ЕСТЬ — строку с предлогом
    («за шт» / «за блок» / «за коробку» / «за кг» / … / "" при отсутствии правила). Импортируй `Product` из `./types`
    и `getPackaging` из `./packaging`. НЕ срезай предлог «за » внутри хелпера — потребители (кнопка шага, снимок заказа)
    срежут его на месте; подпись у цены использует строку с предлогом как есть (обратная совместимость с ProductCard).
    Добавь русский комментарий-шапку: «Единица счёта товара (D-06). Этап 21 — обёртка над getPackaging(); этап 21b —
    сюда добавится приоритет ручной правки, getPackaging() останется дефолтом. Быстрый набор при этом повторно не трогаем.»
    Это единственная точка «шва»: весь фронт должен читать единицу через getUnit, не через getPackaging напрямую (D-06).
  </action>
  <verify>
    <automated>grep -q "export function getUnit" lib/getUnit.ts && grep -q "getPackaging" lib/getUnit.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `lib/getUnit.ts` содержит строку `export function getUnit` с параметром типа `Product`.
    - Тело функции вызывает `getPackaging(product.group, product.name)` и возвращает результат без обрезки «за ».
    - Импорты: `Product` из `./types`, `getPackaging` из `./packaging`.
    - Есть русский комментарий, поясняющий назначение шва под этап 21b.
    - `npx tsc --noEmit` не даёт новых ошибок по этому файлу.
  </acceptance_criteria>
  <done>getUnit(product) существует, компилируется, делегирует getPackaging и возвращает строку с предлогом.</done>
</task>

<task type="auto">
  <name>Task 2: Компонент QuickOrderRow — плотная строка быстрого набора (QORD-01..03)</name>
  <files>components/QuickOrderRow.tsx</files>
  <read_first>
    - components/ProductCard.tsx (режим «Список», строки 368–458 — раскладка flex items-center gap-3, миниатюра 56×56, подпись фасовки под ценой, скромный показ остатка ~434–455). НЕ переносить флип, оборот, CardCornerButton, галерею.
    - components/AddToCartButton.tsx (весь файл — готовый блок «Нет» / «+» / «− N +» с QuantityInput и капом по stock)
    - components/QuantityInput.tsx (props value/max/onCommit, inputMode numeric, stopPropagation)
    - lib/useCart.ts (updateQuantity/getQuantity/addToCartWithQuantity — сигнатуры и встроенный кап Math.min(quantity, stock))
    - lib/getUnit.ts (создан в Task 1 — источник подписи единицы)
    - .planning/phases/21-quick-order/21-PATTERNS.md (раздел «components/QuickOrderRow.tsx», строки 67–164 — образцы миниатюры, цены, остатка, инлайн-количества)
  </read_first>
  <action>
    Создай клиентский компонент (`"use client"`) `QuickOrderRow` с пропсами `{ product: Product; priceForm: PriceForm }`.
    Раскладка — горизонтальный `flex items-center gap-3` (аналог лицевой стороны режима «Список» из ProductCard 368–458),
    БЕЗ флипа, оборота, CardCornerButton и открытия галереи (D-03). Слева — миниатюра `w-14 h-14`
    (`Image` width=56 height=56, `object-contain`, `unoptimized`, локальный `useState` imgError + onError → плейсхолдер,
    как в ProductCard). Центр — название товара (перенос 1–2 строки) и скромный показ остатка КАК в режиме «Список»
    (`product.stock` шт при наличии, «Нет в наличии» иначе) — БЕЗ нового числа-числом и БЕЗ фильтра «в наличии»
    (это этап 22, строго вне объёма). Справа — цена: `effectivePrice(product, priceForm).toFixed(2)` ₽ и под ней
    подпись единицы из `getUnit(product)` (рендерить только если строка непустая) — так реализуется QORD-02
    (единица подписана у поля). Ниже/рядом с ценой — инлайн-количество: вставь готовый `<AddToCartButton product={product} />`
    (он сам даёт «+» для первого добавления и «− N +» с QuantityInput и капом по stock, покрывая QORD-01 и QORD-03,
    без дублирования логики капа — D-07, D-08). Запись идёт в общую корзину (QORD-03): AddToCartButton уже пишет в useCart.
    Кап по остатку НЕ изобретать — он уже в useCart.updateQuantity/addToCartWithQuantity и в QuantityInput (D-08).
    Шаг «+/−» = ±1 целая единица (D-04); дробные значения не вводим. Все комментарии — на русском. Fenced-код брать
    из PATTERNS.md как образец вёрстки; точный размер миниатюры и оформление подписи — на усмотрение (Claude's Discretion).
  </action>
  <verify>
    <automated>grep -q "QuickOrderRow" components/QuickOrderRow.tsx && grep -q "getUnit" components/QuickOrderRow.tsx && grep -Eq "AddToCartButton|useCartContext" components/QuickOrderRow.tsx && grep -q "\"use client\"" components/QuickOrderRow.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Файл начинается с `"use client";`.
    - Компонент называется `QuickOrderRow`, принимает пропсы `product: Product` и `priceForm: PriceForm`.
    - Импортирует `getUnit` из `@/lib/getUnit` и вызывает `getUnit(product)` для подписи единицы; подпись рендерится только при непустой строке.
    - Цена вычисляется через `effectivePrice(product, priceForm)`.
    - Инлайн-количество реализовано через `<AddToCartButton product={product} />` (или напрямую через useCartContext + QuantityInput с `max={product.stock}`); отдельной логики капа по stock в файле НЕТ.
    - НЕТ импортов/вызовов флипа, Lightbox, галереи, CardCornerButton (grep не находит `Lightbox`, `onFlipChange`, `CardCornerButton`).
    - Остаток показан скромно (product.stock), без нового фильтра «в наличии».
    - `npx tsc --noEmit` не даёт новых ошибок; `npm run build` завершается кодом 0.
  </acceptance_criteria>
  <done>QuickOrderRow рендерит плотную строку с фото, названием, ценой+единицей, остатком и инлайн-количеством, пишущим в общую корзину с капом по stock; флипа и галереи нет.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| нет новых | Этап не добавляет серверных поверхностей и не меняет данные — только клиентский UI поверх существующей корзины (localStorage) и роли. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | QuickOrderRow (показ данных) | accept | Строка показывает те же товары/цены, что уже видны в режимах Список/Сетка/Презентация — нового раскрытия данных нет. Роль sales/client — UX-пресет, не граница безопасности (реальная защита — секретный UUID в URL + серверная проверка, не трогаем). |
| T-21-02 | Tampering | кап по остатку (stock) | accept | Кап по stock — клиентская UX-проверка (не даёт задрать количество выше остатка в интерфейсе), НЕ критична для безопасности. Переиспользуется существующий кап в useCart/QuantityInput, новая логика не вводится. |

Пакетных установок (npm/pip/cargo) в этом плане нет — Package Legitimacy Gate не требуется.
</threat_model>

<verification>
- `npm run build` завершается кодом 0.
- `lib/getUnit.ts` и `components/QuickOrderRow.tsx` существуют и компилируются.
- QuickOrderRow не импортирует Lightbox/галерею/флип (grep пустой).
- Кап по stock не продублирован (нет собственной Math.min по stock в QuickOrderRow сверх переиспользуемых компонентов).
</verification>

<success_criteria>
- getUnit(product) — единая точка чтения единицы, делегирует getPackaging (QORD-02, шов D-06).
- QuickOrderRow даёт прямой ввод + «+/−» с шагом ±1 единица, подпись единицы у цены, запись в общую корзину и кап по stock (QORD-01, QORD-02, QORD-03).
- Файлы самодостаточны; витрина/настройки не затронуты (это волна 2).
</success_criteria>

<output>
Create `.planning/phases/21-quick-order/21-01-SUMMARY.md` when done
</output>
