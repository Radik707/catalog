---
phase: 11-indexeddb-usecatalogsync
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified: [components/CatalogView.tsx, app/catalog/[secret]/page.tsx]
autonomous: true
requirements: [OFF-01, OFF-02]

must_haves:
  truths:
    - "page.tsx больше не передаёт проп products в CatalogView, но сохраняет серверную проверку секрета и initialMode из ?filter="
    - "CatalogView при отсутствии пропа products берёт данные из useCatalogSync (клиентский источник вместо SSR)"
    - "В авиарежиме (после однократной онлайн-загрузки) каталог открывается и показывает товары с ценами/остатками/бейджами"
    - "В авиарежиме работают навигация по разделам/подгруппам, поиск и фильтры по ранее загруженным данным"
    - "Первая загрузка (IDB пуст, идёт fetch) показывает скелетон-карточки в сетке витрины, не спиннер"
    - "Офлайн без данных (IDB пуст и сети нет) показывает дружелюбное сообщение-заглушку, а не пустой экран"
  artifacts:
    - path: "components/CatalogView.tsx"
      provides: "Витрина читает данные из useCatalogSync когда products не передан; скелетон и экран-заглушка по статусу хука"
      contains: "useCatalogSync"
    - path: "app/catalog/[secret]/page.tsx"
      provides: "Серверная оболочка: проверка секрета + initialMode, без прокидывания products"
      contains: "CATALOG_SECRET"
  key_links:
    - from: "components/CatalogView.tsx"
      to: "lib/useCatalogSync.ts"
      via: "useCatalogSync() при отсутствии пропа products"
      pattern: "useCatalogSync"
    - from: "app/catalog/[secret]/page.tsx"
      to: "components/CatalogView.tsx"
      via: "<CatalogView initialMode=... /> без products"
      pattern: "<CatalogView"
---

<objective>
Перевести витрину каталога с серверного источника данных на клиентский (хук `useCatalogSync` из плана 02) — это финальный шаг, делающий каталог работающим офлайн. Два связанных файла:
- `components/CatalogView.tsx` — сделать проп `products` опциональным; при отсутствии брать данные из `useCatalogSync()`. По статусу хука показывать скелетон-карточки (D-02, первая загрузка) или дружелюбную заглушку (D-03, офлайн без данных), сохранив всю существующую логику навигации/поиска/фильтров.
- `app/catalog/[secret]/page.tsx` — убрать `getProducts()` и проп `products` (анти-паттерн №2 — иначе офлайн-массив пуст), СОХРАНИТЬ серверную проверку секрета и `initialMode` из `?filter=`.

Purpose: Закрывает OFF-01 (весь каталог офлайн) и OFF-02 (навигация/поиск/фильтры офлайн) — навигация работает автоматически, т.к. CatalogView уже фильтрует массив в памяти; меняется только источник массива. Это центральный анти-паттерн исследования: SSR-данные при force-dynamic не сериализуются в HTML-шелл, поэтому источник обязан прийти из IndexedDB на клиенте.
Output: обновлённые CatalogView.tsx и page.tsx; каталог открывается и работает без сети.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-indexeddb-usecatalogsync/11-CONTEXT.md
@components/CatalogView.tsx
@app/catalog/[secret]/page.tsx
@lib/useCatalogSync.ts

<interfaces>
<!-- Из lib/useCatalogSync.ts (план 02): -->
useCatalogSync(): {
  products: Product[];
  isOnline: boolean;
  syncedAt: number | null;
  status: "loading" | "ready" | "empty-offline";
}

<!-- Текущий контракт CatalogView (components/CatalogView.tsx): -->
interface CatalogViewProps {
  products: Product[];        // → станет опциональным products?: Product[]
  initialMode?: NavMode;      // остаётся как есть, приходит из page.tsx
}

<!-- Сетка карточек (классы Tailwind, переиспользовать для скелетона):
     "grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6 gap-2 p-2" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CatalogView — опциональный products + useCatalogSync + скелетон/заглушка</name>
  <files>components/CatalogView.tsx</files>
  <read_first>
    - components/CatalogView.tsx (весь файл — текущая структура: пропы, useMemo grouped/flatFiltered, containerClass, рендер сетки и пустых состояний)
    - lib/useCatalogSync.ts (план 02 — сигнатура хука и поле status)
    - .planning/research/ARCHITECTURE.md (Pattern 2 — перевод источника данных; Integration Points «CatalogView: принять products? и если нет — useCatalogSync»)
    - .planning/phases/11-indexeddb-usecatalogsync/11-CONTEXT.md (D-01 бесшовная подмена без сброса прокрутки/фильтров; D-02 скелетон-карточки серые контуры с лёгким мерцанием в сетке витрины; D-03 текст заглушки)
  </read_first>
  <action>
    Отредактируй `components/CatalogView.tsx`, сохранив весь существующий рендер и логику (grouped/flatFiltered/containerClass/Lightbox/SearchBar/ScrollToTop). Импортируй `useCatalogSync` из `@/lib/useCatalogSync`.

    1. Контракт пропов: измени `products: Product[]` на `products?: Product[]` в `CatalogViewProps`.

    2. Источник данных: в начале компонента вызывай `useCatalogSync()` БЕЗУСЛОВНО (хук нельзя вызывать в ветке — правило хуков). Затем выбери рабочий массив: `const products = productsProp ?? sync.products;` (переименуй входной проп в `productsProp` в деструктуризации). Когда проп передан (старый путь/тесты) — используется он; когда нет (новый путь из page.tsx) — данные из хука. `status` для UI бери из хука только когда проп не передан: `const status = productsProp ? "ready" : sync.status;`.

    3. Состояние «первая загрузка» (D-02 скелетон): если `status === "loading"` — отрендерить вместо сетки товаров блок скелетон-карточек. Используй тот же контейнерный класс сетки витрины (grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6 gap-2 p-2). Сгенерируй ~12 заглушек-карточек: серые прямоугольники в стилистике карточки (фото-блок `asp ect`/высота под фото + 1–2 серые полоски под название/цену), классы Tailwind `bg-gray-200 rounded animate-pulse` (лёгкое мерцание). SearchBar и шапку можно показать или скрыть на этот момент — на усмотрение, но сетка скелетонов обязательна; НЕ спиннер, НЕ пустой экран.

    4. Состояние «офлайн без данных» (D-03 заглушка): если `status === "empty-offline"` — отрендерить центрированное дружелюбное сообщение с иконкой (можно эмодзи/SVG в спокойном тоне) и текстом смысла «Каталог ещё не загружен. Подключитесь к интернету один раз — дальше он будет работать и без сети». Без технического жаргона, без кнопки (хук сам подтянется по событию online — D-03). Точная формулировка — на усмотрение в рамках стиля.

    5. Готовое состояние (`status === "ready"`): существующий рендер без изменений по содержанию. Подмена `sync.products` свежими данными происходит внутри хука — компонент перерисует список, НЕ размонтируясь, поэтому позиция прокрутки, выбранные раздел/подгруппа и фильтры (они в state/контексте CatalogView и NavProvider) сохраняются (D-01 бесшовно).

    Комментарии к новому коду — на русском. Мобильный вид существующей сетки не менять.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - В CatalogViewProps проп объявлен опциональным: `products?: Product[]`.
    - Компонент вызывает useCatalogSync() безусловно (на верхнем уровне тела, не в ветке).
    - Рабочий массив выбирается как `productsProp ?? sync.products` (проп имеет приоритет, иначе хук).
    - При status === "loading" рендерится сетка скелетон-карточек с классами animate-pulse и bg-gray-200 в контейнере сетки витрины (grid-cols-2 ... 2xl:grid-cols-6); нет спиннера.
    - При status === "empty-offline" рендерится текстовая заглушка со смыслом «Каталог ещё не загружен… подключитесь к интернету один раз», без кнопки.
    - Существующие useMemo grouped/flatFiltered, Lightbox, SearchBar, ScrollToTop сохранены.
    - `npx tsc --noEmit` без ошибок.
  </acceptance_criteria>
  <done>
    CatalogView работает в двух режимах: с пропом products (старый путь) и без него (данные из useCatalogSync). Реализованы скелетон (D-02) и заглушка офлайн-без-данных (D-03); ready-состояние рендерит каталог как прежде, бесшовно подменяя данные.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: page.tsx — убрать SSR-данные, сохранить проверку секрета и initialMode</name>
  <files>app/catalog/[secret]/page.tsx</files>
  <read_first>
    - app/catalog/[secret]/page.tsx (весь файл — проверка секрета, getProducts(), вычисление initialMode, рендер CatalogView)
    - components/CatalogView.tsx (обновлённый Task 1 — теперь принимает опциональный products)
    - .planning/research/ARCHITECTURE.md (Anti-Pattern 2 «Оставить page.tsx как Server Component с SSR-данными» — почему нельзя; Pattern 2 — что остаётся серверным)
    - .planning/phases/11-indexeddb-usecatalogsync/11-CONTEXT.md (Established Patterns — проверка секрета остаётся на сервере, прокидывание products убирается, initialMode сохраняется)
  </read_first>
  <action>
    Отредактируй `app/catalog/[secret]/page.tsx`:
    - СОХРАНИ `export const dynamic = "force-dynamic"` (комментарий про мгновенное применение правок остаётся актуален для онлайн-сценария).
    - СОХРАНИ серверную проверку секрета: `if (params.secret !== process.env.CATALOG_SECRET) notFound();` — это безопасность, убирать нельзя.
    - СОХРАНИ вычисление `initialMode` из `searchParams.filter` (hit/new/catalog) и передачу его пропом в `<CatalogView initialMode={initialMode} />`.
    - УБЕРИ строку `const products = await getProducts();` и проп `products={products}` из рендера (анти-паттерн №2: при офлайн-запуске force-dynamic HTML-шелл не содержит сериализованных данных → массив пуст). CatalogView теперь сам берёт данные из useCatalogSync.
    - УБЕРИ ставший ненужным импорт `getProducts` из `@/lib/sheets` (если он больше нигде в файле не используется). Импорт CatalogView и NavMode оставить.
    - Функцию можно оставить async (App Router это допускает) — это не влияет на корректность; внутри больше нет await getProducts. По желанию можно убрать async, если не остаётся await.
    Обнови/добавь русские комментарии, поясняющие, почему данные больше не прокидываются с сервера (офлайн-источник = IndexedDB на клиенте).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx НЕ содержит вызова `getProducts()`.
    - page.tsx НЕ передаёт проп `products` в CatalogView (рендер: `<CatalogView initialMode={initialMode} />`).
    - Сохранена проверка `params.secret !== process.env.CATALOG_SECRET` → notFound().
    - Сохранены `export const dynamic = "force-dynamic"` и вычисление initialMode из searchParams.filter.
    - Нет неиспользуемого импорта getProducts (иначе линт/сборка предупредит).
    - `npm run build` завершается успешно (код 0).
  </acceptance_criteria>
  <done>
    page.tsx стал серверной оболочкой: проверяет секрет и отдаёт initialMode, не прокидывая данные. Каталог получает товары из IndexedDB на клиенте — анти-паттерн №2 устранён, офлайн-запуск показывает данные.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер → сервер (`/catalog/[secret]`) | Серверный компонент проверяет UUID-секрет до отрисовки оболочки — граница доступа сохраняется неизменной |
| Сервер → клиент (рендер) | Данные каталога больше НЕ сериализуются в HTML; клиент сам читает их из IndexedDB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Elevation of Privilege | app/catalog/[secret]/page.tsx | mitigate | Проверка `params.secret !== CATALOG_SECRET → notFound()` СОХРАНЕНА на сервере. Рефактор убирает только прокидывание данных, не ослабляя контроль доступа. Acceptance criteria явно требуют наличие проверки. |
| T-11-07 | Information Disclosure | клиентский источник данных | accept | Перенос источника данных на клиент не раскрывает ничего нового: те же публичные данные витрины ранее уже отдавались этой же странице. Секрет в IndexedDB не попадает. |
| T-11-SC | Tampering | npm-зависимости | accept | Новых пакетов не добавляется. Package Legitimacy Gate не требуется. |
</threat_model>

<verification>
- `npm run build` завершается успешно.
- Поведенческая (DevTools, ручная на этапе приёмки вехи):
  - Онлайн открыть каталог → товары видны; DevTools → Application → IndexedDB → catalog-db со stores products и meta заполнены.
  - DevTools → Network → Offline (или авиарежим) → перезагрузить → каталог показывает товары; навигация по разделам/подгруппам, поиск и фильтры работают.
  - Чистый профиль + офлайн → видна заглушка «Каталог ещё не загружен…», не пустой экран.
  - Первая загрузка на медленной сети → видны скелетон-карточки.
- Приёмка на реальном iPhone (критерий успеха ROADMAP №5): авиарежим → открыть каталог → товары видны.
</verification>

<success_criteria>
- CatalogView читает данные из useCatalogSync при отсутствии пропа; скелетон (D-02) и заглушка (D-03) реализованы.
- page.tsx не прокидывает products, сохраняет проверку секрета и initialMode.
- Каталог открывается и работает офлайн (OFF-01, OFF-02); навигация/поиск/фильтры функционируют по локальным данным.
- `npm run build` зелёный.
</success_criteria>

<output>
Create `.planning/phases/11-indexeddb-usecatalogsync/11-03-SUMMARY.md` when done
</output>
