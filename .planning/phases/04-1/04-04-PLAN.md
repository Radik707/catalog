---
phase: 04-1-admin-panel
plan: 04
type: execute
wave: 4
depends_on: [04-1-admin-panel-03]
files_modified:
  - uploader/.env.example
autonomous: false
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05]
user_setup:
  - service: admin-panel
    why: "Доступ к панели по отдельному секрету"
    env_vars:
      - name: ADMIN_SECRET
        source: "Сгенерировать на сервере: python -c \"import secrets; print(secrets.token_urlsafe(32))\""
    dashboard_config:
      - task: "Скопировать CLOUDINARY_* из корневого .env в uploader/.env"
        location: "Сервер daniella, /opt/apps/catalog/uploader/.env"
must_haves:
  truths:
    - "Панель доступна на daniella по https://<домен>/<ADMIN_SECRET>/ ; неверный секрет и секрет загрузчика → 404"
    - "Владелец на телефоне открывает панель, видит товары требующие внимания, меняет группу — правка появляется во вкладке «Правки»"
    - "Владелец исправляет название и привязывает фото с телефона — обе правки сохраняются"
    - "«Применить сейчас» пересобирает каталог из последней партии; сайт обновляется; PROCESS_LOCK не даёт двойного запуска"
  decisions: "D-01"
  artifacts:
    - path: "uploader/.env.example"
      provides: "Документированные новые переменные ADMIN_SECRET, CLOUDINARY_*, GOOGLE_*"
      contains: "ADMIN_SECRET"
  key_links:
    - from: "nginx/gunicorn на daniella"
      to: "admin-маршруты uploader/app.py"
      via: "деплой git pull + restart catalog-uploader"
      pattern: "ADMIN_SECRET"
---

<objective>
Финальный ломтик: **боевой запуск панели на сервере daniella** через субагента sysadmin, при наблюдении владельца. Установка cloudinary, настройка ADMIN_SECRET и CLOUDINARY_* в uploader/.env, деплой кода, сквозная проверка всех четырёх возможностей панели на живом телефоне.

Purpose: довести ADM-01..ADM-05 до реально работающей панели на проде. До этого плана весь код существует только локально.
Output: расширение `uploader/.env.example`, развёрнутая и проверенная панель.
</objective>

## Phase Goal

**As a** владелец каталога, **I want to** реально пользоваться панелью со своего телефона на боевом сервере, **so that** я ежедневно управляю каталогом сам — меняю группы, чиню названия, привязываю фото — без программиста.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-1/04-CONTEXT.md
@.planning/phases/04-1/04-01-SUMMARY.md
@.planning/phases/04-1/04-02-SUMMARY.md
@.planning/phases/04-1/04-03-SUMMARY.md
@uploader/README.md
@uploader/.env.example

<server_rules>
ВСЕ серверные операции — ТОЛЬКО через субагента sysadmin (правило проекта, MEMORY: server-via-sysadmin).
Сервер daniella, корень /opt/apps/catalog, служба catalog-uploader, venv /opt/apps/catalog/.venv.
Деплой: git pull на сервере → sudo systemctl restart catalog-uploader.
credentials.json уже на сервере (для scripts/). gspread уже в requirements; новое — cloudinary.
CLOUDINARY_* сейчас в корневом .env, НЕ в uploader/.env — нужно скопировать.
</server_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: uploader/.env.example — документировать новые переменные</name>
  <read_first>
    - uploader/.env.example (текущие переменные APP_SECRET..TG_PROXY)
    - .planning/phases/04-1/04-PATTERNS.md (раздел «uploader/.env.example — расширение» — точный блок для добавления)
  </read_first>
  <action>
    Добавь в конец uploader/.env.example блок «Админ-панель владельца (этап 4)» с переменными: ADMIN_SECRET (с подсказкой генерации `python -c "import secrets; print(secrets.token_urlsafe(32))"`, комментарий «НЕ тот же, что APP_SECRET»); CLOUDINARY_CLOUD_NAME / CLOUDINARY_API_KEY / CLOUDINARY_API_SECRET (комментарий «скопировать из корневого .env»); GOOGLE_CREDENTIALS_PATH=/opt/apps/catalog/scripts/credentials.json и GOOGLE_SHEETS_ID (комментарий «уже используются скриптами»). Пустые значения. Реальный .env в git не коммитить — это .example. Русские комментарии.
  </action>
  <verify>
    <automated>python -X utf8 -c "src=open('uploader/.env.example',encoding='utf-8').read(); assert 'ADMIN_SECRET' in src and 'CLOUDINARY_CLOUD_NAME' in src and 'GOOGLE_SHEETS_ID' in src; print('env documented')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: uploader/.env.example содержит ADMIN_SECRET, CLOUDINARY_CLOUD_NAME, CLOUDINARY_API_KEY, CLOUDINARY_API_SECRET, GOOGLE_SHEETS_ID
    - Source: ADMIN_SECRET снабжён комментарием о генерации и отличии от APP_SECRET
    - Source: значения пустые (реальные секреты в git не попадают)
  </acceptance_criteria>
  <done>.env.example документирует все новые переменные панели с пустыми значениями и подсказками.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Деплой панели на daniella через sysadmin (владелец наблюдает)</name>
  <what-built>Код панели (admin.py, sheet_helper.py, cloudinary_helper.py, правки upload.py/app.py/requirements.txt) готов локально и закоммичен. Нужно развернуть на боевой сервер.</what-built>
  <how-to-verify>
    Поручи субагенту sysadmin (НЕ напрямую по ssh) выполнить на daniella:
    1. git pull в /opt/apps/catalog (подтянуть код панели).
    2. /opt/apps/catalog/.venv/bin/pip install -r uploader/requirements.txt (поставить cloudinary).
    3. В /opt/apps/catalog/uploader/.env прописать ADMIN_SECRET (сгенерировать token_urlsafe(32)), скопировать CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET из корневого .env, убедиться в GOOGLE_SHEETS_ID и GOOGLE_CREDENTIALS_PATH.
    4. sudo systemctl restart catalog-uploader; systemctl status — служба active.
    5. Проверить, что https://<домен>/<APP_SECRET> (загрузчик) по-прежнему открывается — регрессии нет.
    6. Сообщить владельцу URL панели https://<домен>/<ADMIN_SECRET>/ .
    Владелец наблюдает за выводом sysadmin, секрет панели сохраняет у себя.
  </how-to-verify>
  <resume-signal>Напишите «развёрнуто» когда служба active и URL панели получен, либо опишите ошибку.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Сквозная проверка панели на живом телефоне</name>
  <what-built>Панель развёрнута на daniella по секретному URL. Проверяем все четыре возможности на реальном телефоне владельца.</what-built>
  <how-to-verify>
    На телефоне владельца:
    1. Открыть https://<домен>/<ADMIN_SECRET>/ → виден список товаров, по умолчанию «Требуют внимания», карточки с бейджами [!]/[Н] (ADM-01, ADM-05).
    2. Открыть неверный URL и URL загрузчика как админ-секрет → 404 (отдельный доступ, D-02).
    3. Выбрать товар без группы → назначить группу → «Сохранить правку» → во вкладке «Правки» Google-таблицы появилась строка group (ADM-02).
    4. У того же или другого товара исправить «Отображаемое название» → сохранить → строка name в «Правки» (ADM-03).
    5. Привязать фото с камеры/галереи → загрузка проходит, превью обновилось, строка photo «presenter/...» в «Правки» (ADM-04). Попробовать загрузить не-картинку → отклонено с понятным сообщением.
    6. Нажать «Применить сейчас» → сообщение «Запущено обновление...», через 3-5 мин на сайте видно: товар в новой группе / с новым именем / с фото. Повторный клик во время обработки → «Обновление уже идёт...» (PROCESS_LOCK).
    7. Проверить, что при отсутствии последней партии (если применимо) кнопка даёт «Прайсы ещё не загружались».
  </how-to-verify>
  <resume-signal>Напишите «работает» если все 7 пунктов прошли, либо перечислите расхождения для плана закрытия пробелов.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| интернет → nginx TLS → gunicorn → admin.py | публичный вход, защита секретом в URL |
| sysadmin → сервер daniella | привилегированные операции (pip, systemctl, .env) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Spoofing | публичный доступ к панели | mitigate | ADMIN_SECRET token_urlsafe(32) в URL, hmac.compare_digest → 404; TLS на nginx; секрет не в git |
| T-04-14 | Information Disclosure | секреты в .env на сервере | mitigate | .env не коммитится (только .env.example с пустыми значениями); ключи переносятся sysadmin вручную |
| T-04-15 | Elevation of Privilege | прямые ssh-операции в обход регламента | mitigate | все серверные действия — через субагента sysadmin, владелец наблюдает (autonomous:false) |
| T-04-16 | Tampering | регрессия загрузчика после деплоя | mitigate | пункт проверки: страница загрузчика по APP_SECRET открывается; служба catalog-uploader active |
| T-04-SC | Tampering | pip install cloudinary на проде | mitigate | пакет проверен в плане 03 (Task 0), версия закреплена; установка через sysadmin под наблюдением |
</threat_model>

<decisions_covered>
## Решения CONTEXT.md, реализуемые этим планом
- D-01: боевой запуск панели на сервере daniella в составе приложения загрузчика (деплой через sysadmin, Task 2/3) — финальное подтверждение размещения, а не отдельный сайт
</decisions_covered>

<verification>
- uploader/.env.example документирует ADMIN_SECRET, CLOUDINARY_*, GOOGLE_*
- Служба catalog-uploader active после деплоя; загрузчик по APP_SECRET работает (нет регрессии)
- Панель по ADMIN_SECRET открывается; неверный секрет → 404
- На телефоне пройдены 7 пунктов сквозной проверки (группа, название, фото, «Применить сейчас», PROCESS_LOCK, пустая партия)
</verification>

<success_criteria>
Все цели этапа достигнуты на проде: владелец самостоятельно управляет каталогом с телефона — назначает группы (ADM-02), чинит названия (ADM-03), привязывает фото (ADM-04), видит требующие внимания товары (ADM-05), всё под отдельным секретом (ADM-01). «Применить сейчас» даёт мгновенный результат. Регрессии загрузчика нет.
</success_criteria>

<output>
Создай `.planning/phases/04-1/04-04-SUMMARY.md` по завершении.
</output>
