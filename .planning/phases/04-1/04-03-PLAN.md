---
phase: 04-1-admin-panel
plan: 03
type: execute
wave: 3
depends_on: [04-1-admin-panel-02]
files_modified:
  - scripts/cloudinary_helper.py
  - uploader/admin.py
  - uploader/requirements.txt
autonomous: false
requirements: [ADM-04]
user_setup:
  - service: cloudinary
    why: "Загрузка фото товаров из панели в облако (папка presenter/)"
    env_vars:
      - name: CLOUDINARY_CLOUD_NAME
        source: "Корневой .env проекта → скопировать в uploader/.env на сервере"
      - name: CLOUDINARY_API_KEY
        source: "Корневой .env проекта → скопировать в uploader/.env на сервере"
      - name: CLOUDINARY_API_SECRET
        source: "Корневой .env проекта → скопировать в uploader/.env на сервере"
must_haves:
  truths:
    - "Владелец выбирает фото с телефона (камера/галерея) в форме правки — файл валидируется (JPG/PNG/WebP, ≤10 МБ) и грузится в Cloudinary, папка presenter/"
    - "После загрузки во вкладку «Правки» пишется привязка типа photo в формате «presenter/<имя>.<ext>»"
    - "При следующей пересборке товар показывается с привязанным фото (override побеждает авто-маппинг)"
    - "Файл неверного типа/размера отклоняется с понятным сообщением, без загрузки в Cloudinary"
  decisions: "D-06"
  artifacts:
    - path: "scripts/cloudinary_helper.py"
      provides: "Загрузка одного файла в Cloudinary presenter/ → public_id и secure_url"
      contains: "cloudinary.uploader.upload"
    - path: "uploader/admin.py"
      provides: "Маршрут /photo (приём файла, валидация, вызов cloudinary_helper, запись photo в «Правки») + Dropzone в форме"
      contains: "presenter"
    - path: "uploader/requirements.txt"
      provides: "Новая зависимость cloudinary"
      contains: "cloudinary"
  key_links:
    - from: "uploader/admin.py (/photo)"
      to: "scripts/cloudinary_helper.py"
      via: "_run_py загрузка файла в Cloudinary"
      pattern: "cloudinary_helper"
    - from: "uploader/admin.py (/photo)"
      to: "scripts/sheet_helper.py (append_edit type=photo)"
      via: "запись привязки presenter/<file> в «Правки»"
      pattern: "photo"
---

<objective>
Третий ломтик: владелец **привязывает фото к новому товару прямо с телефона**. Выбор файла → валидация → загрузка в Cloudinary (папка presenter/) → запись привязки `photo` в память правок. Замена фото — та же операция (overwrite).

Purpose: ADM-04 — привязка фото без запуска агента или скрипта. Тип правки `photo` уже поддержан этапом 3; панель добавляет ввод и загрузку.
Output: `scripts/cloudinary_helper.py`, маршрут /photo и Dropzone в `uploader/admin.py`, зависимость `cloudinary` в `uploader/requirements.txt`.
</objective>

## Phase Goal

**As a** владелец каталога, **I want to** сфотографировать товар на телефон и привязать снимок к нему в панели, **so that** новый товар появляется в каталоге с фотографией без помощи программиста.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-1/04-CONTEXT.md
@.planning/phases/04-1/04-PATTERNS.md
@.planning/phases/04-1/04-UI-SPEC.md
@.planning/phases/04-1/04-02-SUMMARY.md

<interfaces>
<!-- Образец загрузки в Cloudinary — scripts/upload_photos.py -->
- load_env(): построчный разбор .env (PROJECT_ROOT затем SCRIPT_DIR), os.environ.setdefault
- cloudinary.config(cloud_name, api_key, api_secret, secure=True) — из CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET
- get_public_id(file_name, folder) → "<folder>/<stem>" (без расширения)
- cloudinary.uploader.upload(path_or_bytes, public_id=..., overwrite=True, resource_type="image",
  eager=[{"format":"webp","quality":"auto"}], eager_async=True) → result["secure_url"]

<!-- Формат привязки фото в «Правки» (как scripts/photo_overrides.json) -->
- Тип = "photo", Значение = "presenter/<stem>.<ext>" (всегда папка presenter/ для фото из панели)
- Ключ = normalize_name(имя товара из прайса)

<!-- products_to_rows (upload.py): photo_override побеждает авто-маппинг (этап 3, D-06) -->
- image_url = p.get("photo_override") or get_photo_url(...)

<!-- UI: экран 2, зона загрузки фото (04-UI-SPEC.md) -->
- <input type="file" accept="image/*" capture="environment"> + превью, padding 24px
- Тексты: «Нажмите или перетащите фото» / hint «JPG, PNG, WebP — до 10 МБ»
- Ошибка: «Не удалось загрузить фото. Проверьте формат файла (JPG, PNG, WebP) и размер (до 10 МБ).»
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Проверка легитимности пакета cloudinary (gate)</name>
  <what-built>План добавляет новую для приложения загрузчика зависимость `cloudinary` (PyPI). Пакет уже используется проектом в scripts/upload_photos.py, но в uploader/requirements.txt его ещё нет — формально это новый install для среды загрузчика.</what-built>
  <how-to-verify>
    1. Открыть https://pypi.org/project/cloudinary/ — убедиться: официальный SDK Cloudinary, сотни тысяч загрузок, актуальные релизы, исходники на github.com/cloudinary/pycloudinary.
    2. Подтвердить, что имя пакета — ровно `cloudinary` (без тайпсквоттинга вроде cloudinaryy / cloudinary-sdk).
    3. Версия — закрепить нижнюю границу (например `cloudinary>=1.40`).
  </how-to-verify>
  <resume-signal>Напишите «подтверждаю» после проверки PyPI, либо укажите проблему.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: cloudinary_helper.py — загрузка одного фото в Cloudinary presenter/</name>
  <read_first>
    - scripts/upload_photos.py (строки 60-110 load_env/get_public_id; 156-183 cloudinary.config; 274-302 cloudinary.uploader.upload)
    - .planning/phases/04-1/04-PATTERNS.md (раздел «scripts/cloudinary_helper.py» — load_env, init, get_public_id, upload)
  </read_first>
  <behavior>
    - upload(path, original_name): грузит файл в Cloudinary с public_id="presenter/<stem>", overwrite=True, eager webp; печатает JSON {ok:true, ref:"presenter/<stem>.<ext>", url:<secure_url>}; rc=0
    - отсутствуют ключи Cloudinary → JSON {ok:false, message:"..."} и rc!=0 (graceful, без трассировки наружу)
    - имя public_id строится из original_name (а не из временного пути) — расширение для ref берётся из original_name
  </behavior>
  <action>
    Создай scripts/cloudinary_helper.py по образцу scripts/upload_photos.py. Русские комментарии. Реализуй: load_env() (как в upload_photos.py — PROJECT_ROOT затем SCRIPT_DIR); init Cloudinary из CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET (cloudinary.config secure=True); если ключи не заданы — печатать JSON {ok:false,message:"Cloudinary не настроен"} и sys.exit(1). get_ref(original_name) → "presenter/<stem>.<ext>" (для записи в «Правки»). upload_file(path, original_name) → cloudinary.uploader.upload(path, public_id="presenter/<stem>", overwrite=True, resource_type="image", eager=[{"format":"webp","quality":"auto"}], eager_async=True); печатает JSON {ok:true, ref, url:result["secure_url"]}. CLI argparse: action="upload", --path <временный файл>, --name <оригинальное имя>. Папка ВСЕГДА presenter/ (D-06). Ошибки Cloudinary → JSON {ok:false, message:"Не удалось загрузить фото"} + rc!=0, детали только в log.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/cloudinary_helper.py',encoding='utf-8').read()); print('ok')"; python -X utf8 -c "src=open('scripts/cloudinary_helper.py',encoding='utf-8').read(); assert 'cloudinary.uploader.upload' in src and 'presenter/' in src and 'eager' in src; print('cloudinary helper ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: scripts/cloudinary_helper.py содержит `cloudinary.uploader.upload`, `public_id`, `overwrite=True`, `eager`
    - Source: папка жёстко = "presenter/" (значение ref начинается с presenter/)
    - Source: при отсутствии ключей — graceful JSON ok:false и ненулевой код возврата
    - Behavior: ast.parse проходит без ошибок
  </acceptance_criteria>
  <done>cloudinary_helper.py грузит одно фото в presenter/ с overwrite и webp-eager, возвращает ref и url в JSON; graceful при отсутствии ключей.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin.py — маршрут /photo (валидация+загрузка+запись) и Dropzone в форме</name>
  <read_first>
    - uploader/admin.py (форма правки и /save из планов 01/02; _run_py; apiCall/show в JS)
    - uploader/app.py (строки 420-442 upload — образец приёма request.files и sanitize; 89 MAX_CONTENT_LENGTH)
    - .planning/phases/04-1/04-UI-SPEC.md (экран 2 зона загрузки фото, превью, тексты ошибок/успеха)
    - scripts/cloudinary_helper.py (CLI upload --path --name) и scripts/sheet_helper.py (append_edit --type photo)
  </read_first>
  <behavior>
    - POST /photo с JPG ≤10 МБ: валидирует тип (расширение + content-type в {image/jpeg,image/png,image/webp}) и размер; сохраняет во временный файл; _run_py(cloudinary_helper upload) → ref; _run_py(sheet_helper append_edit --type photo --value ref --key <норм.имя>); возвращает jsonify(ok=True, ref, url, message="Правка сохранена...")
    - POST /photo с .txt или >10 МБ: jsonify(ok=False, message="Не удалось загрузить фото. Проверьте формат...") , НЕ вызывает Cloudinary; временный файл удаляется
    - ошибка Cloudinary/записи: jsonify(ok=False, message=...) , 500; временный файл удаляется в finally
  </behavior>
  <action>
    Добавь в uploader/admin.py маршрут POST /<token>/photo (под check_admin). Принимай файл request.files["photo"] + поле key (нормализованное имя товара). Валидация ДО загрузки: расширение ∈ {.jpg,.jpeg,.png,.webp}, mimetype ∈ {image/jpeg,image/png,image/webp}, размер ≤ 10 МБ (Flask MAX_CONTENT_LENGTH или проверка длины) — иначе jsonify(ok=False, message из UI-SPEC «Не удалось загрузить фото. Проверьте формат...»). Сохрани во временный файл (tempfile, безопасное имя через sanitize по образцу app.py), вызови _run_py(cloudinary_helper,"upload","--path",tmp,"--name",orig); распарси JSON-результат; при ok — _run_py(sheet_helper,"append_edit","--key",key,"--type","photo","--value",ref); удали временный файл в finally. Расширь белый список /save до {group, name, photo} (хотя фото идёт через /photo, согласованность). В форме (экран 2) добавь зону загрузки: <input type="file" accept="image/*" capture="environment"> с текстом «Нажмите или перетащите фото», hint «JPG, PNG, WebP — до 10 МБ», превью выбранного/текущего фото; кнопка «Сбросить фото» (btn-danger btn-ghost) видна при наличии привязки — пишет пустую photo-правку (или удаляет). JS: при выборе файла → FormData → apiCall POST /<token>/photo; show() успех/ошибка из UI-SPEC; обнови превью по url из ответа. Русские комментарии.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('uploader/admin.py',encoding='utf-8').read()); print('ok')"; python -X utf8 -c "src=open('uploader/admin.py',encoding='utf-8').read(); assert '/photo' in src or 'photo' in src; assert 'image/webp' in src or 'webp' in src; assert 'capture' in src; print('photo route+dropzone present')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: uploader/admin.py содержит маршрут с путём `/photo`, проверку расширения/mimetype (image/jpeg|png|webp) и лимита 10 МБ ДО загрузки в Cloudinary
    - Source: при невалидном файле НЕ вызывается cloudinary_helper (валидация раньше _run_py)
    - Source: PAGE содержит `<input type="file"` с `accept="image/*"` и `capture="environment"`, тексты «JPG, PNG, WebP — до 10 МБ»
    - Source: временный файл удаляется в finally
    - Behavior: ast.parse проходит без ошибок
  </acceptance_criteria>
  <done>Маршрут /photo валидирует файл, грузит в presenter/, пишет привязку photo в память; форма имеет зону загрузки с камеры с превью и сбросом.</done>
</task>

<task type="auto">
  <name>Task 3: uploader/requirements.txt — зависимость cloudinary</name>
  <read_first>
    - uploader/requirements.txt (текущий список — Flask/gunicorn/openpyxl/gspread/google-auth/requests/PySocks)
  </read_first>
  <action>
    Добавь в uploader/requirements.txt строку `cloudinary>=1.40` (нижняя граница, подтверждённая на Task 0). Комментарий на русском: «# Загрузка фото из админ-панели в Cloudinary (этап 4)». Не трогай остальные строки.
  </action>
  <verify>
    <automated>python -X utf8 -c "src=open('uploader/requirements.txt',encoding='utf-8').read(); assert 'cloudinary' in src; print('dep added')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: uploader/requirements.txt содержит строку cloudinary с нижней границей версии
    - Source: остальные зависимости не изменены
  </acceptance_criteria>
  <done>cloudinary добавлен в зависимости загрузчика с закреплённой версией.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| телефон владельца → /photo (файл) | загрузка произвольного файла — недоверенный вход |
| admin.py → Cloudinary | внешний сервис, ключи API |
| admin.py → «Правки» (тип photo) | запись привязки фото в память |
| pip install cloudinary | новая зависимость среды загрузчика |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | загрузка не-изображения / огромного файла | mitigate | валидация ДО Cloudinary: расширение {jpg,jpeg,png,webp} + mimetype {image/jpeg,png,webp} + размер ≤10 МБ (MAX_CONTENT_LENGTH); невалидный файл отклоняется без вызова _run_py |
| T-04-10 | Denial of Service | переполнение диска временными файлами | mitigate | временный файл (tempfile) удаляется в finally; Flask MAX_CONTENT_LENGTH рубит сверхкрупные тела до сохранения |
| T-04-11 | Information Disclosure | ключи Cloudinary в ответе/логе | mitigate | ключи только в uploader/.env (не в git); ошибки клиенту — нейтральные; детали в log |
| T-04-12 | Tampering (supply chain) | подмена пакета cloudinary | mitigate | Task 0 — блокирующая проверка PyPI (официальный SDK, без тайпсквоттинга); версия закреплена >=1.40 |
| T-04-SC | Tampering | pip install в среде загрузчика | mitigate | блокирующий human-verify gate (Task 0) перед добавлением зависимости; установка на сервере — план 04 через sysadmin |
</threat_model>

<decisions_covered>
## Решения CONTEXT.md, реализуемые этим планом
- D-06: фото привязывается загрузкой с телефона (camera/галерея) в Cloudinary, папка presenter/, привязка типа photo в формате «presenter/<файл>»; замена — та же операция overwrite (Task 1/2)
</decisions_covered>

<verification>
- `python -c "import ast; ast.parse(open('scripts/cloudinary_helper.py',encoding='utf-8').read())"` → ok; присутствуют cloudinary.uploader.upload, presenter/, eager
- `python -c "import ast; ast.parse(open('uploader/admin.py',encoding='utf-8').read())"` → ok; присутствует /photo, валидация типа/размера, input file capture
- `uploader/requirements.txt` содержит cloudinary>=1.40
- Боевая проверка загрузки фото с телефона — план 04 (sysadmin)
</verification>

<success_criteria>
ADM-04 закрыт: владелец грузит фото с телефона в presenter/, привязка пишется в память типа photo, фото показывается при пересборке (override побеждает авто-маппинг). Невалидные файлы отклоняются до облака. Новая зависимость проверена и закреплена.
</success_criteria>

<output>
Создай `.planning/phases/04-1/04-03-SUMMARY.md` по завершении.
</output>
