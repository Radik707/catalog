# Каталог «Вкусный Дом» — автоматизация и админ-управление

## What This Is

B2B-каталог товаров «Вкусный Дом»: мобильный сайт (Next.js на Vercel), который читает товары из Google-таблицы и показывает их оптовым клиентам — торговый представитель демонстрирует каталог, клиент набирает корзину и отправляет заказ. Данные обновляются из Excel-прайсов поставщиков. Текущая веха (v1.3) — сделать каталог **оффлайн-приложением (PWA)**, чтобы торговый агент работал в полях весь день без интернета: утром разово обновился, дальше всё под рукой без сети. Большой горизонт продукта (приложение для торговых агентов: многопользовательность, заявки, 1С, аналитика) — в `.planning/PRODUCT-VISION.md`.

## Core Value

Каталог всегда актуален и не «ломается»: вечером оператор залил свежие прайсы — к утру каталог обновлён и корректен, без ручной возни и без риска показать клиентам битую витрину. Клиент быстро находит нужное: сверху мало крупных разделов, внутри — понятные подгруппы.

## Current State

**Отгружено:** v1.3 «Оффлайн-режим (PWA)» (2026-06-13) — в проде. Каталог работает офлайн
(IndexedDB + Service Worker: товары, структура, корзина, фото), ставится как PWA на домашний
экран (Android-баннер + iOS-инструкция), плюс скачиваемый .apk (TWA) для установки с любого
браузера, включая Яндекс. ⏸ Ручная iPhone-приёмка офлайна отложена (код проверен, на реальном
iPhone не тестировалось — прогнать перед опорой на офлайн в полях).
До этого: v1.2 «Скрытие товаров и десктоп-вид» (2026-06-11) — глазик скрытия + десктоп-сетка;
v1.1 «Двухуровневая навигация» (разделы → подгруппы, scroll-spy, данные из Google-таблицы).

**Фактическая структура витрины (5 разделов → подгруппы):**
1. **Напитки** → Воды и газировки · Соки · Энергетики *(последними)*
2. **Снэки и прикассовое** → Чипсы/сухарики/орехи/крекеры · Жвачка/Холс/цикорий
3. **Сладкое** → Печенье и вафли · Конфеты *(Подарочные — кластером)* · Шоколад и батончики · Детское
4. **Крупы, лапша, бакалея** → Крупы и бакалея *(+ варпаки Мистраль)* · Лапша и каши б/п · Чай и кофе
5. **Соусы и консервация** → Соусы/кетчуп/майонез/приправы · Консервация

## Current Milestone: v1.4 Фундамент разделения аудиторий + быстрые победы

**Goal:** каталог различает, кто за экраном (роль Торговый/Клиент), помнит отправленные заказы и даёт базовую сортировку и мерч-стикеры — фундамент, на котором дальше встают отдельные ветки опыта для клиента (v1.5) и торгового (v1.6).

**Target features (этапы 15–17):**
- Роль «Торговый / Клиент» поверх устройства — явная, запоминаемая, переключаемая; все device-ветвления переезжают на роль
- Локальная история отправленных заказов (localStorage) — фундамент под «Повторить заказ»
- Сортировка витрины (название / цена / наличие) + стикеры акций на фото

**Key context:**
- Часть большого замысла «разделение двух аудиторий» — мастер-план `.planning/audience-split-roadmap.md` (3 вехи v1.4→v1.5→v1.6), исследование-основа `.planning/ozon-research-and-split.md`.
- Зафиксировано: история заказов — **локально**; следующая ветка после фундамента — **клиент (v1.5)**.
- Архитектурный принцип: роль (`sales`|`client`) + два пресета UI поверх общего ядра, не два дерева; устройство — лишь дефолт-эвристика; роль → свойство аккаунта в v2.0.
- Почти всё — чистый фронт (деплой = git push на Vercel); сервер daniella не трогаем.

**Отложено в задел под v2.0/1С:** стабильный SKU (O6), сканер ШК (T7), шаблоны заказа по клиенту (T6) — в `.planning/BACKLOG.md`.

## Будущие вехи (после v1.4)

- **v1.5 — Опыт клиента (телефон):** нижние табы, «Повторить заказ» + «Мои заказы», стартовая «Главная».
- **v1.6 — Опыт торгового (планшет):** quick-order набор + кратность упаковки, наличие + «Показ клиенту», корзина по поставщику.
- **Большой горизонт (`.planning/PRODUCT-VISION.md`):** Трек A (приложение агентов, 1С, многоаккаунтность v2.0), Трек B (розничный маркетплейс-агрегатор B2C). Разделение аудиторий v1.4–v1.6 — задел под Трек A.

**Общая почва под 1С:** стабильный артикул/SKU (сейчас ключ по нормализованному названию — хрупко). Техдолг CR-01 ждёт уточнения формата.

**Перенесено из v1.3:** ручная iPhone-приёмка офлайна (OFF-01..03, CART-01..02, IMG-01..03, PWA-02 #2/#5) — прогнать на реальном iPhone.

<details>
<summary>Веха v1.3 «Оффлайн-режим (PWA)» — цель и фичи (отгружено 2026-06-13)</summary>

**Goal:** торговый агент работает с каталогом весь день в полях без интернета — утром разово обновился, дальше всё под рукой без сети.

**Доставлено:** офлайн-каталог (IndexedDB + SW), офлайн-корзина и фото (CacheFirst maxEntries:450), индикаторы сети, кнопка ↻ синхронизации, PWA-установка (Android-баннер + iOS-инструкция + .apk/TWA для любого браузера). Путь — PWA (не нативное), на базе текущего Next.js.

</details>

## Requirements

### Validated

<!-- Уже работает (подтверждено картой кода .planning/codebase/). -->

- ✓ Витрина каталога: режимы Список/Сетка/Презентация, фильтры, поиск, корзина, бейджи, скрытие остатка ≤ 1 — существующее
- ✓ Полноэкранный просмотрщик фото с «живыми» свайпами и галереей — существующее
- ✓ Чтение данных из Google Sheet вживую (Next.js API, force-dynamic) — существующее
- ✓ Веб-загрузчик прайсов (Flask, `uploader/`) на сервере daniella по секретной ссылке, запуск `scripts/upload.py` — существующее
- ✓ Telegram-бот администратора: уведомления об итоге + кнопки [Оставить прошлую]/[Всё равно применить]/[Показать файлы] — существующее
- ✓ Подушка безопасности: бэкап «Товары_BACKUP», авто-откат, порог «подозрительно мало товаров» (`scripts/sheet_tool.py`) — существующее
- ✓ Парсер старого формата прайсов (`scripts/upload.py`: A=название/категории, B=цена, C=остаток) — существующее

<!-- Веха v1.0 «автоматизация и админ-управление» — завершена 2026-06-07 (этапы 1–4). -->

- ✓ Поддержка нового формата выгрузки 1С (авто-определение формата, поддержка обоих) — веха v1.0, этап 1
- ✓ Группировка нового формата: группа из текущего каталога по названию; новые товары → «Новинки» + бейдж — веха v1.0, этап 1
- ✓ Асинхронная страница загрузчика: «файлы отправлены», результат владельцу в Telegram, история загрузок — веха v1.0, этап 2
- ✓ Память правок: ручные привязки (группа/фото/название) сохраняются во вкладке «Правки» и применяются автоматически — веха v1.0, этап 3
- ✓ Админ-панель владельца: правка группы/названия, привязка фото с телефона, «Применить сейчас» — веха v1.0, этап 4

<!-- Веха v1.1 «двухуровневая навигация» — завершена 2026-06-10 (этапы 5–7). -->

- ✓ Карта структуры «категория → подгруппа → раздел» (5 разделов) вне кода + порядок подгрупп — v1.1, этап 5
- ✓ `upload.py` раскладывает товары по подгруппам/разделам и кластеризует — v1.1, этап 5
- ✓ Перекладка групп по смыслу: «Стоевъ и Сэнсой» расформирована, «Конфеты и печенье» разделена, варпаки → «Крупы и бакалея» — v1.1, этап 5
- ✓ Двухуровневая навигация на витрине: разделы + подгруппы на месте, товары внутри — v1.1, этап 6
- ✓ Админ-панель: выбор **подгруппы** при правке товара, раздел определяется автоматически — v1.1, этап 7

<!-- Веха v1.2 «Скрытие товаров и десктоп-вид» — завершена 2026-06-11 (этапы 8–9). -->

- ✓ Глазик-скрытие товаров в админ-панели (тип правки «скрыт») + фильтр видимости — v1.2, этап 8
- ✓ Витрина не отдаёт скрытые товары в браузер (фильтр на сервере, диапазон A2:L) — v1.2, этап 8
- ✓ Десктопный вид витрины (сетка xl/2xl, max-w-screen-2xl, hover) — v1.2, этап 9

<!-- Веха v1.3 «Оффлайн-режим (PWA)» — завершена 2026-06-13 (этапы 10–14). ⏸ iPhone-приёмка офлайна отложена. -->

- ✓ Каталог листается офлайн (товары + структура в IndexedDB + SW) — v1.3, этапы 10–11
- ✓ Фото доступны офлайн (CacheFirst maxEntries:450, diff-синхронизация) — v1.3, этап 13
- ✓ Офлайн-корзина + индикаторы сети + кнопка ↻ синхронизации — v1.3, этап 12
- ✓ Установка как приложение: PWA-баннер (Android) + iOS-инструкция + .apk/TWA для любого браузера — v1.3, этап 14
- ✓ Обновление каталога/фото по требованию при наличии сети — v1.3

<!-- Веха v1.4 «Фундамент разделения аудиторий» — в работе (этапы 15–17). -->

- ✓ Роль «Торговый/Клиент»: явная роль (`sales`/`client`), дефолт «Клиент» на всех устройствах, persist в localStorage (переживает офлайн/перезагрузку), ручной переключатель в настройках; угол карточки, главное действие в шапке и дефолт плотности сетки следуют роли, а не типу устройства (SSR-безопасно, `useDeviceClass` удалён) — v1.4, этап 15 (ROLE-01..04; ⏳ живая приёмка SC-1/SC-2 в браузере)

### Active

<!-- Веха v1.4 «Фундамент разделения аудиторий». Требования с REQ-ID — в .planning/REQUIREMENTS.md. -->

- [ ] Веха v1.4: роль Торговый/Клиент, локальная история заказов, сортировка + стикеры акций (этапы 15–17) — см. REQUIREMENTS.md
- [ ] (перенос v1.3) Ручная iPhone-приёмка офлайна на реальном устройстве — OFF/CART/IMG/PWA-02 #2/#5

### Out of Scope

<!-- Явные границы. -->

- Фича «автоновинки» (авто-снятие бейджа «новинка» через 2 недели) — отдельная будущая веха, зависит от памяти правок ([[auto-novinki-feature]])
- Перенос сайта-каталога с Vercel — остаётся на Vercel
- Изменение пайплайна загрузки прайсов и подушки безопасности — в этой вехе не трогаем (работает с v1.0)
- Массовая переразметка фото/описаний всех товаров — только структура и навигация
- Поиск/фильтр поверх новой структуры переделывать с нуля — существующий поиск сохраняется, адаптируется под два уровня по необходимости

## Context

- **Доработка существующего проекта (brownfield).** Полная карта кода — в `.planning/codebase/` (ARCHITECTURE, STACK, STRUCTURE, CONVENTIONS, TESTING, INTEGRATIONS, CONCERNS).
- **Поток данных:** Excel-прайсы → веб-загрузчик (`uploader/app.py`) → `scripts/upload.py` → Google Sheet «Товары» → Next.js API → витрина. Telegram-бот (`admin_bot/`) — боковой канал уведомлений/решений.
- **Сервер daniella** (`/opt/apps/catalog`, `ssh my-vps`): службы `catalog-uploader` и `catalog-admin-bot` (systemd), nginx+TLS, Telegram через SOCKS-прокси `127.0.0.1:1080` (РФ-ограничения). Сайт — на Vercel.
- **Толчок вехи:** поставщик сменил формат выгрузки 1С (нет колонок-категорий) — текущий парсер выдал 0 товаров; подушка безопасности отработала (откат). Нужно поддержать новый формат и дать владельцу инструмент управления категориями/фото.
- **Известные риски** (подробно — `.planning/codebase/CONCERNS.md`): `upload.py` перезаписывает весь лист «Товары»; группировка нового формата по названию — хрупкая; нет автотестов; Telegram-прокси — единая точка отказа; токен бота был засвечен в чате (рекомендована ротация).

## Constraints

- **Tech stack**: сайт — Next.js на Vercel (хостинг не менять); серверная обработка — Python (Flask) + systemd + nginx на daniella — так уже выстроено, не переусложнять.
- **Данные**: Google Sheets как «база»; `upload.py` перезаписывает лист «Товары» целиком — обязательна подушка безопасности (бэкап/откат) при любых изменениях пайплайна.
- **Security**: секреты не в git (`credentials.json`, `.env`); доступ к серверу только через `ssh my-vps`; правки владельца и секреты не должны утекать.
- **Формат прайсов**: к понедельнику уточняется у 1С-программиста, возможен возврат старого формата — поддерживать оба формата одновременно.
- **Пользователи**: владелец (Олег, вайбкодер — не программист) и оператор поставщика (грузит файлы) — интерфейсы должны быть простыми и «защищёнными от ошибки».

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Приём файлов через веб-страницу, а не мессенджер-бот | Файлы приходят оператору в MAX; MAX-бот требует ИП/ООО, VK неудобен, Telegram нестабилен в РФ — веб-страница проще и без лишних пересылок | ✓ Good |
| Поддержать оба формата 1С с авто-определением | Формат может вернуться к старому (уточняется у 1С) — не ломать рабочий путь | — Pending |
| Новые товары нового формата → «Новинки» + бейдж | Категорий в новом формате нет; изолировать новое для ручной разборки владельцем | — Pending |
| Управление категориями/фото через админ-панель с памятью правок | Владелец-непрограммист должен править сам; правки не должны теряться при обновлениях | — Pending |
| Подушка безопасности обязательна для пайплайна | `upload.py` перезаписывает весь каталог — нельзя оставлять витрину битой | ✓ Good |
| Двухуровневая навигация: разделы → подгруппы на месте | ~900 товаров в плоских 14 группах неудобны; раскрытие на месте без лишних переходов | ✓ Good (v1.1) |
| Структура «категория → подгруппа → раздел» — данные вне кода | По образцу `category_map.json`: владелец/правки меняют раскладку без правки кода | ✓ Good (v1.1) |
| Группа «Стоевъ и Сэнсой» расформировывается по смыслу | Группировка по поставщику мешает поиску по смыслу (соусы, консервация, лапша лежат вместе) | ✓ Good (v1.1) |
| В админ-панели — выбор подгруппы вместо плоской группы | Чтобы владелец раскладывал товары по новой структуре, не ломая память правок | ✓ Good (v1.1) |
| Двухуровневую навигацию реализовать как «меню-якорь» (scroll-spy), а не отдельные экраны | Раскрытие на месте на единой прокручиваемой странице — меньше переходов на телефоне | ✓ Good (v1.1, этап 6) |
| Скрытие товара — флаг в данных, не вырезание из листа | Товар должен оставаться в панели, чтобы владелец мог его вернуть; по образцу типа правки «подгруппа» | ✓ Good (v1.2) |
| Скрытые товары не отдавать в браузер вообще (фильтр на сервере) | Безопаснее и чище, чем прятать через CSS на клиенте | ✓ Good (v1.2) |
| Глазик-скрытие и десктоп-вид — в одной вехе v1.2 | Глазик невелик; пара UX-улучшений вместе даёт осмысленный релиз | ✓ Good (v1.2) |
| Оффлайн делать как PWA, а не нативное приложение | Переиспользует текущий сайт Next.js; даёт офлайн + установку + кэш фото быстро и дёшево; путь к Google Play (TWA) остаётся открытым потом | — Pending (v1.3) |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd:transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd:complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-06-27 — этап 16 «Локальная история заказов» выполнен и в проде (HIST-01..04; приёмка владельцем пройдена; CR-01 исправлен — общий OrderHistoryProvider); веха v1.4 в работе (остался этап 17)*
