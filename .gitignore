# dependencies
node_modules/

# next.js
.next/
out/

# env files
.env
.env.local
.env*.local
# резервные копии .env (могут содержать секреты — никогда не коммитить)
*.env.bak*
.env.bak*

# credentials
credentials.json
*.pem

# Android: ключ подписи приложения — НИКОГДА не коммитить (хранится вне репозитория)
*.keystore
*.jks
signing-key-info.txt

# misc
.DS_Store
Thumbs.db

# local media & docs
photos/
pdf/
~$*.xlsx

# python
__pycache__/
*.pyc

# загрузчик: входящие файлы, архив последней партии и журнал истории (рантайм, не хранить в git)
uploader/price/
uploader/last_batch/
uploader/history.json
# база заказов MAX (рантайм, на сервере, не в git)
uploader/*.db

# память правок: серверное состояние (эталон числа правок + отчёт сирот), рантайм, не в git
scripts/edit_memory_state.json
scripts/edit_memory_orphans.json

# авто-новинка: даты первого появления товаров, рантайм на сервере, не в git
# (деплой не должен затирать накопленные даты — иначе сбросится окно новинок)
scripts/novelty_dates.json

# debug
npm-debug.log*

# vercel
.vercel

.vercel
.playwright-mcp/

# PWA: сгенерированные service worker файлы (собираются из app/sw.ts при build)
public/sw.js
public/swe-worker*.js
